# 批次变更日志（Change Log）

> 根 CLAUDE.md「当前焦点·已完成」历史条目归档（RFC `20260815-docs-governance`：状态与历史分离）。
> **规则：新批次合入时，在顶部追加一行**（日期 | 批次 | 一句话摘要 | 测试数变化）。详情以 git log / REVIEW 已修复区为准。

| 日期 | 批次 | 摘要 | 测试数变化 |
|:-----|:-----|:-----|:-----------|
| 2026-09-10 | iOS app 重装批（用户「IOS 手机端安装下」）| **iPhone 16 Pro Max 重装，首次烧录生产域名（取代 IP 直连）**。`flutter build ios --release --dart-define=API_BASE_URL=https://api.adaiadai.com`（19.6MB，自动签名团队 4G3D37YKSB / Apple Development: rottokaka@gmail.com）→ `devicectl device install`（bundle com.adaiadai.adaiApp）→ 首次 launch 被 SBMainWorkspace 拒（免费签名「profile has not been explicitly trusted」）→ 用户在手机「设置 → 通用 → VPN与设备管理」信任后 launch 成功，进程实测在跑（PID 14909）。**关键变化**：历次 iOS 装机（08-20/08-23/08-25/08-30）均烧 `http://82.156.111.146:8080`（旧 IP 直连）；本次起改烧域名——与 backend-deployment §10「app 写 https://api.adaiadai.com，换服务器永不重构建」口径统一，后端 09-01 域名+HTTPS 上线后 iOS 侧首次落地。装机内容 = 当前 main（85ff514，含 learn 喂入入口批 v3.56 等本地 commit 的 app 侧改动；后端/web 侧仍未部署）。生产 API 复验：`api.adaiadai.com/api/v1/auth/me` 401（在线需登录）。**免费签名 7 天有效期（REVIEW P2-用户1）仍未根治**——预计 2026-09-17 前后再次打不开，兜底仍为手机浏览器/PWA 走 adaiadai.com，长期正解待用户拍板付费账号（$99/年） | 无测试数变化（app 173 不变，纯装机无代码改动）|
| 2026-09-10 | learn 喂入入口批（用户拍板「先页面后对话流」批 1 页面喂入；生产 learn 插件已开但无 UI 喂入入口——空态还引导「在对话里说整理」而对话流并未接线 = 死路引导）| **双端页面喂入入口 + 后端提交式消化（v3.56）**。**①后端提交式**（对齐复盘 submitReview 先例——learn 卡片化同走 LLM，同步 POST 几十秒级超时「看似没反应」）：`POST /learn/cards` 改立即返回 `{"status":pending\|running}`（同 user 消化在跑 → running，连点/双端只烧一次 AI）→ 后台 learnSubmitExecutor（LearnSubmitConfig 独立 2 线程/8 队列 daemon，满 → 400「消化任务繁忙」）执行原 digest（fail-visible 不变：失败素材留存 `_raw/`）；内存任务态 DigestJob（jobs 按 userId）——LLM 失败/不可解析 job=failed + 人话 message，done 记 type/title、60s 惰性清理回 idle（重启丢 → 轮询回 idle，已落盘卡不受影响）。**②新增 `GET /learn/digest/status`**（idle/running/done{type,title}/failed{message}）。**③web 资产页**：页头「＋」整理弹窗（素材粘贴 maxLength 50000 + 类型下拉自动/ai/trading/other + 作者/平台/链接可选 → 让阿呆消化 → 弹窗内每 2s 轮询上限 150s → done 关弹窗 + snack + 整树刷新定位打开新卡；failed 弹窗内人话可重试；超时提示后台继续；空态引导改指页面入口 + 空态「整理新内容」按钮）。**④app「最近学习」**：页头「＋」→ 喂入页（同表单/轮询，失败透出后端 error 人话），done 回列表自动打开新卡详情。双端 ApiService submitLearnDigest/getLearnDigestStatus + LearnDigestJob 模型（值复制）。测试：后端 +11（LearnDigestAppServiceTest 提交式 7：受理 done/inflight 去重只烧一次 AI/失败 job failed 素材留存/执行器拒绝 400/空素材与非法 type 不受理/无任务 idle + LearnControllerTest 4：POST 受理 running + inflight running + digest/status 403 + done 定位）→ 1344→**1355**；web +3（learn_page_test 喂入组：提交→轮询 done→打开新卡 / 空素材不提交 / 400 人话透出可重试）→ 180→**183**；app +2（同流程 + 失败人话）→ 171→**173**；三端全量全绿。api-spec v3.56（POST 提交式契约 + digest/status 节）、feature-ref v1.7、status（1355/183/173/端点 127）、backup_prod.sh 注释 data/ 清单补 learn。REVIEW P2-认知3 无关（那是 trading 认知层）。**2026-09-10 已部署生产**（用户确认「1」）：后端 jar 经 `deploy.sh`（重启 + 就绪 401）+ web 产物 tar 原子替换（JS+CanvasKit 补丁校验 config=1/canvas=1、产物含 fonts 与新端点路径 `learn/digest/status`）+ 服务器本机 smoke 全绿（feed/memory/timeline/tags/me/plugins 200；adaiadai.com 200 / api.adaiadai.com 401 语义正常）。**⚠️ 部署中发现：生产 adai 账号此前并未开启 learn 插件**（`/me/plugins` 实为 `["project","trading"]`——用户以为已开，故双端从未显示「学习」入口）；已按 userId 手术 `data/accounts/accounts.json` 补开（改前备份 `.bak-learn-*`，重启后 plugins `["trading","project","learn"]`，`/learn/tree` 与 `/learn/digest/status` 均 200）。app 侧入口已随同日 iOS 装机批（上一条）落到手机（烧域名 https://api.adaiadai.com）。**待办**：①生产 `.env` 的 `ADAI_SMOKE_PASSWORD` 仍为空（deploy 自动记忆重建 + deploy-gate smoke 依赖它，建议补 admin 账号）；②若你在 admin 插件开关点过 learn 却未生效，需单独排查该入口 | 后端 1344→1355（+11）· web 180→183（+3）· app 171→173（+2）· 端点 126→127 |

| 2026-09-09 | 三官深审续修（backend P2：手动记录 fee=null 含费口径）| **手动成交 fee=null 时当日盈亏含费口径统一**：`computeDailyPnl` 买入成本与卖出净额对 fee=null 记录回退系统费率估算（CommissionCalculator.buyCost / sellProceeds——与持仓 avgCost 摊薄同口径），不再「买入少计佣金、卖出不扣印花/过户」的数元级偏差；历史加权成本同步走估算（buyCostOf/sellNetOf）。测试：DailyPnlComputeTest +1（手动卖出无 fee → 净额扣估算费 ≈199.19），后端 1343→**1344** 全绿。未修仍三项（展示层降级标记 / 附注跨日节流与导入日期语义）排后续。本地 commit 未部署 | 后端 1343→1344（+1）|

| 2026-09-09 | 当日盈亏三官深审修复批（用户选 A：深审 → 按结论修；报告 audits/2026-09-09-daily-pnl-review.md）| **三官隔离深审（backend+frontend+adversarial，守护 7 PASS/0 HIT）交叉命中当日盈亏算术缺陷（⭐⭐）并修复**。**①computeDailyPnl v2（T+1 语义）**：卖出成本改「盘前成本」——今日无买入=持仓 avgCost（精确）/ 有买入或已清仓=当日之前历史买入含费加权 / 无基线=持仓成本兜底+诚实附注；仅 T+0 边界（卖出晚于当日最早买入）才对卖出冲抵当日买价并附注——卖光后买回/卖旧+加仓不再符号翻转重叠（backend 推演示例：旧口径 −400 vs 真实 +2100 修复后 = 2100）；持仓日浮动今日买仍持 = max(0,当日买入−T+0 已冲抵)。**②主流程随流水重算（P1-1）**：当日成交 15:05 后落库（POST /trades、/trades/batch、/trade-log/confirm、历史成交导入完成）→ refreshTodayPnl 重算写入 todayPnl（只改该字段、account 存在才写、best-effort）——晚间补流水后当日盈亏自愈，不再静默「浮动冒充全日」。**③导入空明细保留（P1-2）**：资金股份导入明细为空（清仓后导出无行）不覆盖。**④web**：徽标中性灰淡底+字号 11+图例拆独立行+widget 测试（flow 显/import 不显）。测试：后端 1339→**1343**（DailyPnlComputeTest +4：T+1 卖光买回 2100/T+0 混合 2600+附注/空明细保留 428/refresh 只写 todayPnl）· web 179→**180**（徽标 widget +1）；历史成交分组断言改加粗 header 精确匹配。**未修三项如实登记**（展示层降级标记 / 推送跨日节流与导入覆盖日期语义 / 手动 fee=null 数元级含费口径）→ 排后续。api-spec v3.55 含 refresh 注记 + status 同步；本地 commit 未部署 | 后端 1339→1343（+4）· web 179→180（+1）|

| 2026-09-09 | 晚间自主批（低风险技术债，本地闭环未部署）| **①后端锁池收敛**：`TradingAppService.userTradeLocks`（ConcurrentHashMap computeIfAbsent 按 userId 无界——同族 P3）收敛为固定 16 条带（USER_TRADE_LOCKS，同 TradeLogRepository/P2-交易28 模式）。**②收盘当日盈亏附注通知**：收盘任务 computeDailyPnl 存在「实质未计入」附注（缺昨收/无成本基线等）时推送一条说明通知（复用 market 类型、受推送开关门控；「今日无成交记录」纯持有常规提示过滤不打扰），best-effort 不打断收盘更新——「当日盈亏少算了」不再静默。**③web 清仓列表来源徽标（RFC 20260909 批 2 子项）**：`provenance=flow` 的行名称旁蓝「流水」小徽标 + 图例说明（「名称旁流水徽标=由成交流水自动收录」）。**④os/ 数据卫生检查**：0 字节均为 .gitkeep/.done 状态标记，无需清理。测试：后端 1339 全绿不变 · web 179 全绿不变（徽标渲染 analyze 0 + trading/dto 102 用例回归过）。本地 commit 未部署 | 无测试数变化 |
| 2026-09-09 | 当日盈亏精确计算批（用户「当日盈亏完全可以根据持仓算出来」→ 拍板口径①）| **P2-交易37（2026-09-09 生产实测：收盘算出的当日盈亏 428 被晚间资金股份导入清零）根治——当日盈亏改精确计算 + 导入缺列不再清零**。**①精确计算** `TradingAppService.computeDailyPnl(userId,date)`（口径① = 当日已实现（卖出净额 − 卖出成本：当日买入部分按当日含费买入均价冲抵、旧仓部分按持仓 avgCost、当日清仓回退流水历史买入含费加权）+ 持仓日浮动（(现价−昨收)×数量，当日新买入按成本价计）+ 当日股息入账（+）/红利税（−））；诚实降级：缺昨收/缺成本基线的票不计入并返回 notes 人话（不硬给错数，B3-3 原则）；当日无成交记录则 = 持仓日浮动并注明（当日成交需导历史成交/手动记录后重算）。**②收盘任务 15:05**：account.todayPnl 改写精确计算值（替换原仅持仓浮动估算；失败回落原估算并告警；notes 记日志）。**③资金股份导入 importCashQuery**：`parseCash` 增加 `todayPnlColumn` 列存在性检测——明细带「当日盈亏」列 → 以券商真源覆盖 todayPnl；缺列 → 保留既有 todayPnl 不清零（2026-09-09 428→0 根因回归锁）。测试：后端 1333→**1339**（DailyPnlComputeTest 6：已实现+浮动/股息入账/清仓无成本基线诚实附注/清仓历史加权成本/导入缺列保留 428/带列覆盖 −258）；api-spec v3.55 语义注记；（编号注记 P2-交易37）；本地 commit 未部署 | 后端 1333→1339（+6）|
| 2026-09-09 | 交易账目治本 + 清仓级联批 1（A 组治本 + B 组批 1，用户「A+B 开工，不再问我」自主推进）| **①P2-交易34 治本（券商快照锚定防重）**：新增锚定记录（`SnapshotAnchor`/`TradingAnchorRepository`/`trading/snapshot-anchor.json`，持仓 replace 与资金股份导入落锚定日）——手动/确认成交 entryDate ≤ 锚定日 → 400 拒录（提示用历史成交导入补流水或重导快照）；转账 date ≤ 最近资金快照日 → 400 拒录（纯净投入修正走「设置本金」）——2026-09-09 生产复发（21:23 锚定后又补记当天提现 15000 → 现金 −12721.84）同根根治（数据已修：账户对齐券商 2278.16/81357.16）；历史成交导入 entryDate ≤ 锚定日改走补录（只补流水不再回放扣现金/改持仓——9/7 replace+sync 双扣 −3.19 万路径关闭），股息类同日期跳过防双计。**②P2-交易35 治本（现金真源收口）**：`TradingContextContributor`/`MarketContextContributor` 现金改读 `AccountSnapshot.cash`（S5），不再读 positions.md `cashBalance` 展示行——AI 上下文现金口径与账户卡一致。**③P2-交易36 治本（成交编号/手续费链路）**：`TradeLogCandidate` 加 orderId/fee + `PUT /trading/trade-log/meta`（候选确认前补填）+ confirm 落库改走 `recordTradeWithOrderId`（orderId/fee 透传流水）+ `PUT /trading/trades/{tradeId}/meta`（已落库流水按 id 补填，跨月幂等）+ 仓储 `updateTradeMeta`。**④RFC 20260909 清仓级联批 1（双轨 sold.json）**：SoldTrade 加 `provenance`（flow/import，老文件缺省 import）；`ClearanceDetector`——流水证明清仓且 BUY≥SELL（完整）→ `upsertFromFlow` 自动收录 flow 行（P1 只填空白 symbol；幂等不翻倍）；BUY<SELL（基线外）→ 不落脏档案、进 `pendingClearances`；三处触发（recordTrade 卖光 / 历史成交导入 / POST /trading/sync）；`GET /trading/sold` 响应改 `{sold:[…], pendingClearances:[…]}`——web/app 双端对接 + web 清仓 Tab pending 横幅「已清仓但缺复盘档案——导入清仓股导出补全」。测试：后端 1309→**1333**（+24：锚定仓储/守卫/推导 + 清仓推导 5 + 候选补填·confirm 透传 + 流水补填 3 + Contributor 真源 + 既有确认流适配）· web 174→**179**（+5：sold 包装解析 + pending 横幅）· app 171 不变 · 端点 124→**126**；三端全量全绿；REVIEW P2-交易34/35/36 出表；部署前置检查补齐 sold 双轨控制器接线与触发集成测试（见部署修正 2026-09-09） | 后端 1309→1333（+24）· web 174→179（+5）· 端点 124→126 |
| 2026-09-07 | learn V2 审查修复批（learn V2 增量深审出表，用户「按决策来」自主推进）| **learn V2 四 commit（批1 复习流转/编辑 d3b8f4a + 批3 trading 候选联动 0bcebfe + 批4 复习提醒推送 66b4baf + 双端前端接线 ff7451f）与增量深审（audits/2026-09-07-learn-v2-review.md）出表修复全落地**。**①S-learn1 复习提醒口径**：进入 review 时仓储写 frontmatter `review_at`（=进入日，非消化日 created）——`LearnReviewPushService` 按 review_at ≤ today−7 聚合「满 7 天未 done」，同卡 `reminded_at` 7 天节流（markReminded 落盘）——不再「new 放一个月才复习次日收满 7 天假提醒」、不再每晚 nag（30 天 30 次→每 7 天 1 次）。**②S-learn2 开关归属**：learn-review 开关新增 learn 侧 `GET/PUT /learn/push-settings[/learn-review]`（learn 插件门控，复用 PushSettingsRepository 同键同文件）——纯 learn 用户（无 trading 插件）可自关；web/app learn 页头铃铛开关弹窗/底弹。**③P2-learn8 流转约束**：`LearnCard.isValidTransition` 只放行 new↔review / review→done / done→review（跳变 new→done、done→new → 400 LearnException）。**④P1-learn2 寻址歧义根治**：`save` 改同 type+title **任意日期**同名拒绝（源头无跨日同名）；`find` 残留多张同名 → 400 列出 created（不再静默取最新改错卡）。**⑤P1-learn1 回链精确**：候选 `learn_card_id` = `learn/{type}/{date}_{fileStem(title)}.md`（fileStem 上移 domain LearnCard 复用）。**⑥P2-learn6/7 编辑保真**：仓储 `applyEdit` 锁内 find→merge→写原子（并发 PATCH 不丢）；`rewriteManaged` 基于原文件手术替换受管键/正文段——手工未知 frontmatter 键与未知「## 」段原样保留（File First）。**⑦P2-learn3 Feed 类型级门控**：`buildPushEntries` 事件层过滤——learn-review 条目需 learn 插件、交易类条目需 trading 插件（防残留交易 push 漏给纯 learn）；learn-review 条目 tags/domain 按类型派生（不再标行情卡，P2-learn2）。**⑧P1-learn4 候选删除防护**：候选仓储 save/find/delete 同名唯一语义 + 歧义 400。**⑨P2-learn5/4/9/10 前端**：LearnPage 接 tradingEnabled（反哺按钮二次门控）/ web 页头候选管理弹窗（列表+删除，接线 getLearnCandidates/deleteLearnCandidate 消除死代码）+ 不可反哺原因 Tooltip / `_replaceCard` 仅当前选中是更新卡才回写 index / 三写入口 `_busy` 守卫 + clearSnackBars；写失败改 `extractApiErrorMessage` 透出后端人话（P1-learn3）。测试：后端 1292→**1309**（仓储 +11：跨日拒绝/歧义/findOn 语义/流转约束/reviewAt 计时/markReminded/applyEdit 未知段保留 + ReviewPush 4 重写：reviewAt 口径/节流/开关端点 + Digest/Controller 适配）· web 171→**174**（learn_page +3：门控隐藏/候选弹窗/复习开关）· app 170→**171**（learn_page +1：复习提醒铃铛开关）；guard-meta/align PASS。api-spec v3.52→**v3.53**（learn 审查修复批语义 + learn-review 开关端点）+ feature-reference §17 + status.md（1309/174/171/端点 124）| 后端 1292→1309（+17）· web 171→174（+3）· app 170→171（+1）|
| 2026-09-07 | 复盘生成提交式改造 + 交易记录口径批（用户实测「点每日复盘没反应」→ 拍板：触发+轮询 / 非当日成交不入 Feed）| **复盘「点击没反应」根因（生产日志 15:32/15:35 回放实锤）**：`POST /trading/review` 同步阻塞等 AI——生成实测 77~176s（deepseek-v4-pro + 4 万字上下文），App 走 15s 普通客户端必超时（web 120s 也扛不住 176s 那次）→ 前端先断报超时，而复盘其实两次都已生成落盘（883/732 字）。**①后端提交式**：`TradingReviewAppService.submitReview`（ReviewSubmitConfig 2 线程执行器 + `userId\|date` inflight 去重）——POST 立即返回 `{date,status}`：`exists`（已有复盘不重跑烧 AI，前端直接 GET 展示）/ `running`（同日在生成中，连点只跑一次）/ `pending`（后台生成中，失败记日志不落半成品）；`GET /trading/review` 保持 404=未就绪 供轮询。**②双端轮询**：app/web `ApiService.submitReview` + 交易页 `_showReview` 改「提交 → exists 直取 / 轮询 GET 每 3s（上限 240s）→ 弹窗」，生成中横幅文案「复盘生成中，约 1~3 分钟…」（app）/ 页头转圈（web）；连点在途守卫。**③交易记录口径（用户拍板）**：`recordTradeInternal` 仅 `entryDate=今天` 才 `writeTradingRecord`（非当日=历史成交导入/补录/sync 回放不再逐笔写 `domain=trading` 记录进 Feed/时间线——防批量回填刷聊天卡；复盘卡点 2026-08-26 已改「当日真实成交」口径，不依赖记录关键词）。api-spec v3.51→**v3.52**（POST 契约 + GET 404 语义 + 口径注记）。测试：后端 **+5**（ReviewAppService submit ×2：pending+落盘单跑/exists 不重跑、running 去重只入队一次；Controller POST ×3 pending/exists/running 替代旧同步 content 用例；AppService 非当日不写记录 ×1）——隔离 worktree 三测试类全绿，主工作树因并发 learn V2 修复会话（另一批）未收敛暂不能跑全量，合批后复核 | 后端 1292→**1297**（+5，隔离验证）· web 171 不变 · app 170 不变（双端全绿）|
| 2026-09-07 | 交易 web 表格响应式批 + 现金失真审查登记（用户「web 宽度足够却挤」→ ship）| **①web 交易页 4 张表响应式列宽**（用户实测反馈：历史成交「发生金额/成交编号」固定宽截断、宽屏不撑开）：根因 = DataTable/手写行被包在 `SingleChildScrollView(horizontal)` 内会收缩到列内容最小宽（宽屏右留白、列被最长内容绑架）。历史成交改列定义 `(label, minWidth, grow 弹性权重)`——宽屏按内容弹性把富余宽度分给名称/发生金额/成交编号（编号不再截断），窄屏回落 minWidth + 横向滚动（行内 padding 10×2 计入表宽预算防 RenderFlex 溢出）；持仓/自选/清仓三张 DataTable 统一 `_scrollableTable` 包装（可用宽 ≥ minWidth 时直出由 DataTable 均分富余列宽撑满，不足才横向滚动）。资金/规则区块本就是 Wrap/Expanded 自适应，未动。**②生产现金失真审查登记**（用户实测：导入两条今日成交 + 提现 5000 后账户现金/可取 −3.7 万）：根因 = 9/4、9/7 两轮「真实持仓 replace 全量导入 + 当日成交 sync 回放」把已含在 replace 快照里的历史成交重复回放扣现金（合计 −3.19 万）+ 转入资金从未记账（transfers 空）；数据已由券商资金股份导入校准（现金 −37226.29→1488.75、市值→95426、总资产→96914.75，本金 145000 保留，持仓数量不动，精确成本 3 只），备份 `/opt/adaios/data-backup-trading-20260907-2148/` + `.pre-cashfix-20260907` ×2；positions.md cashBalance 展示残留 292.88→1488.75 对齐。REVIEW 登记 **P2-交易34**（replace+sync 回放防重）/ **P2-交易35**（cashBalance 展示双源）/ **P2-交易36**（截图/手动确认缺 orderId+fee）。测试：adai-web 167 全绿 · analyze 0（无新增测试，纯布局重构）| 无测试数变化（web 167 不变）|
| 2026-09-07 | learn 插件 L2 呈现层批（RFC 20260829，用户「L2 开工」）| **learn L2 呈现三件套测试先行落地**。**①后端 LearnKnowledgeSource**（kernel 知识源，name=learn → PluginRegistry 映射 learn 插件门控）：ContextEngine 按 enabledPlugins 注入「你最近的学习笔记」（title+type+核心观点，上限 5 篇 created 倒序；无卡片不注入/损坏与 _raw 跳过/globalContext 注入 + enrich 空防双份）——问答可召回自己消化过的内容（RFC 3.7 ③）。**②web 资产页**：桌面壳导航「学习」项（learn 插件门控，RFC 20260829）→ LearnPage master-detail（左 ai/trading/other 分组卡片目录 + 右单篇全文：核心观点/要点/疑问/交易标注，trade_related 人话提示）；空态引导「说整理这个视频/文章」、加载失败降级 + 重试、刷新保留选中（保活页）；ApiService 加 getLearnTree/getLearnCards（learn_models DTO）。**③app 最近学习**：Launcher「学习」插件槽（learn 门控）→ LearnPage 最近学习列表（三组 merged created 倒序）+ 单篇全文页（核心观点/要点/疑问/交易标注）；错误态人话（403→学习功能未启用）+ 空态 + 重试；ApiService getLearnTree（learn_models 值复制）。测试：后端 +7（LearnKnowledgeSourceTest 6：注入最近/排序上限/隔离/损坏跳过/enrich 不双份 + ContextEngineTest learn 插件门控 1）+ web +8（learn_page_test 7：DTO 解析/树渲染/空态/失败重试/分组 + shell 门控 1：learn 启显禁藏）→ 159→167 + app +6（learn_page_test：DTO/recentAll 倒序/列表/空态/失败重试/单篇详情）→ 164→170；三端全量全绿。api-spec §18 补 L2 问答注入 + feature-reference §17 L2 落地 + status.md（1233/167/170）| 后端 1226→1233（+7）· web 159→167（+8）· app 164→170（+6）|
| 2026-09-06 | learn 插件 V1 后端流水线批（RFC 20260829，用户「开工，代码测试先行」）| **learn 插件（第三个插件，RFC 20260829）V1 后端流水线测试先行落地**。**背景决策**：A 会话技能（learn-digest skill）已落地；用户拍板 §四 六决策点全按建议默认 + **独立 learn 端点喂入**（仿截图入账先例——learn 消化是动作不是记录，不污染记录/记忆/Feed；用户「开工，测试先行」）。**①PluginRegistry**：注册 `PLUGIN_LEARN="learn"`（第三插件，Account.plugins/`GET /me/plugins`/admin 开关自动纳管；不进 domain 收敛体系，type 仅文件分类）。**②domain/learn**：`LearnCard`（ai/trading/other + frontmatter 语义；**trade_related 语义收敛**——非 trading 内容强制 false 防 LLM 幻觉；V1 只记录不联动规则库）+ `LearnCardRepository`（domain 端口）+ `LearnException`。**③LearnCardFileRepository**：md 卡片 File First 落 `data/{userId}/learn/{type}/{yyyy-MM-dd}_{title}.md`（RFC 3.4 frontmatter + 正文四段：核心观点/关键要点/我的疑问/复述）——per-user 条带锁（16 条带）+ **同日同 type 同 title 防覆盖**（400）+ 损坏文件跳过不中断 + fileStem 清洗防路径逃逸 + `learn/_raw/` 原始素材留存（LLM 失败时素材不丢）。**④LearnDigestAppService**：喂入素材 → LLM 结构化卡片（system prompt 输出 JSON：title/type/tags/core_view/key_points/questions/trade_related/trade_note）→ 落盘；**fail-visible**——LLM 失败/输出不可解析/缺标题 → 素材留存 `_raw/` + 400 人话（不产半成品卡片，对齐交易规则层 P0 教训）；type 显式 hint 优先 > LLM 判 > 越界回落 other。**⑤LearnController**：`POST /learn/cards`（喂入消化，content ≤50000）+ `GET /learn/cards`（?type 列表，created 倒序）+ `GET /learn/tree`（资产树按 ai/trading/other 分组）；全需 learn 插件（403「learn 插件未启用」）。**⑥GlobalExceptionHandler**：LearnException → 400 人话。测试：+30（LearnCardFileRepositoryTest 10：round-trip/防覆盖/隔离/损坏跳过/raw 留存/fileStem 清洗 + LearnDigestAppServiceTest 10：trading→trade_related/AI 内容强制 false/type hint 覆盖/LLM 抛异常/不可解析/空标题/空 content 各降级 + LearnControllerTest 10：403 门控 ×3/成功/400 校验/错误人话）+ PluginRegistryTest 断言同步（all()=trading/project/learn），全量 1189→**1226** 全绿；端点 113→**116**；Controller 20→**21**；META-GUARD PASS + GUARD-ALIGN PASS。api-spec v3.47→**v3.48**（§18 learn 契约）+ feature-reference §17 + status.md 同步。**L2 待续**：web 资产页（目录树+全文渲染）+ app 最近学习 + LearnKnowledgeSource 问答注入（价值呈现）；真实 LLM 消化 smoke 留给部署批。**待用户**：adai 账号启用 learn 插件（admin 开关）后即可喂入实测 | 后端 1189→1226（+37）|
| 2026-09-06 | DSH 飞书云文档 MCP 接入批（工具链，用户「go」）| **DSH 工具链接通飞书云文档读取（非仓库代码，配置在 ~/.dsh）**。调研结论：飞书官方 MCP 分「官方远程端点（免部署但 TAT 2h 过期）」与「官方本地 lark-openapi-mcp（推荐，token 自动续期）」两条路；市场 90+「飞书×DSH」插件多为 IM 机器人通道与文档无关；TestKAi005/MCP-Lark、@iflow-mcp/* 为转包勿用。**排坑实录**（沉淀给公司复刻）：远程端点 `X-Lark-MCP-Allowed-Tools` 头是工具发现开关（不带则 tools/list 空）；权限 scope 开通后需发布版本才生效（99991672）；文档须授权给应用（机器人能力）；`~/.npm` root 污染致 EPERM（换 npm_config_cache）；GUI 进程 PATH 精简致 npx NOT FOUND（用绝对路径 + env PATH）。**落地**：`~/.dsh/profiles/web/cordis.patch.yml` 追加 mcp-client 实例 → 本地 stdio `lark-mcp` v0.5.1（nvm v22.9.0 全局已装）`-t preset.doc.default`（6 工具，docx 读/搜/导入 + 辅助），实测 `docx_v1_document_rawContent` 读授权文档成功（备份 cordis.patch.yml.bak-20260906*）。**交付**：公司部署指南《飞书-MCP-接入指南-公司部署版.md》（Qoder/opencode 配置 + 内网适配：npm 镜像/离线拷包、open.feishu.cn 域名白名单硬前置、launchctl setenv 注入 PATH/代理、凭证不入库）输出至桌面。**待用户**：重启 DSH 后新会话验证本地 stdio 工具；验证后重置 App Secret（曾现于聊天）；远期可扩 bitable（改 preset） | —（无仓库代码/测试/端点变动）|
| 2026-09-06 | admin 前端生产部署批（用户「部署」）| **adai-admin 全量修复上线生产**（本地 commit 57c3a17/b3a19ac/eec244b 同源产物；后端免推——认知层 09-05 已上线同源代码，今日后端零改动；web/app 无改动）。构建：`flutter build web --no-tree-shake-icons --base-href=/admin/ --dart-define=API_BASE_URL=https://api.adaiadai.com`（JS+CanvasKit）+ canvasKitBaseUrl 注入（config×1/canvas×1 校验过）+ 字体 CDN 拦截器补丁（fonts.gstatic.com → /admin/fonts/ 本地 woff2）。部署：tar 46MB → scp → 服务器备份 `admin.bak-20260906-141753` → 停 adaios-admin → `/opt/adaios/admin.new` 原子替换 → 启动（systemd active）。**公网验证**：`/admin/` 200 · bootstrap canvasKitBaseUrl 注入在位 · `/admin/fonts/NotoSansSC-Subset.woff2` 200（本地字体不白屏）· `admin` 登录 200（role=admin）· `/auth/me` 正常。含：登录默认值 · P1×4 · 拍板 4 项 · 保护/反馈批 · 打磨 A/B 全部生效 | —（无代码/测试/端点变动）|
| 2026-09-06 | admin UI 打磨批 B（用户「剩下所有继续，不再问」自主推进）| **审查剩余打磨第二批（确定性修复 + 明确不擅自做的边界）**。**①P2-10 行情导入结果**：`MaintenanceResult` 增 `failures` 完整清单（原 message 只塞截断前 3 条）；系统/维护页 `_showResult`——失败带明细弹对话框完整复查（可滚动清单），成功/无明细仍 snackbar；store 不再截断。**②P2-23 档案空态**：identity 名字空→「（未设置名字）」灰占位 + 偏好/协作规则空分区加「暂无偏好/暂无协作规则」（防空卡被当成加载坏了）。**③P3-6** 空持仓加「该用户暂无持仓」占位（原只剩统计 0 + 空表头）。**④P3-17** 新共享组件 `widgets/file_preview.dart`——文件/知识超大内容预览截断（前 4000 字符 + 「内容较长已截断」附注，防全量 SelectableText 卡顿）。**⑤P3-18** 删 `app_theme` cardTheme 死配置（全库无 Material Card，面板统一 AppCard；消除「第二真相」）。**⑥P3-29** 登录错误行 AnimatedSize 防布局跳变。**边界（未擅自做）**：P2-14 对比度 / P2-15 token 化等观感大改 + P3-5/11/27/31 结构性重构 → 登记 audit 进展表「待视觉验收/重构」，由用户回来后定；P2-7/8 历史浏览、P2-5 删除数据语义维持原定案。测试：admin 64 不变 · analyze 0 全绿 | 前端 adai-admin 64 不变 |
| 2026-09-06 | admin UI 打磨批 A（用户「你继续，我休息」自主推进）| **审查剩余打磨项第一批（低风险确定性修复 19 项，改必测/全量回归）**。**布局/文案**：P2-16 两棵文件树窄屏空态文案随 wide 分支（「在左侧/在上方」，原窄屏无"左侧"）· P2-24 记忆 kind 筛选空文案区分「该分类下暂无记忆/暂无记忆」· P2-25 登录首访提示 controller listener 实时刷新 · P3-12 登录按钮「登 录」→「登录」（测试同步）· P3-13 知识页「Domain」→「资产域」· P3-28 记录 id 徽章→弱化小字。**一致性/对齐**：P2-18 行情类型徽标 darkGreen→darkBlue（对齐 adai-web，避免与「绿=跌」行情方向色同屏互斥）· P3-3 当前登录账号图标 lock→account_circle（锁易误读「被锁」）· P3-25 app_theme 15% alpha 双写法统一 withValues。**溢出防护**：P2-20 行情名称 / P2-21 术语 source / P3-14 顶栏用户下拉（限宽 150） / P3-15 账号 userId 均补 ellipsis。**触达/安全区**：P3-20 维护「执行」按钮 34→40 · P3-23 ICP 条包 SafeArea(top:false)。**防连点**：P3-8 复盘生成 busy（生成中…禁点）· P3-9 建号 _creating busy（防双击重复建号）。**细节**：P2-22 内置保护锁 grey6→grey4（1.58:1 不可见）。测试：admin 64 不变（纯 UI/行为回归，analyze 0 全绿） | 前端 adai-admin 64 不变 |
| 2026-09-06 | admin 审查保护/反馈批（用户「不需要我决策，就开始」）| **admin 审查剩余项自动推进（保护/反馈优先，改必测纪律）**。**①P2-2 禁用确认**：账号启用/禁用开关拨向禁用先弹确认（明示「无法登录 + 会话立即失效」）——误触成本高，与删除/重置同保护口径；按钮文案「确认禁用」（避免与状态徽章歧义）。**②P2-3 插件开关反馈**：成功 toast「已开启/关闭 X 的『交易/项目』模块」+ tooltip 注明「保存即生效：影响该用户 app/web 模块显隐」。**③P2-4 静默刷新保旧值**：账号列表操作后静默刷新失败不再整页替换错误态——保留已展示数据 + snackbar「列表为上次数据」（U31，此前与首载同路径一次抖动即丢列表）。**④P2-5 删除文案**：确认框补「会话立即失效、不可再登录、不可撤销」；数据目录清理口径后端待拍板（task-log #149）不臆断。**⑤P2-6 治理浏览**：账号卡新增「治理浏览」按钮直达该用户数据区（壳层切浏览用户 + 跳数据页，enabled 非自身账号显示）——修复「发现账号异常→看它数据」任务流断裂。**⑥P2-9 维护确认**：记忆重建/重补/数据清理执行前确认弹窗（标题 + 影响说明 + 「目标用户：X」），防对错的人执行；行情导入（全局数据）仍走文件选择即确认。**⑦P2-1 401 提示**：全局会话失效回登录前 SnackBar「登录已失效（会话过期或被重置），请重新登录」（scaffoldMessengerKey）。回归：既有禁用/删除/维护测试补确认步骤 + 新增 P2-6 测试；admin 63→**64** 全绿 · analyze 0 | 前端 adai-admin 63→64 |
| 2026-09-06 | admin 审查 P2/P3 拍板修复批（用户拍板 4 项全按推荐 + 新协作规则「改必测/测试现行」）| **admin UI/UX 审查拍板项落地（4 项全按推荐）+ 协作规则确立**。**①P2-11**：系统区「反哺」页移除「标记已处理」本地假操作（`_toggleConflict`/IconButton 删除——无持久化、刷新即丢，会误导「已解决」）；冲突改只读展示（徽章按后端 handled），页尾引导更新为指向「复盘」页签反哺入口；中英残留「Conflicts 冲突」→「规则冲突」（P3-13 顺带）。**②P2-13**：data/ 文件树（全局、含全部账号目录）在数据区 per-user 视图里页头标注「全局 data/ 文件树（含全部账号目录，非当前用户视图）」防误判。**③P2-7**：记录/Feed 预览数据源标注「当天 · 最多展示前 50 条」（防截断误读；历史浏览按拍板另排）。**④P3-2**：账号页重置内置 admin 密码前警示确认弹窗（明示「接管内置管理员、会话全失效 + 引导本人走顶栏改密」——保留逃生通道防再锁死）。回归测试 +5（feedback_tab 只读×1 / widget P3-2×1 / data_tree 全局标注×1 / data_scope 来源标注×2）。**⑤新协作规则**（用户确立）：代码修改必配测试、测试现行（每批前跑基线改后全绿）；可复用经验沉淀为审查清单新检查点 **F61**（认证 401 双义跨端一致）/ **U33**（管理端只读视图数据源自解释）。测试：admin 58→**63** 全绿 · analyze 0 | 前端 adai-admin 58→63 |
| 2026-09-06 | admin 审查 P1 修复批（用户「修」）| **admin UI/UX 专项审查 P1×4 全部出表**。**①P1-A 改密 401 双义**：`api_service.changePassword` 独立 401 处理——响应含「会话/未登录」（AuthFilter 会话失效）才触发全局登出；「原密码错误」（控制器业务 401）抛 ApiException 由弹窗内提示，不再把输错一次的管理员踢回登录页（对齐 adai-web 已修口径）；抽 `_extractError` 复用。**②P1-B 反哺入库日期**：`reviews_tab._promote` 改 `formatDate(review.date)`（原 `review.date.toString()` 带空格毫秒，后端 `@PathVariable LocalDate` 解析必败——反哺主动作从未成功；同页查看/生成早用 formatDate/_dateOf）。**③P1-C 文件树/知识树读内容失败静默吞**：data_tree_tab / os_tree_tab 加 `_contentError` 态（「读取内容失败 + 重试」，与「无内容预览」真空态区分）；`tree_view` 目录懒加载失败行内「加载失败 · 点击重试」，不再伪装空目录。**④P1-D 0 值判红（V9-10）**：market_tab `_changeColor` / positions_tab `_profitColor` 三分支（>0 红 / <0 绿 / ==0 中性 darkGrey3）；`formatPercent` 仅 `v>0` 带 +（0 → `0.00%`）。回归测试 +5（changePassword 双义×2 / reviews_tab 反哺传参×1 / format×2——FakeSystemStore 加 lastPromoteDate 记录）。测试：admin 53→**58** 全绿 · analyze 0 | 前端 adai-admin 53→58 |
| 2026-09-06 | admin UI/UX 专项审查 + 功能手册批（用户「布局一般般」+「都做」）| **adai-admin 首次 UI/UX 专项审查 + 全景手册补齐**（此前管理后台从未专项 UI/UX 审查，全景文档仅有 08-16 资产卡且过期）。**①双官审查**（ui-reviewer + ux-reviewer 隔离并行，仅报告不修改）：**P1×4 成立**（改密原密码错 401 双义未区分→误踢登录页 P1-A / 反哺入库 `review.date.toString()` 传参后端 LocalDate 必败 P1-B / 文件树读内容失败静默吞伪装「无内容」P1-C / 涨跌 0 值判红 + formatPercent 0 输出 +0.00% P1-D）+ P2×25 + P3×31；**P1×1 复核排除**（会话菜单 switch 无 break——Dart 3.12 实测无 break case 不贯通，非「改密即登出」）。**②全景手册 `docs/reference/admin-features.md`**（对照 trading-features 体例：定位边界/登录会话/四区页面清单/端点总表/已知缺陷/变更规则，代码实测口径）→ 登记 `_index.md`/`docs/README.md` 功能手册表（guard-context C1.5 自动纳入）；feature-reference §15「五模块」过时表述更正为「四区（内容已并入数据区）」并指回手册。**③登记**：REVIEW.md（头部摘要 + 最近审核表 + unfixed-gate + P1 表 P1-admin-1~4 ⏳ 待拍板）；审查报告 `docs/review/audits/2026-09-06-admin-uiux-review.md`。修复批次建议见报告（P1-A/B/D 代码级快修 → 保护口径批 → 反馈批 → 文件树/历史浏览需产品拍板），**待用户拍板执行** | —（无代码改动，纯审查+文档）|
| 2026-09-06 | admin 登录页默认账号批（用户「不要给默认账号了」）| **adai-admin 登录页去默认预填账号**。背景：账号矩阵（09-04）把内置管理员迁为 admin、adai 降 role=user 后，登录页仍残留预填 `adai`（`login_page.dart` 建页后从未随迁更新）——默认值把管理员引向「不是管理员账号」的错误登录。修复：`_accountCtrl` 预填移除（留空自输）+ 首访 setup 文案空账号兜底（不再显示「为账号「」设置」）+ 回归测试 1（账号框初始为空、空提交提示请输入账号）+ widget_test lint 清理（`_pumpAccounts` 去下划线）。测试：admin 52→**53** 全绿 · analyze 0 | 前端 adai-admin 52→53 |
| 2026-09-06 | admin 密码重置批（用户遗忘密码，详见当日上一条）| **生产 admin 账号密码重置（纯运维数据手术，无代码变动）**。**①背景**：管理后台 admin 账号（2026-09-04 账号矩阵批设的随机强密码）明文未留存，用户遗忘且尝试登录失败。**②为何不走常规接口**：`POST /auth/setup` 仅当「全系统无任何账号设过密码」时可用一次（adai 已有密码 → 不可用）；`PATCH /accounts/{userId}` 重置密码需 admin 会话（鸡生蛋）。**③执行（生产 82.156.111.146 文件手术，沿用 09-04 账号矩阵批先例）**：本地以与后端同版本 spring-security-crypto 6.3.7 / BCryptPasswordEncoder(10) 生成 `K@ngda1992` 的 `$2a$10$` 哈希（保证兼容，不用 htpasswd $2y$ 前缀引入风险）→ SSH 备份 `accounts.json.bak-resetadmin-20260906-010437` / `sessions.json.bak-resetadmin-20260906-010437` → python 原子改写 `/opt/adaios/data/accounts/accounts.json` 中 `admin.passwordHash`（tmp+os.replace，属主/权限保持 adaios:adaios 644；后端每请求实时读文件，**无需重启**）→ 同步从 `sessions.json` 剔除 admin 全部 4 个未过期会话（09-04 签发、10-04 才到期——残留 token 仍可用，与 PATCH 重置「踢会话」语义对齐；adai/family 会话未动）。**④公网验证**：`admin`+`K@ngda1992` 登录 200（userId=admin / role=admin / plugins=[]）· `/auth/me` 200 role=admin · `adai` 登录不受影响（role=user 照常）· 错误密码 401。**后续**：用户可登录后于 admin 顶栏「修改密码」换独立密码；备份文件确认后可删 | —（无代码/测试/端点变动） |
| 2026-09-05 | 认知层生产部署批（用户「同意」部署）| **交易⑤认知层上线生产**（三官深审修复出表 + 三项口径拍板后）：bootJar 27MB → deploy-gate GATE-BEFORE 3/3 PASS → deploy.sh 装入重启（13:59 CST，systemd adai-core active）→ GATE-AFTER smoke 跳过（.env 未配 ADAI_SMOKE_PASSWORD——用户全局指令含账号密码，可后续补配开全自动）。**公网验证**：`/auth/me` 401（健康）· adai 登录 200（token 64）· `GET /trading/profile` 返回真实画像（168 笔/胜率 33.3%/纪律违反 91 = 新口径 37 扛单 + 54 短打——实锤剔除亏损持仓的新代码已生效）· `GET /trading/advice-history` 200 空数组（留痕自下次建议生成起累积）。遗留：`ADAI_SMOKE_ACCOUNT/PASSWORD` 未配 → smoke 全自动待补；旧数据无建议留痕（新能力，历史不回填——卖前建议回查对历史清仓为空属预期） | 后端 1189 不变 |
| 2026-09-05 | 交易⑤认知层落地批（RFC 20260905 draft，用户拍板「按实施顺序全量执行」）| **⑤认知层两条腿落地（RFC 20260902 五层记忆模型的缺口层）**。**①P0 建议留痕**：AdviceEntry + AdviceHistoryRepository（domain）+ AdviceHistoryFileRepository（按月 JSON `data/{userId}/trading/advice-history/{yyyy-MM}.json`，per-user 条带锁/损坏拒写回）；`TradingAdviceAppService.generateAdvice` 出口逐票落盘（成功 source=manual-advice、LLM 降级 source=degraded 都落——诚实留史）；`GET /trading/advice-history?symbol=&days=` 查询（按票近 N 天回查 / 按月全量）。**②P1 个人画像**：TradingProfileService（客观层系统实时推导：胜率/纪律违反率/中位盈亏/清仓分布；主观层 profile.md 读写）+ TradingProfileContributor（ContextContributor，trading/decision 场景注入画像，PluginRegistry 注册 trading 门控）+ `GET/PUT /trading/profile`；建议引擎 prompt 前置注入画像（A 点核心）。**③P1 建议闭环**：复盘注入「建议对照」段——当日清仓标的回查建议留痕 → 「阿呆当时说 X → 你做了 Y（清仓/持仓期%）→ 规则对照」（数字代码算，红线①）。**④P2 情绪采集**：TradePsychologyService（按交易结构确定性生成 3~5 问，不耗 LLM）+ `GET /sold/{symbol}/psychology-questions` + `POST /sold/{symbol}/psychology/answer`（回填 sold.psychology 追加式 + 沉淀 profile.md 主观层）。**⑤P2 建议遵守率（B 反哺 A）**：卖前有 clear/reduce 建议且 10 天内清仓 = 遵守；`/trading/profile` 响应带 adviceAdherence + 画像注入遵守率文本。**⑥远期拦截预留**：建议引擎逐票注入 symbolHistoryNote（该票在清仓史亏过 → 「上次哪里做得不对」自省对照）。红线自查：数字系统算 / 主语是你 / data/ 隐私零泄漏（gitignore 验证）。测试：1137→**1189**（净 +51：4 新测试类 34 法 [AdviceHistory 6 + Profile 14 + Psychology 8 + Contributor 5] + 既有类新增 17——含实施批、审查修复批[排序/ID毫秒/尾盘留痕/样本门槛]与同会话首页行情条批 +5；三处统一口径见 §七）。**未部署** | 后端 1137→1189 · 端点 108→113 |
| 2026-09-05 | 收口清债批（登记对齐 + 保留字 #149 部分，用户「①复查收口+②顺手清债」）| **REVIEW/登记对齐 + 小代码债**。①docs：REVIEW.md 头部 updated→09-05 + 最近审核表补 09-04 九批登记行（此前只在 change-log/P1 行内留痕）；task-log #149 复核更新——「accounts.json 无锁」确认已由 P1-4 修复，删号清理数据 / 禁建 default 标为待拍板；api-spec 建号 400 段补保留字说明。②core：`AccountController.createAccount` 禁建保留字 `userId=default`（历史遗留测试数据目录名，防与测试夹具混淆）+ 类 javadoc 内置管理员表述 adai→SEED_ADMIN_ID 常量对齐；+1 回归测试（反向验证：无此守卫时建号会成功）。测试：AccountControllerTest 36→**37** 全绿（后端全量 0 失败）。**本地 commit 88ff0ba，未部署** | 后端 +1（AccountControllerTest 36→37）|
| 2026-09-04 | 账号会话闭环批（账号体系审查 P1-1/P1-2，用户「开工」）| **补账号生命周期与自助改密闭环**。**①后端 P1-1（services/adai-core）**：`AccountController.updateAccount` 显式禁用（enabled true→false）与重置密码并列 `kickSessions` 踢全部会话；`deleteAccount` 删前先踢；`AuthService.validateAndTouch` 会话有效后复查账号——**不存在或 enabled=false → 删会话返回 empty**（fail-closed，纵深防御：外部直改账号文件也立即失效）。**②web P1-2**：底部用户行从「点击即退出」升级为**会话菜单**（PopupMenu「登录：@userId / 修改密码 / 退出登录」）+ `_ChangePasswordDialog`（原/新/确认，≥8 位两次一致，成功 SnackBar 带被踢数）；`ApiService.changePassword`（401 双义：原密码错误弹窗人话不登出 / 会话失效走全局登出）。**③app P1-2**：Launcher 会话区「退出登录」上方新增「修改密码」入口 + `ChangePasswordDialog` 组件；`ApiService.changePassword` 同 401 双义。顺带清 web analyze 基线 7 条 info（copyWith 三处多余 this.（无同名参数等价）、2 处 if 块、_settings late final、测试局部函数命名）。测试：后端 1126→**1131**（AuthServiceTest 2：disabled/missing 账号会话删除 + AccountControllerTest 3：禁用踢/启用不踢/删前踢 InOrder）· web 150→**159**（change_password_test 9）· app 152→**164**（change_password_test 12）· 三端 analyze 0。REVIEW P1-账号1/P1-账号2 出表 ✅ | 后端 1126→1131 · web 150→159 · app 152→164 |
| 2026-09-04 | 账号矩阵批（用户「后台管理系统账号改成 admin，app/web 账号 adai，再初始化一个无插件普通账号」）| **内置管理员由 adai 迁为 admin——后台管理专用账号，账号矩阵三类各一（生产 + 本地同款手术）**。**①后端（services/adai-core）**：`Account.SEED_ADMIN_ID` `adai`→`admin`——seed 预置 / 不可删禁降级 / 插件保护 / setup 默认账号全部随常量迁移；AccountFileRepository 播种与老文件迁移文案改常量拼接；AccountController 保护逻辑自动跟随；`AdminController.getAiLogs` 默认 userId=adai 保留（产品主账号仍 adai，语义不变）。**②adai-admin**：`AccountStore.protectedAdminId` `'adai'`→`'admin'`（内置徽章/保护随迁）。**③账号矩阵落地（本地 + 生产 `data/accounts/accounts.json` 同步手术）**：`admin`=role admin、plugins=[]（纯后台管理，随机强密码）· `adai`=role **user**、plugins 保留 [trading,project]、passwordHash 原样保留（app/web 产品主账号照常登录，个人数据 `data/adai/` 不动）· `family`=role user、plugins=[]（普通受限账号，随机强密码）。**④测试同步**：AccountFileRepositoryTest / AccountControllerTest 种子断言 adai→admin；前端 FakeAccountStore seed 改 [admin,alice,adai,bob] + widget_test 放大视口（4 卡超默认 600px 视口 ListView 懒构建）+ AccountStore import + 'admin' 徽章/账号名文本去歧。**⑤生产部署**：bootJar + deploy.sh（seed 保护切到 admin）+ accounts.json 备份 `accounts.json.bak-adminmatrix-20260904` 手术 + admin 前端重建（--base-href=/admin/ + CanvasKit/字体补丁）tar 原子替换重启。**公网验证**：admin/family 登录 200（role/plugins 正确）· admin GET /accounts 见三账号 · family 访问管理口 403 · 无 token 401 · `/admin/` 200（base href 正确）。测试：后端 1126（全量 0 失败）· admin 52 全绿。**用户待办**：用 admin+新密码登录后台（原 adai 后台会话自动失效回登录页）；adai app/web 登录不受影响；忘记 adai 密码现可在后台账号页对 adai 卡一键重置（列表已有多账号，重置入口可见） | 后端 1126 不变 · admin 52 不变 |
| 2026-09-04 | 生产部署批（用户「直接部署」）| **晚间自主批四件套上线生产**（本地 commit 01b84d6..c590b2f 同源产物，git push 因本机网络受限未执行——GitHub 不可达，提交均在本地 main，可稍后自行推送）。**①后端**：`bootJar` + `deploy.sh` 上传重启（systemd adai-core up；.env 无 smoke 密码 → 跳过自动重建记忆，登录验证由用户在浏览器完成）；**②规则包**：`data/adai/trading/rules.yaml` 上传（含 buyVolumeSurge 2.0——此前生产无规则文件走代码默认 1.5，本次一并落地倍量柱口径）；**③.env** 追加 `ADAI_MAX_FILE_SIZE=512MB`/`ADAI_MAX_REQUEST_SIZE=520MB`（MD17 行情包上传需放大 multipart，备份 .env.bak-*）；**④web**（JS+CanvasKit，API_BASE_URL=https://api.adaiadai.com，本地 canvaskit/字体补丁 + 校验）：tar 原子替换 `/opt/adaios/web` + 重启 adaios-web——含批次止损行内编辑 + 资金曲线卡；**⑤admin**（--base-href=/admin/ + 补丁）：tar 原子替换 `/opt/adaios/admin` + 重启 adaios-admin——含「行情数据导入」卡片 + onUnauthorized 修复。**公网验证**：`https://adaiadai.com` 200 · `/admin/` 200 · `api.adaiadai.com/auth/me` 401（健康）· `/api/v1/trading/equity-curve` 无 token 401（路由就绪）· web/admin fonts 200（无白屏/框框风险）。**待用户登录态复查**：资金 Tab 曲线出数、批次明细改止损、admin 维护页上传入口、自选买点信号（新 B1/B2 口径） | —（测试数不变） |
| 2026-09-04 | 资金曲线批（晚间自主批 IV，用户「按推荐来」拍板决策方案 A）| **资金曲线落地（对标调研 ❌ 无 → ✅；交易模块待办三件全清）**。**①后端**：`EquityCurveService`——按日收盘净资产：现金 = account.json 现值**反向锚定**（起点 = 现值 − 全程交易/转账净变化，末端恒等于券商真实现金）+ 逐笔交易/转账事件驱动；持仓数量 = 流水正向回放 + **底仓恒持注入**（positions 有而流水解释不了的部分，等效批次 INIT——已清仓/复购历史期都还原）；市值 = 持仓 × 当日收盘（K 线 tdx 本地优先 → 网络兜底，停牌沿用前收、再缺成本兜底）；净值 = total/invested（invested = 期初投入缺口 + 转账累计净投入，≤0 → null 不给误导值）；回撤 = 峰值到当日回落比例。**②端点**：`EquityCurveController`（独立 Controller，不塞 15 参构造）`GET /api/v1/trading/equity-curve` → `{points:[{date,totalAssets,cash,marketValue,invested,netValue,drawdown}], skippedDays, startDate, endDate}`（LocalDate 显式转字符串）。**③web**：资金 Tab 顶部「资金曲线」卡（_EquityCurveCard：净红/总资产蓝折线 CustomPaint + 峰值虚线 + 最新净值/区间/最大回撤；principal=0 → 自动退画总资产；空/失败人话不打断资金区）。测试：后端 1120→**1126**（Service 4 + Controller 2）· web 150 不变 · 端点 107→**108**。**未部署** | 后端 1120→1126 · 端点 107→108 |
| 2026-09-04 | 买点三重校验批（晚间自主批 III，用户「按推荐来」拍板：B1 课程口径 + B2 2.0× + KDJ/超买/追高排除 + 新鲜度）| **BuyPointDetector 判定重写（REVIEW P1-交易9/20 出表，楚天龙连板误报根治）**。**①B1 几何课程校准**：回撤占波段（窗口最高 high − 最低 low）≥ 回调比例（0.5 ⇔ close ≤ (high+low)/2「涨幅一半位」）替代旧「距前高回撤 ≥50%」腰斩口径；**②B2 放量默认 1.5→2.0**（倍量柱，TradingRuleSettings.defaults + 校验回落 + `data/adai/trading/rules.yaml` + `os/trading-engine/09-scripts/sync-adai-rulepack.sh` 三处同步）；**③B2 三重防护**：KDJ.J 拐头向上 + J 连续 ≥90 高位钝化排除（首日拉起放行——一刀切 <90 会误杀全部首日突破）+ 距窗口低点涨幅 >30% 不追 + 近 2 日连板（≥9.8%）不推；**④信号新鲜度**：WatchBuyPoint 附 `dataDate`（判定 K 线最后日期），15:10 推送只推 dataDate=当日（防滞后一日信号冒充今日）。**⑤文档**：api-spec v3.44 / engine buy-point-rules.md 规格同步 / REVIEW 两行出表。测试：后端 1115→**1120**（BuyPointDetectorTest 重写 3→8：B1 课程命中/B1?/B2 命中/连拉后拦截/追高拦截/连板拦截/none/不足数据）。**未部署** | 后端 1115→1120 |
| 2026-09-04 | 按批次止损批（晚间自主批 II，用户「按推荐来」拍板决策 P1 方案 A）| **每个买入批次可单独设/改止损、事后可调——补批次止损编辑闭环**。**①覆盖层**：`LotStopLossOverrideRepository`（infrastructure/storage）——`data/{userId}/trading/lot-stoploss.json`（lotId→止损价，BigDecimal 存 toPlainString 防科学计数；per-user 条带锁 RMW 原子写 + fail-visible StorageException + 损坏降级空 map）。**②推导合并**：`TradingLotService.derive` 末尾合并覆盖（重建 TradingLot 的 stopLossPrice）——不污染流水（投影侧覆盖），推送/行为标注/批次视图/复盘全部经 derive 自动跟随；effectiveStopLoss 语义 = **覆盖 > 流水止损 > 默认 −7%**。**③端点**：`PUT /trading/lots/{lotId}/stop-loss`（body stopLossPrice >0 且 ≤4 位小数，lotId 推不出 404）+ `DELETE` 同路径（清除覆盖回退；幂等 404）。**④web**：批次明细弹窗改 StatefulWidget，持有批次止损行「✎ 改」→ 输入/清空回退 → PUT/DELETE → 重拉刷新（_fmtStop ≤4 位去尾零）；ApiService updateLotStopLoss/clearLotStopLoss。**⑤app 不做止损编辑**（低频管理操作，沿用「管理去 web」引导，如实记录）。测试：后端 1100→**1115**（OverrideRepo 6 + LotService 4 + Controller 5，3 处旧构造调用补参）· 端点 105→**107** · 决策文档该项标「已按推荐实施」。**未部署** | 后端 1100→1115 · 端点 105→107 |
| 2026-09-04 | 行情数据包导入批（MD17，晚间自主批——用户睡觉期「能做就做」，无拍板点）| **admin 行情数据包导入 UI 上线，替代手工 scp（task-log MD17 出表）**。**①后端**：`TdxDataPackageImporter`（infrastructure/market，`adai.market.tdx-path`）——zip 流式遍历**只按 basename 收集 `(sh\|sz)\d{6}.day`**（天然防 zip-slip，兼容 `vipdoc/` 嵌套与 `lday/` 混装两种布局），单条 ≤32MB / 单包 ≤30000 文件（zip-bomb 护栏）；逐文件用 `TdxFileKlineSource.parse` 校验可解析（坏文件列入 `failed` 不落盘不阻断——读取端回落网络源）；校验后按 sh/sz 前缀分流，`.tmp` + `ATOMIC_MOVE` 覆盖（读方 mtime 缓存只见完整文件）；空包/无 .day/全坏/非 zip → 400 人话。**②端点**：`POST /api/v1/admin/market/tdx-import`（multipart file，AuthFilter role=admin 门控自动覆盖）→ `{status, filename, imported, skipped, failed[], markets{sh,sz}, dayFilesAfter}`。**③application.yml** multipart 上限 env 化（`ADAI_MAX_FILE_SIZE`/`ADAI_MAX_REQUEST_SIZE`，图片默认 5MB 不变，生产行情包配 512MB）。**④adai-admin**：`file_picker` 依赖（^8.0.0）+ 系统「维护」页签「行情数据导入」卡片（选 .zip → withData → 长超时上传 → 结果摘要含失败清单）；ApiService 重构——`_raw` 单一底层 client + 常规 15s / 上传 10min 双超时包装（注入 MockClient 测试可 mock）；SystemStore 接口 + SystemApiStore/FakeSystemStore 同步。**⑤admin main.dart 遗留 onUnauthorized 补传（2026-09-04 原注释）随批归拢提交**。文档：api-spec v3.42 + task-log MD17 ✅ + trading-features §八13 + status。测试：后端 1090→**1100**（importer 7 + AdminController 3）· admin 49→**52**（SystemApiStore 3，analyze 0）· 端点 104→**105**。**未部署**（外向动作待用户确认；部署时 .env 需加 `ADAI_MAX_FILE_SIZE=512MB`/`ADAI_MAX_REQUEST_SIZE=520MB`） | 后端 1090→1100 · admin 49→52 · 端点 104→105 |
| 2026-09-04 | CORS 顺序修复批（2026-09-04 生产 admin 打开即 CORS 报错刷屏根因） | AuthFilter `@Order(LOWEST_PRECEDENCE + 1)` 整数溢出为 `Integer.MIN_VALUE` 反成最高优先级 → 跑在 CorsFilter 之前 → 401/403 响应丢 CORS 头 → 浏览器把未登录/无权限误报为 CORS policy 错误。修复：CorsFilter 改 `FilterRegistrationBean` 显式 `Ordered.HIGHEST_PRECEDENCE` 先写 CORS 头 + AuthFilter order 修为 `HIGHEST_PRECEDENCE + 10` 紧随其后；WebConfig/AuthFilter 注释双份标注历史坑防复犯；回归测试 3（401/403 带 ACAO ×2 + filter 顺序契约防溢出复犯） | 后端 1072→1075 |
| 2026-09-04 | 活跃市值区间开关批（用户拍板「指南针活跃市值=一切的前提，由我来判定多头/空头，app/web 两端都得有，一起上线」）| **把择时判定权还给用户**。背景：2026-09-03 对话确立「指南针活跃市值=一切的前提」（决定多空区间→仓位松紧→操作手法），但阿呆按 0AMV/通达信口径讲、current.md 靠 6/26 一条 OAMV 规则永久锁死「空头」，用户无法表达自己的判断。**①文档先行**：api-spec v3.41（GET/PUT `/trading/market-stage` 契约）+ 设计文档 `docs/architecture/trading-market-stage.md` + feature-reference/trading-features/_index 登记。**②后端**：`TradingMarketStage`（bull/bear 两档）+ `TradingMarketStagePort` + `TradingMarketStageRepository`（`data/{userId}/trading/market-stage.json`，per-user 条带锁原子写，P0-1 写失败抛错）+ GET（无记录 → exists=false）+ PUT（非法 400 人话）；`TradingSessionPushService.readMarketStage` 改**三级读取：用户手动判定 → current.md → 「择时状态未知」**——用户设了推送即以用户判定为准，不再被 OAMV 推断覆盖。**③双端 UI**：Web/App 交易页「活跃市值（指南针）」红绿切换条（多头=红/空头=绿/未判定灰 + 手动·时间副文案；乐观更新 + 失败回滚 + 连点守卫）。**④本地预置** `market-stage.json` = 空头（当前实际状态）。测试：后端 →**1090**（+15：Repo 7 + Controller 6 + 推送三级 3，cleanTest 实测）· web 148→**150**（+2 渲染/切换）· app 151→**152**（+1 渲染/切换）· 端点 102→**104** | 后端 →1090（+15）· web 148→150 · app 151→152 · 端点 102→104 |
| 2026-09-03 | 生产部署批（用户「推送/部署」确认）| **git push origin（a4f875b..4c61089，54 commit 同步远端）+ 生产部署**。**①后端**：`bootJar` + `deploy-gate.sh`（GATE-BEFORE guard 三件全 PASS）→ jar 装入重启（`/auth/me` 401 = 服务健康需登录）。**②web**：`flutter build web --dart-define=API_BASE_URL=https://api.adaiadai.com` → tar 上传 → 备份切目录（`/opt/adaios/web.bak-20260903195935`，fonts 保留）→ 公网验证 `https://adaiadai.com` 200、`api.adaiadai.com` 401 健康。**③deploy.sh 修复**：旧 .env（#178 前模板无 `ADAI_SMOKE_ACCOUNT/PASSWORD`）在 `set -euo pipefail` 下 grep 无匹配致部署误报 FAIL（jar 已装却 exit 1）——加 `\|\| true` 容错，无 smoke 密码走「跳过自动重建记忆」分支正常完成；服务器 .env 补两行（password 留空，自动 rebuild 待用户配 smoke 密码）。GATE-AFTER smoke 需 `ADAI_SMOKE_PASSWORD`（用户账号密码）——留待用户配置后可全自动；当前登录态验证由用户在浏览器完成（登录后可见：web 自选「案例相似 N%」+ 买点徽章红色 + 后端案例缓存/JSON null） | —（测试数不变） |
| 2026-09-03 | P2-UI1 买点徽章配色决策批（用户拍板 A 方案）| **买点提醒徽章 darkGreen → darkRed**（REVIEW P2-UI1 出表）——「买=该买=涨=红」，与「红涨绿亏」硬规则全局一致，不再与「绿=亏」语义撞色。双端配色表各改 1 处：app `feed_card.dart` / web `desktop_feed_card.dart`「买点提醒」行（原 RFC 20260817「买点绿」决策被本次拍板推翻，徽章文案与样式结构不变）。测试：app 151 / web 148 全绿（无断言徽章色，数字不变） | —（测试数不变） |
| 2026-09-03 | 案例未修项修复批（用户「可修复不需决策的直接改」）| **REVIEW 案例库未修项 6 项全出表（功能/契约零改动）**。**①P2-案例1** 窗口不足 60 根（停牌/新股/标注日近窗口起点）→ `ma()` 用可用根数近似致 MA60/黄白线态失真——设计文档 `trading-case-library-design.md` §4.1 + `CaseFeatureExtractor.extract` javadoc 注明已知限制（失真随窗口缩短增大、特征可算不精确）；windowComplete 显式标记列为后续项（不改 schema 避免已落盘案例/匹配引擎连锁迁移）。**②P2-案例2** web 自选 Tab 未适配 `buyPoint="case"`（渲染「case 0%」异常）——`BuyPointDto` 解析 caseMatches + 信号列 case 渲染「案例相似 {N}%」橙色（取最高相似度，弱参考区别于规则命中红）+ 图例补「案例=形态接近历史完美买点」；dto_parse_test + trading_page_test 各 +1（「case 0%」回归防住）。**③P2-案例3** scanWatchlist 每跑全量 `caseRepository.list`（index+逐文件读）——`WatchlistBuyPointService` 加案例库 TTL 缓存 5 分钟（per-user ConcurrentHashMap 不可变快照 List.copyOf；开关关零 IO 不触缓存；标注/回填后最多延迟 TTL 生效，15:10 扫描与 15:35 回填错峰可接受）；+2 测试（两次扫描 repo.list 只 1 次 / 开关关从不读）。**④P2-案例5** `indexEntry` verify 缺失存 0.0 占位 → 改落 JSON null（真实 +5d 恰为 0% 与「数据不足」可区分；web 列表读案例文件 verify 已 null/0 兜底「—」；回填调度器 15:35 回填 + rebuildIndex 自愈已上线）；+1 仓储测试锁 JSON null 语义。**⑤P3-案例1** `trading_case_insight` 未登记模型路由——DeepSeekAiClient.modelFor javadoc + AiTraceContext source 清单显式登记归 flash（默认分支行为无变化）+ 路由测试 `modelFor_tradingCaseInsightUsesFlash` 防误升 pro。**⑥P2-2** 复核 `.claude/settings.local.json` 无旧 IP 残留出表（2026-08-29 已清）。测试：后端 1068→**1072**（+4）· web 146→**148**（+2）· app 151 不变，三端全绿 | 后端 1068→1072 · web 146→148 |
| 2026-09-03 | 交易记忆定位同步批（RFC 20260902 落地 A+B 档）| **RFC 20260902「交易模块定位重设——从建议引擎到交易记忆」全量落地（用户拍板后开工；功能/契约零改动，历史记录/旧 RFC 不动）**。**A 档 文档/注释**：**①手册 `trading-features.md`**：§〇 模块定位改交易记忆（五层记忆模型：①事件②模式③案例④规则已建成、⑤认知层待建；建议引擎等确定性判定机制 = 已建能力非模块目的）；§6 标题「交易建议（核心定位：建议引擎）」→「持仓解读（已建机制，认知层形态待建）」；推送设置补 RFC §四 熟人开号指引（建议类对熟人默认关、owner 保留全开，per-user 开关零代码）；§八 补第 13 条「数据职责分层」红线（个人业务数据归 web 用户端、全局行情归 admin/运维侧，app/web 永不出现行情数据导入）；related 加 20260902。**②`feature-reference.md`** §9 补模块定位导语。**③`api-spec.md`** advice 端点标题「交易模块核心：建议引擎」→「持仓解读（建议引擎机制，已建能力）」+ 正文「建议是输出不是指令」→「输出是解读不是指令」+ 规则参数注释「建议硬约束」→「纪律硬约束」（契约与响应结构零改动）。**④`framework-plugin-gap.md`** S-6 措辞更新。**⑤代码注释**（行为零改动）：`TradingAdviceAppService.java` / `TradingController.java` / `TradingAdviceAppServiceTest.java` / app `trading_page.dart` 顶部定位注释同步 RFC 20260902 口径。**B 档 UI 可见文案（用户拍板「照见你」化）**：app 持仓空态「开始给你建议」→「开始懂你的交易」、建议弹层「查看/收起建议依据」→「查看/收起依据」、「阿呆暂时没有具体建议」→「阿呆暂时没有特别要说的」、止损提示去「建议引擎」字眼；web 规则 Tab「建议硬约束」→「纪律硬约束」标签；测试断言同步（`pages_widget_test.dart`）。**推送默认逻辑（RFC §四）拍板 A 方案：不改代码**（per-user 开关 + 手册指引够用）。**RFC 20260902 status draft→approved**（_index.md 同步）。**⑥Guard**：Java `./gradlew test` PASS + app `flutter test` 151 全过 + META-GUARD/ALIGN 待跑 | — |
| 2026-09-02 | #178 admin 登录改造批（admin 管理口并入统一登录，退役 X-Admin-Token）| **REVIEW #178「admin 管理口鉴权薄弱」根治——admin 并入统一登录**。**①后端（services/adai-core）**：AuthFilter 统一鉴权——管理端点 `/api/v1/admin/**`、`/api/v1/accounts/**`（例外 `/accounts/available`）不再豁免，一律 Bearer 会话校验；admin 范围额外要求会话账号 role=admin（非 admin → 403「仅管理员账号可访问」）；admin 会话保留客户端 X-User-Id（控制台跨账号治理浏览），user 会话仍覆盖为会话 userId；AdminAuthInterceptor 与 WebConfig 注册、`adai.security.admin-token`/env `ADAI_ADMIN_TOKEN` 全部退役删除；AccountController 响应改经 AccountView DTO 过滤 passwordHash（bcrypt 不下发）+ PATCH 保留既有 passwordHash（修复改 enabled 即清密码的 bug）+ PATCH 支持 password 字段重置密码（≥8 位，kickSessions 踢除该账号全部会话）+ 建号仍支持可选初始密码；AuthService 新增 findAccount/kickSessions。**②前端 adai-admin**：登录页 LoginPage（账号密码 + 首访 setup 引导 + role=admin 门禁——普通用户登录提示「不是管理员账号」）；main.dart 会话门——token 持久化（AdminSessionStore，localStorage 键 admin_auth_token，与产品端 auth_token 区分防同源互顶）+ 启动 /auth/me 校验（含 role=admin）+ 401 全局回登录页 + 退出登录；ApiService 删 X-Admin-Token 头改 Authorization Bearer（新增 login/setup/logout/authMe/changePassword，登录失败 401 除外）；AdminShell 真实 store 全经带 token 的 ApiService 构建 + 顶栏会话菜单「修改密码（POST /auth/password）/退出登录」+ 默认浏览用户=登录账号；账号页每卡「重置密码」按钮（当前登录账号自身隐藏并引导顶栏改密）+ 建号表单新增可选「初始密码」；api_config/serve_web.sh 删 ADMIN_TOKEN。测试：后端 1063→1068（+5：删 AdminAuthInterceptorTest 8；AuthFilterTest 重写为统一门禁矩阵 13→19；AccountControllerTest +7 passwordHash/重置密码用例）· admin 34→49（+15，analyze 0 issues）| 后端 1063→1068 · admin 34→49 |
| 2026-09-01 | 登录体系批（用户「按推荐走；直接全部执行完」）| **根治 REVIEW #179 X-User-Id 零鉴权（RFC 20260901-auth-login）——域名上线后实测数据裸奔（P0）**。**①后端**：AuthController 5 端点（login/logout/me/setup 首访一次性/password 改密踢会话）+ AuthFilter（Bearer 校验 + UserIdHeaderRequestWrapper 覆盖 X-User-Id——92 处 Controller 零改动，伪造 header 无效；无 token 一律 401 fail-closed；accounts/available 免鉴权封死）+ SessionRepository（data/accounts/sessions.json 原子写，token 只存 SHA-256，30 天滑动续期）+ Account.passwordHash（bcrypt，spring-security-crypto）+ 登录限流（5 次失败锁 15 分钟按 IP+账号）+ admin 建号可带初始密码。**②web**：登录页（账号密码 + 首访 setup 引导）+ token 存 localStorage + ApiService Bearer/401 全局回登录页 + 壳底部退出登录（替代切换账号）。**③app**：登录页（移动端）+ token 存 shared_preferences + Launcher 退出登录 + iOS 编译验证。**④部署**：后端 deploy + setup 抢注 adai 密码（随机强密码，用户登录后自行改密）+ web 重建上线；**公网实测全链路通过**（无 token 401 / 伪造 X-User-Id 401 / 带 token 伪造被覆盖为会话 userId / 登出后 token 失效）。**⑤deploy-gate smoke 改带 token**（P1-A4 关闭）。测试：后端 1011→1061（+50：AuthController 13 + AuthFilter 11 + AuthService 17 + SessionFileRepo 9）· app 144→149（+5 auth_flow）· web 146（widget_test 重写为会话流程）· api-spec v3.40 §0/§0.1 | 后端 1011→1061 · app 144→149 |
| 2026-08-31 | 双止损位批（用户「需要两个止损位，计算出来的，人工设置的」）| **交易风险管理方案落地**（docs/reference/trading-risk-plan.md：风险预算 → 止损推导 → 满仓度闸门 → 止损生命周期 → 五问门禁 + 166 笔清仓数据画像）。**①生产 3 仓补设止损**（广发 19.80 / 中国稀土 55.42 / 亨通 61.59，公式 min(R÷市值,5%)，备份 positions.md.bak-20260831-riskplan）。**②双止损位系统能力**：Position 加 `computedStopLossPrice`（系统计算止损：R=本金×1%、距离=min(R÷单仓市值,5%)、止损价=成本×(1−距离)，动态算不落盘）+ `effectiveStopLoss`（生效=max(人工,计算) 取更严格）；getPositions 按本金快照注入计算止损（行情失败仍返回）；R66/接近止损/建议引擎/推送/复盘统一改用生效止损；**③web** 持仓表止损列双值展示（主值=生效，人工/计算有差异时副行标注来源）+ PositionItem DTO 解析测试扩展。测试：后端 1004→1011（+7：Position effective 3 + AppService 计算止损 3 + MarketAlert 生效止损 2）· api-spec v3.35 | 后端 1004→1011 |
| 2026-08-31 | 案例库数据使用方案批（用户授权当晚全做：双轨画像+负样本+后验回填）| **「数据如何使用」落地**（docs/architecture/trading-case-data-usage.md）。**①双轨画像**：CaseConsensus `buildProfile(cases, type)` B1/B2 独立 25-75 分位（互不稀释，负样本排除）；CaseSimilarityEngine `topN` 类型过滤 + FAILED 查询 + 权重可配置（`adai.trading.case.sim-weights`，非法回落默认）。**②负样本**：`CaseRecord.TYPE_FAILED`（失败案例，标注入口 web 下拉支持；不参与正样本画像/匹配，单独失败画像 + match `failedSimilarity` 风险警示）。**③后验回填**：`CaseVerifyBackfillScheduler` 每日 15:35 重算缺失 verify（+5d/+10d）回填落盘 + 顺带 `rebuildIndex`（修复文件与 _index.json 摘要不一致）。**④match 双轨响应**：`{type, b1:{hits,total,similarity}, b2:{...}, failedSimilarity}`，类型=命中多的一轨；annotate 共识校验改同类型画像。**⑤web**：匹配弹窗双轨判定卡 + 失败警示 + 标注类型下拉（B1/B2/失败案例）+ FAILED 行红色 + 列表 +5d null→「—」（P2-案例5 半修）。测试：后端 993→1004（+11：双轨共识 3 + 相似度类型/权重 5 + 回填调度 3 + Controller 双轨断言 1）· web 145→146 | 后端 993→1004 · web 145→146 |
| 2026-08-31 | 类型识别部署批 | **解析器类型识别上线生产**：deploy-gate（smoke 6/6）；生产 32 案例类型已全（B1 14 + B2 18）——以后导入新笔记自动带 B1/SB1/B2 类型 | — |
| 2026-08-31 | 导入类型识别批（用户「昂利康 b1，补上」）| **①生产案例按笔记补全类型**：分析 B1/B2 笔记归属 → 批量更新 32 案例（B1 14 + B2 18；新瀚新材 8-01/8-04 两买点、国盛证券=B2 文件国盛金控、昂利康用户定 B1）。**②解析器类型识别**：CaseImportParser 行内 `B1/SB1/B2` 标记 → ImportItem.buyType → 导入自动带类型（无标记 unknown 待定）；AppService.importCases 透传 buyType。测试：后端 991→993（+2 类型解析）。**待部署生产**（解析器改进） | 后端 991→993 |
| 2026-08-31 | 批量导入生产部署批（用户确认「部署」）| **批量导入上线生产**：deploy-gate（smoke 6/6）+ 本地全 A 名称表（data/market/names.json，新浪源 5211 只）上传 + **本地已导入 26 条案例同步生产**（data/adai/trading/cases）+ web 重建（批量导入按钮，全量字体）。**生产验证**：案例列表 29 条 ✓、批量导入幂等（瑞达期货 skipped 已存在、缺日期 skipped）✓、web 200 ✓。**5 条失败分析**：百普塞斯=错字（正确「百普赛斯」301080）、昂立康=无此股票（002365 是永安药业）、国义招标/路桥信息/奔朗新材=北交所 920（本地行情仅沪深）——用户修正名称或忽略即可 | — |
| 2026-08-31 | 批量导入批（用户 B1/B2 完美案例笔记）| **几十上百案例逐条标注太慢 → 批量导入**：`POST /cases/import`——`CaseImportParser` 解析笔记（`## 名称【缩写】`+日期行 / `## 名称[缩写_日期]`、飞书转义 `\-`、8 位日期、区间取首、类型分组跳过、缺日期/无此股明确报告）+ 名称转代码（**本地全 A 名称表** `data/market/names.json`（新浪源 5211 只）精确匹配 → 东财 suggest 兜底；百普塞斯→百普赛斯错字、昂立康不存在、北交所 920 无行情均清晰失败）+ 逐条标注（含前复权/特征/共识校验，幂等跳过已存在）。**实测导入 B1+B2：26 条 ok（含双创 688/301），5 条 failed 原因清晰**。web 案例 Tab「批量导入」弹窗（粘贴 → 结果列表 ✓/⏭/✗）。测试：后端 980→991（+11：解析 7 + 名称表 1 + 导入 1 + 端点 2）+ web 145。**待部署生产** | 后端 980→991 · 端点 96→97 |
| 2026-08-30 | 全量生产部署批（用户确认「部署」）| **案例库全链路 + 数据底座上线生产**（12 个提交：环 1-4/图表/搜索/TDX 源/前复权/指标一致/共识/案例校验/配套）。**后端**：deploy-gate 全流程——GATE-BEFORE guard 3/3 PASS → jar（27MB）上传重启 → GATE-AFTER smoke 6/6 全过；.env 补 `ADAI_TDX_PATH=/opt/adaios/data/market/tdx` + `ADAI_ADJ_PATH`。**TDX 数据**：9367 个 .day（185MB）上传解压至 /opt/adaios/data/market/tdx（sh 4922 + sz 4445，chown adaios）。**生产验证**：标注 600519 2025-11-03 走 TDX 本地 + 前复权（回撤 3.4%、KDJ.J 11.4）+ 搜索 gzmt→贵州茅台，清理完成。**web**：flutter build（API_BASE_URL=生产）+ canvasKit/字体补丁 + 全量字体（1.9MB）→ 原子替换 8082（首次 tar strip 多套一层 web/web 修正为 strip=2，字体/补丁验证 200）。**遗留**：admin（:8083）本轮未动（无相关改动）；TDX 每日盘后同步需用户重新打包后跑 sync_tdx_data.sh | — |
| 2026-08-30 | 配套批（建议 #5-7）| **①TDX 数据抽查** `ai-engineering/09-scripts/verify-tdx-data.py`：随机抽样 A 股个股，TDX 最新交易日收盘 vs 腾讯 qfq（最新日前复权=原始价同口径）——**实测 10/10 通过，通达信数据源可靠**。**②盘后同步脚本** `scripts/sync_tdx_data.sh`：通达信盘后数据打包（7z/zip）→ 解压按文件名前缀分流（sh/sz）覆盖 tdx/（实测 9367 文件幂等）；修 bug：`cp -n` 不覆盖已存在 → 改覆盖、`sh` 不支持进程替换 → for 循环。**③全 A 因子预热** `ai-engineering/09-scripts/prewarm-adj-factors.py`（可选，分批 50+0.5s 限速防风控；懒加载已覆盖主要场景）。**未部署**（外向动作待用户确认） | — |
| 2026-08-30 | 案例质量校验批（建议 #4）| **标注时防脏案例进库**：`POST /cases` 响应加 `consensusCheck`（落盘后基于既有案例库共识画像评估新案例——逐维命中 + 偏离维度；案例 <5 → 无）；标注响应结构改 `{case, consensusCheck}`（Map.of 不接受 null → HashMap）；web 标注成功且命中率 <50% → 提示「这个案例和你的完美买点画像有偏差」（显示偏离维度 + 值 vs 共识区间，不阻止——用户是权威）。测试：后端 977→979（+2 annotateWithCheck）+ web 145 全绿。**已本地重建部署（8082）** | 后端 977→979 |
| 2026-08-30 | 共识判定批（建议 #3，核心价值）| **「根据数据代替图形判定」深化——从案例库统计学习「完美买点画像」**：`CaseConsensus`——案例库 ≥5 时每特征（回撤/量比/KDJ.J/距60日线/MACD柱/盘整）取 **25-75 分位**为共识区间（不硬编码 B1/B2 参数，从真实完美买点归纳）；`POST /cases/match` 响应加 `consensus`（逐维命中 ✓/✗ + 当前值 + 区间 + 命中 N/M 维；案例 <5 → null）；web 匹配弹窗共识卡（≥50% 命中绿框）。分位数线性插值 + 维度样本不足跳过 + null 特征跳过。测试：后端 971→977（+6：CaseConsensus）。**已本地重建部署（8082）** | 后端 971→977 |
| 2026-08-30 | 前后端指标一致批（用户「数据正确性」建议 #2）| **指标序列单一事实源——前端图不再自行重算指标**：后端 `IndicatorSeriesCalculator`（MA5/10/20/60 + KDJ 9,3,3 + MACD 12,26,9 全序列，与 CaseFeatureExtractor 特征同口径；KdjIndicator 补 series）+ `GET /trading/cases/{id}?indicators=true` 随详情返回；前端 CaseKlineChart 直接用后端序列（hover 单日指标值 = 案例特征值，双算不一致从根上消除），CaseIndicators.fromJson + 前端重算仅作兜底。**真实校验**：000831 买点日特征 KDJ.J 8.685 vs 序列 8.69、MACD -0.59 vs -0.59 同源一致。测试：后端 966→971（+5：序列计算）· web 145 全绿。**已本地重建部署（8082）** | 后端 966→972 |
| 2026-08-30 | 前复权批（TDX 数据正确性，用户拍板「先写文档，然后开工」）| **不复权 TDX 数据在除权日跳空 → 回撤/特征/相似度失真——统一前复权口径**（设计 trading-data-adjustment.md）。**①算法**：`AdjustmentCalculator`——每事件除权参考价 refPrice=(除权前收盘×10−派息×10)/(10+(送+转)×10)，每根 K 线因子 = 其日期之前所有事件 factor 乘积（标准前复权，最新价基准，与腾讯 qfq 同口径）。**②因子表**：`AdjFactorRepository`——东财 RPT_SHAREBONUS_DET 分红送转拉取（`SEND_RATIO/CASH_RATIO` 对纯派息为 null → **方案文本正则解析** `10派X元/10送Y转Z派W`）+ 本地缓存 `data/market/adj/factors/{symbol}.json` + 按日 TTL。**③集成**：`TdxFileKlineSource` 读取即换算（TDX 无因子 → 原样）。**④真实校验**：茅台（27 事件，2025-12-19 派 23.957/2026-06-26 派 28.024/股）34 根 K 线 vs 腾讯 qfq **全部 ≤0.5% 偏差**（12/18: 1374.51 vs 1379.02）。**⑤修 bug**：URL `%22` 被 `String.formatted` 当格式符抛异常 → 改 URLEncoder 编码 filter。测试：后端 952→966（+14：算法 8 + 因子 6）。**未部署**（外向动作待用户确认）；全 A 因子预热脚本后置 | 后端 952→966 |
| 2026-08-30 | 验收反馈批 2（三副图固定 + 单日指标查看）| **①副图布局**：主图下**三个副图固定**（成交量 / MACD / KDJ 纵向四区，主图 40%+各 20%）——替代原「副图单区可切换」（用户实际 4 个副图，先三区）。**②单日指标查看（通达信十字光标味）**：鼠标悬停某根 K 线 → 主/副图左上角数值标签实时跟随该日值（MA/成交量/MACD/KDJ）+ 全高光标竖线；离开恢复默认（买点日/最新日）。主图 MA2/MA4/裸K 切换保留；窗口移动（◀▶）保留。图表默认高 320→400。web 145 全绿（测试改三副图断言）。**已本地重建部署（8082）** | web 145 |
| 2026-08-30 | 验收反馈批（标的搜索 + K 线窗口移动）| **①标的搜索**：`GET /trading/search?q=`（东财 suggest：代码/中文名/拼音首字母，如 gzmt→贵州茅台）——`NameToSymbolResolver.search` 候选列表（A 股 6 位过滤，最多 10）；标注/匹配弹窗 `_SymbolSearchField` 搜索输入（防抖 300ms + 候选下拉 + 选中回填）。**②K 线窗口移动**：详情图默认只显示「买点前 60 根 ~ 买点后 3 天」（后验涨幅不再压缩前期形态），◀▶ 按钮移动查看后续走势（0-30 天步进 5）；Y 轴按显示窗口计算。测试：后端 948→952（+4：parseCandidates 2 + search 端点 2）· web 144→145（+1 搜索契约）；端点 95→96；api-spec v3.38。**已本地重建部署（8082）** | 后端 948→952 · web 144→145 · 端点 95→96 |
| 2026-08-30 | 通达信风格图表（验收反馈）| **主/副图左上角指标数值标签 + 指标可切换**（CaseKlineChart 重构）：主图 MA2（白黄，黄白线语义）/ MA4（5/10/20/60 标准配色）/ 裸K 三选 + 副图 KDJ/MACD/成交量 三选（PopupMenu 点指标名切换）；主图标签「MA10:xx MA60:xx」、副图标签「KDJ(9,3,3) K:xx D:xx J:xx」等取最新值；CaseIndicators 补 MA5/MA20。web 143→144（+1 指标切换测试）。**已本地重建部署（8082）** | web 143→144 |
| 2026-08-30 | TDX 通达信本地行情源（用户提议「全量给数据」）| **行情源风控的稳定替代——通达信本地数据第一优先**。背景：验收时腾讯 fqkline 对本机 IP 风控（curl WAF / 后端空），东财兜底也不稳；生产 IP 此前也被东财限过（08-24 主源切换）。**实现**：`TdxFileKlineSource`（解析通达信 `vipdoc/sh|sz/lday/*.day`：32 字节/条小端 int32——date/OHLC(×100)/amount/volume(股→手)/reserved；kline 最近 N 根 + klineRange 日期直查；mtime 缓存失效；文件缺失/损坏 → 空不抛错）+ `KlineService` 优先级 **tdx → 腾讯 → 东财**（tdx 无数据不算失败，走网络源；配置 `adai.market.tdx-path`/`tdx-enabled`，生产 `ADAI_TDX_PATH=/opt/adaios/data/market/tdx`）。**待用户提供数据**：通达信 `vipdoc/sh/` + `vipdoc/sz/` 打包 → 放数据目录。**已知限制**：.day 为不复权原始价（除权日跳空，第一版接受）；最新到上次下载盘后数据（定期同步）。测试：后端 942→948（+6：解析/最近N/范围过滤/缺失/缓存失效/半条损坏）。**未部署**（外向动作待用户确认） | 后端 942→948 |
| 2026-08-30 | 案例库批次审查修复（S1）| **审查发现战略级问题修复**（报告 audits/2026-08-30-case-library-review.md，**降级：子代理环境故障，主会话四视角顺序审**）：`TradingCaseFileRepository.save` 写案例文件成功但 `_index.json` 写失败时，`catch (StorageException)` 提前重抛**跳过回滚** → 文件残留 + 下次标注 `exists(文件)=true` → 409「已标注过」卡死（无路可走）；修复 = 统一 catch（所有异常先回滚删案例文件再重抛）+ 回归测试 `save_indexWriteFailure_rollsBackCaseFile`。登记 P1-案例1（腾讯 klineRange 日期格式待实测）+ P2-案例1~5 + P3-案例1（详见 REVIEW）。测试：后端 938→939 | 后端 938→939 |
| 2026-08-30 | 案例库环 4 二期（扫描接入 + 黄白线参数化）| **判定当下接入每日扫描 + 指标近似配置化**。**①扫描接入**：`WatchlistBuyPointService` 开关 `adai.trading.case.scan-match`（**默认 false**，向前兼容）——开启时每只自选股附「与案例库相似度 Top 3」（复用 CaseSimilarityEngine，无规则命中但案例相似 → `buyPoint="case"` 参考类型，**15:10 定时推送跳过该类型**防误推 B2，web 可见）。**②黄白线参数化（批 5 前置）**：`CaseFeatureExtractor.extract` 重载接受黄/白线均线周期，`TradingCaseAppService` 读 `adai.trading.case.yellow-ma`/`white-ma`（默认 60/10）——用户自定义指标语义近似可调，公式源码到位后替换精确实现。测试：后端 935→938（+3：WatchlistBuyPointService 开关关/开/空库）。**未部署**（外向动作待用户确认） | 后端 935→938 |
| 2026-08-30 | 案例库批 1 环 4（判定当下——核心价值）| **「判定当下某个标的的买点」兑现：当前形态 vs 完美买点案例库相似度匹配**（设计 trading-case-library-design.md P3 环 4）。**①归一化**：`CaseFeatureNormalizer` 7 维特征 min-max → 0-1（回撤/量比/KDJ/MACD/距 60 日线/盘整/信号计数；KDJ/MACD null → 中性 0.5，防数据缺失拉偏）。**②相似度**：`CaseSimilarityEngine` 加权欧氏（权重：回撤 0.25/量比 0.20/KDJ 0.15/距 60 日线 0.15/MACD 0.10/盘整 0.10/信号 0.05，和=1 → 距离∈[0,1] → 相似度=(1−距离)×100%）；Top N 降序、无特征案例跳过。**③端点**：`POST /trading/cases/match`（body {symbol, date?}）——当前标的拉 60 日 K → 特征 → 与案例库 Top 5（caseId/相似度/后验/AI 摘要）；date 空=最近交易日，指定日无数据回落最近；空库 → matches:[] 静默降级（不影响规则判定）；K 线失败 400。**④web**：案例 Tab「匹配买点」按钮 → 输入代码 → 相似案例列表（相似度 ≥80% 绿框提示 + +5d 后验参照）。**双轨语义**：案例相似度是经验增强参考，不覆盖规则硬判定（止损/仓位仍走规则引擎）。测试：后端 921→935（+14：SimilarityEngine 7 + AppService 3 + Controller 4）· web 142→143（+1）；端点 94→95；api-spec v3.37。**未部署**（外向动作待用户确认）；15:10 扫描接入 + buy-points 附 caseMatches（二期，开关默认关）；相似度阈值待用户观察案例库有料后拍板 | 后端 921→935 · web 142→143 · 端点 94→95 |
| 2026-08-30 | 案例库批 1 环 3（AI 理解）| **「为什么这是完美买点」从人话到结构化——LLM 案例理解落盘**（设计 trading-case-library-design.md P2 环 3 第一部分）。**后端**：`POST /trading/cases/{caseId}/insight`——LLM 读特征画像（9 特征）+ K 线统计摘要（前窗口高低点/均价 + 后验走势，token 友好不喂全量）+ 用户描述 → 结构化输出 `{summary, keyFeatures[], confidence}`（prompt 判定优先级：形态>量能>指标>位置）→ aiInsight 落盘（reviewed=false 待人工确认）；LLM 失败/非 JSON/summary 空 → 400「AI 理解生成失败」不落半成品（fail-visible）。**web**：详情弹窗「生成 AI 理解」按钮（POST → 本地刷新）+「阿呆的理解」卡片（summary/keyFeatures/置信度）。测试：后端 914→921（+7：AppService 4 + Controller 3）· web 141→142（+1 契约）；端点 93→94；api-spec v3.36。**未部署**（外向动作待用户确认）；批量归纳（≥20 案例）后置 | 后端 914→921 · web 141→142 · 端点 93→94 |
| 2026-08-30 | 完美买点案例库批 1（第四阶段环 1-2，用户拍板「开工」）| **「案例是手段，判定当下是价值」第一步落地——完美买点案例库环 1-2 最小闭环**（RFC 20260830-trading-perfect-case-library.md + 设计 trading-case-library-design.md）。**①K 线日期范围支持**：`KlineSource.klineRange(symbol, from, to)`——腾讯/东财覆写为数据源日期参数直查（param 带 start/end），默认实现拉 320 根截取；`KlineService.klineRange` 主源→兜底同熔断语义（案例标注历史任意日期可用，不再受「最近 N 根」限制）。**②指标**：`MacdIndicator`（EMA12/26 → DIF/DEA9 → 柱 + 金叉，与 KdjIndicator 同级）。**③特征提取**：`CaseFeatureExtractor` 9 特征全从 OHLCV 重算（距前 20 日最高收盘回撤 %/量比 3÷5 日均/KDJ.J+金叉/MACD 柱+金叉/MA 关系 close vs MA20·MA60/距 60 日线 %/黄白线态 touch·near·above·below + 白线(MA10)在黄线之上=开门/盘整天数/破前高）+ 后验窗口（+5/+10 日收益、最大回撤、破默认 −7% 止损，缺数据 null）。**④存储**：`TradingCaseRepository` domain 端口 + `TradingCaseFileRepository`（`data/{userId}/trading/cases/{buyDate}_{symbol}.json` + `_index.json` 清单原子写 + per-user 条带锁 + 损坏文件视为不存在；格式定 JSON——对齐项目数据惯例，设计文档 §3.1 注明实现修正）。**⑤端点**：`TradingCaseController` 4 个（POST/GET /trading/cases、GET/DELETE /trading/cases/{caseId}，门控/隔离同 trading）；降级：K 线失败 400 不落半成品（fail-visible）、buyDate 无交易数据 400、重复标注 400、后验不足 null。**⑥文档**：api-spec v3.35（4 端点契约）、freeze §2.17（cases 契约）、trading-features §9 端点 + §10 案例库章节、feature-reference、status（914/93/17 Controller）、change-log 本批。测试：后端 894→914（+20：FeatureExtractor 4 + Repository 6 + Controller 10）。**P2 前端（同日完成）**：CaseKlineChart 三区 K 线图（蜡烛 + MA10 白线/MA60 黄线 + 买点标记 + 量 + KDJ/MACD 前端重算）+ 交易页第 7 Tab「案例」（标注/列表/详情/删除）+ ApiService 四方法（web 133→141，+8）。**未部署**（外向动作待用户确认） | 后端 894→914 · web 133→141 · 端点 89→93 |
| 2026-08-30 | 推送口径修复批（用户反馈「休息日别推 + 广发通知」调查 → 拍板「开工」）| **行情推送三修（A/B/C）**。**A 节假日守卫（B5-1 残留）**：`MarketAlertService.poll()` 补 `isTradingDay`——B5-1（08-23）只补了 TradingSessionPushService，本服务漏了：节假日撞工作日时行情静止，break-cost/stop-loss 类只比「价格 vs 阈值」→ 拿上一交易日收盘价每天重推一次（signature 按日去重挡不住跨日，长假可连推 7 天）；mockStatic 回归（isTradingDay=false → 不查行情不推送）。**B 早盘昨收口径（用户实测歧义）**：早盘 9:15 未开盘，行情接口返回上一交易日收盘，`buildDataText` 无日期标注 → LLM 把昨日涨幅写成「今日涨跌」；修复=数据文本按节点标注（早盘「昨收/昨日涨跌」，午间/尾盘「现价/今日涨跌」）+ 提示词第 5 条写明口径；测试捕获 ContextPackage.recordContent 断言早/尾盘标签互斥。**C 轮询首轮 10:00**：cron `9-11,13-15` → `10-11,13-15`（CRON_POLL 常量化）——9:00/9:30 旧数据冒充「今日」（生产 08-27 09:00 实锤：中国稀土「今日跌 -3.11%」实为周三跌幅）且按日去重签名被旧数据烧掉名额、盘中真触发同类型反而不推；首轮 10:00（开盘 30 分钟）一并根治措辞与去重。**调查副产物**：上周五广发无行情提醒=正确判定（-0.79% 不达 -3% 线、未设止损、未破成本），非漏推；早盘「今日」歧义在生产 08-27/08-28 实锤复现。测试：后端 890→894（+4：MarketAlert 2 + SessionPush 2）。**已部署**（用户确认「部署」）：deploy-gate 全流程——GATE-BEFORE 3/3 PASS → jar（27M，15:56 构建）上传重启（15:57 启动 4.4s）→ 记忆重建 → GATE-AFTER smoke 6/6 全过 | 后端 890→894 |
| 2026-08-30 | 续问重复回答修复批（用户反馈「重复回答」，14:43 生产对话实测）| **流式问答批 2 追加型重复缺陷根治（app+web 双端同款）**。**根因**：`_appendToActiveCard` 文本续问分支 meta 定稿时**追加**新 AI turn，而流式草稿已占最后一个 AI turn → 同一回答渲染两遍；点 end 时客户端把本地 turns（含重复）全量上报（生产实证：Conversation end turns=**8**，服务端卡片仅 **6** turn，多出 2 个正是两轮续问的重复草稿；服务端数据本身干净）。**修复**：定稿时最后一个 turn 是 AI（草稿）→ 原位替换，否则追加（无增量降级同步路径没有草稿 turn，追加语义不变）。**测试教训**：初版回归用同步脚本 SSE 未复现（post 内同步完成 → 90ms 节流 Timer 被 finally cancel → 草稿从未渲染），改**门控回放**（delta 渲染草稿后挂起，放行才回 meta）后反向验证变红（未修复时草稿残留 findsOneWidget）→ 修复后绿。测试：app 143→144 · web 132→133。**已部署**（用户确认）：iOS 重装（19.5MB 签名 4G3D37YKSB，devicectl install + launch 验证成功）+ web 重建部署（补丁校验 + 线上 200）；后端零改动 | app 143→144 · web 132→133 |
| 2026-08-30 | 全量生产部署批（用户指示「重新部署所有后端服务，最后移动端」）| **流式问答批（7c8572b）等全量上线生产**：①后端 `deploy-gate.sh` 全流程——GATE-BEFORE 3/3 PASS → jar（26M）上传重启 → 记忆重建 → **GATE-AFTER smoke 6/6 全过**；②web 重建部署（CanvasKit/字体补丁 + 原子替换 admin.new/web.new 模式 + 重启 ：8082/:8083，线上验证 index 拦截器 + canvasKitBaseUrl 注入均生效）；③admin 同法重建部署（烧录 ADMIN_TOKEN）；④**iOS 重装**：`flutter build ios --release`（19.5MB，签名 4G3D37YKSB）→ devicectl install + launch 验证成功（首次 launch 因手机锁屏失败，解锁后重试通过）。**含修复**：web `sse_client_web.dart` 构造签名对齐 IO 变体（接受可选 `httpClient` 参数，fetch 实现注入不了故忽略）——原 `SseClient(httpClient:)` 在 dart2js 下编译失败（流式问答批只验了 VM 侧，条件导入 web 变体漏改，analyze/test 均测不到）；web 132 全绿回归。**遗留观察**：adai-app `sse_client_web.dart` 同样缺参（iOS 走 IO 变体无碍，app 若构建 web 目标会同报错，待下批对齐）| web 132 全绿 · 端点 89 |
| 2026-08-30 | 流式问答批（P2-用户2 批 2，用户拍板「开始吧」）| **「边问边出字」全链路落地——后端 SSE 端点 + adai-app/adai-web 双端流式渲染**。**①后端 `POST /records/ask-stream`**：SSE 事件流（`text` 增量 / `meta` 定稿 recordId·summary·tags·domain·content / `[DONE]` / `error` 人话）；仅问答（显式 question / cardId 续聊），auto-intent 新输入仍走同步端点（批 3）；后端内降级——模型整轮无增量输出回退同步 understand 一次（`JsonTailFilter` 防 JSON 回执跨块切开：每块留尾字符、flush 补发）；同卡同问 5 分钟去重（`QuestionAppService.findDuplicateResend`，去重/建卡逻辑从 RecordController 迁入 service）；UTF-8 byte[] 透传防中文乱码（`StringHttpMessageConverter` 默认 ISO-8859-1，生产同样会乱——byte[] 走 ByteArray 转换器原样透传）；ask-stream 专用线程池（2-4，队列 32，满 503）。**②双端 SseClient**：三文件条件导入（`sse_client.dart` 分发 / `_io.dart` http.Client.send 流式 / `_web.dart` fetch ReadableStream——dart http 浏览器实现无渐进响应 / `_common.dart` 异常+行解析器）；askStream（text→onDelta / meta→RecordResponse 定稿 / error→SseServerException 人话透出 / 未收增量失败自动降级同步端点一次且 intent 同构透传——首问 question、续问 auto-intent）。**③渲染接线**：app main_page 三处（_doAskRequest 首问 / _createNewCard forcedIntent==question / _appendToActiveCard 文本续问）+ web feed_page 两处（_doAskRequest / 文本续问）；90ms 节流草稿（onDelta 每 token 一块，批量 flush 防全页 setState 风暴）；meta 到达以 content 定稿替换草稿 + 刷 feed/标签/记忆缓存。测试：后端 874→890（+16：AskStreamController 5 / JsonTailFilter 5 / QuestionAppService answerStream+dedup 6）· app 137→143（+6）· web 127→132（+5）；端点 88→89；api-spec v3.34。guard-meta/align 双 PASS。**未部署**（外向动作待用户确认） | 后端 874→890 · app 137→143 · web 127→132 · 端点 88→89 |
| 2026-08-30 | 审查修复批（deep 深审 5 官发现，用户拍板「修全部」）| **交易插件规则层审查发现全部修复**（报告 docs/review/audits/2026-08-30-trading-rule-layer-review.md）。**P0×2**：①保存链路 fail-visible——`TradingRuleSettingsRepository.save` 写盘失败抛 StorageException（原吞异常返回 updated=true）、PUT 空 params 400、NaN/Infinity 400、params 非 Map 400（原 CCE 500）；②建议硬约束防掏空——`constraintRuleMin/Max` 限制只能在 R66-R95 内收缩且恒 min≤max（原 min>max 产生空区间 → LLM 无硬约束 fail-open）。**P1×6**：①止损判定线统一——`effectiveStopLoss(lot, userId)` 补传（行为标注与推送口径一致）+ 批次视图 toView 透传；②知识注入同步脚本 `os/trading-engine/09-scripts/sync-adai-rulepack.sh`（os/ 五文件 → data/adai/trading/knowledge.md + rules.yaml 参数校验）；③多用户知识泄漏——`TradingKnowledgeSource` owner 白名单（仅 adai 回落 os/，非 owner 无私有知识不注入，B3 红线）+ 回归测试×2；④domain 依赖 infra——`TradingRuleSettingsPort` 接口（C7 分层红线）；⑤降级语义定稿默认值兜底（Javadoc + 蓝图 §8.2 同步）；⑥前端保存反馈/数值校验/恢复默认/exists 消费（默认 vs 已自定义区分）。**P2×8**：per-user 条带锁（并发 PUT 防丢）、double NaN/Infinity finite 校验、knowledge.md 内容哈希缓存（73KB 不再每次读盘）、save 读改写保留未知键（rules/signals/behaviors 不被抹）、买点参数防轰炸下限（回调≥10%/KDJ≤50/放量≥1.1x）、evaluatePosition 文案区分「你的/默认仓位上限」、引擎 per-user TTL 缓存（5s）、AdaiRulePackRealFileTest 改测试内构造（不依赖真实 data/）。**P3**：死代码常量清理（TradingLotService DEFAULT_* ×4、TradingAdviceAppService CONSTRAINT_* ×2）、PUT 接受字符串数字（与 GET 对称）、Controller 空提交/NaN/params 非 Map 回归测试×4。**文档×13**：api-spec v3.33 头部版本、buy-points 契约改按用户规则 + 主源腾讯、蓝图阶段编号/端点 40→42/规则文件名统一/决策点拍板记录、feature-reference 六 Tab、trading-features 规则引擎按用户、change-log 明细 22、freeze JSON Schema 如实、REVIEW frontmatter、v3.21 行序。**D-14 current.md 脱敏登记遗留待用户确认**。测试：后端 864→874（+10）· web 127 全绿，guard-meta/align 双 PASS | 后端 864→874 · web 127 |
| 2026-08-30 | 交易插件规则层批（第三阶段，用户拍板「按建议来」+「继续」）| **「每个人有自己的交易系统」落地——交易插件从 adai 专属系统演进为通用插件 + 个性化规则层**（蓝图 trading-plugin-architecture.md）。**①规则文件基础设施**：`TradingRuleSettings`（16 参数：仓位上限 25%/默认止损 −7%/浮盈回吐 20%·50%/短线超期 5 天/清仓阈值 5%·5 天/买点 5 参 0.5·0.7·13·1.5·20/打分权重 0.5·0.5/硬约束区间 66-95，fail-closed 非法回落默认）+ `TradingRuleSettingsRepository`（`data/{userId}/trading/rules.yaml` 读写，SnakeYAML SafeConstructor，损坏/非法 → 默认值）。**②引擎按用户**：`TradingRuleEngine` 仓位上限、`TradingLotService` 行为标注阈值（浮盈回吐/短线超期）、`SoldTradeVerdict` 清仓阈值（参数化+规则引用，默认 R66/R53 保前端契约）、`BuyPointDetector` 买点 5 参、`SoldScoreService` 打分权重、`TradingAdviceAppService` 硬约束区间——全部从用户规则读，无规则 → 默认 = adai 现状（降级不坏）。**③知识注入用户私有（D1）**：`TradingKnowledgeSource` 优先读 `data/{userId}/trading/knowledge.md`，os/ 作 adai 默认。**④端点**：`GET/PUT /trading/rules`（参数读/部分字段覆盖）。**⑤降级验证**：`TradingRuleDegradationTest` 5 条（无规则用户 = 纯客观）。**⑥adai 规则包生成**：`data/adai/trading/rules.yaml`（全默认值）+ `knowledge.md`（os/ 五文件合并），真实文件加载回归（行为不变）。**⑦web 规则 Tab**：交易页第 6 Tab「规则」——16 参数中文标签展示 + 编辑弹窗（表单化 PUT）+ 加载失败降级。测试：后端 840→864（+24：规则测试新增 22 条含降级 5/仓库 8/引擎 2/行为 2/verdict 4/真实文件 1 + 既有测试构造器适配；guard-align 实测口径）· web 125→127（+2），三端全绿 + META-GUARD PASS + GUARD-ALIGN PASS。**未部署**（外向动作待用户确认）| 后端 840→864 · web 125→127 · 端点 86→88 |
| 2026-08-29 | 收盘小结 + 流式基础设施批（用户拍板「先修复不需要拍板的」）| **①P2-用户3 交易价值**：每日收盘小结 15:30 推送（Bark 已接直达手机）——当日成交（`getTradeHistory` 过滤股息流水 volume=0，不误计买卖笔数）+ 破止损持仓（`evaluateStopLoss` 判定）+ 待确认候选 + 一句话收尾（破止损→明早按 R66 / 有操作→建议复盘 / 无操作→按计划持有），模板聚合不耗 AI；新推送类型 `close-summary` 入 `PushSettings.ALL_TYPES` + app/web 双端推送开关（可单独关）。**②P2-用户2 流式后端基础设施**：`StreamingAiClient` 端口（kernel，依赖倒置）+ `DeepSeekAiClient.streamGenerate`——body 加 `stream:true` 复用完整聊天上下文，SSE `data:` 行解析 delta.content 逐块回调、完整文本累计返回，非 200/空内容抛 RuntimeException（调用方降级非流式重试，ai-calling-governance 降级矩阵）；本地 SSE stub 测试 ×2（逐块拼接/HTTP 500/请求带 stream 断言）。**待下一批**：SSE 端点（SseEmitter）+ app/web 前端流式渲染。测试：后端 835→840 | 后端 835→840 · app 137 · web 125 |
 **REVIEW B 类 8 项纯技术债全部清理（跳过需拍板项）**：①**P2-UX4 确认流反馈**——回执改阿呆口吻（「好，N 笔已经记进账了」app main_page/trading_page/web feed_page 三处）+ `_ActionButton` 组件（提交中 loading + 禁用、成功后灰态「已确认 ✓」本地兜底，action 完成卡与 push 确认卡统一）；②**P2-UI7 launcher 插件行稳定槽位**——插件加载完成前渲染「加载中…」等高位占位，加载后原地更新，行不跳位（audit P2-3）；③**P2-UI6 World B 下滑误触搜索根治**——下滑返回手势只挂拖拽条（44pt 热区），不再覆盖搜索栏 tap 大目标（四官命中结构性根因）；④**P2-UX2 规则术语图例**——web 自选区块加「B1=回调缩量低吸·B2=放量突破右侧」、清仓区块加「R66=亏超5%扛单没走·R53=短持久持亏损；买点分=入场时机·执行分=纪律执行」（清仓统计行本有人话）；⑤**P2-UI5 双端字号下限**——9/10/10.5px 全局提到 11px；⑥**P2-UI8 触达 ≥44pt**——关键交互（拖拽条/返回箭头/日历格/删除钮/行内小按钮）扩触达；⑦**P2-UI9 硬编码色值收敛**——双端散落 `Color(0x...)` 映射到 `app_colors.dart` token；⑧**P2-3 weekly-audit cron 挂载**——每周一 9:00 `--auto` 追加日志，脚本 W1-W5 验证跑通（与 guard-cost 日账同 crontab）。测试：app +2（UI6/UI7 回归，135→137）· web 125 全绿 | 后端 835 · app 135→137 · web 125 |
| 2026-08-29 | 晚间自主修复批（用户睡觉期间，明早复查）| **针对 REVIEW.md 未修项的可自主处理项批量修复，跳过需用户拍板项（179 登录体系/S7/P1-交易9/P1-交易20/P2-UI1/P2-用户1-3）**。①**历史成交导入三连（用户 08-25 反馈）**：P2-批次4 多文件批量——`_HistoryImportDialog` FilePicker 多选 → 逐份入队处理 → `aggregateImportResults` 聚合（计数求和/对账行去重/summary 取首份 sync）；P2-批次5 进度反馈——队列状态区逐份展示（第 N/共 M、处理中、耗时、成功/跳过/失败，单文件粘贴路径不变）；P2-批次6 尊重源文件——股息类资金事件（volume=0 + reason=源文件备注）前端显示「股息入账/红利税」类型标签取代「买入 0 股」，发生金额 ±、数量/价格/编号/费用 '—'，统计行「买 X 卖 Y · 股息/红利 N」排除股息事件。②**P1-前端1**（web P1-7 同类在 app 复发）：`_loadData` catch 分支——已有持仓/账户数据时刷新失败保留旧数据，仅首载失败显示错误页（30 分钟自动刷新/交易后 _refresh 网络抖动不再整页「请求失败」）。③**P2-UX1**：NL 解析回显 `_fmtPriceInput` ≤5 位去尾零（原 toStringAsFixed(2) 截断 4 位成本价）。④**P2-UI4**：自选买点信号列 ConstrainedBox maxWidth 170 + ellipsis（'、' 多条件不再撑宽整列）；清仓统计标题行 Row→Wrap（窄窗口自动换行不 RenderFlex 溢出）。⑤**后端并发/日志技术债**：P2-交易25 confirm 残余窗口根治——TradeLogRepository.saveMerging 锁内原子「读最新→合并保留集→写回」（latest 读后 save 前窗口闭合，+2 真实仓储测试）；P2-交易28 AccountSnapshot/TradeLog/MarketPush 三处 ConcurrentHashMap 锁池改固定 16 条带锁（#179 任意 userId 撑爆内存根治）；P2-交易27 MarketPush 写失败 warn→error（与账目落盘同级别）；P2-交易33 缺行情收盘跳过时推送「账户今日未自动更新」通知（新股/停牌无昨收不再长期无感，受推送开关门控）；P2-交易24 closeAccountUpdate 残余窗口注释如实化。⑥**P2-交易31（U32）**：principal=0 时 totalPnl 返回 null——双端显示「—」+「未设本金」提示（不再回落浮盈漏已实现盈亏的清仓误导），web statCard 支持 null。⑦**P2-A3 残留**：deploy-gate.sh guard.sh 输出不再 `/dev/null`（HIT 摘要/自坏区分，与 pre-commit 同款）。⑧**REVIEW 残留核实出表**：P2-交易26（B7-3 已同步 §667）、P2-交易29（C5 已收敛同锁）、P2-交易30（C2 已修正 2027 中秋）、P2-交易32（C3/C4 已修空 400 + name 校验）、P2-2（.claude 旧 IP 已清）。测试：后端 +2（saveMerging×2）· app +3（P2-UX1 精度/P1-前端1 保留旧数据/P2-交易31 不给误导数值）· web +3（股息渲染/聚合/股息识别）；三端全绿。**未部署**（外向动作待用户确认） | 后端 833→835 · app 132→135 · web 122→125 |
| 2026-08-27 | 截图缺日期禁落库批（用户拍板，v3.32）| **用户反馈「今日 4 笔其实是昨天」二修**：8-27 早截图入账把昨日（8-26）委托误记成今日成交、误生成今日复盘。首修（v3.31）只把 `entryDate` 从「确认当天」改为候选 `tradeDate`，但截图无日期列时仍回退当天——**二修拍板：截图候选（source=image）缺 `tradeDate` → 禁止落库**（计入 skipped、候选保留、failures 人话「缺少成交日期」），不再回退确认当天；新增 `PUT /trading/trade-log/date` 补写候选成交日期（补后再次确认正常落库，成交日 ≠ 确认日不再记错）；文字归集（当日口语）保持回退当天。**app**：候选行缺日期橙色警示 +「补日期」按钮（日期选择器 → PUT date）+ 确认前拦截提示。**生产数据修正**（用户确认）：8-27 误记 4 笔流水 entryDate 改 8-26 + 补真实成交时间（14:55:43/14:55:55/14:56:09/14:56:27）、positions 3 只 entryDate 改 8-26、删错误 8-27 复盘（has-activity 8-27=false 验证）。测试：后端 +9（confirm 截图缺日期跳过保留/混合场景/补日期后正常落库/补日期 API 各状态码）、app +1。**已部署生产验证** | 后端 818→833 · app 129→130 · 端点 85→86 |
| 2026-08-27 | 自选导入覆盖策略批 + CORS 双保险（工作树遗留补提交）| **①自选导入「合并 upsert → 全量覆盖」**（用户拍板，覆盖+归档）：自选 = 最后一次导入的镜像，通达信删除的自选随之消失；导入前旧列表自动归档 `watchlist.json.bak-<ts>`（清仓股文件误导入 170 只污染事故的直接诱因）；同名条目保留原 addedAt（首加日），仅新增记今天；**格式校验**：缺形态列（非自选导出，如清仓股文件）→ 解析空 → 业务异常 400 + 人话提示（不再静默 no-op）。**②CORS 双保险**：除 MVC 层 `addCorsMappings` 外注册 `CorsFilter`（servlet filter 层）——405/404/500 异常响应也带 CORS 头，避免浏览器把「GET 打到 POST 端点」的 405 误报为 CORS policy 错误（当日线上误报根因）。测试：后端 +9（覆盖/保留 addedAt/归档/格式拒绝 + 解析器核心列校验） | 并入上批计数 |
| 2026-08-26 | 截图入账 + 复盘卡点批（用户拍板：交易闭环）| **用户三问落地：①持仓转圈保留；②复盘没卡点（has-activity 是关键词扫描对话记录，不是真实成交）→ 改「当日真实成交 > 0」才可生成复盘，与截图入账绑成闭环；③交易页加「📷 截图入账」一等入口（用户核心工作流就是发截图，对话框发图太绕）**。**后端**：①`POST /trading/screenshots`（multipart 1-3 张）——VLM 识别 → 归集器 parseLooseBatch 批量提取当日候选，**不建记录/不落原图/不沉淀记忆**（截图入账是交易动作不是记录动作，不污染 Feed/时间线，候选确认落库即权威数据；与 POST /records/media 关键差异）；跨图 sameTrade ±10% 自动去重；逐张失败降级 errors 透出；②`hasTradingActivity` 废除关键词扫描（聊到"买/仓/股"即误报、导入成交后反而不报），改 `getDailyTradeSummary().count > 0` 真实成交口径。**app（交易页）**：③快记区「截图入账」入口（相册多选限 3 张）→ 上传 → 当日候选内嵌列表（每笔 方向徽标/名称/价格/数量 + 丢弃 ×）→「全部确认入账」→ 持仓即时刷新 + 复盘横幅触发；④复盘卡点：无当日成交时 AppBar 复盘按钮点了给「今天还没有导入成交，先截图入账吧」提示，不空转 AI（横幅由后端口径自动收敛）；⑤`getTradeLogCandidates` 类型化（TradeLogCandidateDto）+ 新 DTO（TradingScreenshotResult）。**测试**：后端 +14（ScreenshotAppService 8：单图/多图/超3张/空/失败降级/非图/超限/部分失败 + Controller 4：返回候选/无插件403/service拒绝400 + has-activity 真伪 2）；app +4（候选渲染确认/丢弃/上传归集注入/复盘卡点）；后端 800 全绿 · app 129 全绿。**待部署**：后端 jar + app 重装（iOS）| 后端 786→800 · app 125→129 · 端点 84→85 |
| 2026-08-26 | 三问题修复批（对话卡片/推送/超时，用户开工）| **用户三痛点开工修复 + 生产部署验证**。①**Bark 推送 4 次全失败（时段推送）**：`escape()` 只转义 `\` `"` 漏 `\n`——LLM 多行正文拼进 JSON 字面量 → Bark Go 严格解析 400。修复：逐字符转义 `\n`/`\r`/`\t`/`\b`/`\f` + 控制字符 `\uXXXX`，回归测试（本地 HTTP server 捕获 body 断言无裸换行 + Jackson 可解析）。②**对话超时→卡片重复**：前端 AI 90s < 后端最坏 120.6s（60s×2）→ 前端先断 → 重发 → 同卡 3 条相同 turns（REVIEW S-9 链条复现）。修复：后端单次 45s（45×2+0.6=90.6s）+ 前端 AI 90s→120s（app+web 代码已改，待打包）+ **重发去重幂等**（同 cardId 最近一轮用户问句相同 + 5 分钟窗口 → 直接返回已有回答，不 append 不烧 AI；穿透 AI turn 判定，生产端到端验证命中）。③**模型分层（S-10）**：`deepseek-v4-flash`（快，无长思维链）接高频交互（question/log/brief/intent/media/trading_advice/parse/session），`deepseek-v4-pro` 只留复盘（trading_review）；按 `AiTraceContext.source` 路由（scene() 全是 trading 不可用），新增 `adai.ai.model-flash` 配置 + `AiTraceContext.source()` 读取；**实测 v4-flash 支持 response_format=json_object**（flash 也有思维链但短），understand 接 json_object 消灭「未找到 JSON 降级」；意图识别/brief/generate 保持非 JSON（裸词/正文场景）。**生产验证**：问答/记录 `model=deepseek-v4-flash`（秒级~十几秒）、复盘 `model=deepseek-v4-pro`（70s 深度推理）；去重命中日志确认。④**排序口径（S-8 拍板：最新在底部）**：web `feed_page.dart` ListView 加 `reverse:true` + itemBuilder 从尾部取 + `_loadMore` 更早页改插数组头部（原追加尾部压住最新 → 顺序错乱根因），与 app 聊天式一致；app 本就最新在底。⑤**生产数据清理**：晚间对话卡 card_1787736935528 重复 turns 24→20（删 2 组相同「交易市场如何玩铜呢」轮次，备份 /tmp 兜底）。测试：后端 +6（Bark 换行转义 1 + 模型路由 3 + 去重 2，实测 786）；REVIEW S-8/S-9/S-10 出表 | 后端 786（含本批 +6）· web/app 代码待打包 |
| 2026-08-26 | 截图表格归集批（用户场景：白天发委托截图 5 笔，0 候选）| **用户白天发「当日委托」截图（5 笔：4 已成 + 1 已报 + 1 申购）→ 交易候选 0 条，截图归集没接住表格文字**。根因：归集器 `parseLoose` 按**单笔一句话**设计（"买入 亨通 300股@64.84"），券商委托截图被 VLM 识别成**表格文字**（一行多笔）拆不出 → 静默跳过。**修复**：①`TradingParseAppService.parseLooseBatch`——正则按「名称 代码 价格 买卖 数量 状态」逐行提取多笔；**状态过滤**：只归集「已成」（含"成"字：已成/部成），「已报」（未成交）/「已确认」（非交易）跳过；**新股申购/认购**名称（天博申购 732448）跳过；占位代码（79/80/81/82）跳过（与历史成交导入同口径）；②`TradeLogCollectService.collect` 优先走批量解析，命中多笔逐笔归集（复用 `append` 的 sameTrade 去重：同 symbol+方向+volume±10% 跨截图自动去重），空结果回退单笔宽松解析（一句话场景不变）。**生产验证（用户今天真实截图文字）**：一次归集 4 笔已成候选（广发买 200@21.17/亨通买 300@64.84/稀土买 100@56.04/有研卖 600@50.33），已报云南/申购天博正确滤除；重复发送同笔 → 候选仍 4 条（去重生效）。测试：后端 +8（parseLooseBatch×4：真实截图 4 已成过滤/纯文本空/已报+已成状态过滤/空输入；collect×4：表格多笔归集/跨截图去重/全非成交忽略/脏行跳过）。**待用户**：候选确认落库（`POST /trade-log/confirm`）即同步今天调仓 | 后端 772→786（含工作树遗留未提交测试 +6，本次 +8） |
| 2026-08-25 | 导入卡住排查批 | **用户反馈导入卡住**——日志定位：23:44:31 导入 20260101 文件后 **107 秒静默无日志**（其他文件 6-16 秒/份），前端 90s 超时后用户重试。**数据无损**：2026 年 1-3 月成交 8/23 已导入（423 条含股息记录完整，华泰红利税已记账）。**改进**：importAppend/importSync/applyDividendCash 加**耗时日志**（含锁等待，毫秒级）——下次卡住能精确定位是锁等待/幂等扫描/落盘哪一段 | 后端 772 |
| 2026-08-25 | 股息类记账批（用户拍板方案 A）| **用户问「股息红利/股息入账如何处理」→ 现状是忽略（只计 nonTrades），现金账目不完整**（分红没进、红利税没扣）。**方案 A 实现**：①解析器读备注列 + 发生金额（HistoricalTradeRow 加 occurred/remark）+ 识别股息类（备注含 股息/红利/入账）；②**智能分隔修复真实 bug**——通达信 tab 分隔且空列保留，原 split(`\\s+`) 吞空列 → 股息行（无成交编号）列错位 → 发生金额解析到股东代码崩（`A511358384` NumberFormatException）；③导入时股息类记账：入账 +现金 / 红利税 −现金（不动持仓/批次，落 volume=0 流水 reason=源文件备注可回溯，symbol+日期+金额绝对值幂等）；④批次推导/行为分析加 volume≤0 守卫（股息流水不进批次不除零）。测试：后端 +1（股息入账 +80/红利税 −7.5/买入链式现金 44071.93 + 幂等不重复）；**用户重新导入历史成交即补记此前被忽略的股息**（买入行 orderId 幂等跳过，股息行无幂等键首次落账）。**生产部署验证 + 回滚记录**：实测导入真实格式股息行——华泰红利税 -7.5 正常记账（流水 volume=0 可回溯、批次无 0 股、现金已扣）；但验证时误带一条**虚构的**浦发股息入账 +80 → 已回滚（删流水 + 现金 -80 + 本金恢复 150000 + 清转账记录，账目一致）——教训：生产验证只用用户真实文件内容，不构造虚构数据 | 后端 771→772 |
| 2026-08-25 | 历史成交列精简批（用户反馈）| **用户反馈：历史成交 Tab 费用/成交编号挤一块 + 省略号 + 止损/买点/原因三列全是占位**——历史成交是通达信原始成交单，无止损/买点/原因字段。**修复**：删 止损/买点/原因 三列；**再按用户拍板调整列序**——成交金额/发生金额（源文件原生，买入负扣款）在前、成交编号（源文件标识）、系统计算的「费用」（=｜发生金额−成交金额｜）单独放最后区分开；测试断言更新（含发生金额 −5,061.23 推导验证）。web 122 全绿 | web 122 |
| 2026-08-25 | 导入校验批（非股票占位代码拦截）| **用户反馈：明显非股票代码被照单入库**——`799999 登记指定`（通达信占位段，非真实股票）此前经历史成交导入进流水+持仓。**修复**：`TradingImportParser.isNonTradableCode` 识别 79/80/81/82 开头 6 位占位代码；`importHistoricalTrades` 入口过滤——不落库、计入 `nonTrades`（前端「非交易 N」可见）+ WARN 日志列示例；存量 `799999` 已按用户授权从生产 positions/流水清除（见清理批）。测试：后端 +2（isNonTradableCode 识别 799999/80/81 段 + 不误伤 600519/000725/512690；导入含 799999 → 不落库计入 nonTrades）。**生产验证**：deploy-gate 全流程（smoke 6/6）+ 实测导入含 799999 文件 → `imported:0 / nonTrades:1`（拦截生效不入库）| 后端 769→771 |
| 2026-08-25 | 生产数据清理批（799999 登记指定）| **用户确认后远程清理生产数据**：`799999 登记指定`（通达信账户「股份登记指定」占位，非真实持仓，8/23 历史成交导入带进）从系统彻底移除——①`positions.md` 删行（备份 `positions.md.bak-20260825-799999`）②`trades/2025-04.json` 删该笔买入流水（2→1 条，备份同前缀）③验证：持仓 4 只纯净、批次/对账/流水无 799999、JSON 完整性 OK。数据文件直接修改属 B8/B3 例外（用户明确授权 + 备份兜底）| — |
| 2026-08-25 | iOS app 重装批（批次跟踪功能）| **iPhone 16 Pro Max 重装**：`flutter build ios --release --dart-define=API_BASE_URL=http://82.156.111.146:8080`（19.2MB，自动签名 4G3D37YKSB）→ `devicectl device install` + launch 验证成功。**含批次跟踪 app 端功能**：持仓卡批次简版（N 个批次·最近买入 + 含底仓徽标 + 破止损警示，静默降级）+ 批次行字号 9-10px→11px（P2-批次3）。生产 IP 已烧录 | app 125 |
| 2026-08-25 | 一键同步持仓批（用户场景：中电电机清仓残留）| **生产实测发现 + 修复**：用户导入历史成交后中电电机已清仓但持仓快照残留 1000 股被当初始底仓（旧版 `trades/import` 只补流水不改持仓；新版 sync 也会把过期快照当基线叠加）。**根治三件套**：①derive INIT 判定改进——流水首笔 BUY 且含卖出（闭环自洽）且快照不早于首笔 → 快照多余部分是过期残留不兜底（真底仓按 entryDate 早于流水/无卖出保留）；②新增 `POST /trading/sync` 一键按流水重建持仓（移除已清仓残留 + 保留真底仓 + 报告）；③web 历史成交 Tab「一键同步」按钮（确认弹窗 + 结果 SnackBar）。测试：后端 +6（INIT 判定×2 + sync 服务×2 + controller×2）、web +1；后端 763→769 · web 121→122。**生产部署验证**：deploy-gate 全流程（smoke 6/6）+ web 重建装入重启；`POST /trading/sync` 生产执行 `{positionCount:5, removed:[000725 京东方, 603988 中电电机]}`——中电电机从持仓移除、批次变为两个已清仓回合（8/17 -648 / 8/18 +372），持仓 5 只正确 | 后端 769 · web 122 · app 125 |
| 2026-08-25 | RFC 20260825 批次跟踪生产部署批 | **批次跟踪功能上线生产**（后端 jar + web 重建）：①后端 `deploy-gate.sh` 全流程——GATE-BEFORE 3/3 PASS（guard-meta/align/G1-G7）→ jar 上传重启 → 记忆重建 → **GATE-AFTER smoke 6/6 全过**；②web 重建（`flutter build web --dart-define=API_BASE_URL=http://82.156.111.146:8080`，121 测试产物）→ 备份 `web.bak-20260825` → 原子切换装入 `/opt/adaios/web` → 重启 adaios-web；③**生产验证**：web/API 全 200；`GET /trading/lots` 返回真实批次 11 个（按日合并/初始底仓 `_INIT`/−7% 止损兜底/距止损% 均正确，暴露 4 个破止损批次：有研新材 8/18·8/20、云南锗业 8/17、中电电机底仓——批次级止损推送将单独提醒）；插件门控 403 正常。**待用户**：app 端（iOS）重装后持仓卡批次简版生效（devicectl 或 TestFlight） | 后端 763 · web 121 · app 125 |
| 2026-08-25 | RFC 20260825 逐笔批次跟踪与行为纠偏批 | **批次模型（lot）上线——持仓从「一只股票」细化到「每一笔买入」，破「越套越深」行为闭环（用户场景驱动）**：①**批次推导（纯流水重放不落盘）**——同标的+同方向+同日合并一个批次（一天最多一个买批，成本=当日加权平均含费）；卖出 **LIFO** 先扣最近买入批次、跨批按各自成本分算已实现盈亏（「底仓不动、先走短线」）；批次剩余 0 = 关闭（回合 realizedPnl）；positions.md 覆盖不到的底仓 = 初始批次（`_INIT`，最早建仓最后扣）；②**GET /trading/lots 批次视图**（state=open/closed/all + symbol 过滤，注入现价 + 流水对账提示）；③**导入双模式**——成交都在最近 10 日内 → `syncMode="sync"` 同步持仓/现金/流水（orderId 透传幂等键，recordTradeWithOrderId）；明显历史 → `append` 只补流水（原语义）；④**每日操作总结**（sync 模式响应 `summary`：买卖聚合 + 批次 diff + 行为标注六类：亏损加仓/追高/短线新开/破止损未走/浮盈回吐/短线超期，不耗 AI 秒出）；⑤**批次级止损推送**（现价破批次自己的止损（未设默认 −7% 兜底）→ 单独推「批次止损预警」带批次信息，signature 带 lotId 独立去重）；⑥**推送定时消失**（`pushes/{date}.json` 加 `expiresAt`：行情类次日 09:30 消失（收盘后仍可看）、汇总类次日 23:59；读取过滤 + 写入剔除，用户无需手动删时效推送）。双端 UI：web 持仓 Tab 批次明细弹窗 + 导入总结卡片；app 持仓卡批次简版。修复过程实锤 3 缺陷：derive 只遍历有流水的 symbol 导致空流水时无初始批次、行为分析底仓 entryDate 被加仓覆盖致「亏损加仓」漏判、sync 落盘丢 orderId 致幂等失效。测试：后端 +27（TradingLotServiceTest 12：按日合并/LIFO/回合/初始批次/行为标注/对账/历史日期状态守卫；TradingAppServiceTest +4：sync 同步/幂等/LIFO 扣减/append 补录/行为标注/手动导入交叉防重；TradingControllerTest +5：/lots 视图/过滤/门控/import syncMode+summary；TradingReviewAppServiceTest +1：行为标注注入复盘）；web +9（批次弹窗/导入总结）；app +3（持仓卡批次简版）；**审查修复**：对抗 P0-1 手动/导入交叉防重 + fingerprint 价格归一化、初始批次前置、sync fee 透传、混合窗口拆组、追高 10 日窗口、默认止损文案分级、状态类仅当天注入、行情推送次日 09:30、回合扣卖出费用、reconcile 补持仓 symbol、isExpired 旧数据按类型默认 | 后端 736→763 · web 112→121 · app 122→125 |
| 2026-08-25 | 推送渠道切换批（微信→Bark）| **微信通知关闭 + iOS 原生推送上线（用户拍板）**：①**生产停用 Server酱**——免费版每天 5 条额度不够 AdaiOS 推送量（日志实证天天超限 400 `[AUTH]超过当天的发送次数限制[5]`），`.env` 删 `ADAI_PUSH_WECHAT_SENDKEY` 整行 + 重启，微信渠道静默禁用（代码零改动，`enabled()` 未配置即 false，Feed 不受影响）；②**新增 `BarkPushChannel`**——iOS 原生推送（Bark，免费无限条数直达系统通知），`PushChannel` 插件化第二个外部实现，POST JSON（含 title/body 转义），未配置 key 自动跳过，`base-url` 可配支持自托管；主流程零改动（`List<PushChannel>` 自动纳管）；③**文档/模板同步**——deploy.sh 模板加 `ADAI_PUSH_BARK_KEY`/`ADAI_PUSH_BARK_BASE_URL`，feature-reference/trading-features 渠道描述改 Bark 并标注微信停用（代码保留，恢复需重新配 key）。**待用户**：iPhone 装 Bark App 拿设备 key → 生产 `.env` 配 `ADAI_PUSH_BARK_KEY` + 重启即启用。测试：后端 +4（BarkPushChannelTest：缺 key 禁用/有 key 启用/null 禁用/base-url 回退） | 后端 732→736 |
| 2026-08-24 | 用户反馈修复批（导入分流 + 止损乐观更新 + 腾讯主源）| **用户操作反馈三个问题修复 + 生产部署**：①**导入分流**——页头「批量导入」入口移除（清仓股/资金文本被交易 CSV 解析器校验「买点」拦截的根因）；持仓 Tab 内新增「导入持仓」按钮（只认通达信持仓导出，全量覆盖）；自选/清仓/资金/历史成交各 Tab 保留专属导入按钮；废弃 `_ImportDialog` 删除（交易 CSV 批量导入生产 7 天 0 调用）；`extractApiErrorMessage` 透出本地校验人话（原吞成「网络异常」）。②**止损保存乐观更新**——PUT /positions/{symbol} 保存成功后本地立即更新止损/角色/目标价（不依赖 `_loadAll` 行情注入，此前行情慢→15s 前端超时→页面显示旧值，用户误以为「修改不生效」），后台静默刷新失败不覆盖本地值。③**行情主源腾讯优先（用户确认）**——`TencentMarketDataSource` 补 `KlineSource` 实现；`KlineService` 主源/兜底配置化（`adai.market.kline-primary` 默认 tencent，可切回 eastmoney），生产东财被限不再刷 500+/日 WARN；日志文案按主源名输出。**部署**：后端 bootJar → deploy.sh（服务就绪 + 记忆重建）· web 重建（--dart-define=生产 IP + CanvasKit/字体补丁）→ 装入 /opt/adaios/web 重启；部署后验证：`主源=腾讯` 生效、新进程 0 次东财失败、web/API/CORS 全 200。测试：后端 +1（KlineService eastmoney 配置切换回归）· web 112 全绿（批量导入 Dialog 测试替换为持仓 Tab 导入 2 用例）；顺带修工作区既有编译破损 FeedControllerTest（FeedEntry updatedAt 字段未同步） | 后端 731→732 |
| 2026-08-23 | iOS app 重装批（P1-G6-1 + 体感批）| **手机重装（iPhone 16 Pro Max，devicectl 安装）**：`flutter build ios --release --dart-define=API_BASE_URL=http://82.156.111.146:8080`（19.2MB，自动签名 4G3D37YKSB）→ devicectl install + launch 验证成功。**含改动**：P1-G6-1 timeline_modal mounted 守卫（关闭后响应到达不崩溃）+ app 体感修复批 D1-D9（IndexedStack 保活/排序四修/Feed 错误态/任务编辑 PUT/三态/人话/代际令牌/方向色/时间戳）+ 推送链路 B9-B11 前端改动 + 交易走查前端改动。烧录生产 IP（82.156.111.146:8080） | — |
| 2026-08-23 | 生产部署批（后端 + web 重建）| **部署 4 个待发布批次到生产（commit 6411bb6/79664ce/b317586/a6755e0 + 1a5f5a4）**：后端 `bootJar` 重建（22:58，含历史成交回填 + 交易走查 B1-B7 + 推送 B9-B11 后端改动 690→731 测试）+ `deploy-gate.sh` 部署（GATE-BEFORE 3/3 PASS → jar 上传 → 重启 → 记忆重建 → GATE-AFTER smoke 6/6 全过）；web 产物重建（`flutter build web --dart-define=API_BASE_URL=http://82.156.111.146:8080`，含交易走查 web 改动 + 1a5f5a4 中文框框修复）→ 备份 `web.bak-20260823` → sudo 装入 `/opt/adaios/web` → 重启 adaios-web，首页 200。admin 无新提交跳过。**部署后验证**：三服务 active，feed/timeline/tags/web/admin 全 200。**发现（观察项，非 bug）**：`POST /trading/advice` 生产耗时 **~45-60s**（DeepSeek 生成 4 只持仓建议慢），web 前端 `_aiClient` 90s 超时可覆盖，app 端需确认超时；多次 curl 短超时误判为「丢响应」，实为 AI 生成慢（本地完整路径复现同样耗时）。回滚对比确认非新 jar 回归（旧 jar 同样耗时）。**遗留**：生产旧 IP 已清（.claude 5 处） | — |
| 2026-08-23 | G6 守卫批（P1-G6-1）| **G6 逐点化实锤缺陷修复（commit 待提交）**：`timeline_modal.dart` `_loadTimeline` await 后 setState 补 mounted 守卫×2（成功路径 + catch 降级路径，`if (!mounted) return;`）——Modal 关闭后响应到达不再 `setState after dispose` 崩溃。回归测试×2：①modal 关闭后响应到达不崩溃（Completer 挂起 + barrier 关闭 + 补发响应，**反向验证**：移除守卫测试红 `setState() called after dispose(): _TimelineModalState#... defunct`，恢复守卫测试绿）②正常路径渲染日记录（`textContaining('晨间跑步')` 匹配 `$time  $title` 拼接文本）。status.md 120→122 | app 120→122 |
| 2026-08-23 | 元审核 P2 批（guard 性能 + 逐点化）| **元审核 P1-A3/A1-A5 修复 + G6 逐点化实锤新缺陷**：①**P1-A3 guard-cost 增量缓存**——按会话文件 mtime 跳过未变文件（state/cost-cache.json，gitignore），聚合从缓存桶计算；实测 113 文件 5.35s → 0.044s（**121×**），结果一致；guard-context 25s 超时不再静默降级。②**P1-A5 G6 逐点化**——从「仓库级 mounted 总数统计」改为「async 方法内 await 后 setState 逐点断言」：无守卫即 HIT 定位 文件:行号；同步方法/await 前同步段豁免（18 误报 → 0）；**首战实锤真实缺陷**：`timeline_modal.dart:57/66` await 后 setState 无守卫（全文件零 mounted，旧版总数统计永远 PASS）→ **新登记 REVIEW P1-G6-1**（app 代码，B7 只报告待修）。③顺带：cost-cache.json 补 gitignore。REVIEW 出表 P1-A3/P1-A5 + 新登记 P1-G6-1 | — |
| 2026-08-23 | 元审核 P1 批（对抗审计修复批二）| **元审核未修项 P1/P2 出表 7 项（commit 待提交）**：①**P1-3** audit.md 官表 +1 对抗官（标题/成本描述/README/AGENTS 计数统一 8+1）；②**P1-5** method/README 脚手架标注「⏳ 待建，现为设计稿」；③**P1-6** README 仓库外 ai-engineering-method/ 引用改指仓库内 method/ + 修正注记；④**P2-1** audit.md 归口机制内部矛盾修正（58 行改「audits/ 落盘 + _index 登记」，与 review.md 统一）；⑤**P2-4** frontmatter「9 字段」→「10 字段」全库统一（skills-spec/AGENTS/_index；ADR-005 与 change-log 历史记录不改）；⑥**P2-A2** method/README 状态表对拍（部署门禁/smoke 改 ✅ 已做、定时 audit 脚本已做 cron 待确认）+ research-notes 标注待建；⑦**P2-A3** pre-commit guard.sh 输出区分（HIT 显示摘要、脚本自坏单独报错，实测两分支正确）。另：guard-cost 增量记账回归修正（v1 追加全量同日翻倍实证 94.82 假账 → v2 真增量，commit `7532020`）。剩 P1-A3/A4/A5 + P2-2/P2-3 待后续批 | — |
| 2026-08-23 | AI 上下文工程体系元审核修复批（对抗审计）| **ai-engineering/ 全量元审核（主审核 + adversarial-reviewer 隔离复核 + 5 实证实验，报告 `docs/review/audits/2026-08-23-ai-engineering-meta-audit.md`）修复落地（commit `072dcee`）**：①**guard-meta M4 白名单 AGENTS.local.md**——干净 clone 上 M4 必 FAIL（gitignore 快照不入库，实证修正对抗官 S-A2 机制判断：非 M2 是 M4）；②**新脚本入库**——guard-unfixed.sh / guard-roadmap.sh（原未 git add，S-A3 强制归口依赖不存在脚本）+ 新建 guard-tools.sh；③**_index.md 补登记**（guard-meta/roadmap/unfixed/tools + review-perf，P1-1）；④**pre-commit 隐私闸门加固（P1-A1/S-A1）**——隐私检查前移到类型触发之前（堵 .txt/.json 数据文件绕过，实证修复前 commit 成功 exit 0）+ 新增文件 gitignore 复核（堵 `-f` 强加）+ 删 `--no-verify` 绕过提示；⑤**guard-cost --record 覆盖式改追加**（P1-A2：同日多次记账留痕 + recorded_at + `--log` 按日聚合）；⑥**README 行业定位段修订**（上下文资产>代码层 + .agents/ 非行业标准仅 Qoder 专属约定 + 角色数 8→8客观+1对抗）。新建 `guard-tools.sh`（T1 hook/T2 快照/T3 技能/T4 注册/T5 入口五检，跨工具互通从纸面承诺变可验证状态，替代被否决的静态映射表）。REVIEW 归口 P1×6 + P2×6，未修项待后续批。纯工程/文档改动，测试数不变 | — |
| 2026-08-23 | roadmap S-5 漂移修正批 | **08-20 体检 S-5 落地（guard-roadmap.sh 对拍驱动）**：product-roadmap.md 选号（MD12）状态「⏸ 顺延 v1.0.1」→「✅ 已实现」（app 选号/切换链路已在 120 测试覆盖）——§三能力表/§四 v1.0.0 剩余缺口/§四候选表/§五进入标准 4 处同步 + 文档版本 v1.2→v1.3；新建 `ai-engineering/guard-roadmap.sh`（规划对拍：新鲜度/版本状态/S-5 漂移检查，行级判定）；状态层审查批（guard-context C2 已修过滤 / audit+review 归口强制步骤 / state/_index v2 / issue-log 归档 / guard-unfixed 快照过期提示）| — |
| 2026-08-23 | app 体感修复批（D1-D9）| **2026-08-20 app 体检 P1/P2 + REVIEW UI/UX 落地**：切 World IndexedStack 保活（草稿/对话/上传不丢）+ Launcher 切回刷新；排序四实锤（_loadMore id 去重 / 切回保留更早页 / FeedEntry 透传 updatedAt「刚刚」修复 / 时间线默认日期复核正确）；首载 Feed 失败错误态+重试；任务编辑走 PUT（P-app-08 修复）；launcher 计数失败显「—」；ProjectStatus 三态（spinner+人话+重试）；task/trading 错误文案提取后端人话；task tasks+stats 分离（stats 可降级）；search/timeline 代际令牌；P2-UI2 无快照显「—」；P2-UI3 方向色（买红卖绿）；P2-UX3 快照时间戳。后端 FeedEntry 加 updatedAt（P1-5）；P2-UI1 买点徽章色待拍板 | app 120 全绿 |
| 2026-08-23 | 剩余可修项批（C1-C6）| **隔离审查报告 P2/战略剩余项修复**：C1 confirm 读取锁外合并处理期间新候选（防 save 覆盖清空）；C2 2027 中秋 9/15 修正（原误并国庆）；C3 batch 空 trades 400；C4 batch name ≤32 校验；C5 TradeLog 锁收敛 userId；C6 双锁注释如实化 + trading-features §8 文档化。测试：后端 +3（合并候选/空 400/name 超长）| 后端 728→731 |
| 2026-08-23 | 推送链路修复批（B9-B11，12 项）| **P1-推送1 根因（标题契约断裂）**：MarketPushEvent 加 title 字段 + FeedPushChannel 透传原标题（此前落库丢标题）→ toPushEntry 改用原标题（旧数据 type 兜底）→ 双端「确认并入账」按钮/徽章配色回归生效；gain/break-cost 补专属徽章（原落灰「行情」，P2-推送4）。**P1-推送2（删除无持久化）**：`DELETE /trading/pushes/{id}` 端点 + MarketPushRepository.dismiss（per-user 锁内按 id 移除）+ app 左滑删调后端 + web 推送卡「忽略」按钮（404 幂等）。**P1-推送3（入口 self-lock）**：app 交易页常驻推送设置铃铛。**P2-推送5**：app 推送设置失败透出原因（web B5-6 已修，本批 app 对齐）。**P2-推送6**：session 文案注明含收盘确认。**P1-交易18 接线**：confirm 返回 {confirmed,failed,skipped,failures} + 双端失败明细提示 + `DELETE /trade-log` 丢弃调用。测试：后端 +4 · web +5（确认按钮/忽略/徽章/并存）· app 回归 120 全绿 | 后端 724→728 · web 107→112 · app 120 |
| 2026-08-23 | 隔离审查残留修复批（B6-B7，9 项）| **对 B1-B5 修复批的复核审查（backend+对抗官隔离演示，`audits/2026-08-23-reviewer-isolation-demo.md`）残留修复**。**P0-A**：MarketPush 结构校验升级——`[123]`/`{"a":1}` 合法 JSON 但结构损坏 → 判损坏拒绝写回（原只验语法）；**B6-2**（P1-交易12）：dedupeKey 从字符串桶改 `sameTrade` ±10% 区间判定（10 vs 19 分开、100 vs 110 合并，5 边界测试）；**B6-3**（P1-交易13）：closeAdvice/midday/dataText 三处 changePercent 判空（缺行情显 '-' 不 NPE）；**B6-4**（P1-交易11）：AccountSnapshot 写失败抛 StorageException（不再静默返回 null）+ GlobalExceptionHandler 500 人话 + recordTrade/closeAccountUpdate catch 告警（importCashQuery 显式失败）；**B6-5**（P1-14/17/18）：confirm 单 now 贯穿、`_TabHistoryRefreshListener` 切回历史成交 Tab 静默刷新（keepAlive 防陈旧）、`DELETE /trade-log` 丢弃钉子户候选端点；**B7-2**（P1-交易19）：MarketPush 结构损坏×3 + TradeLogRepositoryTest 并发 append/±10% dedupe/discard×5 真实仓储测试；**B7-3**：api-spec trade-log 契约同步（去重 ±10%/confirm 响应结构/discard 端点）；**B7-1/4**：P1-15 复核不成立（fmt(null)='-' 非 null%）维持登记、P1-16 复核误报（后端阈值早已 -5%）移除。测试：后端 +17（MarketPush 4/TradeLog 5/TradeLogCandidate 5/dedupe 边界等）；REVIEW P1-11~19 出表 6 项、半修 1 项（P1-交易18 前端入口待推送批）、复核 2 项 | 后端 703→720 · web 107 |
| 2026-08-23 | 交易走查修复批（B1-B5，21 项，用户确认后实施）| **web 交易板块全面走查（3 官并行 + 主会话交叉印证）后修复**。**P0×2**：①`TradeLogCollectService.confirm` 失败候选保留不丢（原无条件清空）——成功落库 + 失败/不完整回写，返回 `{confirmed,failed,skipped,failures}`（Controller 透出）；②account.json 写锁统一——`AccountSnapshotFileRepository.update` per-user 锁原子 RMW（recordTrade/importCashQuery/recordTransfer/setPrincipal/closeAccountUpdate 五写路径统一走 update，原 importCashQuery 锁外/closeAccountUpdate 无锁并发互相覆盖）。**P1×5**：direction @NotNull（400 拒写，原 null 静默 200/500 双行为）；batch 逐行字段校验（代码/方向/价格/数量，人话失败）；尾盘模板补 r81Applicable（超 100 万不强制减仓，与建议引擎同口径）；收盘 yesterdayClose 残缺同 price 缺失跳过；AccountSnapshot 写失败 error 告警 + update 返回 null。**前端×3**：资金区块总盈亏 principal=0 失真（复用 totalPnl getter）；历史成交导入错误透出后端人话（顶层 `extractApiErrorMessage`，原 contains 恒 false）；清仓 R53 文案/遵守率口径（verdict 末分支标 R53 延展，R66 文案 10%→5%）。**文档纠偏×5**：buy-point-rules.md 按代码事实重写（P2-交易17 虚标真修：KDJ13/前高窗口/已实现状态）；api-spec 头部 v3.24 + 变更记录补 08-18/08-22/08-23 行 + 去重（P2-交易18 半修真修）；has-activity 唯一例外标注；buy-points 参数 KDJ13；feature-reference §9 同步（cashBalance 真源/五 Tab/三格式导入/推送徽章 P1-推送1 阻断标注）。**P2×6**：节假日 2026 官方 + 2027 预测（原缺 2026 春节等 + 误记 10/8 休市）；MarketAlert 止损与涨跌判定拆开（changePercent 缺失不漏 R66）；TradeLog dedupeKey 加 volume 10% 桶（同股两次买入不互吞）；TradeLog save 加锁（confirm 与 collect 并发不清新候选）；MarketPush 读失败拒写回（防覆盖历史推送）；web 历史成交 Tab keepAlive + 推送设置失败提示。**测试**：后端 +11（confirm 保留/并发无丢失/写失败 null/昨收缺失/超100万 R81/久持 R53/节假日×2/batch 校验/direction/verdict）· web +3（B2-2/3、B3-5）；REVIEW/change-log/api-spec/trading-features 同步 | 后端 690→703 · web 104→107 |
| 2026-08-23 | 历史成交 Tab + 导入回填批（RFC 20260823）| **用户实测「全量重传 8 月不更新」根因修复 + 历史成交升级常驻 Tab**：①**导入回填**——`importHistoricalTrades` 从「纯幂等跳过」升级为「幂等去重 + 缺失成交时间回填」：已存在 orderId 且旧记录 tradeTime=null、新文件带成交时间 → 回填该笔（`TradingHistoryFileRepository.backfillTradeTime` 读-改-写当月文件，仅回填缺失字段，已有时间不回写）；返回体加 `updated` 计数（Controller 透出）；仍不重算持仓/现金（设计取舍不变）。②**Web 第 5 Tab「历史成交」**——取代页头「交易历史」Dialog（receipt 按钮 + `_HistoryDialog` 移除）：日期范围查询（默认近 30 天）+ 按日分组 + **全字段列**（方向/时间/代码/名称/数量/价格/金额/费用/成交编号/止损/买点/原因，横向滚动）+ 区间统计行「共 N 笔 · 买 X 卖 Y」+ 空态带导入入口；独立 `_HistoryImportDialog` **只认通达信历史成交导出**（非历史成交格式人话拒绝不静默落零，用户确认），导入后 inline 展示新增/回填/跳过/非交易 + 对账行；不做定时轮询（进 Tab 加载 + 手动刷新，用户确认）。③前端 `TradeRecordItem` 补 fee/orderId 解析。测试：后端 +5（回填 round-trip×3 / 重传回填 / 已有时间不重填）+ Controller updated 断言；web 103→104（历史成交 Tab 分组全字段 / 空态 / 非历史成交格式拒绝）；RFC 20260823 implemented；api-spec / trading-features / RFC 索引同步 | 后端 685→690 · web 103→104 |
| 2026-08-20 | 生产迁移收尾批（服务器到期迁移）| **旧服务器 49.235.37.220 到期 → 新服务器 82.156.111.146（腾讯云轻量 2核4G · Ubuntu 24.04 LTS）全量迁移 + 收尾修复**：①**数据迁移**——本地备份（100M 全量 + 29M 快备，checksum 校验）→ 新服务器 /opt/adaios 还原（data/.env/jar/web/admin）；②**环境重建**——Java 17.0.19 + Python 3.12 + systemd 三服务（adai-core:8080 / adaios-web:8082 / adaios-admin:8083）+ serve_static.py + 开机自启；③**修复链**——CORS 白名单加新 IP（web 首页加载失败）；**前端产物烧录 localhost 陷阱**（web/admin 原产物烧 `localhost:8080` 白屏 → 用 `--dart-define=API_BASE_URL=http://82.156.111.146:8080` 重建 + CanvasKit/字体本地补丁，gstatic 被墙白屏）；**os/ 资产迁移遗漏**（backup_prod.sh 不含 os/ → 从 git 仓库同步 + `.env` 显式 `ADAI_OS_BASE_PATH=/opt/adaios/os`，否则后端 `../../os` 相对 cwd 解析错）；**admin 知识页 400**（前端 `trading-os` 与后端/目录 `trading-engine` 契约不符 → 前端全局改名 + rules 路径 `11-context`→`knowledge/context` + os 目录树布局溢出修复 Flexible）；④**iOS app 重装**（新 IP 构建安装）；⑤**生产配置文档化**——backend-deployment.md 更新新服务器/域名（adaiadai.com 待配 DNS+HTTPS，§10 规划）/deploy.sh/serve_web.sh×3/AGENTS.md/pitfalls/ship 全库旧 IP 批量替换，backup_prod.sh 加 os/ + DS_Store 排除。**遗留**：web/admin 中文不显示（skwasm 字体加载疑似绕过 fetch 补丁，待专项，旧服务器同产物或同样存在）；os/ 里服务器运行时副本需与 git 保持同步。admin 测试 33→34（布局修复）| admin +1 |
| 2026-08-20 | AI 编程标准化批 | **全库 CLAUDE.md→AGENTS.md 迁移（用户不用 Claude Code，跟随行业 AGENTS.md 标准）+ 技能包体系（建收守闭环）**：①**入口统一**——根 + 6 子项目（services/adai-core、apps/adai-app、apps/adai-web、os/trading-engine、os/life-os、os/project-os）CLAUDE.md→AGENTS.md（git mv），全库引用替换零残留（仅保留故意迁移注记）；AGENTS.md 改统一入口：规则 2 子项目分层应用、审查体系表补技能包/架构红线/技能包规范行；新建 **ARCHITECTURE.md**（架构红线单文件：技术栈/五层架构/分层依赖/数据流/9 条红线清单，AI 进项目直读）；②**技能包体系**——`ai-engineering/assets/skills-spec.md`（SKILL.md 标准：name + frontmatter 9 字段融合、五段正文结构、新增流程「重复≥2 次才封装」）；`ai-engineering/roles/` 8 审查官技能包化（frontmatter 加 name kebab-case + tags 加 skill，正文五段结构）；**建设技能三件套** `ai-engineering/skills/`（new-api 新接口流程 / new-domain 新业务域流程含需求六维检查 / ship 收尾流程）——与 roles/ 审查技能形成「建（建设）→ 收（ship）→ 守（审查）」闭环；③**给人看**：`docs/guides/skills-usage.md`（11 技能清单/三种触发方式/对话示例/维护/FAQ）+ `docs/guides/development.md`（承接原根 CLAUDE.md 构建命令）；docs/README 登记；④**流程同步**：design.md 加需求六维检查小节、boundaries B4-B6 来源改 ARCHITECTURE.md、review-docs 13 处语义更新、guard-meta.sh files 加 skills/ 目录；⑤**ADR-005**（AGENTS.md 统一入口 + 技能包体系，三问全中）。纯文档/工程改动，测试数不变 | — |
| 2026-08-19 | 生产数据同步 + 重部署批 | **用户反馈生产仍推汾酒**（昨天已更新持仓）。定位：①生产 `data/`（`/opt/adaios/data`）与本地数据独立、gitignore 不随部署同步，昨天五份文件导入只落本地；②生产跑 08-18 前旧构建——`positions/import?replace=true` 全量覆盖未生效（变成 upsert，删不掉多余持仓）、`POST /trades/import` 直接 404。**通过生产 API 同步**（用户确认）：五份文件导入（持仓 5 · 自选 25 · 清仓 163 · 资金现金 657.91/资产 112566.91 与券商逐项一致）；旧构建删不掉的多余持仓（山西汾酒 100/利欧股份 3500）用 `POST /trades` SELL 按真实成交（08-17：119.91/5.24）清出，0 股残留在读取侧自动过滤；4 只 08-16 残留自选（埃斯顿/中宠股份/北汽蓝谷/湖南天雁，含两只死叉信号会进 15:10 买点扫描）DELETE 清理；卖出虚增现金后用资金文件重导恢复账目。**重部署生产**（用户确认）：bootJar → deploy.sh 49.235.37.220，补齐 08-17/08-18 批次（replace 全量覆盖/历史成交导入/推送开关/P0 `<think>` 壳泄漏/东财熔断等）；部署后补导历史成交 48 笔流水（此前 404 缺口）。**遗留**：清仓 1 条旧有研新材记录（07-31→08-03，无删除接口）；3 只持仓保留生产旧止损位（按旧成本算，待用户按新成本重设）。无代码改动，测试数不变 | — |
| 2026-08-18 | P2-1 东财熔断修复批 | **根因**：东财在生产被限（连接层封禁 `header parser received no bytes`），每次 K 线查询都先打东财再降级 → 浪费请求 + 日志噪音（22:40 起 10 分钟 188 条 WARN，全天 1154 条）。腾讯兜底稳定（0 失败）但被动等待主源失败才接管。**修复**：`KlineService` 加熔断——连续失败 3 次 → 熔断 5 分钟（直接走腾讯，不再打东财）；冷却后半开探测一次（恢复则清零计数，再失败则重新熔断）。volatile 多线程安全（买点扫描 16 并发）。KlineServiceTest 新增 3 回归（熔断触发/恢复/半开探测）。后端 674→677 | — |
| 2026-08-18 | P1-1 交易归集 unknown 污染修复批 | **根因**：`TradeLogCollectService.collect` 在 symbol 缺失时落 `"unknown"` 占位（确认必失败）+ `complete` 判定漏了 symbol 检查（javadoc 要求 symbol+direction+price+volume 全有，实现只查 price/volume）+ `dedupeKey` 让所有无代码候选共用 `unknown:SELL` 键互相吞并（生产 09:01-09:02 三次归集只剩 2 笔）。**修复**：①symbol 与 name 全无（LLM 幻觉/think 泄漏文本次生）→ 拒绝归集，不落占位；②有 name 无 symbol（「清仓了XX」合法待补充）→ 归集 complete=false，symbol 保留 null；③complete 判定补 hasSymbol（与 javadoc 一致）；④`dedupeKey` 用 name 兜底防互吞；⑤summarize 显示 name 不显示 unknown。TradeLogCollectServiceTest 新增 4 回归。后端 670→674 | — |
| 2026-08-18 | P0-1 `<think>` 壳泄漏修复批 | **根因**：`GlmResponseParser.parse()` JSON 提取失败时用**未剥壳原文**降级（fallback），`stripThinkAnswer` 对未闭合 `<think>`/`<answer>` 剥不掉；GLM-4.1V-Thinking 免费模型输出不稳定（think 吃光 max_tokens=1024 → answer 无输出；同图两次结果不同）。**修复**：①降级路径改用剥壳文本 + 剥壳后为空给默认文案（不再落 think 原文）；②`stripThinkAnswer` 支持未闭合 `<think>`（从标签起删到文末）与未闭合 `<answer>`（剥标签留内容）；③`max_tokens` 1024→2048（thinking 模型留 answer 空间）；④GlmResponseParserTest 新增 4 回归（未闭合 think/answer、剥壳后文本）；⑤**生产数据清洗（用户确认重识别方案）**：7 条脏记录（08-17×2 + 08-18×5，实为 2 张唯一图）用 glm-4v-flash 重识别 → summary=股票交易记录、domain life→trading、tags 修正、正文补完整 OCR；memory 08.md 7 条目同步修正 + 记忆重建（/admin/memory/rebuild）；备份 8 个 .bak-20260818-p0 保留。后端 664→670（含并行交易批 + 本批 4 回归）| — |
| 2026-08-18 | 交易五份文件导入批次 | **用户桌面五份通达信导出全链路落地（确认批次，含实测）**：①五份文件归档 `data/adai/trading/imports/2026-08/`；②**本地后端重建**（此前运行 8/9 旧构建，/trades/parse、/account、/review 全 404）——实测一句话解析（「买了 1000 股京东方 @5.2，止损 4.9，B1」完整结构化）+ 今日复盘生成（45s，五节模板 + 真实持仓盈亏 + 规则对照）；③**app 简化**：交易页去止损位/买点（纯买卖记录，归 web 端设置），价格键盘 `numberWithOptions(decimal:true)` 支持 5 位小数；后端 TradeRequest BUY 止损/买点由必填放开为可选（web 记录对话框/CSV 仍填，app 记录持仓止损缺失 → web 补设恢复纪律判定）；④**第五份文件「历史成交日志」**：新 `POST /trades/import`（通达信历史成交导出解析：卖出负数量取绝对值、fee=\|发生金额−成交金额\| 券商实扣、成交编号 orderId 幂等、股息红利税等非交易事件计 nonTrades、**只补流水不重算持仓** + 对账提示 lines）；`POST /trades/batch` 补实现（web 交易 CSV 批量导入此前 404）；TradeRecord 加 orderId（freeze MINOR）；⑤**持仓导入全量覆盖**：`positions/import?replace=true`（以文件为准删除缺失持仓 + 0 股残留移除，解决 upsert 无删除语义漂移）；⑥**实测结果**：持仓 5 只（成本价对齐券商 4 位口径 6.2031/20.9331/43.5902/56.7894/34.5466，融发核电 0 股残留被 replace 移除）/资金 657.91·资产 112566.91·盈亏 18688.28（与券商文件逐项一致）/历史成交 48 笔（重复导入 skipped=48 幂等验证、fee 3.95/0.55 与实扣吻合）/自选 25 / 清仓 163 / 对账（云南锗业流水净+300=持仓 300 完全一致，京东方/中信建投正确标出基线缺口）；**本金缺口已补**：实测发现 principal=0（新建 account.json 无本金记录 → 总盈亏=资产−本金 失真）→ 加 `PUT /trading/principal`（只写本金不动现金/资产/市值，web 资金区「本金」按钮）实测本金 150000 → 总盈亏 −37,433.09 正确；顺手把 P0-1 一次性工具 ReidentifyImagesTest 改无 key 跳过（全量测试可重复跑）。后端 654→670 · app 120→119 · web 98→101 · 端点 76→79 | — |
| 2026-08-18 | 生产日志审查批 | **今日生产问题总结（只报告不修）**：journalctl 拉取当日 2986 行分级——**P0**：图片记录 summary 泄漏 `<think>` 壳 5 条全中（GlmResponseParser 剥壳未生效，用户可见泄漏 + 交易截图未识别不进归集）；**P1×2**：交易归集 `SELL unknown`、缺数量候选仍确认落库（违 ADR-004）；**P2×4**：东财 K 线 1154 次失败全降级腾讯、凌晨三次重启 + shutdown NoClassDefFoundError、DeepSeek 短响应降级解析 ×4、alice 等账号无个人档案；**P3×4**：公网扫描/连接重置/单条记录不可读/GET 不支持。报告落盘 `docs/review/audits/2026-08-18-production-log.md` + REVIEW 最近审核表登记。纯文档改动，测试数不变 | — |
| 2026-08-18 | 开工自举自动化批 | **用户零操作（用户确立）**：AGENTS.md 新增协作规则 0——任何 AI 开工时**自动执行** `guard-context.sh` 自举（状态/未修项/边界/坑/规范/待办/成本提醒），用户不用手动跑脚本、不用回忆任何事；cost.md「零、先看账」同步注明无需手动（命令保留给深查场景）。纯文档改动，测试数不变 | — |
| 2026-08-18 | 省钱建议落地批 | **成本纪律深化（用户确立）**：`checklists/cost.md` 增 **S7 审查报告控字**（每条问题 ≤3 行，仅 P0/战略级展开，输出 9-27 元/M 最贵通道）、**S8 长会话重复读大文件**（缓存命中价涨后挤牙膏读取重复付钱，一次读够）、**S9 长会话串行 vs 并行子代理**（子代理各带小上下文，审查/批量开独立子会话）；**S6 审查官模型分层**（默认 Flash，仅深审场景切 Pro，差价 3 倍）。同步 `process/audit.md` §3/§4 与 `process/review.md` §3/§4（模型分层 + 输出控字落地）。纯文档改动，测试数不变 | — |
| 2026-08-18 | 协作规则与走查成本纪律 | **规则范围明确（用户确立）**：『讨论与实施分离』只约束代码与 `data/` 数据修改，AI 工程文档（`ai-engineering/`、AGENTS.md）可直接修订（AGENTS.md §7 + conventions W6 同步）。**audit 成本纪律（用户确立）**：走查默认增量（`git diff 基线..HEAD`），全量仅里程碑/发布前或显式 `--full`，高峰时段禁跑——全量 8 官 × 全仓库为全项目最贵 AI 流程。纯文档改动，测试数不变 | — |
| 2026-08-18 | 成本监控与纪律批 | **背景（用户确立）**：DeepSeek 2026-08-17 峰谷定价首日 DSH 单日烧 77.29 元（主会话 1931 次调用、缓存读取 8.2 亿 token，高峰 14-17 点 3 小时 40 元）→ 成本必须盯账。**新增 `ai-engineering/guard-cost.sh`**（读 `~/.dsh/sessions` 按天/会话算钱：`--day/--month/--top/--record/--log`，内置阈值告警 >20 元/缓存>70%/调用>500 次）；**新增 `ai-engineering/checklists/cost.md`**（价格速查 + 烧钱动作 S1-S6 按杀伤力排序 + 省钱原则 C1-C6 + 工程配置检查点）；**guard-context.sh 加 C6.5**（每次开工自动输出今日成本 + 告警，主题含 cost/成本时展开原则全文）；AGENTS.md 审查体系表登记两项；`state/cost-log.jsonl` 成本日志（08-17: 76.06 元 / 08-18 首日 14 元+ 已录入）。无代码改动，测试数不变 | — |
| 2026-08-18 | 流程调整：ADR 三问判断 | **为什么这么定**：ADR 每批必建会变流水账，稀释重要决策分量；改为「三问全中才建」（推翻成本高？有被否决备选？影响未来方向？），否则 change-log 写清「为什么这么定」兜底。同步 ship.md 步骤 4 + guard-sediment S1 + assets/_index 新增规则（用户确立）| — |
| 2026-08-17 | RFC 20260817 三项批 | **①推送体验**：push 卡专属样式（类型徽章按标题配色：早盘蓝/午间紫/尾盘橙/买点绿/预警红）+ 结构化模板（总结+持仓逐行+建议，LLM prompt 与降级模板同步）；**推送开关** per-user（`data/{userId}/trading/push-settings.json`，8 类型，写侧定时任务 + 读侧 Feed 双侧门控）；app 左滑删单条/右滑进设置、web 交易页设置入口。**②图片对话流**：图片卡图置顶、turns 跟随滚动（聊天态与刷新态一致）。**③交易日志自动归集**：截图（VLM 识别）/文字（「清仓了XX」）→ TradingParseAppService 结构化 → 当日候选去重（symbol+方向）→ 收盘 15:15 推送「今日操作汇总」→ 用户「确认并入账」→ recordTrade 链路落库；仅 trading 插件用户触发；`GET/PUT /trading/push-settings` + `GET /trading/trade-log` + `POST /trading/trade-log/confirm` 4 新端点；后端 644→653（+9：PushSettings 4 + TradeLogCollect 5）· app 118→120（+2 push 卡渲染）· web 98 | — |
| 2026-08-17 | Memory 正文 `---` 截断修复批 | **生产 110 条「createdAt 缺失」告警根治**：正文含裸 `---`（笑话/故事分隔线）时旧 ENTRY_SPLIT 正则提前截断 → 后半正文误当下条目 frontmatter → 17 条记忆读不出；新正则要求条目结束 `---` 后紧跟 frontmatter 键值行才截断（正文内 `---` 后是空行/普通文本不截断）；生产 7 月文件 17 条缺失全消除 + 回归测试；后端 643→644 | — |
| 2026-08-17 | 走查前端 8 项批 | **web×6**：_loadDegradable 锁+代际令牌（先判锁再递增/finally 无条件复位/旧代丢弃）；历史 Dialog 日期竞态代际隔离；图片回执自然化（VLM summary 拼「看到你…，已记下」，替代「📷 已记录 N 张」）；记录交易成功补 toast（第一原则文案）；首载失败错误态+重试（不伪装空态，loading/失败/真空三分支）；_extractApiError 透出后端人话（body.error 优先，四页统一）；**app+web**：Feed 卡移除「记录/提问/领域」系统徽章（第一原则，交互保留在更多菜单）；**admin**：涨跌色红涨绿亏（补 darkRed token，行情/持仓两处）；顺手清 web 账户卡 11 处多余 `a!`；app analyze 清 2 死 getter；web 98 · app 118 · admin 34 全绿 | — |
| 2026-08-17 | P2-交易20 guard-align 盲区批 | **A1 端点守护补裸注解**：正则新增 `@GetMapping`/`@GetMapping()`（无路径参数）分支继承类级 base——此前 11 个裸注解漏数（A1 报 61 vs 真相源 72）；修复后 72 全对齐 endpoints.txt；P2-交易20 出表，**P2 表清零**；G1-G7 全 PASS | — |
| 2026-08-17 | S5 现金单一真源批 | **现金唯一真源 = account.json（AccountSnapshot.cash）**：importCashQuery 不再写 positions.md cashBalance（saveCashBalance 调用移除）；TradingAdviceAppService R81 分母、TradingAppService getPortfolioSnapshot、TradingReviewAppService 复盘快照全部改 AccountSnapshot.cash；测试 3 构造器补 AccountSnapshotRepository mock + importCashQuery 断言改验 AccountSnapshot.save + positionPercent 现金走快照 mock；REVIEW S5 + P2-交易4 出表；后端 642 全绿 | — |
| 2026-08-17 | P2 批 C（文档四连）| **W-P2-12** roadmap 状态修正（数据冻结已定稿、#144/#106/#112 已修仍列待清）；**W-P2-13** feature-reference 附录补 7 交易端点 + §16 PATCH 合并语义；**W-P2-14** api-spec trades/review 补 403 插件门控契约；**W-P2-11** os 空文件删除（0 字节残留），alice 脏数据核实为测试账号（gateDomain 已防新增）；后端 642 · app 118 · web 98 · admin 34 全绿 | — |
| 2026-08-17 | P2 批 B（前端六连）| **W-P2-2** app 记忆页日期连点代际守卫（web 有 app 无）；**W-P2-4** Feed 缩略图 cacheWidth 降采样（app 96→192 / web 120→240，避免全分辨率解码）；**W-P2-5** web 图片成功卡 content=用户 caption（与 app _buildMediaSuccessCard 对齐，summary 单列）；**W-P2-3** admin 插件 toggle 串行队列 catchError 恢复（非 ApiException 不永久卡死）；核实 W-P2-1 四项已闭环（删除确认/图片重试字节/全图 Dialog/搜索错误态）+ isProtected 已修（P2-R3）+ 163 admin 记录页=治理设计；app 118 · web 98 · admin 34 全绿 | — |
| 2026-08-17 | P2 批 A（后端六连）| **W-P2-7** accounts.json 原子写（tmp+move，防写一半损坏全系统）；**W-P2-8** TagIndex ConcurrentHashMap（并发读写安全）；**W-P2-9** 交易落 Record 流水线核实已实现（writeTradingRecord → Timeline/Search/Memory 全链路）；**W-P2-10** MemoryService 三缺陷（suggestion 单行化 / createdAt 不再 now() 回退 / 删除哨兵改整行精确匹配）；**W-P2-6** project tasks delete 补插件门控（B40 对称，403 测试）；**08-15 后端×6** enabledPlugins 30 秒 TTL 缓存 + invalidate（每请求少读 accounts.json 数次）；后端 640→642（+2）| — |
| 2026-08-17 | Review 修复批 R11（admin + 收尾）| **admin 操作后静默刷新**（_load(silent) 不闪全页 spinner）；**setPlugins 死接口删除**（页面全走 mergePlugins）；**PromoteResultDto 补 message**（后端响应字段）；核实 latest==null 分支/promote 重复 header 已随演进闭环；task-log 清 10 条已修残留；admin 34 全绿 | — |
| 2026-08-17 | Review 修复批 R10（P3 深水区）| **W-P3-4** _closeChat firstWhere→indexWhere（防 StateError）；**W-P3-13** GlmResponseParser 截断不劈 emoji（surrogate pair）；**W-P3-17** LocalFileStorage stripUserPrefix 与 resolve 同款 default 兜底（路径剥不净）；**W-P3-20** promote 脱敏正则兼容千分位（1,400 漏脱敏）；**W-P3-24/25/26** status 数字核对 + multimodal RFC 待确认勾销 + feed 示例补 domain + _index 补 memory-os-design；核实 W-P3-3（最后记录文案已消失）；后端 640 · web 98 全绿 | — |
| 2026-08-17 | Review 修复批 R9（P3 尾尾批）| **web 插件重试 SnackBar**：失败先 clear 再 show（不队列堆积）+ 重试成功 hide（W-P3-2）；**IndexedStack ValueKey 保活**（插件中部插入不重置 state，W-P3-1）+ _select 传 label + fallback 空列表防御；**app superseded 记忆隐藏待办标记**（172）；**ANALYSIS system 越界 domain 注明会被修正**；CLAUDE.md 目录树去重 + 表格拆行；docs/README 断链修正；task-plugin-model T1.3 标 superseded；api-spec PATCH 契约明确「清空传空数组」；核实 6 条 P3 已闭环（全图 Dialog errorBuilder/卡片删除精确匹配/RFC frontmatter/存量 domain 合法/首帧 await 设计权衡/max_tokens 已修）；后端 640 · web 98 · app 118 全绿 | — |
| 2026-08-17 | Review 修复批 R8（P3 尾批）| **isValidPlugins 查重**（["trading","trading"] 拒绝落盘）；**gateDomain 未知 domain 收敛 life**（白名单防脏数据）；**HEIC/HEIF 正确 MIME**（此前误标 png）；api-spec domain 关键词同步代码（trading +4/project +1）；frontend-reference 补 ask-batch 行；核实 5 条 P3 已随演进闭环（emoji 截断/limit toast/ImagePicker limit/max_tokens/多图回答展开）；后端 640 · app 118 全绿 | — |
| 2026-08-17 | Review 修复批 R7（P3 收尾批）| **八端点 controller 测试补齐**（watchlist/sold/score/transfer/transfers/buy-points/account/imports-cash）；**web DTO 测试补齐**（SoldScore/SoldTrade/Watchlist/Account 6 个）；KlineService 降级日志 debug→warn（生产可观测）；WeChatPushChannel InterruptedException 单独处理（不污染调度线程）；buy-point-rules 登记 engine/README（孤儿规格）；gap lines 修正；change-log 两行补端点增量；后端 629→640（+11）· web 92→98（+6）| — |
| 2026-08-17 | Review 修复批 R6（P2/P3 web 批）| **P2-8** _loadAll 入口 mounted 守卫；**P2-10** 打分空列表短路 + 请求在途去重；**P2-11** 「纪律遵守率」从胜率改 verdict 口径（胜率单独展示）；**P2-12** 行为模式单字误配改双字词组 + 否定排除；**P2-13** 删自选带确认 + 心理标注错误反馈；**P2-14** 账户卡 FittedBox 防溢出 + 千分位；**P2-15** 打分列中性色阶（不借红涨绿亏）+ '—' 固定灰；P3：lookup 300ms 防抖、转账金额 NaN 校验 + 提交反馈、本金 ¥ 千分位、上次更新文案如实（30 分钟自动刷新）、行为模式 Wrap、isTdxExport 认「成本」表头；web 89→92（+3 P2 口径测试）| — |
| 2026-08-17 | Review 修复批 R5（P2 后端 9 连）| **P2-1** 打分线程池 @PreDestroy shutdown + 超时不产空行；**P2-2** 自选扫描 8 并发 + 按标的异常隔离；**P2-3** 腾讯 K 线按日缓存（东财被限时兜底不再每请求打腾讯）；**P2-5** R66 阈值 -10%→-5%（用户确认贴合课程 R67/R72 3-5%）；**P2-6** KDJ 阈值 J<20→J<13（课程锚点）；**P2-7** B1? 候选不推送（「不硬推」）；**P2-21** advice 输出侧过 r81Applicable（超100万不强制）；**P2-22** importPositions avgCost/quantity 校验（防 NPE）+ name 单次 lookup + missingStopLoss 失真修复；P3：B2 推送补止损提醒、节假日守卫（2026-27 主要休市）、调度器 4 线程、Feed 标题按 type 映射、near-stop-loss 文案用可配值、R120/R85 javadoc 修正；后端 626→629（+3 verdict/节假日测试）| — |
| 2026-08-17 | app 稳定性批（自动刷新 + 竞态守卫 + 上传锁）| app 交易页 **30 分钟自动刷新**（对齐 web B3，Timer dispose 清理）；**次级数据代际令牌**（_loadAux 响应乱序旧代不覆盖新代）；**图片上传批次锁**（P3-8：上传期间再次发送提示等待，进度条不互相覆盖）；核实 P3-4 盲弹/P3-6 看图占位已随演进闭环；app 117→118（+1 批次锁测试）| — |
| 2026-08-17 | Review 修复批 R4（P1 五连）| **P1-1 切入自动刷新死代码修复**（web 判 label=='交易' 恒 false，改判 plugin=='trading'，+壳层测试）；**P1-2 recordTrade 买卖同步市值**（总资产=现金+市值不变式，只差手续费，+2 测试）；**P1-3 收盘缺行情跳过保存**（不全覆盖总资产，+2 测试）；**P1-7 web 可降级请求分离**（buy-points 失败不再整页白屏，+1 测试）；**P1-8 清仓打分按序索引匹配**（同代码多笔不错挂）；**P1-10 api-spec buy-points 示例修正**（score 0-100/signals 实际文案）；后端 622→626（+4）· web 87→89（+2）| — |
| 2026-08-17 | app 交易对齐 web（①必对齐三件 + ②自选/清仓只读）| app 交易页对齐：①账户总览卡（总资产/可用/可取/市值/当日盈亏/总盈亏=资产-本金/本金标注，替代旧 3 项卡）、持仓卡显示止损位（未设橙色提示）、记录交易默认 -7% 止损（价格自动带出可改，对齐 web）；②自选股 + B1/B2 买点信号、清仓股复盘 + 三维打分（只读，最近 20 笔提示用桌面端）；app 116→117（+1 对齐渲染测试）| — |
| 2026-08-17 | Review 修复批 R3（导入解析失败禁止落零 P1-交易5）| **资金查询导入落零风险修复**：此前首行格式对不上「余额/可用/可取/参考市值/资产/盈亏」时静默全 0 覆盖 account.json + cashBalance（贴错文件即毁数据）；修：parseCash 返回 headerMatched，未命中抛 TradingException（400 + 人话「无法识别资金股份查询格式」），绝不落零（B51 检查点）；web 导入 Dialog 失败统一 toast 反馈（此前静默）；后端 620→622（+2）| — |
| 2026-08-17 | Review 修复批 R2（简报盈亏失真 + loss 文案按止损区分）| **简报盈亏失真修复**：简报 LLM 拿昨天记录「买入京东方 1000股@5.2」自算 (5.81-5.2)/5.2=11.73% 说「浮盈11.73%」——实际持仓 5300股@6.0421 亏 3.8%。修：简报注入真实持仓快照（positionSummaryLines，权威口径禁从旧记录推算）；**loss 推送文案按是否设止损区分**（已设止损 →「盯紧止损位 X（R66）」；未设 →「你还没设止损位」——之前山西汾酒已补止损却仍说没设）；后端 619→620（+1）| — |
| 2026-08-17 | Review 修复批 R1 续（持仓编辑补端点 + 默认 -7% 止损）| **持仓编辑 404 修复**：前端/测试一直在调 PUT /positions/{symbol} 但后端从未实现（web 点编辑保存必 404）——补 updatePositionMeta + PUT 端点（role/止损位，只更新非空字段，404/400/403 齐全）；**默认 -7% 止损**：记录交易表单填价格自动带出止损=价格×0.93（可改，用户 2026-08-17 设定）；后端 612→619（+7）· 端点 71→72 | — |
| 2026-08-17 | Review 修复批 R1（择时路径 + 推送文案）| **择时状态生产盲区修复**：TradingSessionPushService 的 current.md 从硬编码 `../../os/...` 改配置注入 `adai.knowledge.trading-engine-path`（3487b00 只修了建议引擎、漏了时段推送——生产解析成 /opt/os/... 缺 adaios 级 → 择时状态恒「未知」）；loss 推送文案如实化（「触发止损预警」→「单日大跌，留意风险（你还没设止损位，想好怎么走）」——原文案在未设止损时误导）；后端 610→612（+2 择时路径测试）| — |
| 2026-08-16 | 交易自理批5 续（D2 行为模式）| 清仓心理标注按关键词归类显示行为模式（追高/恐慌/贪心/死扛/犹豫/急躁，标注后自动聚合）；web 87 全绿 | — |
| 2026-08-16 | 交易自理批5（D3 复盘三维打分）| 清仓复盘打分：买点维度（买入日回溯 K 线 → B1/B2 完美图匹配度，追高无形态=25 分）+ 执行维度（verdict 纪律：盈利 90 / R53 45 / R66 15）+ 选股维度预留（数据积累后）；`GET /trading/sold/score` 端点；web 清仓 Tab 三维打分列；后端 605→610（+5）· 端点 70→71 | — |
| 2026-08-16 | 交易自理批4（C2 自选股买点）| 自选股买点判定：东财 K 线主源 + 腾讯降级（KlineService）+ KDJ(9,3,3) + BuyPointDetector（B1 回调≥50%+缩量0.7+KDJ.J<20 / B2 放量1.5+破前高20日，5 参数建议值可配）；`GET /trading/buy-points` 端点；收盘 15:10 定时扫描 + 命中推送「到买点了」；web 自选 Tab 信号列；后端 602→605（+3）· 端点 69→70 | — |
| 2026-08-16 | 交易自理批3（E2/B3/D2）| 静默刷新（已有数据不闪整页 loading）；每 30 分钟自动刷新（跟随交易节奏）；清仓纪律统计（盈/亏/违R66/违R53/遵守率）；web 82 全绿 | — |
| 2026-08-16 | 交易自理批2（D1/C3/E1）| 清仓股 verdict（R53 没涨=错 / R66 止损，162 笔批量判）；接近止损预警（距止损 ≤2% 可配推送）；web Tab 工作区四分区（持仓/自选/清仓/资金替代纵向堆叠）+ 清仓表 verdict 列；后端 596→602 · web 82 | — |
| 2026-08-16 | 交易自理批1（A+B）| 手续费自动计算（佣金万0.854四舍五入/印花税万5去尾/过户费沪万0.1四舍五入，四笔交割实例验证）；转账表单（净投入跟踪 principal/cash）；收盘 15:05 账户自动更新（市值/当日盈亏/浮盈）；顶部上次更新时间戳 + 点击更新；后端 583→593（+10）· web 82 | 端点 67→69 |
| 2026-08-16 | 账户总览卡 + 账户快照 | `GET /trading/account` 账户快照（资产/可用/可取/市值/盈亏/当日盈亏，资金查询导入存 account.json）；web 顶层账户卡（总资产为主 + 6 项）；组合快照行情注入修复（总盈亏 15235.55 与券商一致）；解析器正则 6 组扩展 | 端点 66→67 |
| 2026-08-16 | 交易数据智能——自选/清仓/资金三块落地 | 自选股（WatchlistItem + 通达信自选导入，形态/指标提示=买点原料）；清仓股（SoldTrade + 导入 + 心理标注，复盘闭环）；资金股份查询（现金 292.88 + 精确成本 4 位，R81 分母修正）；Java 解析器 TradingImportParser（表头定位三格式）；web 交易页三区块 + 通用导入 Dialog（选择文件/粘贴）；7 新端点；后端 576→579（+3 解析器测试）· web 82 | 端点 59→66 |
| 2026-08-16 | 导入文件上传留存 + 交易页自动刷新 | 后端 `POST /trading/imports/save`（multipart 留存 data/imports/ + GBK 自动转 UTF-8）；web 导入 Dialog 加「选择文件」（上传→转码→自动解析导入，免复制粘贴）；交易页切入/点记录交易自动刷新（保活缓存不再显示旧数据）；盈亏实时注入（getPositions 行情） | 后端 576（+2）· web 82 | 端点 58→59 |
| 2026-08-16 | 通达信持仓导入 + 代码带名称 | `POST /trading/positions/import` 持仓初始化导入（通达信导出快照，按 symbol upsert，name 缺失行情补全，返回未设止损列表 R68 提示补设）+ `GET /trading/lookup` 代码查名；web 批量导入自动识别通达信格式（表头定位列，制表符/空格）+ 记录交易输入 6 位代码自动带出名称（二次确认可改）；api-spec 登记 2 端点 | 后端 572（+4）· web 81（+4）| 端点 56→58 |
| 2026-08-16 | 生产部署（时段推送 + 知识层上线）| deploy-gate 全过（三关 + smoke 6 端点）；os/trading-engine/knowledge/ 上传生产 + .env 配 `ADAI_TRADING_KNOWLEDGE_PATH`（G-4 路径生产生效）+ `ADAI_PUSH_WECHAT_SENDKEY`（微信渠道生产就绪）；生产实测：京东方建议引擎引用 R81 真实规则（占比 100% 超 25% 上限 → reduce），知识加载 rules=11KB 全通 | — |
| 2026-08-16 | 交易时段节奏推送（RFC 两阶段全做）| `PushChannel` 渠道插件化（kernel/push 接口 + Feed 默认 + 微信 Server酱，未配置 key 自动禁用）；`TradingSessionPushService` 三节点（早盘计划 9:15 / 午间跟踪 12:00 / 尾盘建议 14:50，cron 可配）+ 内容两阶段（LLM 自然语言生成，失败降级模板）；`MarketAlertService` 异动改走渠道（Feed+微信同发，真止损/早盘/午间/尾盘全链路）；择时状态读 `current.md`；feature-reference 补主动推送章节 | 后端 568（+8）|
| 2026-08-16 | 真止损预警（A 最小闭环）| `MarketAlertService` 新增 `stop-loss` 检测：现价跌破用户预设止损位 → R66 硬判定（复用 G-3 引擎口径，与建议引擎一致）→ 主动推送 Feed「已跌破你的止损位 X，按纪律该清仓了」；未设止损持仓跳过（R68）；当日去重沿用；文案无第三视角；api-spec 补 push 类型说明 | 后端 560（+4）|
| 2026-08-16 | 框架+插件审查 P2 清尾批 | FP-P2a~i：parseLlmAdvice 输出侧校验（BREACHED→强制 clear、OVER_WEIGHT→buy 保守改 reduce）；R81 100万前提（超 100 万不强制，参考 R82-R95）；测试补断言；gap 补 frontmatter（D44）；docs/README 登记新文档；三阶段 RFC 升 approved + 实施记录；gap 指向正式总纲；update-current.sh 相对路径 + CLAUDE.md 收录；编号对拍（R1-R120/E1-E30，K39）| 后端 556（+1）|
| 2026-08-16 | 框架+插件审查修复批 | 三官 deep 审查（backend/knowledge/docs）后发现修复：yml 路径 11-context→knowledge/context（运行时断链，FP-P1）；R81 占比分母改总资产（现金纳入，FP-P2 + 测试）；update-current.sh 幂等 + 时间戳语义 + 声明修正（FP-P3/S4）；R66 现价口径注明（FP-P4）；总纲 §五 刷新全 ✅（FP-S1）；引擎口径契约测试 RuleKnowledgeContractTest（FP-S2/B44）；rules-api.md §2/§3 同步（FP-S3）| 后端 555（+4）|
| 2026-08-16 | 框架+插件形态（G-1~G-6 全落地）| 总纲提位正式架构文档（`architecture/framework-plus-plugin-model.md`）+ 交易 Agent 三阶段 RFC + gap 对账；G-1 行情载体归 trading 插件域（git mv）；G-2 交易读端点门控 + Brief 门控；G-3 `domain/trading/engine/` 规则引擎（R66 止损/R81 仓位/matchRules，规格 `os/trading-engine/engine/rules-api.md`）；G-4 `11-context`→`knowledge/context` + `update-current.sh` 半自动刷新；G-5 形态样板（Skill 包 + MCP 映射）；G-6 组合验证测试（读端点 403×5 + 行情注入门控）| 后端 551（+14）|
| 2026-08-16 | 带图发图即对话 + 交易建议引擎 | 带图：发图即分流（ask 直进对话/log 自然回执）+ S-2 聚合卡修复；交易：/trading/advice 建议引擎（R66-R95 硬约束）+ /trades/parse 一句话解析 + 资产卡+建议展示（无执行按钮）| 后端 499（+12）· app 112（+18）· admin 34（+1）|
| 2026-08-16 | P-be-01 安全 + admin 收敛 | 5 维护端点迁 /admin/** 鉴权（X-Admin-Token）+ 用户端恢复 PATCH /memory/{id}；admin 移除个人内容编辑（P-role 系列）；app 补记忆修正/待办完成 | 端点 55 · admin 34 |
| 2026-08-16 | 文档自动对齐门禁 | guard-align（端点/测试数/端点数 A1-A4）+ guard-context（任务上下文注入）+ guard-sediment（沉淀检查）+ pre-commit 四层（隐私/对齐/结构/防复发）+ 方法论放回仓库 | — |
| 2026-08-15 | 走查修复批 W2 前端续 | P1-W5 失败伪装空态五处修复（web search/memory/timeline + app timeline_modal/launcher——错误态+重试，失败不再显示「无数据」）；P1-W7 切 World 不中断上传（dispose 后继续传，UI 由 mounted 守卫）；P1-W9 全图 Dialog 3 处加 errorBuilder/loadingBuilder（防 404 白框）；P1-W11 空态/元信息文案对比度提亮 17 处（darkGrey6→4）| app 94 · web 47 |
| 2026-08-15 | 走查修复批 W2 前端 | P1-W6 三端请求超时 15s（_TimeoutClient 包装，waiting 不再无限转圈）；P1-W8 app 删除确认弹窗（与 web 对拍，DELETE 不可逆）；测试适配 | app 94 · web 47 · admin 33 |
| 2026-08-15 | 走查修复批 W2 后端 | P1-W14 STATEMENT prompt 闭合引号；P1-W12 parseDateTime 不再回退 now()（脏 createdAt 跳过）；P1-W13 门控旁路三修（PATCH domain gateDomain + project tasks 403 + cards/cleanup intent/日期收紧）；P1-W15 启动时全量重建标签索引（TagIndexBootstrap）；+403/round-trip 测试 | 后端 454（+1）|
| 2026-08-15 | 全维度走查修复批 W1 | P0-W1 卡片多行 turn 单行化（对话丢失根治 + round-trip 测试）；P1-W10 LauncherPage SafeArea（误触）；P1-W1 web 图片重试重走 uploadImage（F37）+ P1-W2 app 文本重试复用 cardId（幂等）；P1-W3/W4 搜索+时间线自然化（第一原则漏面）；P1-W16 基建自伤（docs/ai frontmatter 断链 + 审查官 8 + status 端点 52）| 后端 453（+3）· app 94 · web 47 |
| 2026-08-15 | image 记录展示自然化 | 第一原则覆盖 image 记录：标题=VLM 总结、正文逐行去【备注】/【图片文字】标签（用户看到的是自己的话+自然内容，无第三视角）；生产验证 image_qa 自然化生效 | 后端 450（+1）|
| 2026-08-15 | image_qa 展示自然化 + 图片预览修复 | 第一原则「无第三视角」落地：Feed/时间线 image_qa 转自然对话（标题=问句、正文=问/答两行、去问：/答：/图片记录：标签）；输入栏图片预览缓存 Uint8List + cacheWidth 降采样 + gaplessPlayback（打字 rebuild 不再闪烁/大图解码失败）；VISION 沉淀原则 | 后端 449（+3）|
| 2026-08-15 | S-R1/S-R2（deep 战略项）| S-R1 adai-app launcher 插件失败 SnackBar 反馈 + 重试（双端对拍 web）；S-R2 服务端合并插件端点 `PATCH /accounts/{id}/plugins`（账号级锁原子 add/remove，根治 PATCH 全量并发互覆）+ admin 前端改走合并语义 + 内置 admin 插件服务端保护；api-spec v3.20 | 后端 446（+6）|
| 2026-08-15 | deep 审核修复批 S2 | P2-B2 Account 拒绝 null userId（脏 JSON 全局中断）+ findById Objects.equals + MarketAlert filter 防护；P2-R3 admin 内置插件开关 isProtected 门控（禁用 + Tooltip）；P2-R2 launcher 测试补「仅 trading」+「500 降级」分支；P1-D1 review-context 断链引用更新；P2-D1/D2 RFC/docs 状态同步 | 后端 440（+1）· app 94（+2）|
| 2026-08-15 | deep 审核修复批 S | P1-B1 domainEnum 去引号语义（CHAT 双重引号修复）+ P1-B2/B3 时间线聚合跨天/intent/歧义边界 + P1-B4 图片 domain 走 gateDomain + P2-B1 trading 写入口门控（/trades、/review 403）+ P2-R1 admin 插件 toggle 串行队列（双连点竞态）+ 边界测试 | 后端 439（+6）· admin 33（+1）|
| 2026-08-15 | 展示层聚合（S-2 + 时间线 bug）| 产品决策「一次输入 = 一个事件」：TimelineProjection 多轮 chat 每会话只保留首问（时间线单条）+ 带图 ask image_qa 引用图不单独成条（图文事件，缩略图取首图）；FeedAppService 同口径聚合；前端零改动；数据层不变（freeze 不破坏，层 2 另立 v1.0.1）| 后端 433（+4）|
| 2026-08-15 | REVIEW 修复批 R（前端 + 文档）| P1-5 adai-web 桌面壳当前页按 label 重解析（插件加载后索引不错位）+ P2-5 插件拉取失败 SnackBar 反馈/重试；P1-6 adai-app Launcher 插件接口拆独立 try/catch（不再拖垮核心数据）；P2-6 adai-admin 插件开关 toggle 前从最新列表重取（快速连点不再覆盖丢开关）；P1-7 api-spec D1 通用化同步；P2-8 feature-reference 补插件模型章节 + 端点表补 3 端点 | web 47（+1）· app 92 · admin 32 |
| 2026-08-15 | REVIEW 修复批 Q（后端插件门控/健壮性）| S-3 重补路径走 gateDomain（D5 收敛铺满持久化入口）+ S-4 MarketAlert 写侧 trading 插件门控 + P1-4 账号迁移读原始字段存在性（PATCH 清空不被推翻）+ P2-2 domain 规则由关键词常量拼接（单一真相源）+ P2-3 Account 过滤 null 插件元素 + P2-4 ContextPackage 携带收敛 domainEnum（CHAT 模式不硬编码）| 后端 429（+7）|
| 2026-08-15 | research 目录整合 | 按「个人/公司、新/旧、单仓多项目/单项目单库」前提：方法论两篇合并为《项目级 AI 上下文体系方法论》+ 公司侧研究（AI4SE/Workspace 架构/ai-native 系列等）全部移出仓库至同级独立目录 `ai-context-research/`；阿呆早期设计 `ai-context-design.md` 归位 `docs/inbox/`；3 文件初判失误复核后从 git 恢复（哈希校验一致）；CLAUDE.md 移除 research/ 说明 | 不变 |
| 2026-08-15 | 文档治理 | RFC `20260815-docs-governance`：瘦身 + 单一事实源（status.md / change-log.md / CLAUDE.md 指针化 / REVIEW 减负 / P3 迁移 task-log）；v1.0.0 发布顺延 | 不变 |
| 2026-08-15 | Domain=插件模型第二步 | 插件门控全通道：Account.plugins + PluginRegistry/PluginService + ContextEngine 全量门控 + D5 domain 收敛 + `/me/plugins` + Feed/promote 门控 + 三端显隐（RFC `20260814-domain-plugin-model`）| 后端 422（+28）· app 92（+3）· web 46（+2）· admin 32（+1）|
| 2026-08-14 | 带图 ask（多图问答）| 输入栏附图可 log/ask + 上限 3 + `POST /records/media/ask-batch` 多图一次问答 + intent 分流 + GLM 多图降级兜底；adai-web 同步（S-1）| 后端 387（+10）· app 86（+3）· web 44（+2）|
| 2026-08-14 | 概览卡/图片交互/删除残留 | 概览卡 1+3 铺满顶部 + DeepSeek 空内容重试 1 次 + 降级增强（📋/🧠/☕）+ 主动提示待办；拍照入口 + 选图统一 image_picker + 上传进度条；删除残留 P0（Memory.cardId + 双匹配）| 后端 377 · app 83 |
| 2026-08-13 | 键盘收起 | adai-app 点空白/发送后收起键盘（壳层 onTap unfocus + 发送后 unfocus）| app 83（+2）|
| 2026-08-13 | R2 记录↔任务 | domain=project 记录自动转任务（方案 B：sourceRecordId + RecordToTaskLinker + 清记忆待办）；A2 相机动作分析搁置 | 后端 374 · app 81 · web 42 |
| 2026-08-13 | REVIEW 修复批 P | deep 31 项清 22：#234 分页终止口径 + #235-#238 P1 + #240-#246 P2 + P3 14 项 | 后端 362 · app 81 · web 42 |
| 2026-08-12 | REVIEW 收官批 O | 战略 #101/#103/#177 + #179 登记 v1.0.1；P2 #19/#22/#115/#228；P3 顺手 14 项 | 后端 362 · app 78 · web 40 |
| 2026-08-12 | 修复批 N | #216 CardMigration 判定收紧 + #217 rewriteId 锚定 frontmatter + #223 os/ 只读例外 | 后端 359 |
| 2026-08-12 | 顶部摘要优化 | 概览卡去绿点前缀 + 行数 3→5（阿呆 08-12 反馈）| 后端 356 · app 68 · web 30 |
| 2026-08-12 | 修复批 M | #129 promote 前端入口 + #218 visual durationMs + #222 问候加中午段 | 后端 355 · app 68 · web 30 |
| 2026-08-12 | P2 修复批 L | #214 图片追问长度上界 + #215 available 最小集 + #221 降级问候 emoji 按时段 | 后端 355 |
| 2026-08-12 | AI 日志隐私治理 | #210 retention 30 天 + ai-logs 分页/日期上界 | 后端 351 |
| 2026-08-12 | 数据/隐私加固 | #227 重补过滤禁用账号 + #213 追踪上下文请求级清理 + #178 promote 融合提示 | 后端 344 |
| 2026-08-12 | P1 修复批 A-D + #184 | #184 promote 脱敏 + #204-#209 前端/图片追问 + #211/#212 候选命名与迁移 | 后端 340 · app 68 · web 30 |
| 2026-08-12 | R1 AI 交互日志 | LoggingAiClient/VisualAiClient 装饰器 + ai-logs jsonl 落盘 + `GET /admin/ai-logs` + AiTraceContext | 后端 340（+20）|
| 2026-08-12 | 生产反馈三连修 | #14 凌晨问候语 + #15 chat 对话折叠 + #16 输入框上滑误触 | 后端 314 · app 63 |
| 2026-08-11 | 生产验收批 | 8083 CORS 修复 + 图片追问（L4 图片问答）+ adai-admin 改名「阿呆控制台」| 后端 313 · app 61 · web 30 · admin 31 |
| 2026-08-XX | 批 K（多账号 deep 修复）| #180-#190：freeze 契约同步 / rebuild 幂等 / default 无效化 / 切换防重入 / endpoints.txt 生产计数 | 后端 300 · app 60 · web 27 |
| 2026-08-XX | 多账号前端选号/切换 | `/accounts/available` + World B 切换账号 + 记住上次账号 + wasm 白屏修复 + 切换崩溃修复 | — |
| 2026-08-XX | CanvasKit 崩溃修复 | 阿呆系统页入口无动画跳转 + 静态加载占位 | app 60 |
| 2026-08-XX | v1.0.0 验证修复 | updatedAt 时间基准 + #175 分页 + 复盘生成语义（AiClient.generate）| 后端 298 · 前端 60 |
| 2026-08-XX | adai-web 独立桌面端 | 两栏壳 + 8 模块桌面形态（两套 UI 非适配）| web 27 |
| 2026-08-XX | adai-app 即产品入口 | 砍掉 adai-entry，app 直接作为产品入口 | — |
| 2026-08-XX | adai-admin 全栈 MD11-16 | 账号体系 + admin 端点 + 前端四模块 | admin 31 |
| 2026-08-XX | 多账号架构预留 | data/{userId} 分层 + 迁移脚本 + 隔离测试（RFC `20260802-multi-account-prep`）| — |
| 2026-08-XX | v0.2.0 闭环 | action 待办卡 + PATCH done + memory kind/superseded + L5 行情条 | — |
| 2026-08-XX | 记忆系统进化 Phase 1-5 | kind + 主题合并 superseded + actionable 闭环 + 时效淘汰 + 筛选降噪 | — |
| 2026-08-XX | 文档体系精简 | 产品路线 v1 + 文档结构精简（inbox 归位 17 个重复文件）| — |
| 2026-08-XX | 发布版本机制 RFC | 版本号规则 + 发布流程 + Release Notes 模板 | — |
| 2026-08-XX | 记忆系统进化 RFC | 元记忆对比 + 方案落点（draft）| — |
| 2026-08-XX | 第三批审核修复 | #33 审核路由表 + #38/#39/#41 文档同步 + #21/#23 代码 | — |
| 2026-08-XX | 第二批审核修复 | #24 记忆沉淀断裂 + #12 复盘走 ContextEngine + #14 测试缺口（110+）| — |
| 2026-08-XX | 审核/交付流程基建 | /review 三档 + 5 角色 + guard.sh + /ship 闭环 | — |
| 2026-08-XX | docs/ideas 想法归档区 | 未定型想法的正式位置 | — |
| 2026-08-XX | os/ 目录统一 | domains/ 合并入 os/*-os/definition/ | — |
| 2026-08-XX | 任务系统修复 | ID 毫秒防冲突 + save() synchronized + 中文支持 + 清 65MB 损坏文件 | — |
| 2026-08-XX | Project OS 使用指南 | `docs/guides/project-os-usage.md` | — |
| 2026-08-XX | 方向 A Phase 1 | 行情接入（腾讯）+ CHAT 模式上下文注入修复 | — |
| 2026-08-XX | 后端接口测试全覆盖 | 15 Controller 46 端点接口测试（203→236）| — |
| 2026-08-XX | 多模态图片记录 L4 | 图片 → GLM-VLM 文本化进闭环 + 记忆 KIND_INSIGHT + 失败降级 | 256 |
| 2026-08-XX | adai-web 验收批1 | 输入栏内联多图 + ask waiting 态 + 时间戳 + 红涨绿跌 | web 25 |
| 2026-08-XX | adai-app 同步批1 | 同上同步移动端 | app 29 |
| 2026-08-XX | adai-web 验收批2 | FeedEntry.date + mediaPath + 原图 Dialog + 时间线缩略图 | 后端 258 · app 31 · web 27 |
| 2026-08-XX | 语音 stub 移除 | 砍误导性语音入口（REVIEW #164）| app 31 |
| 2026-08-XX | 主轴问题批 E | #108/#113/#102/#162/#132/#131/#123 | app 33 · web 27 |
| 2026-08-XX | 质量锁定批 F | Feed 状态机 12 widget 测试 + 文案全量中文化 | app 45 |
| 2026-08-XX | 6 页面测试批 G | pages_widget_test 14 测试 | app 59 |
| 2026-08-XX | #127 最小封闭鉴权 | X-Admin-Token 拦截 + CORS 白名单 | 后端 262 · admin 31 |
| 2026-08-XX | 桌面残留清理批 H | #102/#132/#161/#131/#124/#158/#159/#118/#165 | web 27 |
| 2026-08-XX | 对话体验收尾批 I | #13+#11 JSON 剥离 + #148 跨日记忆 + MD1 世界切回刷新 | 后端 287 · 前端 60 |
| 2026-08-XX | v1.0.0 定调 + 发布准备 | 版本定调 + 数据格式冻结 + 路线图 v1.0.0-first + Release Notes | — |
| 2026-08-XX | P1 清理批 J | #144 rebuild 幂等 + #147 SELL 报错 + #106 portfolio + #112 CANCELLED + #150 动态计数 | 后端 293 · web 27 |
| 2026-08-06 | 方向 A Phase 2 行情推送 | MarketAlertService 交易时段轮询 → type=push 入 Feed | 后端 276（+14）|
| 2026-08-XX | 数据冻结 3 项差异 | freeze #1/#2 手动维护 + #3 账号 createdAt ISO 统一 + 迁移 | 后端 294 |
