# 状态快照（Status）

> **测试数/端点数/运行环境的唯一事实源**（RFC `20260815-docs-governance` 单一事实源化）。
> **更新规则：每批 /ship 时更新本文件**——AGENTS.md 及子项目 AGENTS.md 一律不复制数字，只指回本文件。

---

## 测试状态

| 端 | 测试数 | 失败 | 备注 |
|:---|:------:|:----:|:-----|
| 后端 adai-core | **2205** | 0（2 skipped）| **2026-09-23 节律批（RFC 20260923 B 批，+27；数字含同日并发会话新增用例）**：A 批止血后的「正解」落地——**节律独立为 Kernel builtin 一等条目**：`kernel/rhythm/`（`Rhythm` 模型 · `RhythmStatus`（active/paused/retired，**刻意无 DONE**）· `RruleSchedule`（RRULE 子集解析 + 命中判定）· `RhythmDetector`（判据单一真相源，从 BriefAppService 私有副本收敛））+ `RhythmFileRepository`（`data/{userId}/rhythm/YYYY/MM.md`）+ `RhythmAppService` + `RhythmController`（4 端点，无插件门控）+ `RecordToRhythmLinker`（**D5 写入侧分流**：周期表述先试节律、推断不出 RRULE 才回落待办）+ 简报**闸 1**（只有 RRULE 命中当天注入，口径 BACKGROUND ONLY）。**顺带补漏**：`.gitignore` 补 `data/*/rhythm/`，并发现 `data/*/todos/` 自 RFC 20260917 起就漏了忽略规则（本地无该目录故未暴露，一旦出现 `git add -A` 会把私人待办入库）一并补上。 **2026-09-23 节律闸门批（RFC 20260923 A 批，+3）**：用户「阿呆 app 概览卡片，天天提醒我」——根因三处：① 09-17 一句状态陈述被 `RecordToTodoLinker` 转成**永久 OPEN 待办**；② 简报 prompt 同时注入该待办（附「主动提醒 1-2 件」）与近 7 天记忆（附「发现习惯就自然提及」）；③ `Memory` 无周期维度/有效期，且简报读不过滤 superseded 的 `recent()`。**止血全在注入侧、零数据改动**：`isRhythmLike` 判据（每周/每月/每天/例会/定期 或 周X＋固定）→ 周期习惯不进提醒段 · `MAX_BRIEF_TODOS=3` 显式化 · 删除「习惯就自然提及」指令 · 改用 `recentActive()` · 规则 7 收紧 + 新增规则 8（禁编造提醒、禁把习惯当要做的事）。**+3 测试**（判据正反例含「周四要交周报」不误伤 · prompt 级断言 · 全节律时提醒段缺席）。**未部署**（B/C 批见 RFC §五）。 **2026-09-23 账目口径批（P2-交易70 曲线修复 + P2-交易66 本金说明，+2）**：① **资金曲线/周期盈亏的持仓重置改用 `positionsReplace`**（原用 `anchor.latest()`——只导资金会把持仓锚定日一起往后拽，09-19~09-22 退回「底仓+流水回放」→ 市值虚高约 2 万、周期盈亏百分比分母跟着虚高；判据「cashImport 推后，两条曲线必须一模一样」，**反向验证通过**）② 账户接口新增 **`principalNote`**（手填本金 + 历史出入金零记录时的置信度说明，双端灰字渲染）；**2026-09-23 分享回执批（REVIEW P1-分享8，+6）**：① `POST /learn/digest` 按来源链接去重——`status=done` 现在也可能是「这条早就整理过了」（不抓取、不调模型、不建新任务，直接以已有卡片回执；判据为链接逐字相同、去空白去尾斜杠；**素材路径不参与**；查重失败按没整理过继续）；② `done` 结果 TTL 60s → **30 分钟**（分享扩展 1 秒关窗、主 App 不被拉起，60 秒早过期 → 进学习页时已回 idle，「我整理好了」根本没机会说出口）；**2026-09-23 现金健康度批（P2-交易69，+5 `CashHealthNoteTest`）**：`GET /trading/account` 新增 `cashDate`（现金的券商快照日期）与 `cashNote`（负现金/无来源/过期的人话提示，阈值 7 天）——起因是现金在两次「资金股份查询」之间会漂（09-15 曾 −6,093.97、09-23 虚高到 24,101.01，券商真值仅 414.86 → 总盈亏少报 2.37 万）而用户侧无提示；同批 `account_returnsSnapshot` 契约测试同步新字段；**2026-09-23 巡检修复批（+7）**：P1-交易63 `poll-cron` 两处真相源收敛（删 yml 键、只留代码常量 `CRON_POLL`）+ 守卫 `MarketAlertCronConfigTest` 2 · P2-交易68 早盘盘前不再渲染恒 0% 的今日涨跌幅（+2）· P2-交易67 收盘复盘「有自算账就照常复盘 + 标注来源」（+2）· P2-交易62 `integrity` note「不可判定」不再冒充确定语气（+1）；**2026-09-23 持仓导入保留券商「现价」批（REVIEW P2-交易65，+4 `PositionCurrentPriceImportTest`）**；**2026-09-23 行情链路韧性批（RFC 20260923 A/B/D，+15）**：腾讯 K 线域名可配 + 区间本地裁剪 2 · 新浪第三源 KlineService 接入与 health 5 · 新浪源解析/单位/降级 6 · 行情健康端点 2；前批：**2026-09-22 S-11 B 批·决策时点对话式提醒（RFC 20260922 §六 B，+29）**：时段推送测试重写 +5（30→35：复盘随同步触发 / 未同步如实说 / 已发过不重发 / 午间知会不催操作 / 行情显式降级 / 四要素渲染）· Controller 接线 +2（导入成功 → 触发复盘 / 空导入不触发）；新增 `TradingDecisionNarratorTest` 12（①②③④ 逐条可指认 + 缺证据不发 + 样本不足直说）；新增 `TradingSyncStateRepositoryTest` 7（读损坏按未同步、写失败不抛、跨字段保留）；`WatchlistBuyPointServiceTest` +3（`unavailable` 与「没信号」分得开）；**数字含同日并发会话新增的用例**；前批：**2026-09-22 图文一体·带图对话链路批（+16：多图投递 5 · 幂等 1 · 仓储 mediaIds 1 · Feed 多图 1 · 端点 4 · 时间线多图/附件抑制 1 · 交易归集前置判别 3 · 搜索薄附件抑制 1）**；**2026-09-22 S-11 A 批·铁证底座（+32）：基础 +14（历史统计分桶与样本门槛 8 · 铁证只读出口端点 6）+ A1 买点形态维度 2 + A4 建议出口带四要素铁证 2 + A3 依据快照 1 + A3 结果回填 13（服务 7 · 留痕仓储往返 4 · 回填出口 2）**；**数字含同日并发会话（多图一次投递批）新增的用例**；同日前批 +8（`integrity` 降级流水 `degraded[]` 4 · 锚定文件日期留痕 3 · 未来日期归一化 1）；前批：**2026-09-19 三官深审当批修复（+1 测试，RFC 20260918 第五轮）**：**P0-1 confirm 同笔指纹纳入成交时间**（本批候选层修好了同价同量分单、**确认层没修** → 第 2 笔会命中第 1 笔刚落的流水被判「这笔之前已经记过了」跳过，卖 400 股仍只记 200 股）+ **P0-2 `appendTradeRecord` 改返回真实 boolean**（原来 catch 全异常从不抛 → `ledgerOnlyTrade` 恒 true，写失败也报「已记进流水」并清候选）+ **P1-3 `ledgerOnlyTrade` 补 per-user 流水锁**（流水仓储 read→add→write）+ **P1-2 `normalizeAnchorDate(null)` 也走同一套归一化**（粘贴导入没有文件名日期，原来 null 早退返回 today → 原事故在该路径原样保留）+ `direction` 白名单（端点 400 / confirm 未知方向保留候选）+ `PUT /trade-log/meta` 旧口径带核心字段 → 400。 + **2026-09-18 A1-4 候选就地编辑批（+3 测试，RFC 20260918）**：`PUT /api/v1/trading/trade-log/meta` 从「只补 orderId/fee」扩展为**候选就地编辑**——改正截图识别错的价格/数量/方向/成交日期（带 `id` 行级定位；改完**重算 `complete`**，补全后即可确认不再被跳过；不带 `id` 的旧口径仍只补元信息，保持兼容）+ `TradeLogRepository.updateFieldsById`。**顺手修掉一处本批引入的真 bug**：补日期/补 meta 的四处重建候选漏传 `tradeTime` → 会把成交时间静默清空（同价同量分单的区分维度），已补回归钉住。 + **2026-09-18 截图入账分单吞并与锚定误锁修复批（+11 测试，REVIEW P0-交易59）**：① `TradingParseAppService` 新增 `extractTradeTime`（横排/竖排两条路径都抽截图「成交时间」列）→ `ParseResult` 与 `TradeLogCandidate` 新增 `tradeTime`，`sameTrade` 纳入时间维度（**同价同量的分单不再被吞**——生产实据 000831 两笔各 200 股 @53.300 / 10:03:44 与 10:04:09）；② 归集改走新 `TradeLogRepository.appendBatch`：**同一批解析结果内互不判重**，只与批前候选去重（同图重传仍不翻倍）；③ 命中券商快照锚定**不再硬拒**，改 `TradingAppService.ledgerOnlyTrade`（**流水照落、持仓/现金以券商快照为准**，与历史成交导入同语义），`POST /trading/trade-log/confirm` 响应新增 `ledgerOnly` 计数、`confirm` 时优先用候选自带的成交时间（原写死 `LocalTime.now()`）；④ 新增 `normalizeAnchorDate`——**盘前（09:30 前）/非交易日导出的「当日」快照**锚定到**上一交易日**（通达信文件名日期是导出日、内容是上一交易日收盘；凌晨导入不再把当天提前锚定 → 当天成交不再被判「已含在快照内」），补导历史快照仍按文件日期。测试 +11（解析抽时间 2 · 批内不吞 2（含**生产截图原文端到端 6 笔 → 6 条候选**）· 锚定降级 2 · 锚定日归一化 5）——**2030 全绿**（0 失败 / 0 错误 / 2 跳过）。 + **2026-09-18 Feed 空态分流批（+4 测试）**：`GET /api/v1/feed` 响应新增 **`hasHistory`**（该用户是否有过任何历史记录，不限当天；REVIEW P1-UI14 复发修复）——空 Feed 从此能区分「真·新账号」（空态播能力引导三问）与「老用户今天恰好没记录」（改「接着上次的聊也行，我记着」，不再摆三问）。判据放服务端（前端本地标记重装/换设备即丢，正是本案场景）；**零额外 IO**（由筛当天本就调用的 `findAll` 派生），端点不变。 + **2026-09-17 RFC 20260917 待办归 Kernel 批（+30 测试）**：撤 project 插件——`domain/project`（Task / TaskStatus / TaskRepository / ProjectContextContributor / package-info）、`ProjectStatusAppService`、`ProjectTaskAppService`、`RecordToTaskLinker`、`ProjectStatusController`、`ProjectFileRepository`、`ProjectKnowledgeSource` 全部删除（连带 30 条旧 project 测试移除）；**待办归 Kernel builtin**（无插件门控）：`kernel/todo/` + `application/TodoAppService` + `RecordToTodoLinker`（记录可执行→自动建待办、不再清记忆）+ `TodoReminderService` + `interfaces/TodoController` + `infrastructure/storage/TodoFileRepository`，两态 `OPEN`/`DONE` + 可选到期日 `due`（null 保持原值、空串清除），并补全 Todo 全链测试（自动建待办 / 完成删除的单向记忆同步 / 到期 08:00·18:00 两次推送 / 门控移除回归 / 旧 `DOING`·`CANCELLED` 读作 `OPEN`）。**2026-09-17 B4 凭据收口批（+7）**：① 新增 `ApiTokenGuard`——外部令牌的**付费动作闸门**（`/learn/digest/confirm` 令牌级频控 5 次/分钟 + 同一把钥匙换 IP / 超频告警，会话调用不受限）+ 4 条回归；② `POST /records` 支持可选 `source` 标记（`external_entry` = Siri/快捷指令入口；缺省/未知落回 `user_input`，存量口径不变）+ 3 条回归；③ `AuthFilter` 注入 `X-Adai-Token-Id`（客户端不可伪造）供闸门识别令牌。**2026-09-17 B1+B2 批（登记一致性 + 巡检遗留收口，+11）**：① `purge` 失败清单 + 新端口 `deleteEmptyDirectories` 回收空目录（+2 回归）② 图片配额 `tryConsumeImages` **锁内原子**「读→判→写」（+4 回归，含 **24 线程并发恰好受理 limit 次**实测）③ 横排表格补「价格 × 数量 = 成交额」交叉校验、抽出 `reconcileVolume` 供竖排/横排共用（+3 回归）④ 兜底源与主源双双失败记 ERROR（30 分钟冷却防刷屏）+ tdx 缺口补齐失败 WARN 含缺口天数（+1 回归）⑤ 股东代码残留脱敏（`TradingImportParserTest` 5 处 → `A000000000`，测试数不变）。**2026-09-17 巡检修复批（P0-交易53，截图入账识别链）**：新增**竖排表格还原** `TradingParseAppService.parseVerticalTable`（VLM 把表格一行拆成多行时，按「行类型」以方向行为锚点重建：向上取价格/代码/名称、向下取数量/成交额/时间；四要素不齐不产出，且做 `价格×数量 = 成交额` 交叉校验修正）+ `TradeLogCandidate.sameTrade` **补价格维度**（原键只看 symbol+方向+数量 → 同标的同方向各 100 股的三笔互相吞并）；**+8 测试**（解析 3 + 去重 3 + 端到端「解析→归集→去重」2，1953 → **1961** · `--rerun-tasks` 真跑 0 失败 / 2 skipped） + **2026-09-17 deep 增量深审 + 当场修复**（四官隔离并行 + 主会话逐条复核，范围 `d060841..HEAD`）：**两条被判 P0 的发现经复核均不成立**——Keychain「没签进包」（实测描述文件为 `keychain-access-groups: ['4G3D37YKSB.*']` **通配授权**；报告引用的归档是旧 entitlements 那次）与「上一交易日盈亏写进今天快照」（`refreshTodayPnl` 经 `computeDailyPnl→dailyPnlDetail` 属实，但新提示落在 `actionable` 里 → 闸2 直接 return 不写回）。**四条真问题当场修复**：① `restoreOrigin` 判据去掉 `status:`（A 形态技能卡模板自带 status → 每张只读卡都能被「认回」成可写，只读保护失效；+1 反向测试）② `/trading/pnl-periods` 盘前覆盖 today 并回填 week/month **双计** → `DailyPnlDetail`/`DailyPnlResult` 加 `effectiveDate`，调用方只在同日时覆盖/回填，写回闸2 同步从「文案前缀」改**显式判定**③ 深链中文 key 匹配不到 → 兜底高亮「最后一张推送卡」= 点 A 亮 B → 改为**只认精确命中**（不猜）④ `change-log.md:9` 残留真实股东代码（写「脱敏」那行又把原值抄了一遍）→ 全仓 0 处，审查报告里的证据也一并打码。**两条加固**：图片配额失败/执行器拒绝**回退**（接口声明「允许负数回退」原是零调用者；+1 测试）· Keychain 迁移**不再回删旧明文** + `write` 的 add 失败恢复旧值（先删后加不是事务）。**一条审查建议经复核不采纳**：前端 `writable` 改 fail-closed（保护边界在后端 `requireWritable`，前端改会让「新前端+旧后端」时用户完全不能编辑）。**+7 测试：1939 → 1946**（其中 5 例来自**并发会话**新增的 learn 记忆注入测试，见 change-log 同批次说明） + **【并发会话】learn 链路打通批（RFC 20260917，`docs/rfc/20260917-learn-representation.md`）**：① **门控 B**——`POST /learn/digest`（含别名 `/cards`）**无 learn 插件不再 403**，改为「只接收、不整理」：素材落成一条记录（`type=note`/`source=external_import`，**不抓取/不转写/不调 LLM，零费用**）并回 `status=recorded`；**入参校验提前到门控之前**；其余 learn 端点 403 不变。② 卡片生成 prompt **回灌用户画像**（偏好/行为模式各前 5 条，明示只决定怎么讲、不复述、不评判；无画像时逐字不变）。③ **双端适配** `recorded` → 停止轮询（无插件轮询必 403）+ 人话提示。④ api-spec **v3.72** + 只读接线盘点 `docs/review/audits/2026-09-17-wiring-audit.md`（5 类 11 条）+ 讨论沉淀 `docs/ideas/20260916-plugin-and-cold-start-discussion.md` + **2b 产物反馈（RFC 20260917 §五）**：新端点 `POST /learn/cards/feedback`——用户对卡片的评价沉淀为 preference 记忆（`Memory.fromFeedback`，置信度 0.9），经画像回流作用于**下一次**生成（闭环无需额外机制）；**不自动重排**（回 `canRepage` 交前端问用户——重排才调 LLM）、**幂等**（同一句不重复沉淀，防刷爆 Top 5）、属「整理能力」**仍 403**；+7 测试：1946 → **1953**+ **2026-09-17 深夜第二批（用户「继续，我还没睡」）**：① **通知点击深链（P2-APNs1）**——APNs payload root 级新增 `adaiDeepLink`（由 `PushMessage.deepLink()` 从已有字段推导，推送构造点零改动）；iOS 把深链随点击一起交给 Dart（编码 `<type>|<deepLink>`，`pendingTap` 冷启动补投通路零改动）；Feed 命中后定位 + 高亮 2.5 秒，命中不到只刷新 + 滚到底（不假装定位成功）；+后端 2 测试 / Dart 2 测试。② **令牌轮换（S-凭据1 剩余项）**——新端点 `POST /auth/tokens/{idOrPrefix}/rotate`：先发新、再撤旧，旧撤不掉则回滚新的（不留两把同时有效）；label/权限继承、有效期重算 90 天、前缀歧义拒绝；+2 测试。③ **到期倒数接进每日巡检（P2-APNs5）**——`guard-prod.sh` 末尾新增 iOS 描述文件/付费账号、公安备案、域名三项倒数，30 天内标红（防「打不开日」重演）。**+4 测试：1935 → 1939**；端点 153→**154** + **2026-09-16 晚间批（用户逐项拍板：盘前盈亏口径 / 图片费用闸 / 来源标记恢复 / 删号语义 / iOS 分享凭据）**：① **盘前「今日盈亏」改用行情所在交易日**（P2-交易51）——`dailyPnlDetail` 在盘前/非交易日按**上一交易日**算并标注（历史日期查询不受影响）；新增 `quoteIsFromPreviousDay` / `previousTradingDay` 两个纯函数 + 3 测试。② **图片整理轻量日配额**（P2-learn26）——`adai.learn.image-daily-limit`（默认 30 张/日，0=不限），账本落 `learn/_quota.json` 的 `images` 键；读不出账本 → fail-closed；+2 测试。③ **读图上限可按调用覆盖**（P2-learn25）——`VisualAiClient.ask(…, maxTokens)` default 实现忽略（老实现零改动），learn 链用 `adai.learn.image-max-tokens`（默认 4096）。④ **新端点 `POST /learn/cards/restore-origin`**（P2-learn21）——origin 被抹掉的卡能认回来；判据 = 正文含 `## 卡片页` 或 frontmatter 带 `status`/`review_at`/`reminded_at`，不成立则人话拒绝（不给别人的卡盖章）；+2 测试。⑤ **删号数据语义**（task-log #149）——`DELETE /accounts/{userId}` 默认保留数据，`?purge=true` 才清理并回 `purgedFiles`；+2 测试。⑥ **iOS 分享凭据加固**（P2-分享2/分享3）——令牌明文从 App Groups 容器升级到**共享 Keychain**（两个 entitlements 加 `keychain-access-groups`；老装机读旧容器自动迁移）；扩展 API 基址改读主 App 运行时写入的地址（Info.plist 仅兜底）。⑦ 工程卫生：真实股东代码机械脱敏（9 文件归零）· `ship.md` 禁 `git add -A` 收尾 · `audit/review` 审查官计数改诚实口径 · 清掉测试文件里一处历史遗留的真 NUL 字节。**+9 测试：1926 → 1935**；端点 152→**153** + **2026-09-16 巡检修复批（Feed 排序 / 失败可见 / 成交折叠 / 短链）**：① **Feed 乱序根治（P1-前端2）**：`time` 只到 `HH:mm` → 同分钟排序键相等，且附加条目原先是**直接追加在末尾**（生产实测 10:03 的 ai_note 排到 21:30 的 record 之后）→ 改 **`time`+`id` 双键** + 附加条目**并入同一条时间轴**再输出；② **消化失败保留 30 分钟（P1-分享7）**：新增 `FAILED_TTL_MS`，不再沿用 done 的 60 秒（done 有卡片兜底、failed 没有，60 秒后用户永远看不到）；③ **t.cn 短链收进微博抓取器跟跳转**（原先它掉进 `ArticleFetcher` 兜底，把落地页当正文抓成废卡还白烧一次 LLM）；④ **同分钟同向成交折叠（P2-UI12）**：展示层按 (date,time,方向) 折叠为「买入 N 笔」并带 `mergedIds`（账目真相源 trades/account/positions 与写侧记录一行未动）。**+3 测试：1923 → 1926** + **2026-09-16 微博分享落地页短链跟跳转 + 消化失败日志记「输入形态」（REVIEW P1-分享7）**：微博 App 分享给第三方的是分享落地页短链 `mapp.api.weibo.cn/fx/<hash>.html`（实测 302 → `m.weibo.cn/status/<id>`），路径里没有任何 id → `midOf` 必然认不出（用户两次分享失败的根因）；修：对该形态最多跟 3 跳 302（`HopFetch.once` 逐跳过出站白名单、落点只做正则解析不再请求），拿到 mid 后走原抓取路径 + 失败日志新增 `urlShape`（记 scheme://host/path + 字节数，**不记 query 内容**；非 URL 文本报字节数与中日韩字符数）——失败从此可定位、不再只能靠猜；其余认不出的形态照旧直接人话失败，不为猜形态白付一次网络超时（+5 测试：WeiboFetcher 2 + LearnDigestAppService 3） + **2026-09-16 第一次见面批（RFC 20260916；用户「直接开工」授权）**：GET /memory/insights 聚合长期观察（patterns/preferences 合并按置信度降序 + observedSince）+ PUT /identity 放宽 name/tags 非空（零画像新用户此前**保存不了任何东西**）+ defaultProfile 默认名由「阿呆」改空（消除「称呼：阿呆」身份串线）+ ContextEngine 空名不注入空称呼行 + **开场问答能力边界 prompt**（已开/未开插件如实告知，禁编造清单外能力）+ BriefAppService 空名问候语不拼称呼 + **聚合窗口修正**（`readAllMemories` 按月读替代按天 365 次解析；insights 用 365 天长期窗口而非默认 30 天——否则两个月没记录的用户会看到「我还不认识你」）（+10 测试） + **2026-09-15 learn 卡片流批（v3.69，未部署；用户「你不准备优化么」授权）**：卡片流「一页一单元」全链——① 消化 prompt 增产页序列（8-14 页，六种 kind）；② 页结构落 md 新段 `## 卡片页`（原四段零改动，旧卡/技能产物/问答召回全不受影响）；③ `GET /learn/content` 暴露 `pages`（老卡为空 → 双端自动降级）；④ App/Web 详情页改卡片流渲染 + 原文折叠区；⑤ 历史卡回填能力 `POST /learn/cards/repages`（用 `_raw/` 素材重排页，只补呈现层、正文一字不动；只读卡/无素材/AI 失败都给人话且不写盘）；⑥ 宽容解析 `parseLenient`（模型直出数组/围栏/前后废话都能认）。**+21 测试**（页解析 13 + 页段手术/回填 5 + 消化带页 3 + Controller 6；learn 全量 330 绿、后端 1905 绿） · **2026-09-14 登录体验批 L2 后端（v3.67，未部署；RFC `docs/rfc/20260914-login-credential-experience.md`）**：登录请求可选上报 `device`（客户端自述、不作安全判定）+ 响应新增 `sessionId`；新增 `GET /auth/sessions`（登录设备列表，按最近活跃倒序、标记 `current`）+ `DELETE /auth/sessions/{idOrPrefix}`（单设备撤销：前缀歧义 **400** 防误撤 / 撤销当前设备 **400** 提示用退出登录 / 找不到 404）；两条端点不续期不写盘且复核账号 enabled；**老会话文件无 `device` 字段读成 null——升级不把已登录设备踢下线**（+14 测试） · **2026-09-14 当日盈亏券商口径批（v3.66，未部署）**：卖出部分基准 建仓成本→**昨收**（与券商 App「今日参考盈亏」同口径；实测系统 5826 vs 券商 2245）+ 行情符号改「持仓 ∪ 今日成交」（今日卖光的票也要拿昨收，否则该笔漏算）+ **新端点 `GET /trading/positions/daily`**（逐股当日盈亏/今日涨跌幅/仓位比例 + 总仓位/现金比例）· **2026-09-14 账本可见性批（v3.65 已上线）**：P2-交易42/43/44/45/47/48 + 工程3/4——历史成交解析丢行带**行号+原因**上报（+ 修一条价格非数字 NPE 隐患）· 截图/一句话归集丢行可见（`dropped`）· 资金股份首行数值读不出 → **fail-closed 400**（原来 null 写进账户）· 画像空串拒绝 · 账户快照新增 `todayPnlSource`（券商/系统计算/未知）· 负成本口径核对（曲线负市值兜底 + 建议引擎百分比 null；**用户拍板「券商负成本」：负成本持仓卖出的旧仓成本改用券商成本价含负**）· `positions.md` 死字段不再进日志 · **18 个文件仓储 JSON 读路径开 `FAIL_ON_TRAILING_TOKENS`**（+16 测试） + 全部 Controller 接口测试全覆盖 + **2026-09-06 learn 插件 V1（RFC 20260829，测试先行，+37）**：PluginRegistry 注册 learn（第三插件）+ LearnCard/domain 端口/LearnException + LearnCardFileRepository（md 卡片 File First：data/{userId}/learn/{type}/{date}_{title}.md，per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 清洗防路径逃逸）+ LearnDigestAppService（喂入→LLM 结构化 RFC 3.4 四段卡片→落盘；LLM 失败/输出不可解析/缺标题→素材留存 learn/_raw/ + 400 fail-visible 不产半成品；type 越界回落 other；trade_related 非 trading 强制 false）+ LearnController 3 端点（POST/GET /learn/cards + GET /learn/tree，learn 插件 403，独立端点喂入仿截图入账不污染记录/记忆/Feed）+ GlobalExceptionHandler LearnException→400 + 多模态 + 鉴权 + 交易建议引擎（止损硬判定）+ 一句话交易解析 + 交易数据模型（逐笔流水/止损/买点）+ 带图 S-2 聚合修复 + G-1~G-6 框架+插件 + 真止损预警 + 时段节奏推送 + 通达信持仓导入/代码查名 + 持仓行情注入 + 导入文件上传留存（GBK 转码）+ C2 自选股买点判定（KDJ/B1回调缩量/B2放量破前高）+ D3 清仓复盘三维打分（买点回溯/执行纪律）+ 择时状态配置驱动读取 + 持仓元信息更新端点（PUT /positions/{symbol}，止损/角色）+ 简报注入真实持仓快照（防 LLM 算错盈亏）+ 导入解析失败禁止落零（P1-交易5）+ 买卖同步市值（P1-2）+ 收盘缺行情跳过（P1-3）+ P2 批（线程池/缓存/阈值-5%/KDJ-13/并发扫描/节假日）+ 八端点 controller 测试 + P2 批（原子写/插件缓存/任务删除门控/记忆三缺陷）+ S5 现金单一真源 + P1-交易4 占比分母含现金（回归测试）+ Memory 正文 `---` 分隔不截断（回归测试）+ RFC 20260817（推送开关/结构化推送/交易日志归集，PushSettings 4 + TradeLogCollect 5）+ 20260818 批次（历史成交导入解析/幂等/对账 + /trades/batch 补实现 + 持仓 replace 全量覆盖 + BUY 止损/买点放开 + 本金设置 PUT /principal）+ P1-1 交易归集 unknown 污染修复（拒绝归集/complete 补 symbol/dedupeKey name 兜底）+ P2-1 东财熔断（连续失败 3 次熔断 5 分钟 + 半开探测）+ 首屏提速（brief/cached 端点 2 测试）+ RFC 20260823（历史成交导入回填：幂等命中且缺成交时间 → 回填 updated 计数，回填 round-trip ×3 + 重传回填 + 已有时间不重填）+ **2026-08-23 走查修复批（B1-B5，21 项）**：confirm 失败候选保留 + account.json 原子 RMW（per-user 锁）+ direction @NotNull + batch 逐行校验 + 尾盘 r81Applicable + 收盘昨收残缺跳过 + 快照写失败告警 + verdict R53 延展 + 节假日 2026 官方/2027 预测 + MarketAlert 判定拆开 + dedupeKey volume 桶 + TradeLog save 锁 + MarketPush 读失败拒写回（+11 测试）+ **隔离审查残留批（B6-B7）**：MarketPush 结构校验 + sameTrade ±10% 区间去重 + 三处 changePercent 判空 + 快照写失败抛 StorageException + confirm 单 now + 切 Tab 静默刷新 + DELETE /trade-log 丢弃端点 + 真实仓储并发/损坏测试（+17 测试）+ **C 批**：confirm 处理期新候选合并保留 + 2027 中秋修正 + batch 空 400 + name 超长校验 + 锁收敛 userId（+3 测试）+ **2026-08-24 行情主源切换批**：KlineService 主源/兜底配置化（`adai.market.kline-primary` 默认 tencent——腾讯主源 + 东财探测兜底，生产东财被限不再刷 500+/日 WARN；Tencent 补 KlineSource 实现，+1 配置切换回归）+ **2026-08-25 推送渠道切换批**：BarkPushChannel（iOS 原生推送，PushChannel 第二外部实现，POST JSON，base-url 可配自托管；微信 Server酱 停用——免费 5 条/天不够，代码保留未配置即禁用；+4 测试）+ **2026-08-25 RFC 20260825 批次跟踪批**：批次推导（按日合并/LIFO/回合/初始批次/对账，TradingLotService）+ GET /trading/lots 批次视图 + 导入双模式（sync 同步持仓/append 补录，orderId 透传幂等）+ 每日操作总结（行为标注六类：亏损加仓/追高/短线新开/破止损未走/浮盈回吐/短线超期）+ 批次级止损推送 + 推送 expiresAt 定时消失（行情类次日 09:30/汇总类次日 23:59）+ recordTradeWithOrderId + 手动/导入交叉防重 + 卖出费用入回合 + 初始批次卖出扣减 + 批次止损推送测试 + 卖清重买不误判（+27 测试）+ **2026-08-26 截图表格归集批**：parseLooseBatch 表格批量解析（委托截图多笔 → 逐笔候选，已成/已报/申购过滤）+ collect 批量归集（跨截图 sameTrade 去重）+ 生产真实验证（+8 测试；工作树含遗留未提交测试 +6，实测 786）+ **2026-08-26 截图入账 + 复盘卡点批**：POST /trading/screenshots 截图入账（multipart 1-3 张 → VLM → 候选，不建记录/不落原图/不沉淀记忆，TradingScreenshotAppService）+ hasTradingActivity 改当日真实成交口径（废除关键词扫描）+ 复盘与截图入账成闭环（+14 测试） + **2026-08-29 晚间自主修复批**：confirm 锁内原子 saveMerging（P2-交易25 残余窗口闭合）+ 三处锁池固定 16 条带（P2-交易28 map 增长根治）+ MarketPush 写失败 warn→error（P2-交易27）+ 收盘缺行情推送通知（P2-交易33）+ closeAccountUpdate 残余窗口注释（P2-交易24）（+2 测试） + **2026-08-29 收盘小结批**（15:30 推送：当日成交+破止损+待确认，close-summary 类型+双端开关，+3 测试） + **2026-08-30 交易插件规则层（第三阶段，trading-plugin-architecture.md）**：TradingRuleSettings（16 参数：仓位上限/默认止损/浮盈回吐/短线超期/清仓阈值/买点 5 参/打分权重/硬约束区间，fail-closed）+ TradingRuleSettingsRepository（data/{userId}/trading/rules.yaml 读写，YAML 真相源）+ 规则引擎按用户（仓位上限/行为标注/清仓 verdict/买点参数/打分权重/建议硬约束区间全部从用户规则读，无规则 → 默认=adai 现状）+ 知识注入用户私有（data/{userId}/trading/knowledge.md 优先，os/ 作默认）+ GET/PUT /trading/rules 端点 + 降级验证（无规则用户=纯客观）+ adai 规则包生成（rules.yaml + knowledge.md，行为不变回归）（+16 测试） + **2026-08-30 审查修复批（deep 深审 5 官）**：P0-1 保存链路 fail-visible（save 抛 StorageException/空提交 400/NaN 拒绝）+ P0-2 硬约束区间 R66-R95 内收缩防 fail-open + P1-1 止损判定线统一（effectiveStopLoss 传 userId）+ P1-3 多用户知识泄漏 owner 白名单 + P1-4 domain 端口接口 TradingRuleSettingsPort + P2 批（per-user 条带锁/NaN finite 校验/knowledge.md 缓存/save 保留未知键/买点参数防轰炸下限/文案区分用户 vs 默认/测试独立文件/引擎 TTL 缓存）+ P1-2 规则包同步脚本（09-scripts/sync-adai-rulepack.sh）+ P1-5 降级语义定稿默认值兜底 + P3 死代码清理（+22 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：POST /records/ask-stream SSE 端点（text 增量/meta 定稿/[DONE]/error 事件；后端内降级——模型无增量回退同步 understand 一次；同卡同问 5 分钟去重 QuestionAppService.findDuplicateResend；JsonTailFilter 防 JSON 回执跨块切开；UTF-8 byte[] 透传防中文乱码；线程池满 503）（+16 测试：AskStreamController 5 + JsonTailFilter 5 + QuestionAppService answerStream/dedup 6）+ **2026-08-30 推送口径修复批（用户反馈）**：行情提醒节假日守卫（B5-1 残留：节假日撞工作日 break-cost/止损类拿前日收盘价每天重推）+ 轮询首轮 9:00/9:30 → 10:00（9 点档行情接口仍返回上一交易日收盘，旧数据冒充「今日」且按日去重签名被烧名额）+ 早盘计划昨收口径（数据文本标注「昨收/昨日涨跌」+ 提示词第 5 条，LLM 不再把昨日涨幅说成「今日」）（+4 测试） + **2026-08-30 完美买点案例库批 1（第四阶段环 1-2，trading-case-library-design.md）**：KlineSource.klineRange 日期范围直查（腾讯/东财覆写为数据源日期参数，默认拉 320 截取）+ MacdIndicator（EMA12/26 + DIF/DEA/柱 + 金叉）+ CaseFeatureExtractor（9 特征：回撤/量比/KDJ/MACD/MA 关系/距 60 日线/黄白线态/盘整/破前高 + 后验窗口）+ TradingCaseRepository（domain 端口 + JSON 落盘 data/{userId}/trading/cases/ + _index.json 清单原子写 + 条带锁）+ TradingCaseAppService + TradingCaseController 4 端点（POST/GET /trading/cases、GET/DELETE /trading/cases/{caseId}）+ 降级（K 线失败不落半成品/后验不足 null/损坏文件视为不存在）（+20 测试）+ **2026-08-30 案例库批 1 环 3（AI 理解）**：POST /trading/cases/{caseId}/insight（LLM 读特征画像 + K 线统计 → 结构化「为什么这是完美买点」aiInsight 落盘，reviewed=false 待人工确认；prompt 判定优先级 形态>量能>指标>位置；LLM 失败/非 JSON 输出 → 400 不落半成品 fail-visible）（+7 测试）+ **2026-08-30 案例库批 1 环 4（判定当下，核心价值）**：CaseFeatureNormalizer（7 维特征 min-max 归一化，KDJ/MACD null → 中性 0.5）+ CaseSimilarityEngine（加权欧氏，权重和=1 → 相似度=1−距离；Top N 降序，无特征跳过）+ POST /trading/cases/match（当前标的拉 60 日 K → 特征 → 与案例库相似度 Top 5；date 可空=最近交易日，指定日无数据回落最近；空库 → matches:[] 静默降级；相似度不覆盖规则硬判定）（+14 测试）+ **2026-08-30 环 4 二期（扫描接入 + 黄白线参数化）**：WatchlistBuyPointService 附案例相似度 Top 3（开关 adai.trading.case.scan-match 默认关，向前兼容；规则未命中但案例相似 → buyPoint="case" 参考类型，15:10 推送跳过）+ TradingCaseAppService 黄白线周期配置化（adai.trading.case.yellow-ma/white-ma 默认 60/10，批 5 前置）（+3 测试）+ **2026-08-30 案例库审查修复（S1）**：save index 写失败回滚案例文件（防 409 卡死）（+1 测试）+ **2026-08-30 日期宽容解析**：case 标注/match 日期接受 yyyyMMdd + yyyy-MM-dd（Controller parseDate，失败 400 人话「日期格式不正确」）（+3 测试）+ **2026-08-30 TDX 通达信本地行情源**：TdxFileKlineSource（.day 二进制解析：32 字节/条小端 int32，价格 ×100、volume 股→手；kline/klineRange 日期直查；mtime 缓存）+ KlineService 优先级 tdx → 腾讯 → 东财（tdx 无数据不算失败）；配置 adai.market.tdx-path/tdx-enabled（+6 测试）+ **2026-08-30 验收反馈批（搜索 + 图表）**：GET /trading/search 标的搜索（东财 suggest 代码/名称/拼音首字母，NameToSymbolResolver.search 候选列表）（+4 测试）+ **2026-08-30 前复权批（TDX 数据正确性，trading-data-adjustment.md）**：AdjustmentCalculator（前复权算法：除权参考价 + 事件前价格逐级下调）+ AdjFactorRepository（东财分红送转拉取 + 方案文本正则解析 + 本地缓存 data/market/adj/ + 按日 TTL）+ TdxFileKlineSource 集成换算（TDX 读取即前复权，与腾讯 qfq 同口径）；真实校验：茅台 34 根全 ≤0.5% 偏差（+14 测试）+ **2026-08-30 前后端指标一致批**：IndicatorSeriesCalculator 全序列（MA/KDJ/MACD 与特征同口径）+ detail?indicators=true + 前端图用后端序列（hover 值=特征值，消除双算）；000831 买点日 KDJ 8.685 vs 8.69 同源（+5 测试）+ **2026-08-30 共识判定批（核心价值，建议 #3）**：CaseConsensus——案例库 ≥5 时统计学习各特征 25-75 分位「完美买点画像」，match 响应加 consensus（逐维命中 ✓/✗ + 命中 N/M 维）；web 匹配弹窗共识卡（+6 测试）+ **2026-08-30 案例质量校验批（建议 #4）**：标注响应带 consensusCheck（案例库 ≥5 时新案例 vs 画像逐维偏离，Map.of 修 null）；web 标注后偏离大 → 提示「与完美买点画像有偏差」确认（不阻止，防脏案例）（+2 测试）+ **2026-08-30 案例库数据批次审查修复**：KDJ latest <10 根 null 语义回归恢复 + 搜索竞态代际令牌（+1 测试）+ **2026-08-31 批量导入批**：POST /cases/import（CaseImportParser 解析 B1/B2 笔记格式 + 本地全 A 名称表 data/market/names.json 精确匹配兜底 suggest + 逐条标注含共识校验 + 北交所明确失败；实测 26 条导入含双创）+ web 批量导入弹窗（+11 测试）+ **2026-08-31 导入类型识别批**：解析器行内 B1/SB1/B2 标记 → 导入自动带买点类型（无标记 unknown）；生产 32 案例按笔记补全类型（B1 14 + B2 18，含新瀚新材两买点）（+2 测试） + **2026-08-31 案例库数据使用方案批（用户授权当晚全做）**：双轨画像（CaseConsensus/Similarity B1/B2 独立 + 负样本排除）+ 负样本入库（TYPE_FAILED + match 失败警示）+ 后验回填调度器（15:35 回填 verify + index 重建）+ match 双轨响应（type/b1/b2/failedSimilarity）+ 权重可配置（sim-weights）+ web 双轨卡/类型下拉/FAILED 红行/+5d「—」（+11 测试）| + **2026-08-31 双止损位批（trading-risk-plan）**：Position 加 `computedStopLossPrice`（系统计算止损：R=本金×1%、距离=min(R÷单仓市值,5%)、止损价=成本×(1−距离)，动态算不落盘）+ `effectiveStopLoss`（生效=max(人工,计算)，更严格者）；getPositions 按本金快照注入计算止损（行情失败也返回）；R66/接近止损/建议引擎/推送/复盘统一改用生效止损；web 持仓表止损列双值展示（主值=生效，副行标注来源）+ DTO 解析测试（+7 测试） + **2026-09-01 登录体系批（RFC 20260901-auth-login，根治 #179）**：AuthController（login/logout/me/setup/password 5 端点）+ AuthFilter（Bearer 校验 + RequestWrapper 覆盖 X-User-Id——92 处 Controller 零改动）+ SessionRepository（sessions.json 原子写，token 只存 SHA-256）+ Account.passwordHash（bcrypt）+ 登录限流（5 次失败锁 15 分钟）+ setup 一次性 + 改密踢会话 + accounts/available 免鉴权封死（+50 测试：AuthController 13 + AuthFilter 11 + AuthService 17 + SessionFileRepo 9） + **2026-09-02 登录体系修复批**：AuthFilter 免鉴权前缀漏无尾斜杠路径（admin 全 401 根因）+ 401 WARN 日志（含 IP/路径/token 状态）（+2 测试） + **2026-09-02 #178 admin 登录改造批**：admin 管理口并入统一登录（AuthFilter role=admin 门禁 + X-User-Id admin 透传，AdminAuthInterceptor/X-Admin-Token/ADAI_ADMIN_TOKEN 退役）+ /accounts 响应 AccountView 过滤 passwordHash + PATCH 保留哈希/支持重置密码（踢会话 kickSessions）+ AuthService findAccount（+5 测试） + **2026-09-03 案例未修项修复批（REVIEW 6 项出表）**：P2-案例1 窗口<60根已知限制注明 / P2-案例2 web 自选适配 buyPoint="case"（案例相似 N% 橙色） / P2-案例3 案例库 TTL 缓存 5min / P2-案例5 index verify 缺失落 JSON null / P3-案例1 路由登记 trading_case_insight 归 flash / P2-2 复核出表（+4 测试） + **2026-09-04 CORS 顺序修复批（生产 admin 打开 CORS 报错刷屏根因）**：AuthFilter `@Order(LOWEST_PRECEDENCE+1)` 整数溢出为 MIN_VALUE 反成最高优先级 → 跑在 CorsFilter 之前 → 401/403 响应丢 CORS 头 → 浏览器把未登录/无权限误报为 CORS policy 错误（页面刷屏）；CorsFilter 改 `FilterRegistrationBean` 显式 `HIGHEST_PRECEDENCE` 先写头 + AuthFilter order 修为紧随其后 + 回归测试 3（401/403 带 ACAO ×2 + filter 顺序契约防溢出复犯）（+3 测试）  + **2026-09-04 活跃市值区间开关批（v3.41，用户拍板「指南针活跃市值=一切的前提，由我判定多空，app/web 两端都得有」）**：TradingMarketStage（bull/bear 两档 + Port + Repository 落 market-stage.json per-user 锁原子写）+ GET/PUT /trading/market-stage（无记录 exists=false/非法 400）+ 推送择时三级读取（用户判定→current.md→未知，用户设了不被 OAMV 旧规则覆盖）+ Web/App 交易页红绿切换条（多头红/空头绿/未判定灰）+ 本地预置空头（+15 测试） + **2026-09-04 行情数据包导入批（MD17）**：TdxDataPackageImporter（zip 流式收集 `(sh\|sz)\d{6}.day` 防 zip-slip、逐文件解析校验、`.tmp`+原子 move 覆盖）+ `POST /api/v1/admin/market/tdx-import`（multipart）+ adai-admin 维护页「行情数据导入」卡片（file_picker 选 zip）+ `application.yml` multipart 上限 env 化（+10 测试） + **2026-09-04 按批次止损批（决策 P1 方案 A）**：LotStopLossOverrideRepository（lot-stoploss.json 覆盖层，per-user 条带锁原子写 + 损坏降级）+ TradingLotService.derive 合并覆盖（覆盖>流水>默认−7%）+ PUT/DELETE /trading/lots/{lotId}/stop-loss（设改/清除）+ web 批次明细弹窗行内「改」止损（api.update/clearLotStopLoss）（+15 测试） + **2026-09-04 买点三重校验批（P1-交易9/20 出表）**：BuyPointDetector 重写——B1 几何课程口径（回撤占波段 ≥50% ⇔ close≤(high+low)/2）+ B2 放量默认 2.0（倍量柱，rules.yaml/os sync 同步）+ B2 三重防护（KDJ 拐头/高位钝化排除/追高 ≤30%/连板排除）+ WatchBuyPoint.dataDate 新鲜度（15:10 只推当日数据）（+5 测试） + **2026-09-04 资金曲线批（决策方案 A）**：EquityCurveService（现金现值锚定反推 + 流水/转账事件 + 底仓恒持 + 收盘价市值；净值=总资产/净投入、回撤）+ GET /trading/equity-curve + web 资金 Tab 曲线迷你图（净红/总资产蓝 + 最大回撤）（+6 测试） + **2026-09-05 首页行情条交易时段窗口批（用户反馈「周末还在给我推行情」）**：大盘行情条只在 A 股交易时段注入 Feed（工作日 9:30-11:30/13:00-15:00，法定节假日守卫复用 TradingSessionPushService 节假日表）——非交易时段打开首页不再出现标「当前时刻」的行情卡，消除「周末还在推行情」误读；FeedAppService 构造注入 Clock（ClockConfig 提供 bean）+ 测试固定交易时钟（+5 测试） + **2026-09-05 交易⑤认知层落地批（RFC 20260905，用户拍板全量执行）**：P0 建议留痕（AdviceEntry/AdviceHistoryRepository/按月 JSON 落盘 data/{userId}/trading/advice-history/+ GET /trading/advice-history 查询 + 建议出口逐票落盘含降级标记）；P1 个人画像（TradingProfileService 客观统计实时算 + profile.md 主观层读写 + TradingProfileContributor 注入 trading/decision 场景+ GET/PUT /trading/profile + 建议引擎 prompt 前置注入画像）；P3 建议闭环（复盘「阿呆当时说 X → 你做了 Y → 结果 Z」对照段——当日清仓回查建议留痕）；P2 情绪采集（TradePsychologyService 确定性提问 3~5 问 + GET /sold/{symbol}/psychology-questions + POST /sold/{symbol}/psychology/answer 回填 sold.psychology 并沉淀 profile.md）；P2 建议遵守率（clear/reduce 建议后 10 天内执行=遵守，B 反哺 A 注入画像 + /profile 响应带 adviceAdherence）；远期拦截预留（建议引擎逐票注入 symbolHistoryNote 回头草对照）。红线：数字系统算/主语是你/隐私在 data/。基线 1137 → 1189（+52：本批净增 + 同会话首页行情条批 +5；审查修复批再 +8——排序/ID毫秒/尾盘留痕/样本门槛/情绪幂等等，见 change-log）  + **2026-09-06 learn 插件 V1（RFC 20260829，测试先行）**：PluginRegistry 注册 learn（第三个插件）+ LearnCard 领域实体（type/trade_related 语义收敛，非 trading 强制 false 防 LLM 幻觉）+ LearnCardFileRepository（md 卡片 File First：data/{userId}/learn/{type}/{date}_{title}.md，per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 清洗防路径逃逸）+ LearnDigestAppService（喂入素材 → LLM 结构化卡片（RFC 3.4 四段）→ 落盘；LLM 失败/输出不可解析/缺标题 → 素材留存 learn/_raw/ + 400，fail-visible 不产半成品；type 越界回落 other）+ LearnController 3 端点（POST /learn/cards 喂入消化、GET /learn/cards 列表、GET /learn/tree 资产树，learn 插件 403 门控，独立端点喂入仿截图入账先例不污染记录/记忆/Feed）+ GlobalExceptionHandler LearnException→400（+37 测试：Repo 10 + AppService 13 + Controller 14）+ **L2 LearnKnowledgeSource（2026-09-07）**：kernel 知识源 name=learn → PluginRegistry 映射 learn 插件 → ContextEngine 按 enabledPlugins 注入最近学习笔记（title+type+核心观点，无卡片不注入/损坏跳过/上限 5/enrich 不双份），问答可召回自己消化过的内容（RFC 3.7 ③） + **learn V2 消化闭环批 1（2026-09-07 复习流转 + 编辑，RFC 20260829）**：status 常量 new/review/done + isValidStatus + 仓储 updateStatus（按 type+title 定位原地改 frontmatter status 行，正文/手写复述段原样保留——File First 不重建文件）+ PATCH /learn/cards/status 端点（body type/title/status）+ 编辑支撑——LearnCard 补 retell 复述段建模（V1 模板四段空段补齐，md 读写对称）+ repository.update（已存在才覆盖、created 变拒绝=防移动文件）+ AppService.edit（部分字段补丁：coreView/keyPoints/questions/retell/tradeRelated/tradeNote/tags，缺省保留原值，非 trading 强制收敛）+ PATCH /learn/cards 端点（?type=&title= 定位）+ 编辑/流转均 per-user 条带锁 + fail-visible + **learn V2 批 3（2026-09-07 trading 候选联动，RFC 20260829 3.5③）**：LearnTradingCandidate（建议卡 record：title/learn_card_id 回链/sourceType/created/coreView/keyPoints/tradeNote/tags）+ LearnTradingCandidateRepository 端口 + LearnTradingCandidateFileRepository（落 data/{userId}/trading/candidates/{date}_{title}.md——RFC 3.3 V2 加 candidates/ 目录；per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 复用防逃逸）+ LearnCandidateAppService（trading+trade_related 卡片 → 候选：非 trading/不存在/未标 trade_related 400 人话——防语义漂移审核闸前置；learn_card_id 回链 learn 源卡；候选只存提炼建议不复制整卡，跨域无双写；不自动入库需在 trading-engine 工作流审核融合）+ LearnController 3 端点（POST /learn/cards/candidate + GET/DELETE /learn/cards/candidates，learn 插件 403） + **learn V2 批 4（2026-09-07 复习提醒推送）**：LearnReviewPushService（每晚 20:00 cron 可配 `adai.learn.review-cron`，遍历 learn 插件用户 → 聚合 status=review 且 created ≥7 天未 done 卡片跨 ai/trading/other → type=learn-review 推 PushChannel 汇总，不自动改状态）+ PushSettings.ALL_TYPES 加 learn-review（推送开关可关默认开）+ FeedAppService push 注入门控 trading-only → trading 或 learn（纯 learn 用户 Feed 可见复习提醒，market 行情条仍 trading-only）+ 双端推送设置项 learn-review（+22：Repo 8 + AppService 7 + Controller 7——1255 全绿）（+28：Repo 12 + AppService 7 + Controller 9——1283 全绿，Learn 测试 56→84）（+9：ReviewPush 7 + FeedAppService 门控 2——1292 全绿）（+7：LearnKnowledgeSourceTest 6 + ContextEngineTest learn 门控 1）+ **learn V2 审查修复批（2026-09-07 learn V2 增量深审，用户「按决策来」：S-learn1/2 + P1-learn1~4 + P2-learn2~8 出表）**：复习流转约束（isValidTransition 只允许 new↔review/review→done/done→review，跳变 400）+ 进入 review 写 review_at + 提醒按 reviewAt 计时满 7 天 + reminded_at 7 天节流（防每晚 nag）+ 仓储 applyEdit 锁内 merge 保留手工未知段（P2-learn6/7）+ 跨日同名拒绝（save/find 歧义防护 P1-learn2）+ learn_card_id 回链精确 fileStem（P1-learn1）+ markReminded/updateStatus 带 today（storage 不取 now）+ learn-review 开关 learn 侧端点（GET/PUT /learn/push-settings，纯 learn 可自关 S-learn2）+ Feed 事件类型级门控（P2-learn3）+ 候选删除歧义防护（P1-learn4）（+17 测试：仓储 11 + ReviewPush 4 重写 + Digest/Controller 适配——1309 全绿）+ **2026-09-10 learn 喂入入口批（v3.56，用户拍板「先页面后对话流」，批 1 页面喂入）**：POST /learn/cards 改**提交式消化**（同步 LLM 几十秒超时先例同复盘——POST 立即返回 status pending/running，后台 learnSubmitExecutor（LearnSubmitConfig 独立 2 线程/8 队列）执行原 digest + 内存任务态 DigestJob（jobs，按 userId）——inflight 去重不重复烧 AI、执行器满 400 人话「消化任务繁忙」、后台 LLM 失败/不可解析 job=failed + 人话 message（素材留存 _raw/ 不变）、done/failed 60s 惰性清理回 idle、重启丢失回 idle）+ 新增 GET /learn/digest/status（idle/running/done{type,title}/failed{message}，done 后前端按 type/title 打开新卡）+ LearnController POST 改提交式（+11 测试：AppService 提交式 7 + Controller 4——1355 全绿） + **2026-09-12 learn 抓取批（v3.57，RFC 20260912 D 形态阶段 1 抓取主干；用户指示「不需要我的干预」自主开工）**：**服务端抓取落地（B 的死因正面修复——不再要用户自己搞素材）**——`LearnSource`/`LearnSourceFetcher` 端口（domain）+ `BilibiliFetcher`（view 元数据 + player/v2 字幕 + playurl 音频线索；412/429 限流人话；退避重试；`downloadAudio` 带 Referer——实测不带 403/带 206）+ `ArticleFetcher`（HTML→正文：去 script/style/nav、实体解码、块级转换行；**403/5xx → Web Archive 快照兜底**；正文 <200 字判反爬空壳页；超长截断 50000）+ `LearnFetchService`（域名策略：首期不做清单 YouTube/公众号/知乎/小红书/X/微博/抖音/头条 → **人话 + 替代路径**，不绕登录墙）+ **转写链路**：`AsrClient`/`AudioTranscoder` 端口（kernel/ai）+ `DashScopeAsrClient`（getPolicy→上传→异步提交→轮询→取成品；**带 `X-DashScope-OssResourceResolve: enable`** 修 pitfall「上传文件下载失败」；multipart 字段对齐官方 SDK）+ `FfmpegAudioTranscoder`（B站 fMP4 必转 16k 单声道 mp3；不可用 fail-visible）+ **费用闸 `LearnTranscriptionService`**（§3.8 六条：字幕优先／同素材只转一次（转写稿落 `_raw/{platform}-{id}-transcript.txt`）／显式触发／月度硬闸 `LearnQuotaFileRepository`（`learn/_quota.json` 按月分键月初自动重置）／单次报价确认／ASR 端口可换）+ **流水线**：提交式加 stage（fetching/transcribing/structuring）与 `needs_confirmation`/`cancelled` 任务态 + **端点数 +3**（`POST /learn/digest` 链接或素材喂入、`POST /learn/digest/confirm` 费用确认、`GET /learn/digest/quota` 本月额度；`POST /learn/cards` 保留兼容别名；`GET /learn/digest/status` 扩展 stage/source/cost）+ 具名留痕 `saveRaw`/`readRaw`（源必留痕 + 幂等复用，文件名清洗防逃逸）+ **附带修掉未修 pitfall「LLM 输出 JSON 夹未转义引号」**（prompt 值内中文引号 + 解析层 `repairJson` 宽松兜底，双做）（抓取批 +137 → **1497 全绿**：Fetcher 22 + FetchService 12 + Transcription 15 + Quota 9 + ASR 11 + Transcoder 6 + RawAsset 10 + 流水线/确认流 18 + 端点 17 + JSON 修复 11 + 提交/确认原子性 4 + 既有适配） + **同日对抗审查修复批（2026-09-12，独立审查官 10 条 P0×2/P1×5/P2×3）**：①**P0-2/P1-1 竞态**（提交前自查已修）——`submit`/`confirm` 的「检查-再动作」原子化（`ConcurrentHashMap.compute`），堵住「两端同时点继续转写 → 同一视频转写两次 = **双花转写费**」；②**P0-1 SSRF + 响应体上限**：新增 `LearnFetchPolicy` 端口（domain）+ `OutboundHostPolicy`（infra）——拒私有/回环/链路本地/云元数据地址与非标准端口，**关闭自动重定向、逐跳复检**，字幕/快照等第三方响应地址收敛域名白名单；`HttpBodies` 流式读取 + 上限（正文 4MB/音频 64MB）不再整包入内存；`hostMatches` 补点（`evilbilibili.com` 不再被当 B站）；③**P1-2 接口报错 ≠ 没字幕**：字幕接口报错/限流不再降级成付费转写（`LearnSource.textUnavailableReason`），且**报价前先确认音频拿得到**；④**P1-3 按实际时长结算**（32kbps CBR mp3 字节/4000），修掉「时长未知按 30 分钟记 → 3 小时视频只记 1800s」的额度虚账；⑤**P1-4 先预留后花钱**：记账前移 + 失败退回预留 + 留痕移到记账成功后，根治「钱花了没入账、重试命中缓存永不再记账」；⑥**P1-5 `repairJson` 尾逗号改状态机**（只在字符串外清理，不再无声改坏串内内容）；⑦**P2-1 账本损坏 fail-closed**；⑧**P2-2 空卡门禁**；⑨**P2-3 等确认不置 stage**（+30 测试：HostPolicy 13 + 回归 17——**1527 全绿**） + **同日读侧对齐批（2026-09-12，用户「ship」）**：把「A 形态卡（Mac 上 DSH 技能写在主题子目录里的手工卡）在生产里**看得见但读不全、改不动还报错莫名**」修掉——①`parseSections` 段名归一（`normalizeSectionName`：去编号前缀 + 去括号后缀，`## 核心观点（一句话）`/`## 二、核心观点` 都归到 `核心观点`；**不做语义改名**，A 的 `## 内容脉络` 不冒充「关键要点」）；②`LearnKnowledgeSource` 召回正则放宽三处（段名前序号 / 段名后后缀 / 标题与正文间允许空行）+ 去加粗标记——原先 A 的 9 篇被召回时**只剩标题**（RFC §9.1 记过）；③新增写路径守卫 `writablePath`（`locatedPath` 定位实际文件 vs `filePath` 算出的产品路径）——别处整理的卡**只读**，四个写入口（save 除外）统一人话拒绝（「这张是在 Mac 上整理的原始卡，我在这里只当资料看、不改动它」），顺带把原先那句莫名的「卡片不存在」换掉；④写侧 `rewriteBodySections` 同步归一，保持**写读对称**（手工改名的受管段不再被当未知段重复补一份）（+10 测试：召回 4 + 段名归一 1 + A 卡解析/只读守卫/产品卡回归/写读对称 5——**1537 全绿**） |；**提交前自查修掉两处「检查-再动作」竞态**（原 get→判断→put/ resume 非原子：两端同时提交会各跑一遍任务、同时点「继续转写」会**双花转写费** → 改 `ConcurrentHashMap.compute` 原子转移，配 8 线程并发确认回归测试断言「只有一个能抢到」）  + **2026-09-12 learn 完整升级批（v3.60，用户「我要的是完整的升级，成熟的方案」）**：**① 落盘统一到主题目录契约**（新卡 `{type}/{topic}/NN-{slug}.md` + 主题 README 只追加自动段 + 素材 `_raw/` 归位，与 Mac 侧技能同契约；老扁平卡原地可读可写 + `POST /learn/migrate` 幂等一次性迁移）；**② origin 归属判定**（新卡带 `origin: product`；别处整理的卡看得见、读得全、改不了；同名消歧本产品卡优先）；**③ 图片源**（`POST /learn/digest/image`：1~3 张书页/PPT/截图 → 视觉模型**忠实提取** → 同一流水线；原图二进制留痕 + 归位）；**④ 找卡与全文**（`GET /learn/find` 规则打分、`GET /learn/content` md 原文 + 去 frontmatter 的 `body`）；**⑤ 闭环修好**（候选回链改真实路径、问答召回带主题且排除 README、app 额度 UI、双端等确认恢复入口）；**⑥ 独立对抗审查 P0×0/P1×2/P2×7/P3×9**——P1 两条当场修（`origin` 判据扫全文致外部卡可被改写 / `submitImages` 占位后失败永久钉死喂入）+ 回归，P2/P3 部分修部分登记（+46 → **1590**；2026-09-13 卡片管理批 +11 → **1665**，端点 130→**134**） + **2026-09-12 交易账实一致性批（RFC 20260912）**：锚定 fail-closed + 幂等统一（跨来源同笔合并）+ 卖超不丢数据（rejected）+ 预检 dryRun + 对账闸门 integrity/anchor + 收盘小结账实自检行（+43 测试） + **2026-09-13 负成本持仓批**：`Position.pnlPercent` 负/零成本 → null（原返回 0% 是谎报「不赚不亏」；负成本下该式语义翻转，实测 600601 算出 −392% 而券商口径 +134%）+ 持仓导入放行负成本（原 `signum()<=0` 抛异常 → 整批 400）+ LLM 上下文百分比字面「—」（Trading/Market 两 Contributor）+ 批次 pnlPct 同口径（+10 测试） + **2026-09-13 交易账本同型风险封堵批**：P2-交易41 的「全仓排查同型风险」落实——12 条「解析→落盘」链逐条按「丢行/可见性/覆盖语义/空值兜底」四问判定，**封堵 4 处同级风险**：①自选导入 fail-closed（新 `parseWatchlistDetailed` 上报没看懂的行，有则拒覆盖——原来 `!matches("\d{6}") continue` 静默丢 + saveAll 全量覆盖 = 丢一行删一只自选）+ 仓储写失败由 `log.warn` 改抛 `StorageException`（原来写盘失败照回「成功 N 只」）；②持仓导入后端自证（`symbol.isBlank()` 由 continue 改 400 fail-closed——前端已防，后端是公共 API）；③清仓导入字段级保护（解析失败的 null/0 不再覆盖既有日期/天数/涨幅——原会清空 sellDate 致 psychology-questions 404、holdPnlPct 归零致 verdict 重算错）；④资金查询负成本（`costPrice() > 0` → `!= 0`，原把合法负成本当哨兵跳过 → 精确成本永不更新）。判据边界：空行 / `#数据来源` 注释 / 分隔线不算「没看懂的行」（否则正常文件被误拒）。登记未修 5 项（P2-交易43~47）（+10 测试 → 1654） + **2026-09-13 当日盈亏券商口径批**：`POST /positions/import` 加 query `todayPnl`（券商「持仓股」导出「当日盈亏」列之和，含 0 股行）——三闸才写（值空/无快照/**文件日≠快照日**均不写）+ 差异 WARN；`refreshTodayPnl` 加两闸（**非交易日不重算** + 实质未计入不覆盖）；`isTradingDayStrict`（自证周末，原 `isTradingDay` 的「周末由 cron 排除」前提禁止被非 cron 路径复用）（+11 测试 → 1679） + **2026-09-13 APNs 自有推送渠道批（v3.63，RFC 20260913，付费开发者账号后「物尽其用」第一刀）**：新增 `ApnsPushChannel`（`PushChannel` 渠道插件化的第三个实现，推送生产方零改动）——此前 10 种推送真正弹到手机这一步只能借第三方 App（Bark）转达；**凭据**：APNs Auth Key（.p8 / ES256，**不随年过期**、sandbox+production 两套网关通用），JWT 缓存 40 分钟（Apple 要求 20~60 分钟内刷新且不得快于 20 分钟）；**DER→JOSE 定长 R||S 转换**自实现（Java 的 `SHA256withECDSA` 出 DER，直接送 APNs 只会得到无解释性的 `InvalidProviderToken`）+ 含前导 0/不足 32 字节两类边界的单测；**环境分流**：deviceToken 分属两套互不相通的网关，`environment` 跟着 token 存（侧载 development 签名 = sandbox，TestFlight/上架 = production），送错只回 `BadDeviceToken` → 日志把 reason 原样记下并附「下一步该干什么」提示；**失败不抛**：410 Unregistered 自动清理设备登记，其它错误 warn 记录不影响推送生产方；**负载走 Jackson**（Bark 2026-08-26「多行 LLM 正文裸换行 → 服务端 400 丢弃」事故的同型防护）+ `thread-id` 按类型归组；`PushDeviceFileRepository`（`data/{userId}/push/devices.json`，per-user 条带锁 + 原子写，**损坏文件读路径降级/写路径拒绝写回**，并开 Jackson `FAIL_ON_TRAILING_TOKENS`——默认宽容会把截断文件的前半段当合法 JSON 读出来）；**新端点 4 个**：POST/GET `/push/devices`、DELETE `/push/devices/{token}`、GET `/push/status`（配完 .p8 一条 GET 确认生效，不必等下一次定时推送）；**灰度白名单** `adai.push.apns.types`（第一刀只放 close-summary + learn-review，验证通过清空即全量）；客户端同批：`Runner.entitlements`（aps-environment）+ pbxproj 三配置挂 `CODE_SIGN_ENTITLEMENTS` + `AppDelegate.swift`（注册/回调/前台横幅/点击）+ `PushService`（Dart）+ 登出注销设备；**实测**：签名构建后产物 entitlements 含 `aps-environment=development`、描述文件被 Xcode 自动重签（能力注册生效）；生产服务器到 `api.sandbox.push.apple.com` 的 HTTP/2 探针返回 403 `InvalidProviderToken`（=传输路径通）（+51 测试 → **1730**：PushDevice 5 + PushDeviceFileRepository 12 + ApnsPushChannel 23 + PushController 11）；同日**部署并真机实测**：生产 v3.63 上线（deploy-gate 三门 + smoke 9/9）、`GET /push/status` 实测 `apns.enabled=true`/`configured=true`/`keyId=8CPXQ4WSTM`、App 装机后 `GET /push/devices` 实测登记 1 台（`environment=sandbox`，与「release 构建 + development 描述文件」的预判一致）；**凭据面双网关探针**：真 .p8 JWT 打 `api.sandbox.push.apple.com` 与 `api.push.apple.com` 均回 `400 BadDeviceToken`（对照假 JWT 的 `403 InvalidProviderToken`）= 凭据配套且两环境都授权；**新增 `ApnsLiveSmokeTest`**（+1 → **1731**，默认跳过需显式给 LIVE 环境变量）：用真 .p8 + 真机 token 走一遍 `ApnsPushChannel` 并断言 APNs 回 **200**（配套把 `deliver` 改为返回 `DeliveryResult`——对 fail-safe 渠道只断言「没抛异常」毫无鉴别力）；实测日志 `APNs 推送成功 | type=close-summary | env=sandbox` + **2026-09-13 平台抓取放开批（用户「按照建议来」）**：微博 / 微信公众号 / 今日头条从「要登录，抓不了」改为**已支持**（生产实测三者免登录可读，只是各有请求头要求——微博要移动端 XHR 头、公众号要伪装 UA、头条要走移动版；出站纪律抽为 `HopFetch` 五个抓取器共用；微博 URL id→mid 的 62 进制换算落在 `WeiboMid`，用公开算法已知样本对钉死字母表顺序）+ **同批修掉一个静默失效**：Web Archive 快照兜底在大陆 8 秒超时（`archive.org` DNS 返回 `2001::1`），原本是「每次抓取失败白等 8 秒再失败」，改为默认关闭（+25 测试 → **1761**） + **2026-09-14 部署前 P0 修复批（增量深审四官出表）**：**外部工具令牌可收回**（`revokeAll` 接入改密/管理员重置密码/禁用/删号四条安全路径 + `validate` 补账号存在与 `enabled` 复核，fail-closed——此前改密踢得掉会话却踢不掉快捷指令里那把钥匙）+ **锁屏脱敏补全**（`PushMessage` 增锁屏标题 `lockScreenTitle`，行情/批次止损/早中尾盘/买点/今日操作确认/当日盈亏附注/学习复习全覆盖，行情提醒的股票名不再进锁屏标题；修 `mergeBySymbol` 合并时丢掉 `lockScreenContent`；APNs/Bark/微信三渠道标题与正文同口径）（+5 测试 → **1805**） + **2026-09-14 晚间批（安全面收口，用户睡前授权）**：外部令牌**默认 90 天到期**（`expiresAt`；存量老令牌为 null 不过期）+ **撤销优先按 `id`（哈希）**（前缀非唯一命中拒绝撤销，`TokenView` 增 `id`/`expiresAt`）+ 令牌文件开 `FAIL_ON_TRAILING_TOKENS`（截断/尾部垃圾不再被读成「合法前半段」后回写覆盖）+ 备注按 **codePoint** 截断（emoji 不再被切成孤立代理）；锁屏脱敏**回退方向改 fail-closed**（漏传锁屏版即发中性文案，不再回落完整正文——P0-1 的机制根因，`guard.sh` 新增 **G8** 机械守卫每个推送构造点）；`Html.toText` 元素删除改**线性扫描** + 输入上限 512K（消 O(n²)：80KB 未闭合 `<head` 曾跑 2.9s 且无超时）+ 标题抽取只看前段；抓取正文进 prompt 做**来源隔离**（显式不可信边界 + 系统提示词铁律 + 召回侧标注「资料≠指令」）→ 后端 **1817**（+12：Html 4 + ApiTokenService 7 + ApiTokenFileRepository 1）|

| 前端 adai-app | **419** | 0 | **2026-09-23 分享回执批（REVIEW P1-分享8，+3）**：学习页进页把「刚整理好的那条」说出来——done 就在列表顶部摆「你刚分享的那条，我整理好了《标题》」+ 点开直达卡片，看过或关掉后本次运行内不再重复（手动喂入的也不重复）；分享扩展收到 `status=done` 改说「这条我早整理过了」；**2026-09-23 现金健康度批（P2-交易69，+1：账户卡渲染后端给的 `cashNote` 提示 + `AccountSnapshotDto` 解析 `cashDate`/`cashNote` 双字段）**；**2026-09-23 行情链路韧性批（RFC 20260923 D，+7）**：交易页消费 `GET /trading/market-data/health`，**只在 `ok=false` 时**出橙色横幅「阿呆最近拿不到行情」+ 后端 note + 可展开细节（取数链/最近成功与失败时刻/连续失败数）；旧后端 404 或请求失败**静默降级**（不出横幅、不报错、不影响主数据）；前批：**2026-09-22 S-11 C 批·app 端形态（+8）**：推送卡改「一句话结论 + 可折叠四要素依据」——默认只露结论（标题/账日期/逐票结论行），「看依据（N 只 · 四要素）」展开 ①②③④ 逐字依据；**没有四要素的推送原样全文**（行情/操作确认/复习提醒/旧后端，行为不变）；`splitPushContent` 纯解析 5 + 卡片渲染 3。**同批核实（不计本批功劳）**：C1 分析层入口（案例库/规则参数/三维打分/行为模式）2026-08-22 已移除 · C2 四项（批次可见 · 止损可改 · 成本/浮盈口径 · 历次回合）已在前批落地；**adai-web 未做折叠（桌面端保留全文，如需再开）**；前批：**2026-09-22 交易决策批（+5：降级流水 DTO 解析 4 · 批次弹窗内改止损 1）**；**数字含同日并发会话（多图一次投递批）新增的用例**；前批：**2026-09-18 交易页 A2 列表化 + B 批数字可信度（+1 测试，RFC 20260918 第三轮）**：持仓改**数据列表**（细分隔线 + 对齐，**比例上主位、金额退次位**）+ 金额口径与 web 统一（`_fmtMoneyFull` 千分位 + 两位小数，账户级不再用「万」）+ 日历「本月」与账户卡**同源**（后端 `pnl-periods`）并标出口径 + 对比度修正（`darkGrey5` → `darkGrey4`：当日指标标签 / 成本止损行 / 仓位行 / 日历月标签）+ 交易页补**下拉刷新** + 隐私兜底（首页默认态不出现任何金额字符串）。 + **2026-09-18 交易页 A1+A2 重排批（+3 测试，RFC 20260918；用户拍板「金额 `••••` + 数据列表风 + A1A2 一起做」）**：**A2** 首屏按「隐私条 → 每日入账 → 持仓 → 折叠区」重排（资金/活跃市值/快照/当日总结收进默认折叠）；**金额私密**——首页 11 处金额走 `_moneyHome()`（默认 `••••`，👁 会话内揭开，详情容器不受控）；「截图入账」从 11px 下划线小字升为 **44pt 一级动作**，输入条对齐首页尺寸档。**A1**：收起回归修复 · 上传在途占位卡 + 重复点击提示 · 「全部确认入账」提卡头 · 行级「×」44pt + 二次确认 · 写操作补确认（删/写护栏装正）· 缺日期按钮置灰写明原因 · **确认结果逐笔回执卡** · **候选行整行可就地编辑**（价格/数量/方向，A1-4）。**B1**：批次 `pnl ?? 0` 假「+0 红色」→ 「—」+ 中性色、0 值判平。✅ **widget 测试已实跑：385 全绿**（2026-09-19 Xcode license 解除后）；`flutter analyze` **0 issue**。 + **2026-09-18 截图入账候选卡交互批（+2 测试，REVIEW P1-UI15）**：候选卡标题行加「**收起 / 展开**」（本地隐藏、**不删后端候选**——确认失败/缺日期的候选会被后端保留，此前整卡没有关闭出口，用户「关不掉」）；展开态底部加「**全部忽略**」（二次确认 → 逐条按 id 丢弃，任一条失败如实告知且不假装删干净）；候选项显示**成交时间**（同价同量分单靠它区分）；确认结果提示改人话并带上 `ledgerOnly`（「这 N 笔已经记进流水了（持仓和现金以券商快照为准，没重复算）」），失败原因多条时说明还有几条（原来只报第一条）。**⚠️ 本批 app 测试已在 Xcode license 解除后实跑（385 全绿）**：`flutter test` 被 **Xcode license 未同意**挡住（`xcrun --show-sdk-path` 返回 license 提示 → `objective_c` native assets hook 崩 `Bad state: No element`；`sudo` 需密码，未擅自处理）；`flutter analyze` **0 issue**，两条新用例待用户执行 `sudo xcodebuild -license accept` 后跑 `flutter test` 验证。 + **2026-09-18 Feed 空态分流批（+2 测试）**：`FeedResponse` 消费 `hasHistory`，空态分流——老用户说「接着上次的聊也行，我记着」且**不再渲染三个开场问句**，新账号保持原文案 + 三问；旧后端缺字段**降级 false**（= 新账号口径）由回归钉住。 + **2026-09-17 RFC 20260917 待办归 Kernel 批（+12 测试）**：删 `project_status_page.dart` / `project_task_page.dart`，新增 `pages/todo_page.dart`（纯清单两态 + 可选到期日 + 完成/删除 + 已完成折叠）；Launcher「任务」→「**待办**」（原生组，副标题「有地方看，会提醒你」），插件组只剩交易/学习；Feed 去待办卡；推送开关加「待办到期提醒」；通知深链 `todo:today` 直接打开待办页（+1 深链测试）。**2026-09-17 B4 凭据收口批（+1 测试）**：记录来源标记——外部入口（Siri / 快捷指令 / `adai://record`）落记录时带 `source=external_entry`，手输不带（后端按 `user_input` 记，存量口径不变）；新增 `record_source_test` 钉住两条路径的请求体。**2026-09-17 B5 三端入口批（+3 测试）**：① learn **只读卡**补「这是我整理的，认回来」入口（P2-审查4：卡被别的工具抹掉 `origin` 后退化成只读，此前没有任何出路；判据在后端，认不回来就把后端人话原样显示）+ 2 条回归；② 分享令牌弹窗每把钥匙加「换一把」（90 天到期不必再「先收回、再签发」留空窗；新明文走与签发同一条 `_freshPlain` 展示路径，防被 spinner 顶掉）+ 1 条回归。**2026-09-17 巡检修复批（P0-UI13 + P1-前端3 + P1-UI14 + P1-交易54 前端侧，+3 测试）**：① 截图候选**缺成交日期时「补日期」与「×丢弃」并存**（原 `missingDate ? 补日期 : ×` 二选一，而截图候选普遍识别不到日期 → 用户面对错误的行**没有任何删除入口**）；② `_refreshFeed` 合并**按 id 去重**（附加条目 action/market/push 撑破 `_pageSize` 时，同一张卡在 older 与 freshCards 各一份 → 同一对话重复展示）；③ 空态**不再假设「新用户」**（「第一次见」→「今天还没听你说点什么」，老用户当天没记录不该被当成新人；三个开场问句按 2026-09-16 拍板逐字保留）；④ **补日期也改走行标识**（P1-交易54 收尾）——候选带 id 时 `PUT /trade-log/date` 传 `id` 而非 `symbol+direction`（同代码同向的多笔才能各自补日期；旧后端不返回 id 时退化）。 + **2026-09-17 learn 反馈入口批（RFC 20260917 §五 2b）**：学习页卡片动作行新增「调教一下」——反馈写 `POST /learn/cards/feedback` 沉淀为长期偏好（**零费用**），有素材时**只问不自动重排**（重排才调 LLM）；顺带补上 `repages` 的前端入口（解 REVIEW P2-审查4）；**+1 测试** + **2026-09-17 深夜第二批（通知点击深链 P2-APNs1）**：`PushService` 拆包原生编码 `<type>|<deepLink>`，回调签名改 `onTap(type, deepLink)`；`iOS AppDelegate` 把 payload root 级的 `adaiDeepLink` 随点击一起交给 Dart（`pendingTap` 冷启动补投通路零改动）；`MainPage` 新增 `pushDeepLink` 监听 → 命中卡片**高亮 2.5 秒**并滚到底，找不到只刷新 + 滚到底（不假装定位成功）。**+2 测试：357 → 359** + **2026-09-16 巡检修复批**：输入框上方**生活快捷条整条下线**（用户拍板；连 `life_quick_entry.dart` 与其 10 条测试一并删除）· Launcher **「原生 / 插件」分组 + 插件角标**（三处硬编码 `_plugins.contains(...)` 收敛成 `_pluginEntries` 单一事实源表，「任务」仍算原生能力；分组后条目下移触发懒加载视口问题，测试按真实情况适配视口）· 学习页**进度汇总一行**（待复习 / 学习中 / 已掌握 / 本周；口径 = 满 7 天 + 可写 + `reviewAt` 非空，与每晚 20:00 推送同一把尺子）· 学习页**进页即可见失败**（`_failedJob` 提示条，兜住「分享完石沉大海」）· 交易页**资金区块**（转入 / 转出 / 设置本金；「导入资金」按拍板不做）+ **收益日历**（`profit_calendar_page.dart`：月历格子 / 本月合计 / 切月 / 点某天看明细，数据只取 `dailyPnl` 一处口径，缺值显示「—」）· 折叠卡**批量删除**（`mergedIds` 逐条删，任一条失败如实告知且不假装删干净）。测试数 359 → **357**（-10 旧测 +8 新测） + **2026-09-16 第一次见面批（RFC 20260916）**：空态由「还没有记录 + 两个点不出结果的冷词 chip」改为**阿呆先开口的欢迎卡 + 3 个可点开场问句**（点击**直接发问**走真实问答链路，不是预填）+ 档案页「阿呆对你的了解」区块（读 /memory/insights，点「✓ 对」写回 identity.preferences）+ 头像预设（9 个，存 preferences.avatar）+ 昵称 label 改「阿呆怎么称呼你」、去掉必填拦截+ 顺手修 Launcher 空名「 · 」空壳与 ListTile/DecoratedBox 断言（+3 测试） + **2026-09-15 分享令牌自动化批（RFC 20260915；用户「按行业方案，不要让我用户有多余操作」）**：分享扩展那把限权令牌从「要用户在学习页手点一次签发」改成**登录后自动签发 + 临期自动续期**——覆盖「没钥匙 / 剩 < 7 天 / 老数据没记到期时间」三种情况都补签；顺序固定为「**先确认新钥匙写进容器、再撤销旧的**」（反了会两把同时失效）；**写入失败绝不撤旧的**；任何异常静默、绝不阻塞登录；非 iOS 不动作；并发防重入（登录钩子与启动钩子会几乎同时触发）。容器扩展存 `expiresAt`（`ShareBridge.swift` + `ShareExtensionService`），扩展失效话术由「先去学习页」改为「打开阿呆一次就会自动接上」。新增 `ShareTokenKeeper`（+15 测试 → **352 全绿**） + **2026-09-14 iOS 分享扩展批（RFC 20260914；分享面板直达阿呆）**：新增 iOS `ShareExtension` target——B站/抖音分享面板的「阿呆阿呆」→ 扩展**自进程内**择链接 POST `/learn/digest` →「已交给阿呆，正在读…」约 1 秒自动关，**不拉起主 App**；凭据经 App Groups（`group.com.adaiadai.adaiApp`）共享 `learn:digest` 限权令牌（签发即自动写入 + 回读校验；撤销/登出按 id 清空，判不准保守清空）；`adai/share` 通道 + `ShareExtensionService`（仅 iOS）+ 学习页弹窗三态（已就绪／接过但已失效／还差一步）；**对抗审查后修 15 项**（登出清容器 · **扩展剥离 Flutter 引擎**（独立 xcconfig；`otool -L` 实测 flutter 依赖 **0**）· 正则补全角「）」· 回调切主线程 + 5 秒兜底 · 按后端 `status` 区分「已收下／没排上」不谎报）；`flutter analyze` 0 issue · **337 全绿**（本批 +9：`share_token_dialog_test` 19→27、`auth_flow_test` 1 条）· `flutter build ios --release` **通过（21.8MB）** · 产物实测 `PlugIns/ShareExtension.appex` 在、两 target entitlements 同挂 App Group、版本号 `1.0.0/1` · **✅ 真机分享链路 2026-09-15 实测通过**（生产日志：13:49:24 签发令牌 → 13:50:12 抓取 B站 `BV1CrKA6YETu` → 13:50:53 卡片落盘） · **✅ 真机验收（2026-09-14，iPhone 真机装机实测）**：打开 App（未进主页）即弹 **Face ID 启动门禁**——L2 客户端链路在真机生效（钥匙串 token 读取／老值迁移 → `isAvailable` → Face ID → 放行）；「存储密码」提示待一次重新登录触发验证 · **2026-09-14 登录体验批 L2 客户端（v3.67，未部署）**：token 从 `shared_preferences`（iOS 上是明文 plist）**迁到系统钥匙串**（Keychain / EncryptedSharedPreferences；老值首次读取自动迁移并清明文残留；通道带 3 秒超时兜底 + `FLUTTER_TEST` 短路，防 VM 测试死等）· `local_auth` **Face ID 启动门禁**（冷启动过闸；取消/失败 → 回登录页但**保留 token**；设备未录入生物识别则直接进）· 登录页「记住我这台设备」开关（关闭 → token 只留内存）· 个人档案页「登录设备」入口（列表 + 单台撤销，对接 `GET/DELETE /auth/sessions`）· 顺手修掉启动 401 路径**漏清分享扩展共享容器**（换账号串号）+ `_booting` 未收导致 `pumpAndSettle` 超时（+10 测试）· **2026-09-14 iOS 分享扩展批（RFC 20260914；分享面板直达阿呆）**：新增 iOS `ShareExtension` target——B站/抖音分享面板第一排的「阿呆阿呆」→ 扩展**自进程内**择链接 POST `/learn/digest`（提交式秒回）→「已交给阿呆，正在读…」约 1 秒自动关，**不拉起主 App**；凭据经 App Groups（`group.com.adaiadai.adaiApp`）共享 `learn:digest` 限权令牌（签发成功即由 App 自动写入 + **回读校验**；撤销按 id 清空、判不准保守清空）；`adai/share` 通道 + `ShareExtensionService`（仅 iOS 原生）+ 学习页弹窗 iOS 三态（已就绪／还差一步／还没接上，容器不可用时保留快捷指令退路）；`flutter analyze` 0 issue · **327 全绿**（本批 +7，`share_token_dialog_test` 19→26）· `flutter build ios --release` **通过（21.7MB）** · 产物实测 `PlugIns/ShareExtension.appex` 在、**两 target entitlements 同挂 App Group**、扩展版本号 `1.0.0/1` · **✅ 真机分享链路 2026-09-15 实测通过**（生产日志：13:49:24 签发令牌 → 13:50:12 抓取 B站 `BV1CrKA6YETu` → 13:50:53 卡片落盘） · **2026-09-14 登录体验批 L1（v3.67，未部署；本批 +2，另 +7 为 guard-align 源码声明口径回填）**：登录页包 `AutofillGroup` + `autofillHints`（账号 username / 密码 password / 设密码态 newPassword）+ 提交成功后 `TextInput.finishAutofillContext(shouldSave: true)`——iOS 钥匙串据此提示「存储密码」、下次键盘上方给建议并经 Face ID 填入（+2 测试） · **2026-09-14 当日口径批（v3.66，未部署）**：持仓卡新增「当日 / 今日 / 仓位」三样（当日盈亏·今日涨跌幅·仓位比例，null → 「—」不回落 0）+ 持仓区头部「仓位 X% · 现金 Y%」总仓位行 + notes 如实提示（「有几笔我没算进去」darkOrange）；数据源新端点 `/trading/positions/daily`，失败静默降级回旧端点 · **2026-09-14 账本可见性批（v3.65 已上线）**：截图入账「这张截图有 N 行没记」橙色可展开卡（`dropped` 明细，默认展开）+ 账户卡「当日盈亏」下标注来源与日期（`todayPnlSource`：券商口径 · 09-11 / 系统计算 · 09-14（已过期）；未知不标）+ ApiService 字段防御式解析（+8 测试） + **2026-09-07 learn L2 最近学习**（Launcher「学习」行 learn 插件门控 → 最近学习列表（合并 created 倒序）+ 单篇全文页，错误态/空态/重试；ApiService getLearnTree）+ **2026-09-10 喂入入口批（页面喂入）**：「最近学习」页头「＋」喂入页（粘贴素材 + 类型下拉/来源可选 → 提交式消化 → 轮询 done 回列表自动打开新卡，失败人话可重试、超时后台继续素材留存；ApiService submitLearnDigest/getLearnDigestStatus，+2 测试）+ Feed 状态机 12 + 6 页面 + 选号/切换链路 + 插件门控 + 带图发图即对话 + 交易建议 UI + 交易止损/买点 P0 + 交易对齐 web（账户总览卡/止损显示/默认-7%止损/自选买点/清仓打分）+ 30分钟自动刷新 + 次级数据代际守卫 + 上传批次锁 + 第一原则徽章移除（无记录/提问/领域系统标签）+ 20260818 简化（纯买卖记录去止损/买点 + 价格小键盘小数点）+ **2026-08-23 app 体感批**（IndexedStack 保活/排序四修/Feed 错误态/任务编辑 PUT/launcher 伪0/三态/人话/代际令牌/方向色/时间戳）+ **P1-G6-1 守卫批**（timeline_modal await 后 setState 补 mounted 守卫×2 + 回归测试×2：关闭后响应到达不崩溃/正常路径渲染）+ **2026-08-25 批次简版批**（持仓卡第三行批次信息：批次数/最近买入/含底仓徽标/破止损警示，getLots 静默降级，+3 测试）+ **2026-08-26 截图入账批**（交易页「📷 截图入账」入口 + 当日候选内嵌列表逐笔确认/丢弃 + 复盘卡点：无真实成交时复盘按钮引导先导入，+4 测试）+ **2026-08-27 缺日期禁落库批（v3.32）**：候选行缺成交日期警示 + 「补日期」入口（日期选择 → PUT /trade-log/date）+ 确认前拦截缺日期候选（+1 测试）+ **2026-08-28 批次明细批**（持仓卡批次行可点 → 批次明细弹窗：每笔买入的日期/状态徽标（初始底仓/持有中/已清仓）/剩余·成本·现价/盈亏+盈亏%（开放批次浮动 pnlPct、已清仓回合收益率前端算）/破止损橙色警示，连点守卫防叠两层，+2 测试） + **2026-08-29 晚间批**（P1-前端1 静默刷新失败保留旧数据 / P2-UX1 NL 回显 ≤5 位去尾零 / P2-交易31 principal=0 总盈亏「—」不给误导数值，+3 测试） + **B 类批**（确认流阿呆口吻+loading 灰态/插件行稳定槽位/下滑返回挂拖拽条/字号下限 11px/触达 44pt，+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 三文件条件导入（IO http.send 流式 / Web fetch ReadableStream / common 协议件）+ askStream（text 增量 onDelta / meta 定稿 / error 人话 SseServerException / 未收增量自动降级同步端点且 intent 同构透传）+ main_page 三处接线（首问/建卡 question/续问文本分支）+ 90ms 节流草稿 + meta 定稿替换（+6 测试：ask_stream 5 + 流式状态机 1）+ **2026-08-30 续问重复回答修复批**：_appendToActiveCard meta 定稿原位替换草稿 turn（原追加 → 草稿+定稿双份，用户实测每轮续问回答显示两遍/end 上报 turns 带重复 8 vs 服务端 6）+ 门控回归测试（反向验证红，+1 测试） + **2026-09-01 登录体系（RFC 20260901-auth-login）**：登录页（账号密码 + 首访 setup 引导）+ token 持久化（shared_preferences）+ ApiService Bearer/401 全局回登录页 + Launcher「退出登录」替代选号切换 + iOS 编译验证（+5 测试：auth_flow_test） + **2026-09-02 修复批**：DualWorldShell 显式注入带 token ApiService（全 401 根因）+ multipart（截图/发图）带 Bearer 回归 + 2 回归测试| + **2026-09-04 活跃市值区间开关批（v3.41）**：交易页「活跃市值（指南针）」红绿切换卡（多头红/空头绿/未判定灰，乐观更新+失败回滚+连点守卫）+ ApiService get/setMarketStage（+1 测试） + **2026-09-12 learn 抓取批（v3.57 前端接线）**：「最近学习」喂入页顶部加**链接输入框**（首选入口，占位「粘贴 B站 / 文章链接，我来整理」），素材框保留为降级（抓不到时粘正文），旧的「原文链接（可选）」重复框合并删除；`stage` 驱动进度人话（正在抓取原文／正在转写，可能要几分钟／正在整理成卡片）+ 抓到的来源一行（《标题》· UP主 · 时长）；**`needs_confirmation` → 停止轮询**并展示后端报价 message +「继续转写」/「先不转写」（继续 → confirm(true) 恢复轮询；先不 → confirm(false) 收尾，含返回入口）；confirm 失败可见可重试；新增**代际令牌 `_gen`**（重试/确认/dispose 后旧轮询自失效，防两条轮询并行）；顺带按 B1 清掉本页系统口径文案（「请求超时，请检查网络」→「等太久了，检查下网络再试」等）；ApiService `submitLearnDigest` 改打 `/api/v1/learn/digest`（url 优先、content 变可选）+ 新增 `confirmLearnTranscription`/`getLearnQuota`；模型 `LearnDigestJob` 增 stage/source/cost + `LearnDigestSource`/`LearnDigestCost`/`LearnQuotaDto`（防御式 null 解析，message 缺失时用 cost 自拼人话）（+10 测试：Job 解析 5 + LearnPage 5——链接喂入→done→打开新卡／needs_confirmation 亮费用且不自动继续／继续转写→confirm(true)→done／先不转写→confirm(false) 不进 done／双空拦截）——**183 全绿**  + **2026-09-12 learn 完整升级批（v3.60）**：「最近学习」**type→topic 二级分组** + 单篇全文走 `GET /learn/content`（用后端 `body`，产品未建模的段也读得到）+ **只读卡**（`writable=false`）锁图标 + 一行说明且隐藏写入口 + 喂入**选图整理**（1~3 张 → `/learn/digest/image`）+ **本月转写额度展示**（P2-learn15）+ 进页查任务态给「**有一件事等你拍板**」恢复入口（P2-learn17）+ 顶部**搜索**（`/learn/find`）+ **对话流入口**（「含链接 + 整理/消化/学习留存」走同一流水线并在 Feed 内回执、done 可跳该卡；「打开那篇 / 打开《X》」找卡回全文；未命中人话兜底**并照常走原记录/问答**）+ `await` 后 `mounted` 守卫（guard G6 PASS）（+26 → **209**；后续交易批再 +11 → **220**） + **2026-09-13 app 端接账实提示（P2-交易39 出表）**：交易页消费 `GET /trading/integrity`，**drift/gaps 非空才**在页顶出橙色可展开横幅「阿呆发现账对不上」+ 逐行人话明细（drift 应有/基线/锚点后流水/落地/差；gaps 方向·名称·数量·价格·原因），修正指路电脑端（app 无导入入口）；失败静默降级（旧后端/网络抖动/500 → 不出横幅、不报错、主数据照常）——「不知道」不渲染成「没问题」（+3 测试 → **223**）  + **2026-09-13 learn 卡片管理批（v3.61）**：单篇全文页动作行（仅 `writable` 卡）加「移动到主题」（预填当前主题 → PATCH `/learn/cards/topic` → 回列表刷新并重新定位打开）与「删除」（二次确认说明「进回收站 + 级联掉交易候选」→ DELETE `/learn/cards`，级联条数如实回执）；只读卡不显示入口；动作级 busy 守卫 + 代际令牌 + `mounted` 守卫（+8 → **231**） + **2026-09-13 APNs 推送接入批（v3.63）**：新增 `PushService`（只认 iOS 原生：Web/PWA/Android 一律降级 unavailable 且不碰原生通道——app 的 web 构建还要部署到 `adaiadai.com/m/`，绝不能被通知能力弄崩；所有平台调用包 try/catch，旧版本 App 无原生通道时静默降级）+ 壳层登录后接入（申请通知权限 → 取 deviceToken → 上报后端）+ **通知点击回 Feed 世界并刷新** + **拒绝授权时一条可点的补救引导（「去开启」跳系统设置）** + **注册失败把原因显示出来**（fail-visible）+ 登出注销本机设备（换账号不串推送）+ `ApiService` 新增 `registerPushDevice`/`unregisterPushDevice`/`getPushStatus`（+18 测试 → **249**） + **2026-09-13 外部入口批（v3.63，RFC 20260913 第二刀）**：把记录的摩擦从「解锁 → 找 App → 点开 → 打字」降到**一句话**——新增 `EntryIntentService`（仅 iOS 原生，Web/PWA/Android 降级不碰原生通道）+ `MainPage` 消费入口（**有内容直接落成记录**，不要求再点一次发送；**空内容不猜**，只准备输入框）+ iOS 侧 `RecordIntent`（App Intent，iOS 16+，带「内容」参数，`openAppWhenRun`）+ `AdaiAppShortcuts`（免配置 Siri 短语）+ `CFBundleURLTypes`（`adai://` scheme）+ `SceneDelegate` 的 **warm 与 cold 两条 URL 路径**（`openURLContexts` 与 `willConnectTo` 的 `connectionOptions.urlContexts`，两条都调 `super`）+ `ExternalEntry` 桥（落 UserDefaults + 同进程通知**双路径投递**，冷启动引擎未就绪也不丢；drain 即清空 → 天然消费一次）+ AppDelegate `adai/entry` 通道（+12 测试）+ **2026-09-13 分享与整理动作批（用户「B站能不能直接分享到阿呆」→「要做」；配好快捷指令后真机反馈「整理这个动作没跑」）**：**从「靠关键词猜」改成「靠动作说」**——原生 `ExternalEntryAction` 动作表（record/digest；认不出的动作**既不认领也不回落**，回落会把「整理」悄悄变成「记一条」）+ `DigestIntent`（App Intent「阿呆阿呆整理」，**参数用 String 不用 URL**——分享常是夹着链接的口令文本；刻意不进 `AdaiAppShortcuts` 免给 Siri 添噪声）+ `adai://digest?url=…` + Dart 侧 `_consumeExternalEntry` **按动作分派**、`_startLearnDigestFromEntry` 直达 learn（不经关键词判定、不受「正在对话不抢话」限制、保留插件门控且坚持「查不到≠没开」；夹口令能择链接；没链接给人话且**不退化成记录**）（+8 测试 → **269**）|  + **2026-09-14 晚间批（外部入口与分享令牌弹窗修复）**：入口**单槽改 FIFO 队列**（UserDefaults 追加 + AppDelegate 整批 drain + Dart 队列，冷启动连发两条不再丢第一条）；队列**绑定 userId**、登出/401 清空（防跨账号串号）；`take()` 改**先判 mounted 再取**（未挂载不消费、剩余留队）；3 秒 URL 去重保留但**命中留日志**（不再静默吞合法重复）；裸 `adai://` 明确映射 record、删掉不可达兜底（未知动作仍不回落）；**digest 提交前一次点击确认**（会抓原文/可能花钱），record 保持直达；分享令牌弹窗：**明文块排在 loading 之前**（后端只存哈希，被 spinner 顶掉就真丢）、`_busy` 两分支复位、未复制即关窗二次确认、失败态与空态区分可重试、缺明文给人话、日期守卫、`expiresAt` 显示「有效期至/长期有效」、**撤销优先按 id**、补「会花钱/别分享」文案、内联提示替代被遮挡的 SnackBar；learn 页分享入口 44pt 热区 + Tooltip。app **276 → 305**（+29；analyze 0 issue、test 全绿、iOS 编译通过） |

| 前端 adai-web | **343** | 0 | **2026-09-23 现金健康度批（P2-交易69，+1：账户卡渲染后端给的 `cashNote` + `AccountSnapshotDto` 解析 `cashDate`/`cashNote`）**；**2026-09-23 持仓导入「现价」列解析并上送（REVIEW P2-交易65，+1）**；**2026-09-23 行情链路韧性批（RFC 20260923 D，+5）**：桌面端同 app 口径——`ok=false` 才出横幅、失败静默降级；前批：**2026-09-22 交易决策批（+1：降级流水 DTO 与 hasIssue 口径）**；**数字含同日并发会话（多图一次投递批）新增的用例**；前批：**2026-09-18 Feed 空态分流批（+1 测试）**：桌面端空态与 app 同契约同文案——`hasHistory` 为 true 时不渲染三问、只留「接着上次的聊也行，我记着」；缺字段降级 false（既有 2 条用例正好覆盖旧后端口径）。 + **2026-09-17 RFC 20260917 待办归 Kernel 批（+9 测试）**：导航删「项目」+ 删 `pages/project_page.dart` + 新做「待办」页（`todo_page.dart`：两态 + 可选到期日 + 完成/删除 + 已完成折叠）；Feed 去待办卡与任务统计行；ApiService 换 `getTodos` / `createTodo` / `updateTodo` / `deleteTodo` / `getTodoStats`（`TodoResponse` / `TodoStatsResponse`）。**2026-09-17 B5 三端入口批（+1 测试）**：learn 只读卡补「这是我整理的，认回来」——此前 `_actionButtons` 对只读卡直接 `return const []`，用户在 web 上完全没有出路；现在只读卡的唯一动作就是认回（判据在后端，不做本地猜测）。**2026-09-17 巡检修复批（P1-前端3 + P1-UI14，+3 测试）**：① `_loadMore` 合并**按 id 去重**（与 app 侧 `_refreshFeed` 同族隐患——附加条目撑破分页边界后同一 id 可能两份；web 因刷新是整表替换才一直没炸）；② 空态**不再假设「新用户」**——「第一次见，你先随便问我一句」→「今天还没听你说点什么，随便问我一句」（老用户当天没记录不该被当成新人），并新增 web 空态用例 2 条（此前无）。 + **2026-09-17 learn 反馈入口批（RFC 20260917 §五 2b）**：卡片详情「调教一下」（反馈 + 可选重排）**一次补上 feedback 与 repages 两个前端入口**；`canRepage` 只问不自动重排；**+2 测试** + **2026-09-16 巡检修复批**：侧栏**「原生 / 插件」分组 + 插件角标**（只改渲染顺序、`_items` 下标原样保留，规避 IndexedStack 索引错位；顺手 `Flexible` 防标签溢出）· 学习页**进度汇总一行**（与 app 同口径，抽成 `LearnProgressSummary`，7 天判据对齐后端 `REVIEW_STALE_DAYS`）· 学习页**进页即可见失败** · 折叠卡**批量删除**（`mergedIds`，新增 `feed_merged_delete_test`）。**+16 测试：286 → 302** + **2026-09-16 第一次见面批（RFC 20260916）**：空态改「阿呆先开口」欢迎卡 + 3 个可点开场问句（直接发问）+ 零数据收起空右栏（不再三连空壳）+ 档案页「阿呆对你的了解」区块（长期观察渲染 + 置信度 + 确认回流 identity.preferences）+ 头像预设（9 个，存 preferences.avatar）+ 昵称 label/默认值修正（+6 测试） + **2026-09-14 登录体验批 L1（v3.67，未部署）**：登录页同 app 补 `AutofillGroup`/`autofillHints` + `finishAutofillContext(shouldSave: true)`（浏览器密码管理器据此提示保存；Flutter Web 上的实际识别率**待真机实测**，见 RFC §三 L1）（+1 测试） · **2026-09-14 当日口径批（v3.66，未部署）**：持仓表新增三列「仓位占比 / 当日盈亏 / 今日涨跌幅」（null → 「—」不回落 0%，红涨绿亏）+ 顶部「仓位 X% · 现金 Y%」总仓位行 + notes 如实提示（无引述前缀）；数据源新端点 `/trading/positions/daily`，失败静默降级回旧端点（minWidth 1150→1520） · **2026-09-14 账本可见性批（v3.65 已上线）**：历史成交导入结果「有 N 行没能识别（这些成交没有导入）」橙色块 + 逐行展开（`unparsed`，排在 `rejected` 之前）+ 截图入账 dropped 明细 + 资金导入「另有 N 行明细没认出来，这些持仓的精确成本本次没更新」（`unparsedRows`）+ 账户卡当日盈亏标来源与日期（`todayPnlSource`，未知不标/过期标注）+ ApiService 字段防御式解析（+15 测试） + **2026-09-07 learn L2 资产页**（导航项「学习」learn 插件门控 + master-detail：左 ai/trading/other 分组卡片目录 + 右单篇全文渲染（核心观点/要点/疑问/交易标注/复述段），空态/失败降级/刷新保留选择；ApiService getLearnTree/getLearnCards + **2026-09-10 喂入入口批（页面喂入）**：页头「＋」整理弹窗（素材粘贴/类型下拉/作者·平台·链接可选 → 提交式消化 → 弹窗内轮询 done 自动定位打开新卡，失败人话可重试、超时后台继续；空态引导改指页面入口；ApiService submitLearnDigest/getLearnDigestStatus + _DigestDialog，+3 测试）+ **V2 UI 接线（2026-09-07）**：状态徽标（待复习/复习中/已完成）+ 详情操作（去复习/标记完成流转 + 写复述弹窗就地保存 + trading tradeRelated 反哺候选按钮，updateLearnStatus/editLearnCard/createLearnCandidate，+4 测试）+ + 交易管理端（持仓编辑/批量导入/复盘历史）+ 通达信导入解析 + 自选/清仓/资金区块 + 自选买点信号列（C2）+ 清仓三维打分列（D3）+ 行为模式统计（D2）+ 切入自动刷新修复（P1-1）+ 打分按序匹配（P1-8）+ 可降级请求分离（P1-7）+ P2 批（mounted守卫/胜率口径/否定词/千分位/打分列色/删除确认）+ DTO 测试补齐 + P1-5 回归 + 走查 8 项（可降级代际/历史日期竞态/图片回执自然化/交易成功 toast/首载失败错误态/错误人话/Feed 徽章移除）+ 20260818 批次（历史成交/持仓/CSV 三格式识别测试）+ RFC 20260823（历史成交 Tab 分组全字段渲染 / 空态 + 导入入口 / 非历史成交格式人话拒绝）+ **2026-08-23 走查修复批**：资金区块 principal=0 总盈亏失真（totalPnl getter）+ 历史成交导入错误透出人话（extractApiErrorMessage）+ 久持小亏遵守率 R53（+3 测试）+ **2026-08-25 批次视图批（RFC 20260825）**：持仓行「批次」弹窗（批次明细/初始底仓/回合盈亏/对账警告）+ 导入结果每日操作总结卡片（syncMode + summary 行为标注，红涨绿亏）+ getLots DTO（+9 测试）+ **2026-08-28 批次盈亏%列**（批次弹窗加「盈亏%」：开放批次浮动 pnlPct；已清仓回合收益率 = realizedPnl/(成本×买入量) 前端算，红涨绿亏）  + **2026-08-29 晚间批**（历史成交多文件批量 + 逐份进度反馈 + 股息入账/红利税类型标签 / P2-UI4 溢出 / P2-交易31 总盈亏「—」，+3 测试） + **2026-08-30 规则 Tab（第三阶段）**：交易页第 6 Tab「规则」——我的交易规则参数展示（16 参数中文标签）+ 编辑弹窗（表单化 PUT /trading/rules）+ 加载失败降级（+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 值复制（同 app 三文件条件导入）+ askStream（增量/meta 定稿/error 透出/降级 intent 同构）+ feed_page 两处接线（_doAskRequest/_appendToActiveCard 文本分支）+ 90ms 节流草稿（+5 测试）+ **2026-08-30 续问重复回答修复批**：同 app 原位替换定稿（追加型重复根治）+ 门控回归测试（反向验证红，+1 测试） + **2026-08-30 完美买点案例库批 1 前端（第四阶段环 1-2）**：案例 K 线图 CaseKlineChart（三区 CustomPaint：主图蜡烛+MA10 白线+MA60 黄线（黄白线语义近似）+买点日标记 / 成交量 / KDJ+MACD 前端重算）+ 交易页第 7 Tab「案例」（列表/标注弹窗/详情弹窗含 K 线还原与特征后验/删除确认）+ ApiService cases 四方法（annotateCase/listCases/getCaseDetail?kline/deleteCase）（+8 测试）+ **环 3 AI 理解**：详情弹窗「生成 AI 理解」按钮（POST /cases/{id}/insight）+「阿呆的理解」卡片（summary/keyFeatures/置信度）+ ApiService.generateCaseInsight（+1 测试）+ **环 4 判定当下**：「匹配买点」按钮（案例 Tab）→ 输入代码（日期可空）→ POST /cases/match → 相似案例列表（名称/日期/类型/相似度%绿框≥80/+5d 后验）+ ApiService.matchCases（+1 测试）+ **2026-08-30 通达信风格图表**：CaseKlineChart 指标切换（主图 MA2/MA4/裸K + 副图 KDJ/MACD/成交量，PopupMenu）+ 主/副图左上角指标数值标签（MA/KDJ/MACD 当前值）（+1 测试）+ **2026-08-30 验收反馈批**：K 线显示窗口默认买点后 3 天 + ◀▶ 按钮移动查看后验（防后验涨幅压缩前期形态）+ 标注/匹配弹窗标的搜索候选下拉（_SymbolSearchField 防抖 300ms，代码/名称/拼音首字母）+ ApiService.searchSymbols（+1 测试）+ **验收反馈批 2**：主图下三副图固定（量/MACD/KDJ 四区）+ 悬停单日指标标签跟随（十字光标式，光标竖线）+ 图表高 400  + **2026-08-31 案例库双轨前端批**：匹配弹窗双轨判定卡（B1/B2 各自命中 + 类型判定 + 失败画像警示）+ 标注类型下拉（B1/B2/失败案例）+ FAILED 列表行红色 + 列表 +5d null→「—」（+1 测试） + **2026-09-01 登录体系（RFC 20260901-auth-login）**：登录页 + setup 引导 + token 存 localStorage + ApiService Bearer/401 + 壳底部退出登录（测试数不变，widget_test 重写为会话流程） + **2026-09-03 案例未修项修复批（REVIEW 6 项出表）**：P2-案例1 窗口<60根已知限制注明 / P2-案例2 web 自选适配 buyPoint="case"（案例相似 N% 橙色） / P2-案例3 案例库 TTL 缓存 5min / P2-案例5 index verify 缺失落 JSON null / P3-案例1 路由登记 trading_case_insight 归 flash / P2-2 复核出表（+4 测试）   + **2026-09-04 活跃市值区间开关批（v3.41）**：交易页顶部活跃市值区间切换条（多头红/空头绿/未判定灰 + 手动时间副文案）+ ApiService get/setMarketStage（+2 测试） + **2026-09-12 learn 抓取批（v3.57 前端接线）**：整理弹窗顶部加**链接输入框**（首选入口，后端自己去抓）+ 素材框保留为降级；`stage` 驱动进度人话（正在抓取原文／正在转写，可能要几分钟／正在整理成卡片）+ 抓到的来源一行；**`needs_confirmation` → 停止轮询**，弹窗标题「转写要花钱，你说了算」+ 报价文案 +「继续转写」/「先不转写」（含连点守卫 `_answering`）；confirm(true) 恢复轮询、confirm(false) 收尾且**作废旧轮询**（代际 `_gen++`，防迟到响应污染状态）；顺带按 B1 清理系统口径文案；ApiService `submitLearnDigest` 改打 `/api/v1/learn/digest`（url 优先、content 可选）+ 新增 `confirmLearnTranscription`/`getLearnQuota`；模型 `LearnDigestJob` 增 stage/source/cost（+`LearnQuotaDto`，防御式 null 解析）（+10 测试：抓取批解析/链接喂入→done→打开新卡／needs_confirmation 停轮询且不自动花钱／继续转写→confirm(true)→恢复轮询／先不转写→confirm(false) 不进 done／只填链接可提交）——**193 全绿**  + **2026-09-12 learn 完整升级批（v3.60）**：学习页 **type→topic 二级分组** + 详情走 `GET /learn/content` 渲染 md 原文（手工卡独有的「关键内容详解/金句/概念关系」不再读不到）+ **只读卡**（`writable=false`）隐藏写入口并说明来源 + 喂入**选图整理** + 顶部**搜索** + 进页任务态恢复入口（等确认提示条 + 继续/先不转写）+ **对话流入口**（「整理 <链接>」/「打开那篇」，未命中不吞问题）（+25 → 218）  + **2026-09-12 交易账实一致性批（v3.61，RFC 20260912）**：历史成交导入升级为**预检 → 确认两段式**（先 `dryRun` 逐份预检并汇总计划卡：新增/合并/跳过/非交易 + 无法归属明细 + 锚定状态，点「确认导入」才落盘）+ **rejected 橙卡**（「有 N 笔成交没能并入持仓（已记账，未动持仓/现金）」逐条 方向·名称（代码）·数量·价格·原因，弹窗与 Tab inline 双份）+ 锚定缺失/基线未记录的下一步提示（400 人话原样透出 + 「先导快照」/「仅补流水 append」两条路）；交易页加载读 `GET /trading/integrity`，**有 drift/gaps 才**在顶部出可展开橙色「账对不上」横幅（无差异零显示、失败静默降级）；持仓/资金快照导入带 `snapshotDate`（文件名解析，假日期判掉，取不到不传）；新 DTO `RejectedLineDto`/`AnchorStatusDto`/`ImportPlanDto`/`DriftLineDto`/`IntegrityReportDto`（+18 测试） + **2026-09-13 负成本持仓批（用户实测事故）**：通达信持仓解析接受负成本（原 `cost<=0` 静默丢弃 → 3 只只进来 2 只）+ 0 股残留行归「已清空跳过」（原误报为错误）+ **有看不懂的行 → fail-closed 拒绝全量覆盖**（replace 覆盖下漏一行 = 静默删持仓；改弹窗逐行摆原因、不发请求）+ 修复只校验数量列却读成本列的 RangeError（粘贴半截文件即崩）+ 持仓/批次盈亏% 负成本显示「—」（+6 测试 → **242**）  + **2026-09-13 learn 卡片管理批（v3.61）**：学习页详情操作行（仅 `writable` 卡）加「移动到主题」「删除」两个 chip + 确认/输入弹窗，级联候选如实提示，成功后就地刷新并保持/回落选中；在途变灰防连点（+8 → **250**）|

| 前端 adai-admin | **69** | 0 | **2026-09-17 RFC 20260917 待办归 Kernel 批（-2 测试）**：账号页 `project` 插件开关删除（插件开关只剩交易/学习）；数据页「任务」页签与相关 DTO / API 删除——project 插件整体撤销。**2026-09-17 B5 三端入口批（+2 测试）**：删号对话框补「连它的数据一起清理」（`?purge=true` 此前三端零调用 = 要清数据只能 SSH 删）；默认**不勾**（个人数据是不可逆资产），勾了必须**照抄账号名**才放行。**2026-09-16 第一次见面批（RFC 20260916）**：账号页插件开关补 **learn**（后端 PluginRegistry 早已注册、PATCH 早已接受，前端硬编码只有 trading/project → **学习功能此前无处可勾**）+ pluginLabels 补「学习」+ 改 Wrap 防三开关溢出（+1 测试） + 账号/数据/系统/知识页（治理只读收敛）+ 插件开关 + 涨跌色红涨绿亏（darkRed token）+ 登录体系（REVIEW #178：登录页 + role=admin 门禁 + Bearer/401 回登录 + 顶栏修改密码/退出 + 账号页重置密码/建号初始密码 + X-Admin-Token 退役，+15 测试）+ **2026-09-04 行情数据包导入（MD17）**：维护页「行情数据导入」卡片（file_picker 选 .zip → 长超时上传 POST /admin/market/tdx-import → 结果摘要含失败清单）+ ApiService 上传走注入 client + 长超时 client（+3 测试）+ **2026-09-06 审查修复批**（登录默认值 + P1-A~D + P2/P3 拍板 4 项 + 保护/反馈批）：登录页留空自输 · 改密 401 双义 · 反哺 formatDate · 文件树/知识树错误态 · 0 值中性灰 · 反哺冲突只读 · 文件树全局标注 · 记录/Feed 来源标注 · 内置 admin 重置警示 · **保护/反馈批**（禁用确认 P2-2 / 插件开关成功反馈+生效说明 P2-3 / 静默刷新保留旧值 P2-4 / 删除文案会话失效 P2-5 / 账号卡「治理浏览」直达 P2-6 / 维护三操作确认+目标用户 P2-9 / 全局 401 提示 P2-1）|

## 端点/控制器计数（Gradle `endpoints.txt` 单一口径，REVIEW #228）

- Controller：**24**（`interfaces/*Controller.java` 实测，2026-09-22 复核：+ `TradingEvidenceController`（S-11 铁证底座 A 批，独立控制器以避免动 5+ 处直接 new 的既有测试）；原 22 = 21 + `PushController`（APNs 批）；注意直接 grep `@RestController` 会数成 24——`GlobalExceptionHandler` 是 `@RestControllerAdvice`，不能按 grep 计数）；原 17 漏数 9 月新增 Auth/AskStream/EquityCurve/Learn；GlobalExceptionHandler 为 @RestControllerAdvice 非 Controller 不计）
- 端点：**165**（**+4（2026-09-23 RFC 20260923 B 批）：`GET|POST /api/v1/rhythms` + `PUT|DELETE /api/v1/rhythms/{id}`（节律——周期习惯独立于待办；Kernel builtin 无插件门控）**；**+1（2026-09-23 RFC 20260923 D 批）：`GET /trading/market-data/health`（行情〔K 线〕链路可用性：ok/note/lastSuccessSource/连续失败数/取数链——双端只在 ok=false 出横幅）**；**+1（2026-09-22 S-11 B 批）：`GET /trading/buy-points/scan`（完整扫描 `{hits, unavailable, dataDate}`——把「没能判定的标的〔行情取不到〕」与「判定了但没信号」**分开报**，P1-交易60 收口；`/buy-points` 数组形状不变）**；**+1（2026-09-22 A 批 A3）：`POST /trading/evidence/backfill`（建议结果回填——幂等 / 只记事实不判对错 / 数据不全不写半成品）**；**+1（同日并发会话·多图一次投递批）：`POST /records/media/batch`**；**+2（2026-09-22 S-11 A 批·铁证底座）：`GET /trading/evidence/history`（铁证①本人历史统计，门槛 N≥5，不足则 sufficient=false）+ `GET /trading/evidence/rule/{ruleRef}`（铁证③规则原文逐字引用，找不到 404 不编造）**；**-1（2026-09-17 RFC 20260917 待办归 Kernel 批）：撤 project 插件删 6 个 `/api/v1/project/**` 端点（breaking，无兼容别名），新增 5 个 `/api/v1/todos*` 端点——净 -1（155 → 154）**；**+1（2026-09-17 learn 反馈批）：POST /learn/cards/feedback（产物反馈→长期偏好，RFC 20260917 §五 2b；不自动重排 / 幂等 / 属整理能力仍 403）**；**+1（2026-09-17 深夜第二批）：POST /auth/tokens/{idOrPrefix}/rotate（令牌轮换）**；**+1（2026-09-16 晚间批）**+1（2026-09-16 晚间批）：POST /learn/cards/restore-origin（认回被抹掉的 origin 来源标记）**；**+1（2026-09-16 第一次见面批 RFC 20260916）：GET /memory/insights（长期观察聚合——档案页「阿呆对你的了解」数据源）**；**+1（2026-09-15 learn 卡片流批 v3.69）：POST /learn/cards/repages（历史卡回填——用 _raw 素材给老卡补「一页一单元」页序列，只补呈现层不动正文）**；`build/resources/main/META-INF/endpoints.txt` 实测；**+2（2026-09-14 登录体验批 L2 v3.67）：GET /auth/sessions（登录设备列表）+ DELETE /auth/sessions/{idOrPrefix}（单设备撤销）**；**+4（2026-09-13 APNs 自有推送渠道批 v3.63）：POST/GET /push/devices + DELETE /push/devices/{token} + GET /push/status**；**+3（2026-09-12 交易账实一致性批）：GET /trading/integrity + GET /trading/anchor + PUT /trading/anchor**；**+2（2026-09-13 learn 卡片管理批 v3.61）：DELETE /learn/cards（软删除+级联候选）+ PATCH /learn/cards/topic（改主题）**；**+4（2026-09-12 learn 完整升级批 v3.60）：GET /learn/content（md 原文全文）+ GET /learn/find（找卡片，纯规则）+ POST /learn/digest/image（图片喂入）+ POST /learn/migrate（老扁平卡幂等迁移）**；**+3：POST /learn/digest + POST /learn/digest/confirm + GET /learn/digest/quota（2026-09-12 learn 抓取批 v3.57）**；含 **POST/GET /learn/cards 学习卡片 + GET /learn/tree 资产树（2026-09-06 learn 插件 V1）+ PATCH /learn/cards/status + PATCH /learn/cards（2026-09-07 learn V2 复习流转/编辑）+ POST /learn/cards/candidate + GET/DELETE /learn/cards/candidates（2026-09-07 learn V2 批 3 trading 候选联动）+ GET /learn/digest/status 消化任务状态（2026-09-10 喂入入口批 v3.56，POST /learn/cards 同步改提交式配套；**2026-09-12 v3.57 扩展 stage/source/cost + needs_confirmation/cancelled**） + **POST /learn/digest 链接/素材喂入（服务端抓取 B站/文章）+ POST /learn/digest/confirm 转写费用确认 + GET /learn/digest/quota 本月转写额度（2026-09-12 learn 抓取批 v3.57，RFC 20260912）**、/trading/advice、/trading/trades/parse、GET /trading/trades 流水、/admin/** 维护端点迁移、/trading/lookup 代码查名、/trading/positions/import 持仓导入、PUT /trading/positions/{symbol} 持仓编辑、/trading/imports/save 文件留存、/trading/watchlist* 自选、/trading/buy-points 买点信号、/trading/sold* 清仓、/trading/sold/score 复盘打分、/trading/imports/cash 资金、/trading/account 账户快照、/trading/transfer 转账、/trading/transfers 流水、GET/PUT /trading/push-settings、/trading/trade-log、/trading/trade-log/confirm、POST /trading/trades/batch 批量记录、POST /trading/trades/import 历史成交导入、PUT /trading/principal 本金设置、**GET /trading/lots 批次视图（RFC 20260825）**、**POST /trading/sync 一键按流水重建持仓（2026-08-25）**、**POST /trading/screenshots 截图入账（2026-08-26）**、**PUT /trading/trade-log/date 候选补日期（2026-08-27 v3.32）**、**GET/PUT /trading/rules 交易规则参数（2026-08-30 v3.33）**、**POST/GET /trading/cases 完美买点案例 + GET/DELETE /trading/cases/{caseId} 案例详情/删除 + POST /trading/cases/{caseId}/insight AI 理解 + POST /trading/cases/match 判定当下 + GET /trading/search 标的搜索 + POST /trading/cases/import 批量导入（2026-08-31 v3.39 批量导入）**、/brief/cached 缓存简报 v3.24、**POST /records/ask-stream 流式问答 SSE（2026-08-30 v3.34）**、**POST/GET /auth/login·logout·me + POST /auth/setup·password 认证 5 端点（2026-09-01 v3.40 登录体系）**、**POST /admin/market/tdx-import 行情数据包导入（2026-09-04 MD17）**、**PUT/DELETE /trading/lots/{lotId}/stop-loss 批次止损设改/清除（2026-09-04 按批次止损批）**、**GET /trading/equity-curve 资金曲线（2026-09-04 决策方案 A）**、**GET /trading/advice-history 建议留痕查询 + GET/PUT /trading/profile 个人画像 + GET /trading/sold/{symbol}/psychology-questions 情绪提问 + POST /trading/sold/{symbol}/psychology/answer 情绪回答（2026-09-05 v3.47 交易⑤认知层 RFC 20260905）**、**PUT /trading/trade-log/meta + PUT /trading/trades/{tradeId}/meta 候选/流水补成交编号与手续费（2026-09-09 P2-交易36 治本）**）
- 端点计数规则：以 Gradle 生成的 `endpoints.txt` 为准，禁止扫源码回退（REVIEW #228/#187）

## 运行环境

- 后端：`localhost:8080`（DeepSeek 模式 + GLM 视觉——`.env` 需配 `GLM_API_KEY` 才有真 VLM 理解，无 key 时上传降级不丢数据）
- 前端：adai-app `localhost:8081`（移动端入口，Web 形态）· adai-web `localhost:8082`（桌面端入口）· adai-admin `localhost:8083`（产品后台）（均 Flutter Web + CanvasKit 补丁）
- **生产服务器**：`82.156.111.146`（北京 · 腾讯云轻量 · 2核4G · Ubuntu 24.04 LTS · 2026-08-19 从 49.235.37.220 迁移，旧服务器到期下线）
- **生产域名**：`adaiadai.com`（2026-09-01 已上线：ICP 备案通过 `京ICP备2026056893号` + DNS + Caddy HTTPS + 登录体系，见 `docs/deployment/backend-deployment.md` §10）
- 生产访问：web `https://adaiadai.com` · admin `https://adaiadai.com/admin/` · API `https://api.adaiadai.com`（手机端网页入口 `adaiadai.com/m/` **已于 2026-09-23 停用**——用户拍板「手机端只认 iOS 原生 App」，入口改为指路页，详见下）（**需登录 Bearer token**；**2026-09-15 端口真收敛**：8080/8082/8083/8084 全部只绑 `127.0.0.1`，外网直连实测不可达——此前本节写「旧 IP:8080 直连已不对外」但实际是 `0.0.0.0` 对公网开放，当天实测静态站 **202 条外网直连记录**，属「文档声称 ≠ 实际」，已修）
- **手机端网页入口（2026-09-13 装机批）→ ⛔ 已于 2026-09-23 停用（用户拍板）**：`adaiadai.com/m/` 原为 `adai-app` 的 web 构建（与 iOS 原生 app 同一份代码，Safari 加主屏后独立 App 形式全屏运行，曾用来兜住「免费签名 7 天过期就用不了」的入口断裂 REVIEW P2-用户1）。**停用原因**：① 用户明确「手机端只认 iOS 原生 App」；② 该产物自 2026-09-17 起未再重建、落后 6 天（REVIEW P2-工程9）。**现状**：Caddy `handle /m/*` 改为**指路页**（HTTP 200 +「阿呆的手机端请用 iOS App」），`adaios-app.service`（`:8084`）与 `/opt/adaios/app-web` **保留不删**——回滚＝还原 `/etc/caddy/Caddyfile.bak-20260923-mfreeze` 后 `systemctl reload caddy`；**发版清单已排除 app-web**（改 `apps/adai-app/` 不再推出它）。构建：`cd apps/adai-app && sh scripts/build_web.sh <API_BASE_URL> /m/`（脚本内置 base-href + CanvasKit/字体补丁 + 三条硬校验）。**入口即未验证项**：实机「添加到主屏幕」需用户在手机上完成（本机无浏览器代跑）
> **历史（2026-09-14 第五次部署 · v3.65；已被 09-17 第八次部署 v3.70 取代——勿当作当前生产版本，2026-09-17 B1 批修正此处原有的「✅ HEAD = 生产」过时断言）**：本批改动（交易账本丢行/丢值可见化 + 账户快照 `todayPnlSource` + 全仓 JSON 读路径严格化；后端测试 1817→**1831**）已按 `bootJar` + `deploy-gate.sh` 上线（GATE-BEFORE 三门 PASS + GATE-AFTER smoke **9/9**），**四项真链探针 PASS**：`GET /trading/account` 带 `todayPnlSource` 字段 · `PUT /trading/profile` 空串 → 400 人话 · 历史成交 `dryRun` 坏行 → `unparsed` 带「第 3 行…成交日期不是 yyyyMMdd」· 资金首行 `余额:1.2.3` → 400 点名「余额」；web 与 PWA（/m/）产物同日原子替换（公网 `main.dart.js` 解码校验命中「这些持仓的精确成本本次没更新」「券商口径」「行没能识别」「这张截图有」）。

- **2026-09-15 运维加固（端口收敛 + 访问日志；用户「这两点修一下」授权）**：① **端口收敛**——`adai-core.service` 加 `Environment=SERVER_ADDRESS=127.0.0.1`、`serve_static.py` 由绑 `0.0.0.0` 改为 `127.0.0.1`，**8080/8082/8083/8084 四个端口全部退到回环**，公网一律经 Caddy；**外部视角实测四端口全部不可达**（`ss` 确认全为 `127.0.0.1:*`）。② **Caddy 访问日志**——两个 site 各加 `log` 块（`/var/log/caddy/{adaiadai,api}-access.log`，20MiB × 10 份 × 14 天滚动），补上「公网请求量/4xx/5xx 完全没有记录」的观测盲区（此前只能靠后端日志反推）。③ **契约同步**：`ai-engineering/deploy-gate.sh` 的 smoke `BASE` 由 `http://IP:8080` 改走 `https://api.adaiadai.com`（脚本内自行 `export no_proxy`，顺带把 Caddy+HTTPS 链路验在内）+ `services/adai-core/deploy.sh` 尾行验证提示同步。**验证证据**：五服务 active · 域名四入口 web/PWA/admin **200** + api **401**（需登录，正确）· 走域名 smoke **6/6 200** · 账实自检 `drift/gaps` 双空 · 访问日志实测落盘。**踩坑（已进 pitfalls）**：`caddy validate` 会以 root 真的打开一次日志 writer，留下 `root:root 600` 空日志文件 → 服务 reload 报 `permission denied`（Caddy 保留旧配置继续服务，未中断；处置：删掉空文件重 reload 或 chown caddy）
- **生产当前版本（2026-09-23 第十七次部署，v3.88；用户「3+4」授权）**：**后端 + Web 双上线 = 账目口径批**——① **P2-交易70（曲线核心修复）**：资金曲线与周期盈亏的**持仓重置日**由 `anchor.latest()` 改为 **`anchor.positionsReplace()`**——原写法把「资金快照日」当成「持仓快照日」（用户 09-23 **只导了资金**），持仓重置被推迟到 09-23，**09-19~09-22 退回「底仓 + 流水回放」**；② **P2-交易66**：`GET /trading/account` 新增 **`principalNote`**（本金置信度说明——手填本金 + 历史出入金零记录，双端中性灰小字）。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta **155** 文件 · guard-align **160** 端点 + 四数对齐【后端 **2158** / app **412** / admin 69 / web **343**】· guard.sh）+ **GATE-AFTER smoke 9/9**。**真链验证（比单测更有力）**：部署后拉 `GET /trading/equity-curve` —— **09-22 持仓市值由 125,364 → 105,488**（**与券商 09-22 收盘逐分吻合**）、09-21 由 125,925 → 106,162；`pnl-periods` 的 base 回到真实量级（**周盈亏 1,003 → 550** · 月 −1,175.26 → −1,628.26 · 今日 0.22% → **0.26%**）。**Web 产物原子替换**（tar 管道；`privacy.html` 先补进产物再替换）。**公网实测**：`/` **200** · `/privacy` **200** · `/main.dart.js` **200** 且含 `principalNote`。**未部署项**：iOS 原生包（本批 app 侧仅渲染改动，攒下次构建）。
- **上一版本（2026-09-23 第十六次部署，v3.87；用户「1 go」授权）**：**后端 + Web 双上线 = 现金健康度批（REVIEW P2-交易69）**——`GET /trading/account` 新增 **`cashDate`**（现金这个数对应的券商快照日期，≠ 每天被刷新的 `snapshotDate`）与 **`cashNote`**（负现金 / 无券商来源 / 过期〔>7 天〕的一句人话，`null` = 不打扰），双端账户卡渲染（文案单一真相源在后端）。**起因**：用户导入「资金股份查询」后真值露出水面——券商现金 **414.86**，系统此前显示 24,101.01（**虚高 23,686.15**），真实总盈亏 **−43,819.14**（系统少报 2.37 万）。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta **154** 文件 · guard-align **160** 端点 + 四数对齐【后端 **2150** / app **409** / admin 69 / web **343**】· guard.sh）+ **GATE-AFTER smoke 9/9**。**发版清单机制首次实战生效**——自动算出 `artifacts=backend,web`（据上次部署 commit `6ac2927`..HEAD 的改动路径），巡检随即显示 `web=09-23 22:46`（清单内、不落后）、`app-web`/`admin` 标「本批未含」**不再误报红**。**Web 产物原子替换**（tar 管道；`privacy.html` 已按 2026-09-22 的坑**先补进产物**再替换，未中断）。**公网实测**：`/` **200** · `/privacy` **200** · `/main.dart.js` **200** 且含本批新字段 `cashDate` · api 账户端点 **401**（需登录，正确）。**未部署项**：iOS 原生包（本批 app 侧只加了一条提示行，可攒到下次构建，省一次 Beta 审核）。
- **上一版本（2026-09-23 第十五次部署，v3.86；用户「每日巡检」→「按推荐来」授权）**：**后端 v3.86 上线 = 巡检修复批（4 项）**——① **P1-交易63 盘前假「现价」根治**：`application.yml` 删除 `poll-cron` 键（旧值 `9-11,13-15` 一直覆盖代码默认值，导致 2026-08-30「首轮改 10:00」的修复空转近一个月、09:00 拿前一交易日收盘价当「现价」推送），生效口径只留 `MarketAlertService.CRON_POLL`，并加 `MarketAlertCronConfigTest` 守卫；② **P2-交易67 收盘复盘口径**：15:05 已由系统自算账（`snapshotDate=今天`）时**照常复盘并标注来源**，无交易日不再「空等数据」；③ **P2-交易68**：早盘 09:15 盘前不再渲染恒 0% 的今日涨跌幅；④ **P2-交易62**：`integrity` note 在「锚定无文件日期」时不再冒充确定语气。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta **154** 文件 · guard-align **160** 端点 + 四数对齐【后端 **2145** / app 408 / admin 69 / web 342】· guard.sh）+ **GATE-AFTER smoke 9/9**（feed / 记忆 / 交易建议 / 一句话解析 / 时间线 / 标签 + 账实自检 + 锚点自检 + 导入 fail-visible 契约）。**生产真链探针**：`unzip -p adai-core.jar BOOT-INF/classes/application.yml` 里 `poll-cron` 出现 **0** 次（旧值确已消失、首轮回到 10:00，**明天 09:00 不再推假「现价」**）· 启动 `Started AdaiCoreApplication in 3.891 seconds` · 部署后 **ERROR 0** · 服务 **active**。**本批零前端改动**（三端静态产物时间戳不变）。
- **上一版本（2026-09-23 第十四次部署，v3.85；用户「收尾，提交，最后部署」授权）**：**后端 v3.85 上线（两批同批上线）**——① **持仓导入保留券商「现价」批（REVIEW P2-交易65，本次会话）**：`POST /trading/positions/import` body 新增可选 `currentPrice`（券商「持仓股」导出的「现价」列），取舍顺序 **券商现价 > 保留原有存储价 > 成本价兜底**——此前该列被整列丢弃、落库拿 `avgCost` 顶替，故生产 `positions.md` 现价全等于成本价（002428 成本 41.58 / 券商现价 93.50），行情源不可用时会显示成「0 盈亏 + 市值退回成本 84,678.70」的假象（真值 105,488.00）；② **行情（K 线）链路韧性批（RFC `20260923` A/B/D，提交 `4c303b9`，此前已提交但未部署，随本次一并上线）**——腾讯 K 线域名可配（默认换实测可用的备用域名 `proxy.finance.qq.com`）+ 新增**新浪第三源**（取数链变 tdx → 腾讯 → 东财 → 新浪）+ 新端点 **`GET /trading/market-data/health`**（`note` 可直接展示）+ 双端交易页只在 `ok=false` 出横幅。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta **154** 文件 · guard-align **160** 端点 + 四数对齐【后端 2138 / app 408 / admin 69 / web 342】· guard.sh）+ **GATE-AFTER smoke 9/9**（feed / 记忆 / 交易建议 / 一句话解析 / 时间线 / 标签 + 账实一致性自检 + 锚点自检 + 导入 fail-visible 契约）。**web 产物原子替换**（`/opt/adaios/web`，`main.dart.js` 3,594,249 B 含 `currentPrice` 3 处 / 「现价」unicode 2 处）。**公网实测**：`/` **200** · `/main.dart.js` **200** · api **401**（需登录，正确）；**新端点真链探针**：`GET /trading/market-data/health` → **200**（`sources: [tdx, 腾讯, 东财, 新浪]`，证明行情韧性批确已上线）。五服务（adai-core / adaios-web / adaios-admin / adaios-app / caddy）全 **active**。**同批数据修复**：生产 `trades/` 14 笔 `0股@0.00` 股息脏流水已清（**1722 → 1708 笔**，回读无残留；备份 `/opt/adaios/data-backup-trading-20260923-p2-65/`）。**未部署项**：iOS/App 端未重建（本次未改 app 代码）。
- **上一版本（2026-09-22 第十三次部署，v3.83；用户「1->2 go」授权：部署后端 B 批 → 构建 iOS）**：**后端 v3.83 上线（S-11 B 批·决策时点的对话式提醒）**——① **B1 早盘**（原 15:10 独立「买点提醒」取消并入 09:15；持仓概览 + 自选买点逐条**四要素铁证**；正文**确定性渲染、不再走 LLM**）；② **B2 尾盘**（只列触发 R66/R81 的持仓 + 四要素 + **账日期标注**）；③ **B3 收盘复盘**（改**数据同步后触发**：≥15:00 导入即出、15:30 兜底；未同步如实说且不落「已发」标记；每天至多一条）；④ **B4 行情失败显式降级**（不发带空洞数字的推送 + 新增 `GET /trading/buy-points/scan` 把「没取到行情」与「没信号」分开报——**收口 P1-交易60 推送侧**）；⑤ **B5 午间**改**知会**（只报事实与位置、不催操作、无异常不发）。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta 153 文件 · guard-align 159 端点 + 四数对齐 · guard.sh 10 PASS/0 HIT）+ **GATE-AFTER smoke 6/6 + 领域自检 3/3**。**线上真链探针（B 批新面）**：`GET /trading/buy-points/scan` → **200**（`{hits, unavailable, dataDate}` 形状在位，且**当场如实报出「22 只没取到行情」**——B4 的可观测性在生产立刻用上了）；`GET /trading/buy-points` → **数组形状不变**（三端兼容）；`GET /trading/push-settings` → `buy-point` 开关在位；`GET /trading/evidence/rule/R33` → **200**（narrator 铁证③ 依赖同一条规则路径）。**iOS 构建 10 已上传并送审**（2026-09-22 23:42 `UPLOAD SUCCEEDED` · Delivery UUID `e165e650-7dda-4715-9fe9-14070052a730` · 23.76MB · 主 App 签名 `iPhone Distribution: kangda wang (4G3D37YKSB)`；包内含 S-11 C 批的推送卡「一句话 + 可折叠四要素」）；`pubspec.yaml` 已同步 `1.0.0+10`（脚本指定构建号时不回写文件，手工同步防下次撞号）；Apple 侧 23:43 **VALID** → 23:47 **加入外部组「阿呆外测」并提交 Beta App Review**（外测每个新构建都需重走一次审核）。**⚠️ 同批发现一个既有生产缺陷（与 B/C 批改动无关，见 REVIEW P1-交易62）**：腾讯 **K 线**域名 `web.ifzq.gtimg.cn` 从生产返回 **501 + 腾讯 WAF 拦截页**（加 UA/Referer 无效 = **IP 级**），东财长期被限、tdx 数据包滞后（最后一根 09-18）→ **K 线链路整段不可用**（影响资金曲线 / 周期盈亏 / 买点扫描 / 案例库；**早/尾盘推送用的是实时行情 `qt.gtimg.cn`，不受影响**）；已实测备用域名 `proxy.finance.qq.com/ifzqgtimg/appstock/app/newfqkline/get` → **200 且返回有效 K 线（含 09-21）**——修复方案在手，**是否动手待用户拍板**（属独立批次）。
- **上一版本（2026-09-22 第十二次部署，v3.82；用户「推送」→ 选「后端 + Web 先上，App 等 B 批」授权）**：**后端 + Web 上线，App 端（iOS）挂起**。① **后端 v3.82**——把 09-20 之后的四批一次上线：**多图一次投递**（`POST /records/media/batch`，一次投递 = 一个回合一张卡 + 投递级 `Idempotency-Key` 幂等——治「客户端超时重试 → 同一张图两份」）+ **薄附件不再泄漏**（Feed / 时间线 / 简报 / 搜索 / AI 上下文 / 领域活跃度六面过滤，收敛为单一事实源 `MediaAttachments.referencedIds()`）+ **S-11 铁证底座**（`GET /trading/evidence/history` 铁证①本人历史统计〔样本门槛 N≥5〕· `GET /trading/evidence/rule/{ruleRef}` 铁证③规则原文逐字〔找不到 404 不编造〕· `POST /trading/evidence/backfill` 铁证④结果回填〔幂等 · 只记事实不判对错〕）+ **账实自检「假绿」可见**（`GET /trading/integrity` 新增 `degraded[]`——锚定日被推断时当天成交「只记流水、未进持仓」结构上查不出来，生产实据 09-18 那 6 笔；落盘 `snapshot-anchor.json` 新增 `positionsFileDate`/`cashFileDate` 作唯一判据 + `normalizeAnchorDate` 补未来日期不可信）+ **建议出口带四要素铁证**（`POST /trading/advice` 每条 `advice[]` 新增 `evidence`）。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta 153 文件 · guard-align 158 端点 + 四数对齐 · guard.sh）+ **GATE-AFTER smoke 6/6 + 领域自检 3/3**；**线上真链探针**：`GET /trading/integrity` → **`degraded` 字段在场 · 6 条**（锚定日 2026-09-18 · `inferred=false` 事实说明）· `GET /trading/evidence/history` → **200** · `GET /trading/evidence/rule/R66` → **200**。② **Web v3.82**——产物 tar 管道**整目录原子替换** `/opt/adaios/web`（部署前备份 `backups/web-before-20260922-mediabatch.tar.gz`，20M）；**公网四入口实测**：`/` **200** · `/privacy` **200** · `/m/` **200** · `/admin/` **200** · api **401**（需登录，正确）；产物校验：`main.dart.js` 3,590,927 B 含本批新串（`笔成交没进持仓` · `Idempotency-Key` · `mediaPaths`）+ `canvasKitBaseUrl` 注入唯一（config 块内 1 次 / 顶层 config 1 个）+ 字体三件与生产一致。⚠️ **本次踩到一个部署陷阱（已当场避开）**：web 部署是 `rm -rf /opt/adaios/web` 整目录替换，而 `privacy.html` 的源文件在 `apps/adai-app/web/`（**不在** `apps/adai-web/web/`）→ 不处理会把 09-21 刚上线、TestFlight 外测正依赖的隐私政策页抹成 404；本次部署前显式补进 `build/web/` 才未中断。**根治建议**：把该文件同步一份到 `apps/adai-web/web/`（`docs/legal/privacy-policy.md` 现写「下次 Flutter web 构建也会自动带上」——**这句目前是错的**），待用户拍板。③ **App 端（iOS）本批挂起**——按用户选择等 **S-11 B 批（三条推送改形态）**做完再一并构建送审（外测组每上一个新构建都要重走一次 Beta App Review ≈21 小时，攒批可省一次）。**三端测试**：后端 **2090** · app **393** · web **336** · admin **69**，全绿。端点 **158**。 + **上一版本（2026-09-20 第十一次部署，v3.73；用户「前后端都部署下」授权）**：**后端 v3.73**——把 09-18/19 的交易批一次上线：① **截图入账两处数据缺陷**（同价同量分单不再被吞 / 锚定日不再把当天提前锚定 / 命中锚定由硬拒改**降级只记流水**）② **交易 App 端按「记录→对账→照见」重排**（首屏只剩「每日入账 + 持仓」、**金额私密 `••••`+👁**、数据列表化、候选可就地编辑、逐笔回执、下拉刷新）③ **三官深审 36 条闭环 20 条**。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS** + **GATE-AFTER smoke 9/9**；web 产物 tar 管道原子替换（服务 active · 公网 `/` **200** · api **401** 需登录）；**iOS 构建 9 已上传**（`UPLOAD SUCCEEDED` · Delivery UUID `deebfb68-1e77-4e77-996c-c5e6b7bbc5b8` · 23.7MB；Apple 侧 2026-09-20 01:14 实测 **VALID · ✅ 可测试**，过期 2026-12-18）。**三端测试首次全量实跑**：后端 **2034** · app **385** · web **318**，全绿。端点 **154** 不变。 + **上一版本（2026-09-17 第十次部署，v3.72；用户「部署」授权）**：后端 **v3.72**——RFC 20260917「待办归 Kernel builtin + 撤 project 插件」上线：`/api/v1/project/**` 6 端点删除、`/api/v1/todos` 5 端点上线（**无插件门控**）、存储 `data/{userId}/todos/YYYY/MM.md`（两态 + 可选到期日）、Feed 去待办卡、新推送类型 `todo-due`（到期当天 08:00 / 18:00）+ 通知深链 `todo:today`、admin 数据页「任务」页签与账号页 project 开关下线。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta 151 文件 · guard-align 154 端点 + 四数对齐 · guard.sh 10 PASS / 0 HIT）+ **GATE-AFTER smoke 9/9**。**三端静态产物同日原子替换**：Web `/opt/adaios/web` · PWA `/opt/adaios/app-web` · admin `/opt/adaios/admin`（均 active）。**公网实测**：`/` **200** · `/m/` **200** · `/admin/` **200** · api **401**（需登录，正确）；**新端点真链探针**：`GET /todos` **10 条**（迁移数据线上可见）· `stats {total:10,open:10,done:0}` · 旧端点 **404** · 空标题 **400 人话** · `todo-due` 开关可关（false → 恢复 true）；**Feed 反向验证**：`action` 条目 **0**；五服务全 active、部署后日志无 ERROR。**同期数据迁移**：旧 `data/adai/project/tasks/2026/{08,09}.md`（10 条全 TODO）→ `data/adai/todos/2026/{08,09}.md`（旧文件一字未动 + 备份 `backups/adai-project-tasks-20260917.tar.gz`）。端点 **154**。
- **上一版本（2026-09-17 第九次部署，v3.71；用户「按照建议来」授权）**：后端 **v3.71**——把 09-17 晚间三批一次上线：① **B1 登记一致性**（REVIEW 14 处「✅ vs 正文」对齐 + 股东代码残留脱敏 + status/api-spec 口径修正）；② **B2 巡检遗留收口**（`purge` 失败清单 + 回收空目录 · 图片配额锁内原子化 · 横排成交额交叉校验 · 兜底源双双失败记 ERROR）；③ **B4 凭据收口**（新增 `ApiTokenGuard`：付费动作令牌级频控 5 次/分钟 + 换 IP/超频告警；`POST /records` 支持 `source` 来源标记）。**B5 三端入口**（restore-origin / rotate / purge）随后端同批上线。**部署凭证**：`deploy-gate.sh` **GATE-BEFORE 三门 PASS**（guard-meta 149 文件 · guard-align 1985/367/71/308 · guard.sh 10 PASS/0 HIT）+ **GATE-AFTER smoke 9/9**（6 核心端点 + 账实一致性 / 快照锚定 / 导入 fail-visible 三项自检）。**三端静态产物同日替换**：Web `/opt/adaios/web` · PWA `/opt/adaios/app-web` · admin `/opt/adaios/admin`（三者均 `active`，构建补丁校验通过：canvasKitBaseUrl 唯一 + 字体带 `/m/` 前缀）。**公网四入口实测**：`/` **200** · `/m/` **200** · `/admin/` **200** · api **401**（需登录，正确）。端点 **155** 不变。
- **上一版本（2026-09-17 第八次部署，v3.70；用户「更新，部署，发版」授权）**：后端 **v3.70**——把 09-17 全天的五个代码提交一次性上线：① **通知点击深链（P2-APNs1）**：APNs payload **root 级** `adaiDeepLink`（由 `PushMessage.deepLink()` 从已有字段推导，推送构造点零改动）+ 客户端 `pendingTap` 冷启动补投 + 命中高亮 2.5 秒（找不到只刷新，不假装定位成功）；② **令牌轮换（S-凭据1 剩余项）**：新端点 `POST /auth/tokens/{idOrPrefix}/rotate`（先发新再撤旧，撤不掉则回滚新的）；③ **learn 反馈闭环**：新端点 `POST /learn/cards/feedback`（评价沉淀为 preference 记忆，幂等、不自动重排、属整理能力仍 403）+ **门控 B**（`/learn/digest` 无 learn 插件不再 403，改「只接收不整理」零费用）+ 卡片生成 prompt 回灌画像；④ **deep 深审当场修复四条**（`restoreOrigin` 判据收紧、盘前周月双计、深链点A亮B 改只认精确命中、股东代码残留清零）；⑤ 到期倒数接进每日巡检。**端点 153 → 155**。**部署凭证**：三端测试全绿（**后端 1953**（0 失败 / 0 错误 / 2 跳过）· app **360** · web **304**）——后端测试用 `--rerun-tasks --no-build-cache` **绕开 gradle 缓存真跑**（首轮 `:test` 是 `UP-TO-DATE` / `FROM-CACHE` 的假绿，未采信）→ `gradlew build` → `deploy-gate.sh`：GATE-BEFORE 三门 PASS（guard-meta 148 文件 · guard-align 端点 155 + 测试数对齐 · guard.sh）+ GATE-AFTER smoke **9/9**（feed/记忆/交易建议/一句话解析/时间线/标签 + 账实自检 + 锚定自检 + 导入 fail-visible 契约）。**前端原子替换**：Web（`/opt/adaios/web`）与 PWA（`/opt/adaios/app-web`）tar 管道替换；**公网三入口 200**（`/`、`/m/`、`/admin/`）+ api **401**（需登录，正确），五服务（adai-core / adaios-web / adaios-app / adaios-admin / caddy）全 active。**iOS 构建号 7 已上传**：`UPLOAD SUCCEEDED`（Delivery UUID `a2c6739c-af85-4dd6-a24b-16f51141efe2`，23.7MB，主 App 签名 `iPhone Distribution: kangda wang (4G3D37YKSB)`）；`pubspec.yaml` 由脚本自动写为 `1.0.0+7`；Apple 侧处理中（`--status` 当前仍只列到构建 6）。**同批文档**：新增 `docs/guides/qoder-parallel-workflow.md`（Qoder CN 并行工作流手册）——详见 change-log。
- **上一版本（2026-09-16 第七次部署，learn 卡片流批 v3.69；用户「1+3」授权）**：后端 **v3.69**（卡片流「一页一单元」全链：消化产页序列 + md 新段 `## 卡片页` + `GET /learn/content` 暴露 `pages` + 历史卡回填端点 `POST /learn/cards/repages`；部署走 deploy-gate：GATE-BEFORE 三门 PASS（guard-meta/guard-align/guard.sh）+ GATE-AFTER smoke **9/9**）。**同批前端原子替换**：Web（`/opt/adaios/web`，探针命中新增 ValueKey `learn-page-`）与 PWA（`/opt/adaios/app-web`，探针命中 `learn-original-toggle`），两服务 active、公网 200。**6 张历史卡全部回填完成**：每张 13-14 页（kind 分布健康：points/table/compare/diagram/numbers/quote 混合，非清一色 points），**正文四段与 `origin: product` 一字未动**（逐卡读回校验），单卡耗时 24-35 秒；**回填前已备份** `/opt/adaios/backups/learn-before-cards-v369.tar.gz`（61K/34 条目）。**iOS 原生包同批重建并上传**：构建号 **4**（App Store Connect 上传 `UPLOAD SUCCEEDED`，Delivery UUID `03039012-ccd4-4182-8511-c6fe4ddaba14`；Apple 侧处理中——此前 1/2/3 均 VALID）。包内含卡片流渲染与原文折叠区；**手机上更新 TestFlight 即可看到新形态**。
- **上一版本（2026-09-14 第六次部署，登录体验批 v3.67；用户「部署」授权）**：后端 **v3.67**——登录可选上报 `device` + 响应新增 `sessionId` · 新增 **`GET /auth/sessions`**（登录设备列表，标记 `current`）与 **`DELETE /auth/sessions/{idOrPrefix}`**（单设备撤销：前缀歧义/当前设备 400、找不到 404）· 老会话文件无 `device` 字段 → 读成 null（升级不踢已登录设备）。部署：GATE-BEFORE 三门 PASS + GATE-AFTER smoke **9/9**（6 核心端点 + 账实/锚定/导入三项自检）。**上线后线上实测**：login 响应含 `sessionId` ✓ · 设备列表 `current` 标记与 `device` 字段 ✓ · 撤销当前设备 → **400 人话**「这台就是当前设备；要退出它请用『退出登录』」✓。**同日数据治理（用户授权）**：生产历史会话 **111 → 0**——110 条无设备信息的旧版/调试/smoke 遗留会话 + 1 条部署自检会话已清；**各端需重新登录一次**（重登后新会话带设备信息，「登录设备」列表从此干净）。app 侧本批改动（钥匙串/Face ID/记住我/登录设备页）由用户侧载装机，**真机实测 Face ID 启动门禁通过**（见 app 行）
- **上一版本（2026-09-14 第五次部署，账本可见性批 v3.65；用户「部署」授权）**：后端 **v3.65**（历史成交/截图/归集**丢行带行号回显** · 资金股份首行数值读不出 → **400 点名哪一项** · 画像空串与 null 同拒 · 账户快照 **`todayPnlSource`**（券商/系统计算/未知）· **负成本持仓卖出改券商口径**（含负成本）· 全仓 18 个文件仓储 **JSON 读路径严格化**；部署：GATE-BEFORE 三门 PASS + GATE-AFTER smoke **9/9** + 四项真链探针 PASS；前端 web 与 PWA 产物同日原子替换（公网解码校验命中新文案））。**已知小瑕疵（如实登记）**：`deploy.sh` 内「重建记忆」步骤仍用已退役的 admin token 方式（REVIEW #178 后需 role=admin），日志报「仅管理员账号可访问」——**不影响部署与 smoke**，重建记忆可在 admin 后台手动触发；待随下批修掉
- **上一版本（2026-09-14 第四次部署，部署前深审修复批；用户睡前授权「修复所有登记 + 重新部署」）**：后端 **v3.64**（安全面收口——外部令牌 **90 天有效期 + 按 hash 撤销 + 改密/重置/禁用/删号联动**、令牌文件 `FAIL_ON_TRAILING_TOKENS`、备注 codePoint 截断、**锁屏脱敏补全且回退改 fail-closed**、`Html.toText` 线性化 + 512K 上限、抓取正文 **prompt 来源隔离**；**deploy-gate 部署**：GATE-BEFORE 三门 PASS + GATE-AFTER smoke **9/9**【feed/记忆/交易建议/一句话解析/时间线/标签 + 账实自检 + 锚定自检 + 导入 fail-visible 契约】；**部署后真链验证 PASS**：登录 → 签发令牌实测 `id`（64 位）+ `expiresAt` **90.0 天** → 列表带 `id`/`expiresAt` → 令牌打 `learn/digest/quota` **200**、打 `/auth/me` 与 `/feed` **403**（不能自提权、不能读个人数据）→ **按 id 撤销 200、撤销后立即 401**；验证用令牌已全部回收，生产当前 **0 把外部令牌**）· **web 产物原子替换**（adai-web，含「非 iOS 推送开关去假」文案，公网 `main.dart.js` 探针命中）· **PWA（/m/）原子替换**（adai-app web 构建，base-href=/m/ + CanvasKit/字体补丁三条硬校验通过；公网探针命中新文案「要我现在整理这个链接吗」「有效期至」）· **app 包已构建待装机**：Android release APK（57.0MB，**验签通过** SHA-256 `c849b4f2…` = 正式证书）+ iOS release（20.0MB，Team `4G3D37YKSB`）；`devicectl` 显示设备 `unavailable`，装机待手机可达
- **上一版本（2026-09-13 第三次部署，APNs 批）**：后端 **v3.63**（APNs 自有推送渠道批；deploy-gate 三门 PASS + 部署后 smoke 9/9【6 核心端点 + 账实/锚定/fail-visible 三项自检】；**APNs 渠道实测上线**：`GET /push/status` → `apns.enabled=true / configured=true / keyId=8CPXQ4WSTM`，`GET /push/devices` → 真机登记 1 台 `environment=sandbox`，`ApnsLiveSmokeTest` 真凭据+真 token 投递 APNs 回 **200**（手机收到阿呆通知）；iOS 包同步重建装机（含 `aps-environment` entitlement 与 PushService）；**已全量**：首刀灰度（`close-summary,learn-review`）真机验证通过后**清空白名单**——10 类推送全部可达手机；**同批关闭 Bark**（`ADAI_PUSH_BARK_KEY` 注释、值移出 .env）避免同一件事弹两条；重启后实测 `apns enabled=true / 白名单=全部 / bark enabled=false / feed enabled=true`、设备 1 台）。上一版本：后端 **v3.62**（当日盈亏券商口径批 + 账本同型风险封堵批，deploy-gate 前置三门 PASS + 部署后 smoke 6/6 200 + 自检 3 项【账实一致性 / 快照锚定 / 导入 fail-visible】PASS；**探针实测**：`todayPnl=abc` → 400「todayPnl 需为数字」、持仓行缺代码 → 400「第 2 行没有股票代码…本次导入已取消」且持仓未动 = 两批后端均已生效）· **web 产物同日替换**（`sh scripts/serve_web.sh --build-only https://api.adaiadai.com`，产物实测含 `todayPnl` 3 处 + 字体/canvaskit 补丁校验唯一；公网 index/main.dart.js/canvaskit.wasm/字体 全 200）· **PWA（/m/）hash 与本地构建一致**（`6df8d1c1…`，含「阿呆发现账对不上」横幅）——03:05 那次构建已含本批 app 改动，故本次替换为幂等 no-op· 上一版本：后端 **v3.61**（交易账实一致性批，RFC 20260912；deploy-gate 前置三门 PASS + 部署后自检 3 项 PASS，自检当场报出「券商快照锚定缺失 known=false」= 本批根因可见化）· web 产物同日替换（含预检→确认两段式/rejected 卡/账实横幅；API 指向 `https://api.adaiadai.com` + CanvasKit/字体本地化补丁，产物校验：`main.dart.js` 3,510,014 B 含新 UI 串、canvaskit 本地 200）· **⚠️ 生产交易账实仍未校准（P2-交易38）**：锚定缺失 + 存量持仓/现金为 09-12 双计后的错口径——需用户先导一次「持仓股/资金股份」快照建立锚定再核对· **learn 前置全部就位（2026-09-13）：`ffmpeg 6.1.1` ✅ / `DASHSCOPE_API_KEY` 已配（凭证不入库，.env 权限 640）✅ / 月度额度 10 小时 ✅**；转写真链已在生产实测跑通（B站 37 分钟无字幕视频 → 报价确认 → 转写 → 卡片 + `_raw/` 转写稿与元数据归位，约 100 秒，账本 `2244s / 0.1795 元`）；「有字幕视频」链仍不可达（B站 字幕接口对未登录请求返回空，需可选 `ADAI_BILIBILI_COOKIE`）· **iOS app**：release 包已构建签名就绪（`apps/adai-app/build/ios/iphoneos/Runner.app`，19.6MB，API 指向生产域名），待手机可达即装机（本轮 `devicectl` 报设备 unavailable）· 生产 `.env` 已补 `ADAI_SMOKE_ACCOUNT/PASSWORD`；**注意 deploy-gate 的 GATE-AFTER smoke 读的是「本机环境变量」**，须 `export ADAI_SMOKE_ACCOUNT=adai ADAI_SMOKE_PASSWORD=… no_proxy=82.156.111.146` 再跑门禁（本机代理会拦 IP:8080 → 拿不到 token；2026-09-12 已用这个跑法让 GATE-BEFORE + 部署 + GATE-AFTER smoke 6/6 全绿）；本次已按同口径手工跑通 smoke 9/9 200（feed/记忆/交易建议/一句话解析/时间线/标签 + learn 资产树/全文/找卡）
- **历史：2026-09-14 晚间批（v3.64 时代）**：learn 抓取放开（微博/公众号/头条）· 锁屏脱敏补全 + 令牌有效期/按 id 撤销 + 抓取健壮性 · Siri/快捷指令外部入口与分享令牌弹窗 · Android 正式签名 —— **均已上线**（详见上方对应版本段；2026-09-17 第八次部署时该行的「✅ HEAD = 生产」断言已过时，改为历史条目以免误导）。app 侧改动已进 PWA（`/m/`）与 Android/iOS 包。
- 生产目录：`/opt/adaios`（backend/data/web/admin/**app-web**/os；`.env` 含密钥不进 git）
- 数据路径：`data/{userId}/...`（本机账号 = `data/adai/`，default 已迁移移除；测试可用 `data/default/`）
- **账号与订阅**：Apple Developer Program 付费个人账号（Team `4G3D37YKSB`，2026-09-13 落地）
- **iOS 描述文件到期**：**2027-09-13**（到期当天 App 打不开，提前 30 天提醒；见 REVIEW P2-APNs5）
- **TestFlight**：**✅ 已打通（2026-09-15）**——iOS 构建已上传且 App Store Connect 侧 **VALID**（过期 2026-12-14，即 90 天）：构建 `1`/`2`（09-15 下午）· **构建 `3`（09-15 深夜，v3.68 日周月盈亏 + 防重复入账批）**——本轮把 `ASC_ISSUER_ID` 落盘到 `~/.appstoreconnect/issuer_id` 并让 `release_testflight.sh` / `testflight_status.py` 自动发现（此前它只活在首次打通那一次的会话内存里，换个会话就得重新问用户）；分发签名**绕开 Xcode 云签名**（云托管分发证书需 Admin 角色，App Manager 的 API Key 会被拒），改由 `apps/adai-app/scripts/asc_signing.py` 用 App Store Connect API 直建**分发证书**（id `2M8DTF4TPM`，到期 2027-09-15）+ 两个 App Store 描述文件，再由 `release_testflight.sh` 手动签名导出 + `altool` 上传；全程无需 Admin、无需在 Xcode 登录 Apple ID。完整方案见 `docs/deployment/ios-release.md`。**真机复验（2026-09-15 晚）**：装机 ✅（`state=INSTALLED`）· **Face ID 门禁 ✅ 通过** · **分享扩展 ❌**（全新安装后共享容器里没有令牌，报「没拿到钥匙」——**不是 bug 而是设计缺口**：限权令牌的「签发」仍靠用户在学习页手点一次，全新安装/给别人用必然踩到；已开 RFC `20260915-share-extension-credentials.md` 待排期）。内测构建 90 天有效。**构建 8（2026-09-18 凌晨，v3.72 待办归 Kernel 批）已上传**：`UPLOAD SUCCEEDED`（Delivery UUID `6f9c0130-f05a-40b3-aae6-0ceaeb1359b7`，23.7MB，主 App 签名 `iPhone Distribution: kangda wang (4G3D37YKSB)`）；Apple 侧已 **VALID**（2026-09-18 00:14 实测「✅ 可测试」，过期 2026-12-16）；`pubspec.yaml` 已同步为 `1.0.0+8`（本次用 `--build-number 8`，脚本不回写文件，手工同步防下次 +1 撞号）。**构建 9（2026-09-19，v3.72 待办归 Kernel 批之后）已上传且 VALID**（过期 2026-12-18）。**构建 10（2026-09-22 23:42，S-11 C 批）已上传**：`UPLOAD SUCCEEDED`（Delivery UUID `e165e650-7dda-4715-9fe9-14070052a730`，23.76MB，包内 `CFBundleVersion=10`；`pubspec.yaml` 同步 `1.0.0+10`）——Apple 侧 **23:43 VALID** → **23:47 `--assign-build` 入外部组「阿呆外测」+ `--submit-review` 提交 Beta App Review**（首次约 24~48h；过审后外部测试员即可在 TestFlight 装到构建 10）。**外部测试（2026-09-21~22 打通，用户「TestFlight 我要分享给外部测试人员，通过邮箱地址」）**：外测与内测的关键差别是**首次必须过苹果 Beta App Review**，而提交审核要求①隐私政策公开 URL ②审核联系人 ③可独立登录的测试账号三样齐备——开工实测**三样全空**。本批：**隐私政策上线 `https://adaiadai.com/privacy`**（页面 `apps/adai-app/web/privacy.html` + 生产 Caddy `handle /privacy`；正文 `docs/legal/privacy-policy.md` **draft → active**，填运营者「王康达」与联系邮箱）；**新建外部测试组「阿呆外测」**（公开链接**关**）+ 填测试信息（隐私政策 URL 在 **TestFlight 测试信息与 App Store 资料两处** + 反馈邮箱 + Beta App 描述 + What to Test）+ 审核信息（Kangda Wang / `+8618610915614` / rottokaka@gmail.com，测试账号 `applereview`）→ 2026-09-21 23:20 提交 → **2026-09-22 20:43 `APPROVED`（约 21 小时）**；构建 9 已加入外部组。新增脚本 `apps/adai-app/scripts/testflight_external.py`（`--status` / `--fill` / `--review-info` / `--submit-review` / `--assign-build` / `--invite`；测试账号密码只经 `ASC_DEMO_PASSWORD` 传入、不落盘不打印）。**下一步**：外部测试员邮箱列表到手 → 每人建独立账号 → `--invite` 发邮箱邀请（邮箱不要求是 Apple ID，但对方要用自己的 Apple ID 登录 TestFlight）。⚠️ 审核期间不可改 `applereview` 密码（审核员正拿它登录），**现已过审可改强**，改后用 `--review-info --demo-password` 同步苹果侧字段。
- **域名 / 服务器到期**：域名 `adaiadai.com` 注册至 **2027-01-30**（DNSPod）；服务器续费日未登记
- **公安联网备案**：**⏳ 已提交（2026-09-21），北京属地公安审核中**——最迟 **2026-09-30**（ICP 后 30 天内；见 REVIEW P1-合规1）。通过后须把公安备案号 + 图标/HTML 代码挂到 web/admin 底部 **4 处**（清单 `docs/deployment/gongan-filing.md` §6）

## 发布状态

- **v1.0.0**：Release Notes + 数据冻结 + 插件门控就绪；tag + 部署待用户确认触发（2026-08-15 用户决策：**先治理流程，发布顺延**）
- 后续候选：多账号前端选号（v1.0.1）、登录体系（#179，v1.0.1 立项）

## 未修项

- 见 `docs/review/REVIEW.md`（战略 + P0-P2 唯一滚动区）
- 待办/观察项：见 `docs/reference/task-log.md`
