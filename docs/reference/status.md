# 状态快照（Status）

> **测试数/端点数/运行环境的唯一事实源**（RFC `20260815-docs-governance` 单一事实源化）。
> **更新规则：每批 /ship 时更新本文件**——AGENTS.md 及子项目 AGENTS.md 一律不复制数字，只指回本文件。

---

## 测试状态

| 端 | 测试数 | 失败 | 备注 |
|:---|:------:|:----:|:-----|
| 后端 adai-core | **1497** | 0 | 全部 Controller 接口测试全覆盖 + **2026-09-06 learn 插件 V1（RFC 20260829，测试先行，+37）**：PluginRegistry 注册 learn（第三插件）+ LearnCard/domain 端口/LearnException + LearnCardFileRepository（md 卡片 File First：data/{userId}/learn/{type}/{date}_{title}.md，per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 清洗防路径逃逸）+ LearnDigestAppService（喂入→LLM 结构化 RFC 3.4 四段卡片→落盘；LLM 失败/输出不可解析/缺标题→素材留存 learn/_raw/ + 400 fail-visible 不产半成品；type 越界回落 other；trade_related 非 trading 强制 false）+ LearnController 3 端点（POST/GET /learn/cards + GET /learn/tree，learn 插件 403，独立端点喂入仿截图入账不污染记录/记忆/Feed）+ GlobalExceptionHandler LearnException→400 + 多模态 + 鉴权 + 交易建议引擎（止损硬判定）+ 一句话交易解析 + 交易数据模型（逐笔流水/止损/买点）+ 带图 S-2 聚合修复 + G-1~G-6 框架+插件 + 真止损预警 + 时段节奏推送 + 通达信持仓导入/代码查名 + 持仓行情注入 + 导入文件上传留存（GBK 转码）+ C2 自选股买点判定（KDJ/B1回调缩量/B2放量破前高）+ D3 清仓复盘三维打分（买点回溯/执行纪律）+ 择时状态配置驱动读取 + 持仓元信息更新端点（PUT /positions/{symbol}，止损/角色）+ 简报注入真实持仓快照（防 LLM 算错盈亏）+ 导入解析失败禁止落零（P1-交易5）+ 买卖同步市值（P1-2）+ 收盘缺行情跳过（P1-3）+ P2 批（线程池/缓存/阈值-5%/KDJ-13/并发扫描/节假日）+ 八端点 controller 测试 + P2 批（原子写/插件缓存/任务删除门控/记忆三缺陷）+ S5 现金单一真源 + P1-交易4 占比分母含现金（回归测试）+ Memory 正文 `---` 分隔不截断（回归测试）+ RFC 20260817（推送开关/结构化推送/交易日志归集，PushSettings 4 + TradeLogCollect 5）+ 20260818 批次（历史成交导入解析/幂等/对账 + /trades/batch 补实现 + 持仓 replace 全量覆盖 + BUY 止损/买点放开 + 本金设置 PUT /principal）+ P1-1 交易归集 unknown 污染修复（拒绝归集/complete 补 symbol/dedupeKey name 兜底）+ P2-1 东财熔断（连续失败 3 次熔断 5 分钟 + 半开探测）+ 首屏提速（brief/cached 端点 2 测试）+ RFC 20260823（历史成交导入回填：幂等命中且缺成交时间 → 回填 updated 计数，回填 round-trip ×3 + 重传回填 + 已有时间不重填）+ **2026-08-23 走查修复批（B1-B5，21 项）**：confirm 失败候选保留 + account.json 原子 RMW（per-user 锁）+ direction @NotNull + batch 逐行校验 + 尾盘 r81Applicable + 收盘昨收残缺跳过 + 快照写失败告警 + verdict R53 延展 + 节假日 2026 官方/2027 预测 + MarketAlert 判定拆开 + dedupeKey volume 桶 + TradeLog save 锁 + MarketPush 读失败拒写回（+11 测试）+ **隔离审查残留批（B6-B7）**：MarketPush 结构校验 + sameTrade ±10% 区间去重 + 三处 changePercent 判空 + 快照写失败抛 StorageException + confirm 单 now + 切 Tab 静默刷新 + DELETE /trade-log 丢弃端点 + 真实仓储并发/损坏测试（+17 测试）+ **C 批**：confirm 处理期新候选合并保留 + 2027 中秋修正 + batch 空 400 + name 超长校验 + 锁收敛 userId（+3 测试）+ **2026-08-24 行情主源切换批**：KlineService 主源/兜底配置化（`adai.market.kline-primary` 默认 tencent——腾讯主源 + 东财探测兜底，生产东财被限不再刷 500+/日 WARN；Tencent 补 KlineSource 实现，+1 配置切换回归）+ **2026-08-25 推送渠道切换批**：BarkPushChannel（iOS 原生推送，PushChannel 第二外部实现，POST JSON，base-url 可配自托管；微信 Server酱 停用——免费 5 条/天不够，代码保留未配置即禁用；+4 测试）+ **2026-08-25 RFC 20260825 批次跟踪批**：批次推导（按日合并/LIFO/回合/初始批次/对账，TradingLotService）+ GET /trading/lots 批次视图 + 导入双模式（sync 同步持仓/append 补录，orderId 透传幂等）+ 每日操作总结（行为标注六类：亏损加仓/追高/短线新开/破止损未走/浮盈回吐/短线超期）+ 批次级止损推送 + 推送 expiresAt 定时消失（行情类次日 09:30/汇总类次日 23:59）+ recordTradeWithOrderId + 手动/导入交叉防重 + 卖出费用入回合 + 初始批次卖出扣减 + 批次止损推送测试 + 卖清重买不误判（+27 测试）+ **2026-08-26 截图表格归集批**：parseLooseBatch 表格批量解析（委托截图多笔 → 逐笔候选，已成/已报/申购过滤）+ collect 批量归集（跨截图 sameTrade 去重）+ 生产真实验证（+8 测试；工作树含遗留未提交测试 +6，实测 786）+ **2026-08-26 截图入账 + 复盘卡点批**：POST /trading/screenshots 截图入账（multipart 1-3 张 → VLM → 候选，不建记录/不落原图/不沉淀记忆，TradingScreenshotAppService）+ hasTradingActivity 改当日真实成交口径（废除关键词扫描）+ 复盘与截图入账成闭环（+14 测试） + **2026-08-29 晚间自主修复批**：confirm 锁内原子 saveMerging（P2-交易25 残余窗口闭合）+ 三处锁池固定 16 条带（P2-交易28 map 增长根治）+ MarketPush 写失败 warn→error（P2-交易27）+ 收盘缺行情推送通知（P2-交易33）+ closeAccountUpdate 残余窗口注释（P2-交易24）（+2 测试） + **2026-08-29 收盘小结批**（15:30 推送：当日成交+破止损+待确认，close-summary 类型+双端开关，+3 测试） + **2026-08-30 交易插件规则层（第三阶段，trading-plugin-architecture.md）**：TradingRuleSettings（16 参数：仓位上限/默认止损/浮盈回吐/短线超期/清仓阈值/买点 5 参/打分权重/硬约束区间，fail-closed）+ TradingRuleSettingsRepository（data/{userId}/trading/rules.yaml 读写，YAML 真相源）+ 规则引擎按用户（仓位上限/行为标注/清仓 verdict/买点参数/打分权重/建议硬约束区间全部从用户规则读，无规则 → 默认=adai 现状）+ 知识注入用户私有（data/{userId}/trading/knowledge.md 优先，os/ 作默认）+ GET/PUT /trading/rules 端点 + 降级验证（无规则用户=纯客观）+ adai 规则包生成（rules.yaml + knowledge.md，行为不变回归）（+16 测试） + **2026-08-30 审查修复批（deep 深审 5 官）**：P0-1 保存链路 fail-visible（save 抛 StorageException/空提交 400/NaN 拒绝）+ P0-2 硬约束区间 R66-R95 内收缩防 fail-open + P1-1 止损判定线统一（effectiveStopLoss 传 userId）+ P1-3 多用户知识泄漏 owner 白名单 + P1-4 domain 端口接口 TradingRuleSettingsPort + P2 批（per-user 条带锁/NaN finite 校验/knowledge.md 缓存/save 保留未知键/买点参数防轰炸下限/文案区分用户 vs 默认/测试独立文件/引擎 TTL 缓存）+ P1-2 规则包同步脚本（09-scripts/sync-adai-rulepack.sh）+ P1-5 降级语义定稿默认值兜底 + P3 死代码清理（+22 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：POST /records/ask-stream SSE 端点（text 增量/meta 定稿/[DONE]/error 事件；后端内降级——模型无增量回退同步 understand 一次；同卡同问 5 分钟去重 QuestionAppService.findDuplicateResend；JsonTailFilter 防 JSON 回执跨块切开；UTF-8 byte[] 透传防中文乱码；线程池满 503）（+16 测试：AskStreamController 5 + JsonTailFilter 5 + QuestionAppService answerStream/dedup 6）+ **2026-08-30 推送口径修复批（用户反馈）**：行情提醒节假日守卫（B5-1 残留：节假日撞工作日 break-cost/止损类拿前日收盘价每天重推）+ 轮询首轮 9:00/9:30 → 10:00（9 点档行情接口仍返回上一交易日收盘，旧数据冒充「今日」且按日去重签名被烧名额）+ 早盘计划昨收口径（数据文本标注「昨收/昨日涨跌」+ 提示词第 5 条，LLM 不再把昨日涨幅说成「今日」）（+4 测试） + **2026-08-30 完美买点案例库批 1（第四阶段环 1-2，trading-case-library-design.md）**：KlineSource.klineRange 日期范围直查（腾讯/东财覆写为数据源日期参数，默认拉 320 截取）+ MacdIndicator（EMA12/26 + DIF/DEA/柱 + 金叉）+ CaseFeatureExtractor（9 特征：回撤/量比/KDJ/MACD/MA 关系/距 60 日线/黄白线态/盘整/破前高 + 后验窗口）+ TradingCaseRepository（domain 端口 + JSON 落盘 data/{userId}/trading/cases/ + _index.json 清单原子写 + 条带锁）+ TradingCaseAppService + TradingCaseController 4 端点（POST/GET /trading/cases、GET/DELETE /trading/cases/{caseId}）+ 降级（K 线失败不落半成品/后验不足 null/损坏文件视为不存在）（+20 测试）+ **2026-08-30 案例库批 1 环 3（AI 理解）**：POST /trading/cases/{caseId}/insight（LLM 读特征画像 + K 线统计 → 结构化「为什么这是完美买点」aiInsight 落盘，reviewed=false 待人工确认；prompt 判定优先级 形态>量能>指标>位置；LLM 失败/非 JSON 输出 → 400 不落半成品 fail-visible）（+7 测试）+ **2026-08-30 案例库批 1 环 4（判定当下，核心价值）**：CaseFeatureNormalizer（7 维特征 min-max 归一化，KDJ/MACD null → 中性 0.5）+ CaseSimilarityEngine（加权欧氏，权重和=1 → 相似度=1−距离；Top N 降序，无特征跳过）+ POST /trading/cases/match（当前标的拉 60 日 K → 特征 → 与案例库相似度 Top 5；date 可空=最近交易日，指定日无数据回落最近；空库 → matches:[] 静默降级；相似度不覆盖规则硬判定）（+14 测试）+ **2026-08-30 环 4 二期（扫描接入 + 黄白线参数化）**：WatchlistBuyPointService 附案例相似度 Top 3（开关 adai.trading.case.scan-match 默认关，向前兼容；规则未命中但案例相似 → buyPoint="case" 参考类型，15:10 推送跳过）+ TradingCaseAppService 黄白线周期配置化（adai.trading.case.yellow-ma/white-ma 默认 60/10，批 5 前置）（+3 测试）+ **2026-08-30 案例库审查修复（S1）**：save index 写失败回滚案例文件（防 409 卡死）（+1 测试）+ **2026-08-30 日期宽容解析**：case 标注/match 日期接受 yyyyMMdd + yyyy-MM-dd（Controller parseDate，失败 400 人话「日期格式不正确」）（+3 测试）+ **2026-08-30 TDX 通达信本地行情源**：TdxFileKlineSource（.day 二进制解析：32 字节/条小端 int32，价格 ×100、volume 股→手；kline/klineRange 日期直查；mtime 缓存）+ KlineService 优先级 tdx → 腾讯 → 东财（tdx 无数据不算失败）；配置 adai.market.tdx-path/tdx-enabled（+6 测试）+ **2026-08-30 验收反馈批（搜索 + 图表）**：GET /trading/search 标的搜索（东财 suggest 代码/名称/拼音首字母，NameToSymbolResolver.search 候选列表）（+4 测试）+ **2026-08-30 前复权批（TDX 数据正确性，trading-data-adjustment.md）**：AdjustmentCalculator（前复权算法：除权参考价 + 事件前价格逐级下调）+ AdjFactorRepository（东财分红送转拉取 + 方案文本正则解析 + 本地缓存 data/market/adj/ + 按日 TTL）+ TdxFileKlineSource 集成换算（TDX 读取即前复权，与腾讯 qfq 同口径）；真实校验：茅台 34 根全 ≤0.5% 偏差（+14 测试）+ **2026-08-30 前后端指标一致批**：IndicatorSeriesCalculator 全序列（MA/KDJ/MACD 与特征同口径）+ detail?indicators=true + 前端图用后端序列（hover 值=特征值，消除双算）；000831 买点日 KDJ 8.685 vs 8.69 同源（+5 测试）+ **2026-08-30 共识判定批（核心价值，建议 #3）**：CaseConsensus——案例库 ≥5 时统计学习各特征 25-75 分位「完美买点画像」，match 响应加 consensus（逐维命中 ✓/✗ + 命中 N/M 维）；web 匹配弹窗共识卡（+6 测试）+ **2026-08-30 案例质量校验批（建议 #4）**：标注响应带 consensusCheck（案例库 ≥5 时新案例 vs 画像逐维偏离，Map.of 修 null）；web 标注后偏离大 → 提示「与完美买点画像有偏差」确认（不阻止，防脏案例）（+2 测试）+ **2026-08-30 案例库数据批次审查修复**：KDJ latest <10 根 null 语义回归恢复 + 搜索竞态代际令牌（+1 测试）+ **2026-08-31 批量导入批**：POST /cases/import（CaseImportParser 解析 B1/B2 笔记格式 + 本地全 A 名称表 data/market/names.json 精确匹配兜底 suggest + 逐条标注含共识校验 + 北交所明确失败；实测 26 条导入含双创）+ web 批量导入弹窗（+11 测试）+ **2026-08-31 导入类型识别批**：解析器行内 B1/SB1/B2 标记 → 导入自动带买点类型（无标记 unknown）；生产 32 案例按笔记补全类型（B1 14 + B2 18，含新瀚新材两买点）（+2 测试） + **2026-08-31 案例库数据使用方案批（用户授权当晚全做）**：双轨画像（CaseConsensus/Similarity B1/B2 独立 + 负样本排除）+ 负样本入库（TYPE_FAILED + match 失败警示）+ 后验回填调度器（15:35 回填 verify + index 重建）+ match 双轨响应（type/b1/b2/failedSimilarity）+ 权重可配置（sim-weights）+ web 双轨卡/类型下拉/FAILED 红行/+5d「—」（+11 测试）| + **2026-08-31 双止损位批（trading-risk-plan）**：Position 加 `computedStopLossPrice`（系统计算止损：R=本金×1%、距离=min(R÷单仓市值,5%)、止损价=成本×(1−距离)，动态算不落盘）+ `effectiveStopLoss`（生效=max(人工,计算)，更严格者）；getPositions 按本金快照注入计算止损（行情失败也返回）；R66/接近止损/建议引擎/推送/复盘统一改用生效止损；web 持仓表止损列双值展示（主值=生效，副行标注来源）+ DTO 解析测试（+7 测试） + **2026-09-01 登录体系批（RFC 20260901-auth-login，根治 #179）**：AuthController（login/logout/me/setup/password 5 端点）+ AuthFilter（Bearer 校验 + RequestWrapper 覆盖 X-User-Id——92 处 Controller 零改动）+ SessionRepository（sessions.json 原子写，token 只存 SHA-256）+ Account.passwordHash（bcrypt）+ 登录限流（5 次失败锁 15 分钟）+ setup 一次性 + 改密踢会话 + accounts/available 免鉴权封死（+50 测试：AuthController 13 + AuthFilter 11 + AuthService 17 + SessionFileRepo 9） + **2026-09-02 登录体系修复批**：AuthFilter 免鉴权前缀漏无尾斜杠路径（admin 全 401 根因）+ 401 WARN 日志（含 IP/路径/token 状态）（+2 测试） + **2026-09-02 #178 admin 登录改造批**：admin 管理口并入统一登录（AuthFilter role=admin 门禁 + X-User-Id admin 透传，AdminAuthInterceptor/X-Admin-Token/ADAI_ADMIN_TOKEN 退役）+ /accounts 响应 AccountView 过滤 passwordHash + PATCH 保留哈希/支持重置密码（踢会话 kickSessions）+ AuthService findAccount（+5 测试） + **2026-09-03 案例未修项修复批（REVIEW 6 项出表）**：P2-案例1 窗口<60根已知限制注明 / P2-案例2 web 自选适配 buyPoint="case"（案例相似 N% 橙色） / P2-案例3 案例库 TTL 缓存 5min / P2-案例5 index verify 缺失落 JSON null / P3-案例1 路由登记 trading_case_insight 归 flash / P2-2 复核出表（+4 测试） + **2026-09-04 CORS 顺序修复批（生产 admin 打开 CORS 报错刷屏根因）**：AuthFilter `@Order(LOWEST_PRECEDENCE+1)` 整数溢出为 MIN_VALUE 反成最高优先级 → 跑在 CorsFilter 之前 → 401/403 响应丢 CORS 头 → 浏览器把未登录/无权限误报为 CORS policy 错误（页面刷屏）；CorsFilter 改 `FilterRegistrationBean` 显式 `HIGHEST_PRECEDENCE` 先写头 + AuthFilter order 修为紧随其后 + 回归测试 3（401/403 带 ACAO ×2 + filter 顺序契约防溢出复犯）（+3 测试）  + **2026-09-04 活跃市值区间开关批（v3.41，用户拍板「指南针活跃市值=一切的前提，由我判定多空，app/web 两端都得有」）**：TradingMarketStage（bull/bear 两档 + Port + Repository 落 market-stage.json per-user 锁原子写）+ GET/PUT /trading/market-stage（无记录 exists=false/非法 400）+ 推送择时三级读取（用户判定→current.md→未知，用户设了不被 OAMV 旧规则覆盖）+ Web/App 交易页红绿切换条（多头红/空头绿/未判定灰）+ 本地预置空头（+15 测试） + **2026-09-04 行情数据包导入批（MD17）**：TdxDataPackageImporter（zip 流式收集 `(sh\|sz)\d{6}.day` 防 zip-slip、逐文件解析校验、`.tmp`+原子 move 覆盖）+ `POST /api/v1/admin/market/tdx-import`（multipart）+ adai-admin 维护页「行情数据导入」卡片（file_picker 选 zip）+ `application.yml` multipart 上限 env 化（+10 测试） + **2026-09-04 按批次止损批（决策 P1 方案 A）**：LotStopLossOverrideRepository（lot-stoploss.json 覆盖层，per-user 条带锁原子写 + 损坏降级）+ TradingLotService.derive 合并覆盖（覆盖>流水>默认−7%）+ PUT/DELETE /trading/lots/{lotId}/stop-loss（设改/清除）+ web 批次明细弹窗行内「改」止损（api.update/clearLotStopLoss）（+15 测试） + **2026-09-04 买点三重校验批（P1-交易9/20 出表）**：BuyPointDetector 重写——B1 几何课程口径（回撤占波段 ≥50% ⇔ close≤(high+low)/2）+ B2 放量默认 2.0（倍量柱，rules.yaml/os sync 同步）+ B2 三重防护（KDJ 拐头/高位钝化排除/追高 ≤30%/连板排除）+ WatchBuyPoint.dataDate 新鲜度（15:10 只推当日数据）（+5 测试） + **2026-09-04 资金曲线批（决策方案 A）**：EquityCurveService（现金现值锚定反推 + 流水/转账事件 + 底仓恒持 + 收盘价市值；净值=总资产/净投入、回撤）+ GET /trading/equity-curve + web 资金 Tab 曲线迷你图（净红/总资产蓝 + 最大回撤）（+6 测试） + **2026-09-05 首页行情条交易时段窗口批（用户反馈「周末还在给我推行情」）**：大盘行情条只在 A 股交易时段注入 Feed（工作日 9:30-11:30/13:00-15:00，法定节假日守卫复用 TradingSessionPushService 节假日表）——非交易时段打开首页不再出现标「当前时刻」的行情卡，消除「周末还在推行情」误读；FeedAppService 构造注入 Clock（ClockConfig 提供 bean）+ 测试固定交易时钟（+5 测试） + **2026-09-05 交易⑤认知层落地批（RFC 20260905，用户拍板全量执行）**：P0 建议留痕（AdviceEntry/AdviceHistoryRepository/按月 JSON 落盘 data/{userId}/trading/advice-history/+ GET /trading/advice-history 查询 + 建议出口逐票落盘含降级标记）；P1 个人画像（TradingProfileService 客观统计实时算 + profile.md 主观层读写 + TradingProfileContributor 注入 trading/decision 场景+ GET/PUT /trading/profile + 建议引擎 prompt 前置注入画像）；P3 建议闭环（复盘「阿呆当时说 X → 你做了 Y → 结果 Z」对照段——当日清仓回查建议留痕）；P2 情绪采集（TradePsychologyService 确定性提问 3~5 问 + GET /sold/{symbol}/psychology-questions + POST /sold/{symbol}/psychology/answer 回填 sold.psychology 并沉淀 profile.md）；P2 建议遵守率（clear/reduce 建议后 10 天内执行=遵守，B 反哺 A 注入画像 + /profile 响应带 adviceAdherence）；远期拦截预留（建议引擎逐票注入 symbolHistoryNote 回头草对照）。红线：数字系统算/主语是你/隐私在 data/。基线 1137 → 1189（+52：本批净增 + 同会话首页行情条批 +5；审查修复批再 +8——排序/ID毫秒/尾盘留痕/样本门槛/情绪幂等等，见 change-log）  + **2026-09-06 learn 插件 V1（RFC 20260829，测试先行）**：PluginRegistry 注册 learn（第三个插件）+ LearnCard 领域实体（type/trade_related 语义收敛，非 trading 强制 false 防 LLM 幻觉）+ LearnCardFileRepository（md 卡片 File First：data/{userId}/learn/{type}/{date}_{title}.md，per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 清洗防路径逃逸）+ LearnDigestAppService（喂入素材 → LLM 结构化卡片（RFC 3.4 四段）→ 落盘；LLM 失败/输出不可解析/缺标题 → 素材留存 learn/_raw/ + 400，fail-visible 不产半成品；type 越界回落 other）+ LearnController 3 端点（POST /learn/cards 喂入消化、GET /learn/cards 列表、GET /learn/tree 资产树，learn 插件 403 门控，独立端点喂入仿截图入账先例不污染记录/记忆/Feed）+ GlobalExceptionHandler LearnException→400（+37 测试：Repo 10 + AppService 13 + Controller 14）+ **L2 LearnKnowledgeSource（2026-09-07）**：kernel 知识源 name=learn → PluginRegistry 映射 learn 插件 → ContextEngine 按 enabledPlugins 注入最近学习笔记（title+type+核心观点，无卡片不注入/损坏跳过/上限 5/enrich 不双份），问答可召回自己消化过的内容（RFC 3.7 ③） + **learn V2 消化闭环批 1（2026-09-07 复习流转 + 编辑，RFC 20260829）**：status 常量 new/review/done + isValidStatus + 仓储 updateStatus（按 type+title 定位原地改 frontmatter status 行，正文/手写复述段原样保留——File First 不重建文件）+ PATCH /learn/cards/status 端点（body type/title/status）+ 编辑支撑——LearnCard 补 retell 复述段建模（V1 模板四段空段补齐，md 读写对称）+ repository.update（已存在才覆盖、created 变拒绝=防移动文件）+ AppService.edit（部分字段补丁：coreView/keyPoints/questions/retell/tradeRelated/tradeNote/tags，缺省保留原值，非 trading 强制收敛）+ PATCH /learn/cards 端点（?type=&title= 定位）+ 编辑/流转均 per-user 条带锁 + fail-visible + **learn V2 批 3（2026-09-07 trading 候选联动，RFC 20260829 3.5③）**：LearnTradingCandidate（建议卡 record：title/learn_card_id 回链/sourceType/created/coreView/keyPoints/tradeNote/tags）+ LearnTradingCandidateRepository 端口 + LearnTradingCandidateFileRepository（落 data/{userId}/trading/candidates/{date}_{title}.md——RFC 3.3 V2 加 candidates/ 目录；per-user 条带锁 + 同日同名防覆盖 + 损坏跳过 + fileStem 复用防逃逸）+ LearnCandidateAppService（trading+trade_related 卡片 → 候选：非 trading/不存在/未标 trade_related 400 人话——防语义漂移审核闸前置；learn_card_id 回链 learn 源卡；候选只存提炼建议不复制整卡，跨域无双写；不自动入库需在 trading-engine 工作流审核融合）+ LearnController 3 端点（POST /learn/cards/candidate + GET/DELETE /learn/cards/candidates，learn 插件 403） + **learn V2 批 4（2026-09-07 复习提醒推送）**：LearnReviewPushService（每晚 20:00 cron 可配 `adai.learn.review-cron`，遍历 learn 插件用户 → 聚合 status=review 且 created ≥7 天未 done 卡片跨 ai/trading/other → type=learn-review 推 PushChannel 汇总，不自动改状态）+ PushSettings.ALL_TYPES 加 learn-review（推送开关可关默认开）+ FeedAppService push 注入门控 trading-only → trading 或 learn（纯 learn 用户 Feed 可见复习提醒，market 行情条仍 trading-only）+ 双端推送设置项 learn-review（+22：Repo 8 + AppService 7 + Controller 7——1255 全绿）（+28：Repo 12 + AppService 7 + Controller 9——1283 全绿，Learn 测试 56→84）（+9：ReviewPush 7 + FeedAppService 门控 2——1292 全绿）（+7：LearnKnowledgeSourceTest 6 + ContextEngineTest learn 门控 1）+ **learn V2 审查修复批（2026-09-07 learn V2 增量深审，用户「按决策来」：S-learn1/2 + P1-learn1~4 + P2-learn2~8 出表）**：复习流转约束（isValidTransition 只允许 new↔review/review→done/done→review，跳变 400）+ 进入 review 写 review_at + 提醒按 reviewAt 计时满 7 天 + reminded_at 7 天节流（防每晚 nag）+ 仓储 applyEdit 锁内 merge 保留手工未知段（P2-learn6/7）+ 跨日同名拒绝（save/find 歧义防护 P1-learn2）+ learn_card_id 回链精确 fileStem（P1-learn1）+ markReminded/updateStatus 带 today（storage 不取 now）+ learn-review 开关 learn 侧端点（GET/PUT /learn/push-settings，纯 learn 可自关 S-learn2）+ Feed 事件类型级门控（P2-learn3）+ 候选删除歧义防护（P1-learn4）（+17 测试：仓储 11 + ReviewPush 4 重写 + Digest/Controller 适配——1309 全绿）+ **2026-09-10 learn 喂入入口批（v3.56，用户拍板「先页面后对话流」，批 1 页面喂入）**：POST /learn/cards 改**提交式消化**（同步 LLM 几十秒超时先例同复盘——POST 立即返回 status pending/running，后台 learnSubmitExecutor（LearnSubmitConfig 独立 2 线程/8 队列）执行原 digest + 内存任务态 DigestJob（jobs，按 userId）——inflight 去重不重复烧 AI、执行器满 400 人话「消化任务繁忙」、后台 LLM 失败/不可解析 job=failed + 人话 message（素材留存 _raw/ 不变）、done/failed 60s 惰性清理回 idle、重启丢失回 idle）+ 新增 GET /learn/digest/status（idle/running/done{type,title}/failed{message}，done 后前端按 type/title 打开新卡）+ LearnController POST 改提交式（+11 测试：AppService 提交式 7 + Controller 4——1355 全绿） + **2026-09-12 learn 抓取批（v3.57，RFC 20260912 D 形态阶段 1 抓取主干；用户指示「不需要我的干预」自主开工）**：**服务端抓取落地（B 的死因正面修复——不再要用户自己搞素材）**——`LearnSource`/`LearnSourceFetcher` 端口（domain）+ `BilibiliFetcher`（view 元数据 + player/v2 字幕 + playurl 音频线索；412/429 限流人话；退避重试；`downloadAudio` 带 Referer——实测不带 403/带 206）+ `ArticleFetcher`（HTML→正文：去 script/style/nav、实体解码、块级转换行；**403/5xx → Web Archive 快照兜底**；正文 <200 字判反爬空壳页；超长截断 50000）+ `LearnFetchService`（域名策略：首期不做清单 YouTube/公众号/知乎/小红书/X/微博/抖音/头条 → **人话 + 替代路径**，不绕登录墙）+ **转写链路**：`AsrClient`/`AudioTranscoder` 端口（kernel/ai）+ `DashScopeAsrClient`（getPolicy→上传→异步提交→轮询→取成品；**带 `X-DashScope-OssResourceResolve: enable`** 修 pitfall「上传文件下载失败」；multipart 字段对齐官方 SDK）+ `FfmpegAudioTranscoder`（B站 fMP4 必转 16k 单声道 mp3；不可用 fail-visible）+ **费用闸 `LearnTranscriptionService`**（§3.8 六条：字幕优先／同素材只转一次（转写稿落 `_raw/{platform}-{id}-transcript.txt`）／显式触发／月度硬闸 `LearnQuotaFileRepository`（`learn/_quota.json` 按月分键月初自动重置）／单次报价确认／ASR 端口可换）+ **流水线**：提交式加 stage（fetching/transcribing/structuring）与 `needs_confirmation`/`cancelled` 任务态 + **端点数 +3**（`POST /learn/digest` 链接或素材喂入、`POST /learn/digest/confirm` 费用确认、`GET /learn/digest/quota` 本月额度；`POST /learn/cards` 保留兼容别名；`GET /learn/digest/status` 扩展 stage/source/cost）+ 具名留痕 `saveRaw`/`readRaw`（源必留痕 + 幂等复用，文件名清洗防逃逸）+ **附带修掉未修 pitfall「LLM 输出 JSON 夹未转义引号」**（prompt 值内中文引号 + 解析层 `repairJson` 宽松兜底，双做）（+137 测试：Fetcher 22 + FetchService 12 + Transcription 15 + Quota 9 + ASR 11 + Transcoder 6 + RawAsset 10 + 流水线/确认流 18 + 端点 17 + JSON 修复 11 + 既有适配 + 记账失败停机 1 + **提交/确认原子性 4**——**1497 全绿**）；**提交前自查修掉两处「检查-再动作」竞态**（原 get→判断→put/ resume 非原子：两端同时提交会各跑一遍任务、同时点「继续转写」会**双花转写费** → 改 `ConcurrentHashMap.compute` 原子转移，配 8 线程并发确认回归测试断言「只有一个能抢到」） |

| 前端 adai-app | **183** | 0 | **2026-09-07 learn L2 最近学习**（Launcher「学习」行 learn 插件门控 → 最近学习列表（合并 created 倒序）+ 单篇全文页，错误态/空态/重试；ApiService getLearnTree）+ **2026-09-10 喂入入口批（页面喂入）**：「最近学习」页头「＋」喂入页（粘贴素材 + 类型下拉/来源可选 → 提交式消化 → 轮询 done 回列表自动打开新卡，失败人话可重试、超时后台继续素材留存；ApiService submitLearnDigest/getLearnDigestStatus，+2 测试）+ Feed 状态机 12 + 6 页面 + 选号/切换链路 + 插件门控 + 带图发图即对话 + 交易建议 UI + 交易止损/买点 P0 + 交易对齐 web（账户总览卡/止损显示/默认-7%止损/自选买点/清仓打分）+ 30分钟自动刷新 + 次级数据代际守卫 + 上传批次锁 + 第一原则徽章移除（无记录/提问/领域系统标签）+ 20260818 简化（纯买卖记录去止损/买点 + 价格小键盘小数点）+ **2026-08-23 app 体感批**（IndexedStack 保活/排序四修/Feed 错误态/任务编辑 PUT/launcher 伪0/三态/人话/代际令牌/方向色/时间戳）+ **P1-G6-1 守卫批**（timeline_modal await 后 setState 补 mounted 守卫×2 + 回归测试×2：关闭后响应到达不崩溃/正常路径渲染）+ **2026-08-25 批次简版批**（持仓卡第三行批次信息：批次数/最近买入/含底仓徽标/破止损警示，getLots 静默降级，+3 测试）+ **2026-08-26 截图入账批**（交易页「📷 截图入账」入口 + 当日候选内嵌列表逐笔确认/丢弃 + 复盘卡点：无真实成交时复盘按钮引导先导入，+4 测试）+ **2026-08-27 缺日期禁落库批（v3.32）**：候选行缺成交日期警示 + 「补日期」入口（日期选择 → PUT /trade-log/date）+ 确认前拦截缺日期候选（+1 测试）+ **2026-08-28 批次明细批**（持仓卡批次行可点 → 批次明细弹窗：每笔买入的日期/状态徽标（初始底仓/持有中/已清仓）/剩余·成本·现价/盈亏+盈亏%（开放批次浮动 pnlPct、已清仓回合收益率前端算）/破止损橙色警示，连点守卫防叠两层，+2 测试） + **2026-08-29 晚间批**（P1-前端1 静默刷新失败保留旧数据 / P2-UX1 NL 回显 ≤5 位去尾零 / P2-交易31 principal=0 总盈亏「—」不给误导数值，+3 测试） + **B 类批**（确认流阿呆口吻+loading 灰态/插件行稳定槽位/下滑返回挂拖拽条/字号下限 11px/触达 44pt，+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 三文件条件导入（IO http.send 流式 / Web fetch ReadableStream / common 协议件）+ askStream（text 增量 onDelta / meta 定稿 / error 人话 SseServerException / 未收增量自动降级同步端点且 intent 同构透传）+ main_page 三处接线（首问/建卡 question/续问文本分支）+ 90ms 节流草稿 + meta 定稿替换（+6 测试：ask_stream 5 + 流式状态机 1）+ **2026-08-30 续问重复回答修复批**：_appendToActiveCard meta 定稿原位替换草稿 turn（原追加 → 草稿+定稿双份，用户实测每轮续问回答显示两遍/end 上报 turns 带重复 8 vs 服务端 6）+ 门控回归测试（反向验证红，+1 测试） + **2026-09-01 登录体系（RFC 20260901-auth-login）**：登录页（账号密码 + 首访 setup 引导）+ token 持久化（shared_preferences）+ ApiService Bearer/401 全局回登录页 + Launcher「退出登录」替代选号切换 + iOS 编译验证（+5 测试：auth_flow_test） + **2026-09-02 修复批**：DualWorldShell 显式注入带 token ApiService（全 401 根因）+ multipart（截图/发图）带 Bearer 回归 + 2 回归测试| + **2026-09-04 活跃市值区间开关批（v3.41）**：交易页「活跃市值（指南针）」红绿切换卡（多头红/空头绿/未判定灰，乐观更新+失败回滚+连点守卫）+ ApiService get/setMarketStage（+1 测试） + **2026-09-12 learn 抓取批（v3.57 前端接线）**：「最近学习」喂入页顶部加**链接输入框**（首选入口，占位「粘贴 B站 / 文章链接，我来整理」），素材框保留为降级（抓不到时粘正文），旧的「原文链接（可选）」重复框合并删除；`stage` 驱动进度人话（正在抓取原文／正在转写，可能要几分钟／正在整理成卡片）+ 抓到的来源一行（《标题》· UP主 · 时长）；**`needs_confirmation` → 停止轮询**并展示后端报价 message +「继续转写」/「先不转写」（继续 → confirm(true) 恢复轮询；先不 → confirm(false) 收尾，含返回入口）；confirm 失败可见可重试；新增**代际令牌 `_gen`**（重试/确认/dispose 后旧轮询自失效，防两条轮询并行）；顺带按 B1 清掉本页系统口径文案（「请求超时，请检查网络」→「等太久了，检查下网络再试」等）；ApiService `submitLearnDigest` 改打 `/api/v1/learn/digest`（url 优先、content 变可选）+ 新增 `confirmLearnTranscription`/`getLearnQuota`；模型 `LearnDigestJob` 增 stage/source/cost + `LearnDigestSource`/`LearnDigestCost`/`LearnQuotaDto`（防御式 null 解析，message 缺失时用 cost 自拼人话）（+10 测试：Job 解析 5 + LearnPage 5——链接喂入→done→打开新卡／needs_confirmation 亮费用且不自动继续／继续转写→confirm(true)→done／先不转写→confirm(false) 不进 done／双空拦截）——**183 全绿** |

| 前端 adai-web | **193** | 0 | **2026-09-07 learn L2 资产页**（导航项「学习」learn 插件门控 + master-detail：左 ai/trading/other 分组卡片目录 + 右单篇全文渲染（核心观点/要点/疑问/交易标注/复述段），空态/失败降级/刷新保留选择；ApiService getLearnTree/getLearnCards + **2026-09-10 喂入入口批（页面喂入）**：页头「＋」整理弹窗（素材粘贴/类型下拉/作者·平台·链接可选 → 提交式消化 → 弹窗内轮询 done 自动定位打开新卡，失败人话可重试、超时后台继续；空态引导改指页面入口；ApiService submitLearnDigest/getLearnDigestStatus + _DigestDialog，+3 测试）+ **V2 UI 接线（2026-09-07）**：状态徽标（待复习/复习中/已完成）+ 详情操作（去复习/标记完成流转 + 写复述弹窗就地保存 + trading tradeRelated 反哺候选按钮，updateLearnStatus/editLearnCard/createLearnCandidate，+4 测试）+ + 交易管理端（持仓编辑/批量导入/复盘历史）+ 通达信导入解析 + 自选/清仓/资金区块 + 自选买点信号列（C2）+ 清仓三维打分列（D3）+ 行为模式统计（D2）+ 切入自动刷新修复（P1-1）+ 打分按序匹配（P1-8）+ 可降级请求分离（P1-7）+ P2 批（mounted守卫/胜率口径/否定词/千分位/打分列色/删除确认）+ DTO 测试补齐 + P1-5 回归 + 走查 8 项（可降级代际/历史日期竞态/图片回执自然化/交易成功 toast/首载失败错误态/错误人话/Feed 徽章移除）+ 20260818 批次（历史成交/持仓/CSV 三格式识别测试）+ RFC 20260823（历史成交 Tab 分组全字段渲染 / 空态 + 导入入口 / 非历史成交格式人话拒绝）+ **2026-08-23 走查修复批**：资金区块 principal=0 总盈亏失真（totalPnl getter）+ 历史成交导入错误透出人话（extractApiErrorMessage）+ 久持小亏遵守率 R53（+3 测试）+ **2026-08-25 批次视图批（RFC 20260825）**：持仓行「批次」弹窗（批次明细/初始底仓/回合盈亏/对账警告）+ 导入结果每日操作总结卡片（syncMode + summary 行为标注，红涨绿亏）+ getLots DTO（+9 测试）+ **2026-08-28 批次盈亏%列**（批次弹窗加「盈亏%」：开放批次浮动 pnlPct；已清仓回合收益率 = realizedPnl/(成本×买入量) 前端算，红涨绿亏）  + **2026-08-29 晚间批**（历史成交多文件批量 + 逐份进度反馈 + 股息入账/红利税类型标签 / P2-UI4 溢出 / P2-交易31 总盈亏「—」，+3 测试） + **2026-08-30 规则 Tab（第三阶段）**：交易页第 6 Tab「规则」——我的交易规则参数展示（16 参数中文标签）+ 编辑弹窗（表单化 PUT /trading/rules）+ 加载失败降级（+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 值复制（同 app 三文件条件导入）+ askStream（增量/meta 定稿/error 透出/降级 intent 同构）+ feed_page 两处接线（_doAskRequest/_appendToActiveCard 文本分支）+ 90ms 节流草稿（+5 测试）+ **2026-08-30 续问重复回答修复批**：同 app 原位替换定稿（追加型重复根治）+ 门控回归测试（反向验证红，+1 测试） + **2026-08-30 完美买点案例库批 1 前端（第四阶段环 1-2）**：案例 K 线图 CaseKlineChart（三区 CustomPaint：主图蜡烛+MA10 白线+MA60 黄线（黄白线语义近似）+买点日标记 / 成交量 / KDJ+MACD 前端重算）+ 交易页第 7 Tab「案例」（列表/标注弹窗/详情弹窗含 K 线还原与特征后验/删除确认）+ ApiService cases 四方法（annotateCase/listCases/getCaseDetail?kline/deleteCase）（+8 测试）+ **环 3 AI 理解**：详情弹窗「生成 AI 理解」按钮（POST /cases/{id}/insight）+「阿呆的理解」卡片（summary/keyFeatures/置信度）+ ApiService.generateCaseInsight（+1 测试）+ **环 4 判定当下**：「匹配买点」按钮（案例 Tab）→ 输入代码（日期可空）→ POST /cases/match → 相似案例列表（名称/日期/类型/相似度%绿框≥80/+5d 后验）+ ApiService.matchCases（+1 测试）+ **2026-08-30 通达信风格图表**：CaseKlineChart 指标切换（主图 MA2/MA4/裸K + 副图 KDJ/MACD/成交量，PopupMenu）+ 主/副图左上角指标数值标签（MA/KDJ/MACD 当前值）（+1 测试）+ **2026-08-30 验收反馈批**：K 线显示窗口默认买点后 3 天 + ◀▶ 按钮移动查看后验（防后验涨幅压缩前期形态）+ 标注/匹配弹窗标的搜索候选下拉（_SymbolSearchField 防抖 300ms，代码/名称/拼音首字母）+ ApiService.searchSymbols（+1 测试）+ **验收反馈批 2**：主图下三副图固定（量/MACD/KDJ 四区）+ 悬停单日指标标签跟随（十字光标式，光标竖线）+ 图表高 400  + **2026-08-31 案例库双轨前端批**：匹配弹窗双轨判定卡（B1/B2 各自命中 + 类型判定 + 失败画像警示）+ 标注类型下拉（B1/B2/失败案例）+ FAILED 列表行红色 + 列表 +5d null→「—」（+1 测试） + **2026-09-01 登录体系（RFC 20260901-auth-login）**：登录页 + setup 引导 + token 存 localStorage + ApiService Bearer/401 + 壳底部退出登录（测试数不变，widget_test 重写为会话流程） + **2026-09-03 案例未修项修复批（REVIEW 6 项出表）**：P2-案例1 窗口<60根已知限制注明 / P2-案例2 web 自选适配 buyPoint="case"（案例相似 N% 橙色） / P2-案例3 案例库 TTL 缓存 5min / P2-案例5 index verify 缺失落 JSON null / P3-案例1 路由登记 trading_case_insight 归 flash / P2-2 复核出表（+4 测试）   + **2026-09-04 活跃市值区间开关批（v3.41）**：交易页顶部活跃市值区间切换条（多头红/空头绿/未判定灰 + 手动时间副文案）+ ApiService get/setMarketStage（+2 测试） + **2026-09-12 learn 抓取批（v3.57 前端接线）**：整理弹窗顶部加**链接输入框**（首选入口，后端自己去抓）+ 素材框保留为降级；`stage` 驱动进度人话（正在抓取原文／正在转写，可能要几分钟／正在整理成卡片）+ 抓到的来源一行；**`needs_confirmation` → 停止轮询**，弹窗标题「转写要花钱，你说了算」+ 报价文案 +「继续转写」/「先不转写」（含连点守卫 `_answering`）；confirm(true) 恢复轮询、confirm(false) 收尾且**作废旧轮询**（代际 `_gen++`，防迟到响应污染状态）；顺带按 B1 清理系统口径文案；ApiService `submitLearnDigest` 改打 `/api/v1/learn/digest`（url 优先、content 可选）+ 新增 `confirmLearnTranscription`/`getLearnQuota`；模型 `LearnDigestJob` 增 stage/source/cost（+`LearnQuotaDto`，防御式 null 解析）（+10 测试：抓取批解析/链接喂入→done→打开新卡／needs_confirmation 停轮询且不自动花钱／继续转写→confirm(true)→恢复轮询／先不转写→confirm(false) 不进 done／只填链接可提交）——**193 全绿** |

| 前端 adai-admin | **68** | 0 | 账号/数据/系统/知识页（治理只读收敛）+ 插件开关 + 涨跌色红涨绿亏（darkRed token）+ 登录体系（REVIEW #178：登录页 + role=admin 门禁 + Bearer/401 回登录 + 顶栏修改密码/退出 + 账号页重置密码/建号初始密码 + X-Admin-Token 退役，+15 测试）+ **2026-09-04 行情数据包导入（MD17）**：维护页「行情数据导入」卡片（file_picker 选 .zip → 长超时上传 POST /admin/market/tdx-import → 结果摘要含失败清单）+ ApiService 上传走注入 client + 长超时 client（+3 测试）+ **2026-09-06 审查修复批**（登录默认值 + P1-A~D + P2/P3 拍板 4 项 + 保护/反馈批）：登录页留空自输 · 改密 401 双义 · 反哺 formatDate · 文件树/知识树错误态 · 0 值中性灰 · 反哺冲突只读 · 文件树全局标注 · 记录/Feed 来源标注 · 内置 admin 重置警示 · **保护/反馈批**（禁用确认 P2-2 / 插件开关成功反馈+生效说明 P2-3 / 静默刷新保留旧值 P2-4 / 删除文案会话失效 P2-5 / 账号卡「治理浏览」直达 P2-6 / 维护三操作确认+目标用户 P2-9 / 全局 401 提示 P2-1）|

## 端点/控制器计数（Gradle `endpoints.txt` 单一口径，REVIEW #228）

- Controller：**21**（`interfaces/*Controller.java` 实测，2026-09-05 复核：原 17 漏数 9 月新增 Auth/AskStream/EquityCurve/Learn；GlobalExceptionHandler 为 @RestControllerAdvice 非 Controller 不计）
- 端点：**130**（`build/resources/main/META-INF/endpoints.txt` 实测；**+3：POST /learn/digest + POST /learn/digest/confirm + GET /learn/digest/quota（2026-09-12 learn 抓取批 v3.57）**；含 **POST/GET /learn/cards 学习卡片 + GET /learn/tree 资产树（2026-09-06 learn 插件 V1）+ PATCH /learn/cards/status + PATCH /learn/cards（2026-09-07 learn V2 复习流转/编辑）+ POST /learn/cards/candidate + GET/DELETE /learn/cards/candidates（2026-09-07 learn V2 批 3 trading 候选联动）+ GET /learn/digest/status 消化任务状态（2026-09-10 喂入入口批 v3.56，POST /learn/cards 同步改提交式配套；**2026-09-12 v3.57 扩展 stage/source/cost + needs_confirmation/cancelled**） + **POST /learn/digest 链接/素材喂入（服务端抓取 B站/文章）+ POST /learn/digest/confirm 转写费用确认 + GET /learn/digest/quota 本月转写额度（2026-09-12 learn 抓取批 v3.57，RFC 20260912）**、/trading/advice、/trading/trades/parse、GET /trading/trades 流水、/admin/** 维护端点迁移、/trading/lookup 代码查名、/trading/positions/import 持仓导入、PUT /trading/positions/{symbol} 持仓编辑、/trading/imports/save 文件留存、/trading/watchlist* 自选、/trading/buy-points 买点信号、/trading/sold* 清仓、/trading/sold/score 复盘打分、/trading/imports/cash 资金、/trading/account 账户快照、/trading/transfer 转账、/trading/transfers 流水、GET/PUT /trading/push-settings、/trading/trade-log、/trading/trade-log/confirm、POST /trading/trades/batch 批量记录、POST /trading/trades/import 历史成交导入、PUT /trading/principal 本金设置、**GET /trading/lots 批次视图（RFC 20260825）**、**POST /trading/sync 一键按流水重建持仓（2026-08-25）**、**POST /trading/screenshots 截图入账（2026-08-26）**、**PUT /trading/trade-log/date 候选补日期（2026-08-27 v3.32）**、**GET/PUT /trading/rules 交易规则参数（2026-08-30 v3.33）**、**POST/GET /trading/cases 完美买点案例 + GET/DELETE /trading/cases/{caseId} 案例详情/删除 + POST /trading/cases/{caseId}/insight AI 理解 + POST /trading/cases/match 判定当下 + GET /trading/search 标的搜索 + POST /trading/cases/import 批量导入（2026-08-31 v3.39 批量导入）**、/brief/cached 缓存简报 v3.24、**POST /records/ask-stream 流式问答 SSE（2026-08-30 v3.34）**、**POST/GET /auth/login·logout·me + POST /auth/setup·password 认证 5 端点（2026-09-01 v3.40 登录体系）**、**POST /admin/market/tdx-import 行情数据包导入（2026-09-04 MD17）**、**PUT/DELETE /trading/lots/{lotId}/stop-loss 批次止损设改/清除（2026-09-04 按批次止损批）**、**GET /trading/equity-curve 资金曲线（2026-09-04 决策方案 A）**、**GET /trading/advice-history 建议留痕查询 + GET/PUT /trading/profile 个人画像 + GET /trading/sold/{symbol}/psychology-questions 情绪提问 + POST /trading/sold/{symbol}/psychology/answer 情绪回答（2026-09-05 v3.47 交易⑤认知层 RFC 20260905）**、**PUT /trading/trade-log/meta + PUT /trading/trades/{tradeId}/meta 候选/流水补成交编号与手续费（2026-09-09 P2-交易36 治本）**）
- 端点计数规则：以 Gradle 生成的 `endpoints.txt` 为准，禁止扫源码回退（REVIEW #228/#187）

## 运行环境

- 后端：`localhost:8080`（DeepSeek 模式 + GLM 视觉——`.env` 需配 `GLM_API_KEY` 才有真 VLM 理解，无 key 时上传降级不丢数据）
- 前端：adai-app `localhost:8081`（移动端入口，Web 形态）· adai-web `localhost:8082`（桌面端入口）· adai-admin `localhost:8083`（产品后台）（均 Flutter Web + CanvasKit 补丁）
- **生产服务器**：`82.156.111.146`（北京 · 腾讯云轻量 · 2核4G · Ubuntu 24.04 LTS · 2026-08-19 从 49.235.37.220 迁移，旧服务器到期下线）
- **生产域名**：`adaiadai.com`（2026-09-01 已上线：ICP 备案通过 `京ICP备2026056893号` + DNS + Caddy HTTPS + 登录体系，见 `docs/deployment/backend-deployment.md` §10）
- 生产访问：web `https://adaiadai.com` · admin `https://adaiadai.com/admin/` · API `https://api.adaiadai.com`（**需登录 Bearer token**，旧 IP:8080 直连已不对外）
- 生产目录：`/opt/adaios`（backend/data/web/admin/os；`.env` 含密钥不进 git）
- 数据路径：`data/{userId}/...`（本机账号 = `data/adai/`，default 已迁移移除；测试可用 `data/default/`）

## 发布状态

- **v1.0.0**：Release Notes + 数据冻结 + 插件门控就绪；tag + 部署待用户确认触发（2026-08-15 用户决策：**先治理流程，发布顺延**）
- 后续候选：多账号前端选号（v1.0.1）、登录体系（#179，v1.0.1 立项）

## 未修项

- 见 `docs/review/REVIEW.md`（战略 + P0-P2 唯一滚动区）
- 待办/观察项：见 `docs/reference/task-log.md`
