# 状态快照（Status）

> **测试数/端点数/运行环境的唯一事实源**（RFC `20260815-docs-governance` 单一事实源化）。
> **更新规则：每批 /ship 时更新本文件**——AGENTS.md 及子项目 AGENTS.md 一律不复制数字，只指回本文件。

---

## 测试状态

| 端 | 测试数 | 失败 | 备注 |
|:---|:------:|:----:|:-----|
| 后端 adai-core | **948** | 0 | 16 Controller 接口测试全覆盖 + 多模态 + 鉴权 + 交易建议引擎（止损硬判定）+ 一句话交易解析 + 交易数据模型（逐笔流水/止损/买点）+ 带图 S-2 聚合修复 + G-1~G-6 框架+插件 + 真止损预警 + 时段节奏推送 + 通达信持仓导入/代码查名 + 持仓行情注入 + 导入文件上传留存（GBK 转码）+ C2 自选股买点判定（KDJ/B1回调缩量/B2放量破前高）+ D3 清仓复盘三维打分（买点回溯/执行纪律）+ 择时状态配置驱动读取 + 持仓元信息更新端点（PUT /positions/{symbol}，止损/角色）+ 简报注入真实持仓快照（防 LLM 算错盈亏）+ 导入解析失败禁止落零（P1-交易5）+ 买卖同步市值（P1-2）+ 收盘缺行情跳过（P1-3）+ P2 批（线程池/缓存/阈值-5%/KDJ-13/并发扫描/节假日）+ 八端点 controller 测试 + P2 批（原子写/插件缓存/任务删除门控/记忆三缺陷）+ S5 现金单一真源 + P1-交易4 占比分母含现金（回归测试）+ Memory 正文 `---` 分隔不截断（回归测试）+ RFC 20260817（推送开关/结构化推送/交易日志归集，PushSettings 4 + TradeLogCollect 5）+ 20260818 批次（历史成交导入解析/幂等/对账 + /trades/batch 补实现 + 持仓 replace 全量覆盖 + BUY 止损/买点放开 + 本金设置 PUT /principal）+ P1-1 交易归集 unknown 污染修复（拒绝归集/complete 补 symbol/dedupeKey name 兜底）+ P2-1 东财熔断（连续失败 3 次熔断 5 分钟 + 半开探测）+ 首屏提速（brief/cached 端点 2 测试）+ RFC 20260823（历史成交导入回填：幂等命中且缺成交时间 → 回填 updated 计数，回填 round-trip ×3 + 重传回填 + 已有时间不重填）+ **2026-08-23 走查修复批（B1-B5，21 项）**：confirm 失败候选保留 + account.json 原子 RMW（per-user 锁）+ direction @NotNull + batch 逐行校验 + 尾盘 r81Applicable + 收盘昨收残缺跳过 + 快照写失败告警 + verdict R53 延展 + 节假日 2026 官方/2027 预测 + MarketAlert 判定拆开 + dedupeKey volume 桶 + TradeLog save 锁 + MarketPush 读失败拒写回（+11 测试）+ **隔离审查残留批（B6-B7）**：MarketPush 结构校验 + sameTrade ±10% 区间去重 + 三处 changePercent 判空 + 快照写失败抛 StorageException + confirm 单 now + 切 Tab 静默刷新 + DELETE /trade-log 丢弃端点 + 真实仓储并发/损坏测试（+17 测试）+ **C 批**：confirm 处理期新候选合并保留 + 2027 中秋修正 + batch 空 400 + name 超长校验 + 锁收敛 userId（+3 测试）+ **2026-08-24 行情主源切换批**：KlineService 主源/兜底配置化（`adai.market.kline-primary` 默认 tencent——腾讯主源 + 东财探测兜底，生产东财被限不再刷 500+/日 WARN；Tencent 补 KlineSource 实现，+1 配置切换回归）+ **2026-08-25 推送渠道切换批**：BarkPushChannel（iOS 原生推送，PushChannel 第二外部实现，POST JSON，base-url 可配自托管；微信 Server酱 停用——免费 5 条/天不够，代码保留未配置即禁用；+4 测试）+ **2026-08-25 RFC 20260825 批次跟踪批**：批次推导（按日合并/LIFO/回合/初始批次/对账，TradingLotService）+ GET /trading/lots 批次视图 + 导入双模式（sync 同步持仓/append 补录，orderId 透传幂等）+ 每日操作总结（行为标注六类：亏损加仓/追高/短线新开/破止损未走/浮盈回吐/短线超期）+ 批次级止损推送 + 推送 expiresAt 定时消失（行情类次日 09:30/汇总类次日 23:59）+ recordTradeWithOrderId + 手动/导入交叉防重 + 卖出费用入回合 + 初始批次卖出扣减 + 批次止损推送测试 + 卖清重买不误判（+27 测试）+ **2026-08-26 截图表格归集批**：parseLooseBatch 表格批量解析（委托截图多笔 → 逐笔候选，已成/已报/申购过滤）+ collect 批量归集（跨截图 sameTrade 去重）+ 生产真实验证（+8 测试；工作树含遗留未提交测试 +6，实测 786）+ **2026-08-26 截图入账 + 复盘卡点批**：POST /trading/screenshots 截图入账（multipart 1-3 张 → VLM → 候选，不建记录/不落原图/不沉淀记忆，TradingScreenshotAppService）+ hasTradingActivity 改当日真实成交口径（废除关键词扫描）+ 复盘与截图入账成闭环（+14 测试） + **2026-08-29 晚间自主修复批**：confirm 锁内原子 saveMerging（P2-交易25 残余窗口闭合）+ 三处锁池固定 16 条带（P2-交易28 map 增长根治）+ MarketPush 写失败 warn→error（P2-交易27）+ 收盘缺行情推送通知（P2-交易33）+ closeAccountUpdate 残余窗口注释（P2-交易24）（+2 测试） + **2026-08-29 收盘小结批**（15:30 推送：当日成交+破止损+待确认，close-summary 类型+双端开关，+3 测试） + **2026-08-30 交易插件规则层（第三阶段，trading-plugin-architecture.md）**：TradingRuleSettings（16 参数：仓位上限/默认止损/浮盈回吐/短线超期/清仓阈值/买点 5 参/打分权重/硬约束区间，fail-closed）+ TradingRuleSettingsRepository（data/{userId}/trading/rules.yaml 读写，YAML 真相源）+ 规则引擎按用户（仓位上限/行为标注/清仓 verdict/买点参数/打分权重/建议硬约束区间全部从用户规则读，无规则 → 默认=adai 现状）+ 知识注入用户私有（data/{userId}/trading/knowledge.md 优先，os/ 作默认）+ GET/PUT /trading/rules 端点 + 降级验证（无规则用户=纯客观）+ adai 规则包生成（rules.yaml + knowledge.md，行为不变回归）（+16 测试） + **2026-08-30 审查修复批（deep 深审 5 官）**：P0-1 保存链路 fail-visible（save 抛 StorageException/空提交 400/NaN 拒绝）+ P0-2 硬约束区间 R66-R95 内收缩防 fail-open + P1-1 止损判定线统一（effectiveStopLoss 传 userId）+ P1-3 多用户知识泄漏 owner 白名单 + P1-4 domain 端口接口 TradingRuleSettingsPort + P2 批（per-user 条带锁/NaN finite 校验/knowledge.md 缓存/save 保留未知键/买点参数防轰炸下限/文案区分用户 vs 默认/测试独立文件/引擎 TTL 缓存）+ P1-2 规则包同步脚本（09-scripts/sync-adai-rulepack.sh）+ P1-5 降级语义定稿默认值兜底 + P3 死代码清理（+22 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：POST /records/ask-stream SSE 端点（text 增量/meta 定稿/[DONE]/error 事件；后端内降级——模型无增量回退同步 understand 一次；同卡同问 5 分钟去重 QuestionAppService.findDuplicateResend；JsonTailFilter 防 JSON 回执跨块切开；UTF-8 byte[] 透传防中文乱码；线程池满 503）（+16 测试：AskStreamController 5 + JsonTailFilter 5 + QuestionAppService answerStream/dedup 6）+ **2026-08-30 推送口径修复批（用户反馈）**：行情提醒节假日守卫（B5-1 残留：节假日撞工作日 break-cost/止损类拿前日收盘价每天重推）+ 轮询首轮 9:00/9:30 → 10:00（9 点档行情接口仍返回上一交易日收盘，旧数据冒充「今日」且按日去重签名被烧名额）+ 早盘计划昨收口径（数据文本标注「昨收/昨日涨跌」+ 提示词第 5 条，LLM 不再把昨日涨幅说成「今日」）（+4 测试） + **2026-08-30 完美买点案例库批 1（第四阶段环 1-2，trading-case-library-design.md）**：KlineSource.klineRange 日期范围直查（腾讯/东财覆写为数据源日期参数，默认拉 320 截取）+ MacdIndicator（EMA12/26 + DIF/DEA/柱 + 金叉）+ CaseFeatureExtractor（9 特征：回撤/量比/KDJ/MACD/MA 关系/距 60 日线/黄白线态/盘整/破前高 + 后验窗口）+ TradingCaseRepository（domain 端口 + JSON 落盘 data/{userId}/trading/cases/ + _index.json 清单原子写 + 条带锁）+ TradingCaseAppService + TradingCaseController 4 端点（POST/GET /trading/cases、GET/DELETE /trading/cases/{caseId}）+ 降级（K 线失败不落半成品/后验不足 null/损坏文件视为不存在）（+20 测试）+ **2026-08-30 案例库批 1 环 3（AI 理解）**：POST /trading/cases/{caseId}/insight（LLM 读特征画像 + K 线统计 → 结构化「为什么这是完美买点」aiInsight 落盘，reviewed=false 待人工确认；prompt 判定优先级 形态>量能>指标>位置；LLM 失败/非 JSON 输出 → 400 不落半成品 fail-visible）（+7 测试）+ **2026-08-30 案例库批 1 环 4（判定当下，核心价值）**：CaseFeatureNormalizer（7 维特征 min-max 归一化，KDJ/MACD null → 中性 0.5）+ CaseSimilarityEngine（加权欧氏，权重和=1 → 相似度=1−距离；Top N 降序，无特征跳过）+ POST /trading/cases/match（当前标的拉 60 日 K → 特征 → 与案例库相似度 Top 5；date 可空=最近交易日，指定日无数据回落最近；空库 → matches:[] 静默降级；相似度不覆盖规则硬判定）（+14 测试）+ **2026-08-30 环 4 二期（扫描接入 + 黄白线参数化）**：WatchlistBuyPointService 附案例相似度 Top 3（开关 adai.trading.case.scan-match 默认关，向前兼容；规则未命中但案例相似 → buyPoint="case" 参考类型，15:10 推送跳过）+ TradingCaseAppService 黄白线周期配置化（adai.trading.case.yellow-ma/white-ma 默认 60/10，批 5 前置）（+3 测试）+ **2026-08-30 案例库审查修复（S1）**：save index 写失败回滚案例文件（防 409 卡死）（+1 测试）+ **2026-08-30 日期宽容解析**：case 标注/match 日期接受 yyyyMMdd + yyyy-MM-dd（Controller parseDate，失败 400 人话「日期格式不正确」）（+3 测试）+ **2026-08-30 TDX 通达信本地行情源**：TdxFileKlineSource（.day 二进制解析：32 字节/条小端 int32，价格 ×100、volume 股→手；kline/klineRange 日期直查；mtime 缓存）+ KlineService 优先级 tdx → 腾讯 → 东财（tdx 无数据不算失败）；配置 adai.market.tdx-path/tdx-enabled（+6 测试）|

| 前端 adai-app | **144** | 0 | Feed 状态机 12 + 6 页面 + 选号/切换链路 + 插件门控 + 带图发图即对话 + 交易建议 UI + 交易止损/买点 P0 + 交易对齐 web（账户总览卡/止损显示/默认-7%止损/自选买点/清仓打分）+ 30分钟自动刷新 + 次级数据代际守卫 + 上传批次锁 + 第一原则徽章移除（无记录/提问/领域系统标签）+ 20260818 简化（纯买卖记录去止损/买点 + 价格小键盘小数点）+ **2026-08-23 app 体感批**（IndexedStack 保活/排序四修/Feed 错误态/任务编辑 PUT/launcher 伪0/三态/人话/代际令牌/方向色/时间戳）+ **P1-G6-1 守卫批**（timeline_modal await 后 setState 补 mounted 守卫×2 + 回归测试×2：关闭后响应到达不崩溃/正常路径渲染）+ **2026-08-25 批次简版批**（持仓卡第三行批次信息：批次数/最近买入/含底仓徽标/破止损警示，getLots 静默降级，+3 测试）+ **2026-08-26 截图入账批**（交易页「📷 截图入账」入口 + 当日候选内嵌列表逐笔确认/丢弃 + 复盘卡点：无真实成交时复盘按钮引导先导入，+4 测试）+ **2026-08-27 缺日期禁落库批（v3.32）**：候选行缺成交日期警示 + 「补日期」入口（日期选择 → PUT /trade-log/date）+ 确认前拦截缺日期候选（+1 测试）+ **2026-08-28 批次明细批**（持仓卡批次行可点 → 批次明细弹窗：每笔买入的日期/状态徽标（初始底仓/持有中/已清仓）/剩余·成本·现价/盈亏+盈亏%（开放批次浮动 pnlPct、已清仓回合收益率前端算）/破止损橙色警示，连点守卫防叠两层，+2 测试） + **2026-08-29 晚间批**（P1-前端1 静默刷新失败保留旧数据 / P2-UX1 NL 回显 ≤5 位去尾零 / P2-交易31 principal=0 总盈亏「—」不给误导数值，+3 测试） + **B 类批**（确认流阿呆口吻+loading 灰态/插件行稳定槽位/下滑返回挂拖拽条/字号下限 11px/触达 44pt，+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 三文件条件导入（IO http.send 流式 / Web fetch ReadableStream / common 协议件）+ askStream（text 增量 onDelta / meta 定稿 / error 人话 SseServerException / 未收增量自动降级同步端点且 intent 同构透传）+ main_page 三处接线（首问/建卡 question/续问文本分支）+ 90ms 节流草稿 + meta 定稿替换（+6 测试：ask_stream 5 + 流式状态机 1）+ **2026-08-30 续问重复回答修复批**：_appendToActiveCard meta 定稿原位替换草稿 turn（原追加 → 草稿+定稿双份，用户实测每轮续问回答显示两遍/end 上报 turns 带重复 8 vs 服务端 6）+ 门控回归测试（反向验证红，+1 测试）|

| 前端 adai-web | **144** | 0 | 桌面壳 + 交易管理端（持仓编辑/批量导入/复盘历史）+ 通达信导入解析 + 自选/清仓/资金区块 + 自选买点信号列（C2）+ 清仓三维打分列（D3）+ 行为模式统计（D2）+ 切入自动刷新修复（P1-1）+ 打分按序匹配（P1-8）+ 可降级请求分离（P1-7）+ P2 批（mounted守卫/胜率口径/否定词/千分位/打分列色/删除确认）+ DTO 测试补齐 + P1-5 回归 + 走查 8 项（可降级代际/历史日期竞态/图片回执自然化/交易成功 toast/首载失败错误态/错误人话/Feed 徽章移除）+ 20260818 批次（历史成交/持仓/CSV 三格式识别测试）+ RFC 20260823（历史成交 Tab 分组全字段渲染 / 空态 + 导入入口 / 非历史成交格式人话拒绝）+ **2026-08-23 走查修复批**：资金区块 principal=0 总盈亏失真（totalPnl getter）+ 历史成交导入错误透出人话（extractApiErrorMessage）+ 久持小亏遵守率 R53（+3 测试）+ **2026-08-25 批次视图批（RFC 20260825）**：持仓行「批次」弹窗（批次明细/初始底仓/回合盈亏/对账警告）+ 导入结果每日操作总结卡片（syncMode + summary 行为标注，红涨绿亏）+ getLots DTO（+9 测试）+ **2026-08-28 批次盈亏%列**（批次弹窗加「盈亏%」：开放批次浮动 pnlPct；已清仓回合收益率 = realizedPnl/(成本×买入量) 前端算，红涨绿亏）  + **2026-08-29 晚间批**（历史成交多文件批量 + 逐份进度反馈 + 股息入账/红利税类型标签 / P2-UI4 溢出 / P2-交易31 总盈亏「—」，+3 测试） + **2026-08-30 规则 Tab（第三阶段）**：交易页第 6 Tab「规则」——我的交易规则参数展示（16 参数中文标签）+ 编辑弹窗（表单化 PUT /trading/rules）+ 加载失败降级（+2 测试） + **2026-08-30 流式问答批（P2-用户2 批 2）**：SseClient 值复制（同 app 三文件条件导入）+ askStream（增量/meta 定稿/error 透出/降级 intent 同构）+ feed_page 两处接线（_doAskRequest/_appendToActiveCard 文本分支）+ 90ms 节流草稿（+5 测试）+ **2026-08-30 续问重复回答修复批**：同 app 原位替换定稿（追加型重复根治）+ 门控回归测试（反向验证红，+1 测试） + **2026-08-30 完美买点案例库批 1 前端（第四阶段环 1-2）**：案例 K 线图 CaseKlineChart（三区 CustomPaint：主图蜡烛+MA10 白线+MA60 黄线（黄白线语义近似）+买点日标记 / 成交量 / KDJ+MACD 前端重算）+ 交易页第 7 Tab「案例」（列表/标注弹窗/详情弹窗含 K 线还原与特征后验/删除确认）+ ApiService cases 四方法（annotateCase/listCases/getCaseDetail?kline/deleteCase）（+8 测试）+ **环 3 AI 理解**：详情弹窗「生成 AI 理解」按钮（POST /cases/{id}/insight）+「阿呆的理解」卡片（summary/keyFeatures/置信度）+ ApiService.generateCaseInsight（+1 测试）+ **环 4 判定当下**：「匹配买点」按钮（案例 Tab）→ 输入代码（日期可空）→ POST /cases/match → 相似案例列表（名称/日期/类型/相似度%绿框≥80/+5d 后验）+ ApiService.matchCases（+1 测试）+ **2026-08-30 通达信风格图表**：CaseKlineChart 指标切换（主图 MA2/MA4/裸K + 副图 KDJ/MACD/成交量，PopupMenu）+ 主/副图左上角指标数值标签（MA/KDJ/MACD 当前值）（+1 测试）|

| 前端 adai-admin | **34** | 0 | 账号/数据/系统/知识页（治理只读收敛）+ 插件开关 + 涨跌色红涨绿亏（darkRed token）|

## 端点/控制器计数（Gradle `endpoints.txt` 单一口径，REVIEW #228）

- Controller：**17**（`interfaces/*Controller.java`，含 MeController + TradingCaseController 2026-08-30 第四阶段；GlobalExceptionHandler 非 Controller）
- 端点：**95**（`build/resources/main/META-INF/endpoints.txt` 实测；含 /trading/advice、/trading/trades/parse、GET /trading/trades 流水、/admin/** 维护端点迁移、/trading/lookup 代码查名、/trading/positions/import 持仓导入、PUT /trading/positions/{symbol} 持仓编辑、/trading/imports/save 文件留存、/trading/watchlist* 自选、/trading/buy-points 买点信号、/trading/sold* 清仓、/trading/sold/score 复盘打分、/trading/imports/cash 资金、/trading/account 账户快照、/trading/transfer 转账、/trading/transfers 流水、GET/PUT /trading/push-settings、/trading/trade-log、/trading/trade-log/confirm、POST /trading/trades/batch 批量记录、POST /trading/trades/import 历史成交导入、PUT /trading/principal 本金设置、**GET /trading/lots 批次视图（RFC 20260825）**、**POST /trading/sync 一键按流水重建持仓（2026-08-25）**、**POST /trading/screenshots 截图入账（2026-08-26）**、**PUT /trading/trade-log/date 候选补日期（2026-08-27 v3.32）**、**GET/PUT /trading/rules 交易规则参数（2026-08-30 v3.33）**、**POST/GET /trading/cases 完美买点案例 + GET/DELETE /trading/cases/{caseId} 案例详情/删除 + POST /trading/cases/{caseId}/insight AI 理解 + POST /trading/cases/match 判定当下（2026-08-30 v3.37 第四阶段环 1-4）**、/brief/cached 缓存简报 v3.24、**POST /records/ask-stream 流式问答 SSE（2026-08-30 v3.34）**）
- 端点计数规则：以 Gradle 生成的 `endpoints.txt` 为准，禁止扫源码回退（REVIEW #228/#187）

## 运行环境

- 后端：`localhost:8080`（DeepSeek 模式 + GLM 视觉——`.env` 需配 `GLM_API_KEY` 才有真 VLM 理解，无 key 时上传降级不丢数据）
- 前端：adai-app `localhost:8081`（移动端入口，Web 形态）· adai-web `localhost:8082`（桌面端入口）· adai-admin `localhost:8083`（产品后台）（均 Flutter Web + CanvasKit 补丁）
- **生产服务器**：`82.156.111.146`（北京 · 腾讯云轻量 · 2核4G · Ubuntu 24.04 LTS · 2026-08-19 从 49.235.37.220 迁移，旧服务器到期下线）
- **生产域名**：`adaiadai.com`（已注册；DNS 解析 + HTTPS 待配置，规划见 `docs/deployment/backend-deployment.md` §10）
- 生产访问：web `http://82.156.111.146:8082` · admin `http://82.156.111.146:8083` · 后端 `http://82.156.111.146:8080`
- 生产目录：`/opt/adaios`（backend/data/web/admin/os；`.env` 含密钥不进 git）
- 数据路径：`data/{userId}/...`（本机账号 = `data/adai/`，default 已迁移移除；测试可用 `data/default/`）

## 发布状态

- **v1.0.0**：Release Notes + 数据冻结 + 插件门控就绪；tag + 部署待用户确认触发（2026-08-15 用户决策：**先治理流程，发布顺延**）
- 后续候选：多账号前端选号（v1.0.1）、登录体系（#179，v1.0.1 立项）

## 未修项

- 见 `docs/review/REVIEW.md`（战略 + P0-P2 唯一滚动区）
- 待办/观察项：见 `docs/reference/task-log.md`
