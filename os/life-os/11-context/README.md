# 11-context / 生活系统上下文接口层

## 定位

`11-context/` 是 Life OS 对 AI / 外部系统暴露的认知接口层。

目前只有 `identity.md`，后续随生活数据积累逐步扩展。

## 文件清单

| 文件 | 内容 |
|:----|:-----|
| `identity.md` | 我是谁，管什么 |

## 维护原则

1. 内容来自 `05-system/` + `04-rules/`，不新增知识
2. 系统收敛后同步更新
