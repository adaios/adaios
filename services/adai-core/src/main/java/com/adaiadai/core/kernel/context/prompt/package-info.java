/**
 * Prompt — 上下文构建规则管理。
 * <p>
 * Prompt 本质是上下文构建规则，不是 AI 模型能力。
 * 管理针对不同 Domain 的 Prompt 模板、组装规则、注入策略。
 * 例如：Trading Context 的 Prompt 规则与 Life Context 的 Prompt 规则不同。
 */
package com.adaiadai.core.kernel.context.prompt;
