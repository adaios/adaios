---
title: 边界声明（Boundaries）
description: AdaiOS「不做什么」的集中声明——原则级（不可违反）与功能级（当前不做）；任何新功能先查边界再定方案
version: 1
created: 2026-08-15
updated: 2026-08-15
status: active
lines: 48
depends-on: []
related:
  - ../../docs/VISION.md
  - adr/ADR-001.md
tags: [ai, assets, boundaries]
---

# 边界声明

> **定位**：回答「不做什么」。原则级 = 不可违反（违反即 P0）；功能级 = 当前不做（可因阶段变化解除，须有 RFC 记录）。任何新功能/新方案先查本表再定。

## 一、原则级边界（违反 = P0）

| # | 边界 | 来源 |
|:-:|:-----|:-----|
| B1 | **不出现第三视角**：所有用户可见展示必须是「我和阿呆」的自然对话，无系统标签（问：/答：/图片记录：/【备注】）| VISION §6 第一原则 |
| B2 | **File First 不倒退**：`os/` 与 `data/` 长期知识以文件为准，数据库为查询存在；不把知识锁进数据库 | VISION §3.5 |
| B3 | **数据隐私不泄**：`data/` 个人数据不进 git；新增落盘目录必须 gitignore 验证 | 安全基线 #127 |
| B4 | **不提前微服务化**：Modular Monolith 是默认，直到满足独立生命周期/数据边界/部署/多人维护四条 | CLAUDE.md 开发规则 |
| B5 | **不混合代码和知识**：代码仓库只放代码/配置/构建；Prompt 模板归 `kernel/context/prompt` | CLAUDE.md 开发规则 |
| B6 | **Domain 间不直接依赖**：跨域协作经 application 层编排 | CLAUDE.md 分层规则 |
| B7 | **审查只报告不直接修**（除 P0 数据丢失可与用户确认后修）| AGENTS.md 规则 6 |

## 二、功能级边界（当前不做，解除须 RFC）

| # | 边界 | 理由/状态 |
|:-:|:-----|:----------|
| F1 | 不做传统 CRUD 后台产品化 | 产品定位是 Personal AI OS，非管理工具（VISION）|
| F2 | 不做多用户 SaaS 化（当前单人多账号）| 多账号已架构预留，但产品不面向公网多租户 |
| F3 | 不做移动端原生拍照/相册深度集成（当前 Web 优先）| 图片记录走 GLM-VLM 文本化闭环，原生能力顺延 |
| F4 | 不做实时消息/推送系统（当前行情主动推送仅交易域）| L5 主动推送已实现交易场景，通用推送顺延 |

## 三、边界变更流程

1. 触碰原则级边界 → **不实施**，先讨论（重大方向变化走 RFC）
2. 解除功能级边界 → 写 RFC（3-5 句：为什么现在可以了/代价）
3. 新边界出现 → 由 AI 提请，人确认后入本表（沉淀过滤器）

---
**追加方式**：新边界出现 → 补对应分组一行，注明来源（RFC/VISION/会话）。
